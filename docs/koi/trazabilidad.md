# Trazabilidad del proyecto

Registro acumulativo de decisiones y ajustes por etapa y agente.

## Entradas

### 2026-06-11 10:00 - analista-funcional
- Etapa: Discovery + Analisis
- Cambio: relevamiento inicial del proyecto KOI. Análisis exhaustivo de los dos Excel fuente (Estado de Resultados 2024/2025/2026 y Reparto de Utilidades con 100 puntos / 15 inversores). Alcance base de 10 bloques funcionales, 15 casos de uso, criterios de aceptación y banderas (EF sí, integración externa no en base, máquina de estados acotada, migración de datos sí).
- Motivo: pedido del cliente — sistema web con dashboard para inversores que reemplace ambos Excel.
- Impacto en capas: Presentacion, Negocio y Datos (sistema nuevo completo).
- Riesgos/supuestos: fórmulas inconsistentes en el Excel (bases A/B mezcladas en impuestos — el sistema normaliza sobre Ventas A por regla confirmada del cliente); reparto = Resultado Ejercicio sin ajustes; un solo local.

### 2026-06-11 10:30 - analista-funcional
- Etapa: Analisis
- Cambio: tres decisiones de alcance confirmadas por el cliente: (1) cámaras vía web client Hik-Connect embebido (sin RTSP nativo), (2) carga inicial de históricos 2024–2026 INCLUIDA como excepción a la exclusión estándar, (3) integración Ayres POS declarada etapa 2, solo mención sin precio.
- Motivo: definición de alcance previa al diseño para no presupuestar funcionalidades indefinidas.
- Impacto en capas: Negocio y Datos (módulo de importación), Presentacion (pantalla de cámaras).
- Riesgos/supuestos: migración de datos con riesgo alto declarado, acotado a su módulo.

### 2026-06-11 11:00 - disenador-funcional
- Etapa: Diseno
- Cambio: 13 pantallas con wireframes textuales, 12 ViewModels, máquina de estados de período (Abierto/Cerrado/Reabierto) y liquidación (Pendiente/Pagada), lógica de distribución estándar (cards KPI → gráficos → tabla) y theme dark/light sobre tokens del design system Olvidata.
- Motivo: bajar el análisis aprobado a diseño implementable, con el dashboard como core de UX.
- Impacto en capas: Presentacion (pantallas/ViewModels), Negocio (contratos de cálculo y cierre), Datos (requerimientos por pantalla).
- Riesgos/supuestos: iframe Hik-Connect con fallback a pestaña nueva; vista anual solo lectura.

### 2026-06-11 11:30 - arquitecto-mvc
- Etapa: Arquitectura
- Cambio: arquitectura sobre blankproject OlvidataSoft (.NET 10 + EF Core + MySQL 8): 15 entidades nuevas (~21 tablas con Identity), 6 servicios de Application, policies por rol con aislamiento del inversor en Application, migración EF inicial + seed. Snapshot de % aplicado por movimiento para no recalcular meses cerrados. Gate aprobado para presupuesto.
- Motivo: validar soporte técnico del diseño y declarar EF/permisos antes de estimar.
- Impacto en capas: Domain/Application/Infrastructure/Web.
- Riesgos/supuestos: volumen bajo (12 períodos/año, 16 usuarios); migración de históricos reconstruye vigencias de puntos.

### 2026-06-11 12:00 - presupuestador
- Etapa: Presupuesto
- Cambio: presupuesto inicial v1 con anclaje histórico (Paso 0 sobre lumitrack/recotrack/delicias/ganaderia): 14 módulos, M total 65.5 h, PERT base 68.79 h, techo interno 78.27 h, fórmula vigente M × $16.80 → USD 1.134 + plan de mantenimiento PREMIUM USD 400/año. Sanity check del total vs delicias-naturales: 4.91 vs 5.0 h base/módulo (ratio 0.98, sin recalibrar). Único ítem con recargo extra: carga inicial de históricos (+25 % riesgo de migración, excepción documentada). Documento cliente emitido en /docs/koi/presupuesto.md.
- Motivo: cierre de la cadena 1→4 para entregar presupuesto al cliente antes de implementar.
- Impacto en capas: n/a (documento).
- Riesgos/supuestos: gatillo de reestimación si los Excel finales difieren de los analizados o si aparece integración no relevada; pendiente aprobación del cliente y cierre de calibración estimado vs real al fin del sprint.

### 2026-06-11 13:00 - presupuestador
- Etapa: Presupuesto
- Cambio: presupuesto v2. (1) Nueva regla global en 27-presupuesto-parametros: cargo fijo de USD 100 por uso de tokens IA en todo presupuesto de proyecto — aplicado a KOI: total desarrollo USD 1.234 (1.134 módulos + 100 tokens IA). (2) manual-de-uso.md actualizado: tarifa USD 14/h reemplazada por la política vigente (USD 35/h, fórmula M × $16.80, horas no expuestas al cliente, cargo tokens IA). (3) Documento cliente /docs/koi/presupuesto.md rehecho con la especificación funcional detallada (11 secciones) para envío al cliente.
- Motivo: pedido del cliente interno — incorporar costo de tokens IA y entregar al cliente final un detalle funcional completo junto al precio.
- Impacto en capas: n/a (documentos y parámetros).
- Riesgos/supuestos: sin cambios en módulos ni horas (M total 65.5 h); solo se agregó la línea fija de tokens IA.

### 2026-06-11 14:00 - analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador
- Etapa: Analisis → Presupuesto (cascada por cambio de alcance)
- Cambio: nueva funcionalidad pedida por el cliente: notificación automática por correo a los inversores al cerrar el ejercicio mensual (resumen del mes + liquidación personal + aviso de disponibilidad en la web). Cascada completa: CU-16 y criterios en 1-analista (exclusión de correos acotada a "otros envíos"); pantalla P-14, plantilla HTML y efecto en el cierre en 2-disenador; entidades NotificacionConfig/NotificacionEnvio, NotificacionService e IEmailSender (MailKit, post-commit, no bloqueante) en 3-arquitecto (~23 tablas); módulo 15 en 4-presupuestador: M 4.0 h (ref. delicias Notificaciones 3.5 base, ratio 1.24 justificado), USD 67.20. Total desarrollo: USD 1.301 (1.201 módulos + 100 tokens IA) + USD 400/año. Documento cliente actualizado (sección 3.12 y tabla de precios).
- Motivo: pedido del cliente posterior al presupuesto v2; bandera de integración externa pasa a "Sí, acotada (SMTP)".
- Impacto en capas: Presentacion (P-14, plantilla), Negocio (NotificacionService, idempotencia por período), Datos (2 entidades nuevas).
- Riesgos/supuestos: entregabilidad del correo depende del servicio SMTP del cliente; fallos de envío no bloquean el cierre; re-cierre no duplica correos sin confirmación.

### 2026-06-11 15:00 - analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador
- Etapa: Análisis → Presupuesto (cascada por sesión de definición — cierre P-A01 a P-A07)
- Cambio: 7 preguntas abiertas cerradas en sesión con el cliente. Cambios incorporados a los 4 documentos:
  - **P-A01** (comensales): campo `CantidadComensales` agregado a `IndicadorVenta` y a P-06; Ayres en etapa 2.
  - **P-A02** (ventas 4 campos): `VentaMensual` pasa a `VentasASalon / VentasBSalon / VentasADelivery / VentasBDelivery`. Agregados calculados: VentasA, VentasTotales, VentasSalon, VentasDelivery. Nueva torta Salón/Delivery en Dashboard. Riesgo M14 elevado: históricos pueden no tener apertura Salón/Delivery desagregada.
  - **P-A03** (TC con selector): integración `ICotizacionService` copiada de VirtualWallet (ArgentinaDatos histórico + DolarApi hoy, cache 30min/6h). UX: tabla de cotizaciones por casa + default blue promedio + edición manual antes de guardar.
  - **P-A04** (error post-cierre): sin mecanismo técnico. Estado Reabierto del período **eliminado**. Admin + SuperUsuario operan fuera del sistema.
  - **P-A05** (dashboard mes abierto): Dashboard muestra mes abierto con datos parciales; TC pendiente → valores USD como "pendiente".
  - **P-A06** (notificaciones sin expiración): confirmado, sin cambio de arquitectura.
  - **P-A07** (preview editable): P-08 opera en dos modos — Modo A (preview antes del cierre, consumos editables, botón "Confirmar y cerrar") y Modo B (gestión post-cierre). Nuevo ViewModel `LiquidacionPreviewVM`. Nueva entidad `AjusteLiquidacion` para auditar ajuste manual de monto a repartir.
- Motivo: sesión de definición con el cliente — cierre de todas las hipótesis abiertas.
- Impacto en capas: Presentacion (P-03 4 campos, P-06 comensales, P-08 preview dual, Dashboard parcial+torta), Negocio (CotizacionService, eliminación Reabierto, flujo preview→cierre), Datos (VentaMensual 4 campos, AjusteLiquidacion nueva, IndicadorVenta+comensales, ~24 tablas).
- Presupuesto: v3 → **v4 = USD 1.411** (+USD 110 por P-A01/02/03/05/07; ahorro eliminación Reabierto absorbido por complejidad M14 y CotizacionService). Tabla de precios actualizada en presupuesto.md.
- Riesgos/supuestos: riesgo M14 re-elevado y luego **CERRADO en la misma sesión** — ver entrada siguiente.

### 2026-06-11 15:30 - analista-funcional
- Etapa: Análisis (cierre de riesgo M14)
- Cambio: confirmado por el cliente que los Excel históricos **no traen apertura Salón/Delivery** (solo A/B). Estrategia de migración M14 cerrada: `VentasADelivery = VentasBDelivery = 0`; `VentasASalon = VentasA`, `VentasBSalon = VentasB`. Cada período migrado recibe observación automática `"Desglose Salón/Delivery no disponible en datos fuente"`. El Dashboard mostrará 100 % Salón en los períodos históricos hasta el primer mes con datos reales. Delta USD 17 en M14 confirmado y justificado.
- Motivo: respuesta del cliente a la pregunta abierta de M14.
- Impacto en capas: Datos (ImportacionInicialService: lógica de mapeo definida). Sin cambio en entidades ni en el resto del modelo.
- Riesgos/supuestos: R-A2 del arquitecto cerrado. No quedan preguntas abiertas en el análisis funcional de KOI.

### 2026-07-13 — implementador-dotnet
- Etapa: Implementación (refactor UI/UX puntual, autorizado directo por el cliente/dueño del estudio sin ciclo completo Análisis→Diseño→Arquitectura→Presupuesto — reutiliza DTOs/servicios existentes, sin entidades ni migración EF)
- Cambio: fusión de las pantallas Dashboard e Indicadores (KPIs de venta) en una sola pantalla Dashboard con 3 secciones diferenciadas (Ventas / Gastos / Evolución Histórica) + hero de KPIs de resultado global (Ventas Totales, Total Gastos, Resultado, Resultado USD, Rentabilidad %). Todo el contenido —incluido el desglose Facturado(A)/Informal(B) antes solo-Admin— queda visible para todos los roles autenticados (Admin, SuperUsuario, Inversor). `DashboardService` deja de recalcular KPIs de venta y delega en `IIndicadoresService` (fuente de verdad ya existente) para evitar lógica duplicada. Se eliminan `IndicadoresController`, `Views/Indicadores/Index.cshtml`, `IndicadoresIndexViewModel` y el link "Indicadores de Venta" del sidebar; `IndicadoresDto`/`IIndicadoresService`/`IndicadoresService` se mantienen como capa Application/Infrastructure reutilizada.
- Motivo: pedido directo del cliente para eliminar el overlap de contenido entre las dos pantallas y mejorar jerarquía visual (criterio de diseñador senior — icon badges circulares, `border-0 shadow-sm` consistente en toda la pantalla).
- Impacto en capas: Application (`DashboardDto` simplificado, nuevo campo `Ventas: IndicadoresDto?`), Infrastructure (`DashboardService` inyecta `IIndicadoresService`), Web (`Views/Dashboard/Index.cshtml` reescrita, controller/view/viewmodel de Indicadores eliminados, sidebar actualizado). Sin migración EF.
- Riesgos/supuestos: cambio de permisos deliberado (Facturado/Informal pasa de solo-Admin a todos los roles autenticados) — confirmado como decisión de negocio explícita del cliente, no un descuido; verificado por grep que no quedan referencias a `Indicadores` en Web tras el borrado. Build `dotnet build` → 0 errores. Sin proyecto de tests automatizados en el repo; validación pendiente vía QA manual (smoke de los 3 roles + guards de sin-datos/TC-pendiente/período-abierto).

### 2026-07-13 — implementador-dotnet (sesión actual)
- Etapa: Implementación (fix de UI transversal, autorizado directo por el cliente/dueño del estudio — sin cambio de alcance funcional ni de datos)
- Cambio: diagnóstico y corrección sistémica del tema oscuro en toda la aplicación. Causa raíz: Bootstrap 5.1.0 (versión instalada) fija `color:#212529` como literal en `.table` en vez de vía variable CSS, por lo que el override previo de tema oscuro (que seteaba `--bs-table-color`) nunca se aplicaba — el texto de todas las tablas/DataTables del sistema quedaba ilegible en oscuro. Además `.card-header`/`.card-footer` usaban un token fijo no redefinido en oscuro (cabeceras blancas), la paginación de DataTables (`.page-link`) no tenía override de tema, el botón de toggle de tema tenía fondo blanco por defecto del navegador, y las alerts nativas de Bootstrap no estaban tematizadas. Se corrigió todo en un único archivo compartido (`wwwroot/css/olvidata-theme.css`) en vez de parchear pantalla por pantalla. Adicionalmente, el toggle de tema (`_Layout.cshtml`) pasó de togglear el atributo en vivo (dejando gráficos Chart.js ya renderizados con colores desactualizados) a esperar la confirmación del servidor y recargar la página, garantizando que todo widget dependiente del tema se re-renderice correctamente por diseño.
- Motivo: pedido explícito del cliente — "que el tema oscuro no sea una traba para el desarrollo del sistema" — se buscó la corrección más simple y efectiva posible, centralizada en el design system en vez de fixes puntuales.
- Impacto en capas: Web/Presentación únicamente (CSS + un handler JS). Sin cambios en Domain/Application/Infrastructure, sin migración EF.
- Riesgos/supuestos: validado con Playwright headless (login real, click real en el toggle, screenshots antes/después) en 6 pantallas representativas (Dashboard, Estado de Resultados Mensual/Anual, Tipo de Cambio, Configuración/Rubros, Inversores) — 0 errores de consola, legibilidad confirmada en carga fresca y en toggle en vivo. `olvidata-theme.css` es compartido entre proyectos del estudio sobre el mismo blankproject base — si otro proyecto activo usa Bootstrap 5.1 con este mismo archivo, es candidato a heredar el mismo fix (marcado en la memoria del implementador).

### 2026-07-13 — analista-funcional (auditoría a pedido del cliente)
- Etapa: Verificación de cobertura funcional (post-implementación, sin cambio de código)
- Cambio: auditoría cruzada de los dos Excel fuente (`Estado de Resultados KOI (Inversores).xlsx`, `Reparto de Utilidades Inversores.xlsx`, ambos presentes en `KoiDumplings.Web/`) contra el código real (controllers/services) para confirmar si el sistema reemplaza a los Excel en su totalidad. Resultado:
  - **Cobertura funcional: completa.** Los 16 CU de `1-analista-funcional.md` tienen controller/service propio implementado (`EstadoResultadosController`, `PuntosController`, `LiquidacionesController`, `MiInversionController`, `RepartoGeneralController`, `CamarasController`, `ImportacionInicialController`, `NotificacionCierreController`, etc.). Se verificaron en código las fórmulas clave contra el Excel: bases de cálculo normalizadas (`EstadoResultadosService.RecalcularInternamente` — % s/Ventas Totales vs % s/Ventas A, según CU-06), utilidad por punto = Resultado Ejercicio ÷ 100 (`CerrarPeriodoAsync`), liquidación = puntos × utilidad − consumos con validación bloqueante si consumos > bruto (`CerrarPeriodoAsync`), renta mensual = NetoUsd ÷ Capital aportado y recupero = dividendos USD acumulados ÷ Capital aportado (`InversionesService`) — todas coinciden con las fórmulas de las hojas GENERAL y por-inversor del Excel de reparto.
  - **Gap detectado: la migración de datos históricos (CU-15) NO fue ejecutada.** `ImportacionInicialService` está completo (plantilla XLSX intermedia de 6 hojas, validación dry-run, importación transaccional) pero requiere que alguien transforme primero el Excel real del cliente a esa plantilla intermedia — no ingiere el Excel del cliente directamente. No existe en el repo ningún archivo de plantilla ya completado, no hay entrada en `trazabilidad.md` ni en `6-qa.md` que registre la ejecución de la importación, y la base de datos de dev solo tiene los 2 períodos de demo (2026/06, 2026/07) — cero períodos 2024/2025. El criterio de aceptación de CU-15 ("los totales anuales 2024 y 2025 coinciden con los Excel fuente") nunca fue validado.
  - Hallazgo secundario: `6-qa.md` (última actualización 2026-06-29) quedó desactualizado — marca KOI-002 (Export Excel) y KOI-005 (CamarasController) como pendientes, pero `5-implementador.md` registra ambos como resueltos en una sesión posterior. Recomendado refrescar la memoria de QA en el próximo pase.
- Motivo: pedido del cliente de verificar reemplazo total de los Excel antes de instruir al documentador sobre el manual de usuario.
- Impacto en capas: n/a (auditoría, sin cambio de código).
- Riesgos/supuestos: mientras la migración histórica no se ejecute, el sistema no puede afirmarse como reemplazo total de los Excel en la práctica — la cobertura funcional está lista pero el dato histórico todavía vive solo en los Excel. Acción pendiente: transformar ambos Excel a la plantilla de `ImportacionInicial`, ejecutar la importación, y validar los 3 meses de muestra que pide el criterio de aceptación de CU-15 antes de que el documentador afirme "reemplazo total" en el manual de usuario.

### 2026-07-13 — implementador-dotnet (migración histórica ejecutada, sesión actual)
- Etapa: Implementación (CU-15, carga inicial de históricos) — ejecutada en el **entorno local** a pedido explícito del cliente ("ejecutar la migración en el entorno local. Borrar datos actuales y reemplazar").
- Cambio: se transformaron ambos Excel fuente (`Estado de Resultados KOI (Inversores).xlsx`, `Reparto de Utilidades Inversores.xlsx`) a la plantilla intermedia de 6 hojas que consume `ImportacionInicialService`, se vació la base de datos local (períodos/ventas/gastos/liquidaciones/asignaciones/inversores de demo — se preservaron los logins de prueba desvinculándolos de su inversor demo) y se ejecutó la importación real a través del endpoint `/ImportacionInicial` (rol SuperUsuario, flujo Validar→Importar, sin bypass de la transacción del servicio). Resultado: **19 períodos** (nov-2024 a may-2026), **19 ventas**, **373 líneas de gasto**, **15 inversores**, **16 asignaciones de puntos** (con segmentos de vigencia donde el Excel muestra cambios de puntos mes a mes), **265 liquidaciones históricas** — 0 advertencias, 0 errores.
- **Mapeo Rubro/Subgrupo**: el Excel tiene ~30 líneas de gasto ad-hoc por mes; el catálogo actual del sistema tiene 21 subgrupos (más simple, por diseño). Se mapeó cada línea del Excel a un subgrupo existente (ej.: Contador→Personal/Honorarios, Ingresos Brutos→Impuestos y Cargas/IIBB, Publicidad y Propaganda→Gastos Generales/Publicidad; el resto sin equivalente directo cae en Gastos Generales/Otros Gastos). El mapeo no afecta el total: se autovalidó línea por línea contra el "Total Gastos" que el propio Excel calculaba.
- **Hallazgo — inconsistencias reales del Excel entre meses (confirma R1 del análisis)**: la fórmula "Total Gastos" del Excel no es uniforme mes a mes — en algunos meses de 2025/2026 suma o no una sección "Gastos Extras" ad-hoc, y el rango de la fórmula "Servicios" excluye "Comisiones Tarjetas/PedidosYa" en varios meses de 2026 pero no en 2025. Se migró **respetando fielmente estas inconsistencias mes a mes** (parseando el rango real de cada fórmula por columna) en vez de normalizarlas — el sistema reproduce exactamente lo que el Excel mostraba cada mes, no una versión "corregida".
- **Validación**: Total Gastos de los 19 períodos reconcilia contra el "Total Gastos" propio del Excel con diferencia ≤ 0.01 (redondeo) en el 100% de los casos. Capital total de inversores migrado = USD 287.500,00, coincide exactamente con el dato del análisis funcional. Suma de Bruto de liquidaciones por período es sistemáticamente menor a la "Utilidad Total" de la hoja GENERAL, explicado en su totalidad por puntos sin inversor nombrado en el Excel (5 a 11 de 100 pts/mes) — no es un error, es fiel al Excel original. Verificado también visualmente (Dashboard, Reparto General) con datos reales tras el import.
- **Decisiones de migración documentadas** (ídem precedente de M14 para Salón/Delivery): VentasASalon=VentasA y VentasBSalon=VentasB del Excel (sin apertura Pedidos/Mostrador histórica); CantidadComensales y CantidadVentas en 0 (no trackeados en el Excel histórico); Consumos de liquidaciones históricas en 0 (el Excel no registra consumos por mes, solo un cálculo del mes corriente fuera de la tabla histórica); FechaCierre de cada período = fecha de pago de la hoja GENERAL para ese mes.
- Motivo: pedido directo del cliente, tras la auditoría de la sesión anterior que detectó que la migración nunca se había ejecutado.
- Impacto en capas: solo datos (vía el mecanismo de importación ya existente). Sin cambios de código, sin migración EF.
- Riesgos/supuestos: ejecutado y validado solo en el **entorno local** de desarrollo — replicar en el entorno del cliente cuando corresponda, con la misma plantilla (conservada en el historial de esta sesión) tras confirmar que la base de destino esté igualmente lista para el reemplazo. `docs/koi/definiciones/7-documentador.md` actualizado: ya puede afirmarse "reemplazo total" de los Excel en el manual de usuario.

### 2026-07-13 — implementador-dotnet (migraciones aplicadas en PRODUCCIÓN, sesión actual)
- Etapa: Implementación — despliegue de base de datos en producción, a pedido explícito del cliente ("aplicar migraciones del modelo y de datos en producción").
- **Alcance confirmado con el cliente antes de ejecutar** (dos preguntas explícitas): (1) "datos" = mismo histórico completo que en local, no solo el seed básico; (2) SuperUsuario de producción queda con las credenciales por defecto del código (`no-reply@olvidata.com.ar` / `Super123!`) por ahora — pendiente que el cliente las cambie antes de exponer el sitio públicamente.
- **Hallazgo previo (solo lectura, antes de tocar nada):** la base de producción (`db_a7251f_koidump` en `MYSQL5044.site4now.net`) estaba completamente vacía (0 tablas) — primer aprovisionamiento, sin riesgo de pisar datos reales del cliente.
- **Paso 1 — Modelo:** `dotnet ef database update` (proyecto `KoiDumplings.Infrastructure`, startup `KoiDumplings.Web`) contra la cadena de conexión de producción. 22 tablas creadas. El comando logueó una `HostAbortedException` que **no es un error real** — es el mecanismo interno de EF Core para resolver el host en tiempo de diseño; se confirmó por lectura directa que el esquema se creó correctamente.
- **Paso 2 — Seed básico:** se corrió la app localmente apuntando a producción (`ASPNETCORE_ENVIRONMENT=Production`, `AllowedHosts=*` vía variable de entorno para permitir el binding local sin tocar el `appsettings.Production.json` committeado) para disparar `SeedData.InitializeAsync` (roles, catálogo de Rubros/Subgrupos/ParametrosPorcentaje, SuperUsuario). **Incidente:** la conexión al hosting compartido remoto se interrumpió a mitad del seed de catálogo (`SeedRubrosAsync` hace un `SaveChangesAsync` por cada Rubro en un `foreach`, sin transacción envolvente) dejando 3 de 6 Rubros y 11 de 21 Subgrupos — y como el método tiene guard `if (Rubros.Any()) return;`, un reinicio simple no lo hubiera completado nunca. Se detectó por sondeo directo a la base, se limpiaron manualmente las filas parciales (Rubros/Subgrupos/ParametrosPorcentaje) y se reintentó: la segunda corrida sí completó los 6 Rubros / 21 Subgrupos / 8 ParametrosPorcentaje (con reintentos de lectura para confirmar, dado que cada escritura tarda 1-3s por la latencia del hosting).
- **Paso 3 — Datos históricos:** se ejecutó la importación real vía el endpoint `/ImportacionInicial` (SuperUsuario) con la misma plantilla ya validada localmente contra ambos Excel (ver entrada previa del 2026-07-13, "migración histórica ejecutada"). **Incidente:** el primer intento de clic en "Sí, importar" excedió el timeout de navegación del navegador de prueba (la transacción tarda en el hosting remoto) y el navegador se cerró, lo que canceló la request y provocó un rollback completo (0 filas). Se reintentó desacoplando la confirmación del sondeo: se disparó el submit y se sondeó la base cada 5s en vez de esperar la navegación del lado del cliente. La importación completó en ~50s (**HTTP devolvió 500 por timeout de respuesta, pero la transacción ya había hecho commit** — se confirmó con lectura directa, no es una inconsistencia de datos).
- **Resultado verificado en producción** (lectura directa + visual en Dashboard/Reparto General con el mismo período de muestra que en local): 19 períodos, 19 ventas, 373 conceptos de gasto, 15 inversores, 16 asignaciones, 265 liquidaciones, capital total USD 287.500,00 — **idéntico al resultado validado en local**.
- Motivo: pedido directo del cliente para desplegar el modelo y el histórico en el ambiente productivo.
- Impacto en capas: Datos únicamente (esquema vía EF + contenido vía el mecanismo de importación ya existente). Sin cambios de código.
- Riesgos/supuestos: **el hosting de MySQL en `site4now.net` mostró desconexiones intermitentes durante operaciones largas con muchos round-trips secuenciales** (confirmado dos veces, en el seed de catálogo y en el intento inicial de import) — a tener en cuenta para cualquier operación futura similar contra este mismo hosting (preferir bloques con pocas escrituras grandes en vez de muchas escrituras chicas secuenciales; considerar reintentos). Credenciales de SuperUsuario de producción quedaron en el valor por defecto del código — pendiente que el cliente las cambie antes de exponer el sitio públicamente (decisión explícita del cliente, no un descuido). No se tocó el `appsettings.Production.json` del repo (la cadena de conexión ya estaba committeada ahí desde antes de esta sesión).

### 2026-07-13 — documentador (manual de usuario completo, sesión actual)
- Etapa: Documentación (cliente) — entrega de `docs/koi/manual-usuario.md`, manual de usuario completo (no resumen de sprint), a pedido explícito del cliente y en formato `.md` (excepción al canal habitual de Google Doc).
- Cambio: manual cubre ingreso al sistema, roles, Dashboard, carga de Estado de Resultados, cierre de período/liquidaciones, Reparto General, Mi Inversión y Configuración, en base al alcance funcional aprobado (1-analista-funcional.md) y a la implementación real verificada en sesiones anteriores. Se excluyeron a pedido del cliente: Cámaras IP, Fichador/huellas y la integración automática con Ayres (los tres son candidatos a Etapa 2). Se agregó al final una sección de transparencia sobre el origen del histórico migrado (ya ejecutado y validado en la sesión anterior).
- Se creó en **producción** el usuario de prueba pedido por el cliente (`juani.skari@gmail.com`, rol Administrador) vía la pantalla real de Usuarios (no SQL directo, para respetar el hash de Identity), y se verificó login exitoso. Se confirmó que el hosting productivo ya está en línea sirviendo la aplicación real.
- Motivo: pedido directo del cliente para poder entender el uso completo del sistema.
- Impacto en capas: n/a (documento) + un usuario nuevo en producción (dato, sin cambio de código).
- Riesgos/supuestos: el manual no menciona el rol SuperUsuario ni las pantallas de uso interno (Auditoría, Sistema, Importación histórica), según la regla vigente de no exponer accesos internos del estudio en documentación entregada al cliente.

### 2026-06-24 — analista-funcional
- Etapa: Relevamiento Etapa 2
- Cambio: recepción del informe de relevamiento técnico de integraciones del local (Ayres, Fichador ZKTeco/QuickPass, Cámaras HikConnect). Las tres áreas quedan **fuera del alcance de la Etapa 1** y se definen como candidatos a **Etapa 2** (ver sección 10 del documento analista-funcional).
- Motivo: el cliente solicita que los ingresos mensuales se completen desde el sistema de ventas Ayres y que se agregue una pantalla de fichador de empleados. Se recibió informe técnico de Olvidata Soft con arquitectura relevada de cada módulo.
- Impacto en capas: ninguno en Etapa 1. El alcance base queda intacto.
- Riesgos/supuestos: múltiples pendientes de resolución (credenciales MySQL/API Ayres, token QuickPass, credenciales cámaras). Hasta que se resuelvan, no es posible estimar ni iniciar Etapa 2.

### 2026-08-10 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- Etapa: Análisis → Diseño → Arquitectura → Presupuesto del módulo E2-02 "Fichador de empleados" (registro de huellas/asistencia vía QuickPass v4 / hardware ZKTeco MB360).
- Cambio: a pedido explícito del dueño del estudio ("quiero avanzar con el módulo de registro de huellas de los empleados"), se corrió la cadena completa usando el relevamiento técnico ya hecho (§10.2 de 1-analista-funcional.md, 2026-06-24) como base de Discovery, sin repetir la visita. Cliente confirmó (mismo día) que **todavía no tiene** el token de API ni las credenciales admin de QuickPass.
  - **Análisis** (`1-analista-funcional.md` §11): alcance cerrado — pantalla única con 3 vistas (Hoy / Rango / Empleados), solo Administrador, sin persistencia local (decisión: QuickPass es fuente de verdad, volumen bajo). 3 CU formalizados (CU-E2-02a/b/c), criterios de aceptación, riesgo bloqueante documentado.
  - **Diseño** (`2-disenador-funcional.md` §10): escaneo cross-proyecto sin coincidencias — primer patrón de integración REST de solo lectura con Bearer token estático del estudio, documentado para reuso futuro (candidato directo para E2-01 Ayres). Pantalla P-15 con 3 tabs, `daterangepicker` + DataTable, 4 historias de usuario con criterios de aceptación.
  - **Arquitectura** (`3-arquitecto-mvc.md` §8): `IQuickPassService`/`QuickPassService` con `HttpClient` tipado vía `IHttpClientFactory`, Bearer token en configuración, sin entidades ni migración EF, `FichadorController` bajo policy `RequireAdministracion`. Gate de presupuesto aprobado.
  - **Presupuesto** (`4-presupuestador.md` §16): clasificado como Merge sobre sistema propio ya entregado (precio de lista, sin descuento de expansión agresiva). Referencia "Integración WS simple" 3.5 h base, ajustado a M 5.5 h (+57 %, justificado por 3 pantallas UI adicionales sobre la referencia). Riesgo Alto, contingencia 25 %. **USD 92** a comunicar al cliente. Tokens IA no aplica (horas facturables por debajo del piso de 4 h). Sin impacto en plan de mantenimiento (sin tablas nuevas).
- Motivo: pedido directo del dueño del estudio de dejar el módulo listo para implementar en cuanto el cliente entregue las credenciales, sin bloquear las etapas de definición por un pendiente que no depende de ellas.
- Impacto en capas: n/a en esta sesión (solo documentos — Análisis/Diseño/Arquitectura/Presupuesto, sin código).
- Riesgos/supuestos: **Implementación explícitamente en espera** hasta que el cliente entregue token de API + credenciales admin de QuickPass — gate declarado en las 4 definiciones. Presupuesto (USD 92) queda aprobable por el cliente independientemente de esa fecha. Gatillo de reestimación si la API real difiere del contrato relevado.

### 2026-08-10 — analista-funcional (relevamiento técnico E2-03, sesión actual)
- Etapa: Relevamiento / Discovery (módulo E2-03 — Cámaras). No llega a Análisis todavía: faltan datos concretos del NVR.
- Cambio: el cliente reportó una restricción de red no relevada en la visita del 2026-06-24 — el local tiene un NVR centralizado sobre una LAN aislada, sin URL directa alcanzable a las cámaras (solo acceso por cable dentro del local). Se investigó (fetch a la documentación oficial de soporte de Hikvision + research de arquitecturas de streaming) cómo integrar esto a la web:
  - Formato confirmado de URL RTSP de Hikvision: `rtsp://usuario:password@IP:puerto/Streaming/channels/<canal><stream>` (puerto 554 sin auth / 10554 con auth; sufijo 01=main, 02=substream).
  - RTSP no es reproducible nativamente en navegador — hace falta un gateway de protocolo. Arquitectura recomendada: NVR (LAN aislada) → gateway local siempre encendido corriendo **MediaMTX** (RTSP→HLS/WebRTC, open source, gratis) → **Cloudflare Tunnel** (conexión saliente desde el equipo del local, sin abrir puertos ni requerir IP pública — resuelve exactamente la restricción de red reportada) → pantalla P-11 de KOI reemplaza el iframe de HikConnect por un player nativo (`video.js`+`hls.js`).
  - Detalle completo, diagrama y tabla de pendientes actualizados en `1-analista-funcional.md` §10.3.
- Motivo: el cliente pidió investigar cómo integrar el streaming dado que no puede exponer las cámaras por URL directa; alguien le recomendó "hacerlo por stream" y aportó el artículo de soporte de Hikvision sobre RTSP como punto de partida.
- Impacto en capas: n/a en esta sesión (solo investigación/documento) — a futuro, si se aprueba, impacta Presentación (nuevo player en P-11) y opcionalmente Infraestructura si se decide correr algo del lado del servidor KOI (no es necesario con esta arquitectura: el server KOI solo embebe una URL pública, el procesamiento de video queda 100% en el local).
- Riesgos/supuestos: arquitectura no validada aún contra el NVR real (falta confirmar marca/modelo — se asumió Hikvision por continuidad con HikConnect, pero no está confirmado que el NVR sea de esa marca). Pendiente crítico nuevo: definir si hay equipo que quede prendido 24/7 en el local o si hay que sumar costo de hardware (mini PC). Sin este dato no se puede pasar a Análisis formal ni presupuestar.

**Actualización misma sesión:** el dueño del estudio confirmó que **ya existe un equipo prendido 24/7 en el local** (candidato: la PC de Ayres POS, a confirmar cuál puntualmente) — se reutiliza para correr MediaMTX + cloudflared, **sin costo de hardware adicional** para este módulo. Queda resuelto el pendiente de mayor impacto en presupuesto; siguen pendientes solo datos puntuales del NVR (marca/modelo, IP, credenciales, cantidad de cámaras) antes de poder pasar a Análisis formal.

### 2026-08-10 — orquestador → arquitecto-mvc (gate de Implementación del módulo E2-02 Fichador, sesión 2)
- Etapa: Arquitectura (corrección de contrato) → Implementación (delegada)
- Cambio: el cliente entregó el token real de QuickPass (`ApiKey`/`IdEmpresa`) y la documentación oficial completa (`InstructivoAPIEntidades-QuickPass.pdf` + `InstructivoAPIReportingQuickPass.pdf`, guardados en `KoiDumplings/manual fichador/`), desbloqueando el gate declarado en la sesión anterior. Al leer la documentación real se detectaron 2 diferencias respecto a lo arquitecturado en §8 (evaluadas en el nuevo §8.8 de `3-arquitecto-mvc.md`):
  1. Autenticación real es `ApiKey`+`IdEmpresa` por header estático (sin expiración) — no Bearer token como se había asumido.
  2. La API Reporting de QuickPass (`/HorasTrabajadas`, `/HorasTrabajadas/ResumenPorUsuario`) ya devuelve horas trabajadas/extras/tardanzas/ausencias calculadas — el sistema no necesita recalcularlas, solo mapear la respuesta.
  - Ambos cambios **simplifican** la implementación respecto a lo presupuestado (menos lógica propia) — se evaluó el gatillo de reestimación y se decidió **no ajustar el presupuesto** (USD 92 se mantiene). Se corrigieron `IQuickPassService`, el diseño de los dos `HttpClient` (Entidades + Reporting, bases URL distintas) y se documentó el detalle crítico de formato de fechas por endpoint (`yyyyMMdd` vs `yyyyMMddHHmm`, límite de 31 días en Reporting) para que el implementador no repita errores ya anticipados por la documentación de QuickPass.
  - Gate de Implementación (`no iniciar Implementacion sin Presupuesto aprobado por el cliente`) se considera desbloqueado: pedido explícito del dueño del estudio de implementar ahora.
- Motivo: pedido directo del dueño del estudio ("implementar funcionalidad con el /agentes-ia-orquestador"), tras conseguir las credenciales que estaban pendientes desde la sesión anterior.
- Impacto en capas: pendiente de ejecución — se delega a continuación al subagent `agentes-ia-implementador`.
- Riesgos/supuestos: la ApiKey/IdEmpresa del cliente (`idEmpresa=4364`) quedan en `appsettings.json` (no distinguen ambiente, a diferencia de la connection string de MySQL). Pendiente de implementación real: verificar contra la API viva que el mapeo de campos asumido en los DTOs coincide exactamente con la respuesta real (la documentación no detalla el schema completo de respuesta de `/HorasTrabajadas`).

### 2026-08-10 — implementador-dotnet (setup de ejecución local + deploy a producción, sesión 3)
- Etapa: Implementación (infraestructura de desarrollo local) + despliegue a producción del módulo E2-02.
- **Setup local (Ctrl+F5 desde VS Code):** se detectó que `KoiDumplings.Web` usaba los puertos por defecto de la plantilla compartida (`7200`/`5015`), que ya estaban ocupados por otro proyecto del estudio (`virtualwallet`) corriendo en la misma máquina — el arranque fallaba con `AddressInUseException`. Se reasignaron puertos propios para KoiDumplings: `https://localhost:7100` / `http://localhost:5100`, en `Properties/launchSettings.json` y `.vscode/launch.json`. Se corrigió además el bug conocido del template base (`10-blankproject-base.instructions.md`): el patrón de `serverReadyAction` en `launch.json` era ambiguo (`https?://`) y podía abrir el navegador en la URL HTTP, que redirige a `https://localhost` sin puerto (implica 443) y falla localmente — se forzó el patrón a exigir `https://` explícito. Verificado end-to-end: build limpio, arranque directo del `.dll` (mismo mecanismo que usa el debugger de VS Code, no `dotnet run`) con los headers/env de `launch.json`, `200 OK` en `https://localhost:7100/`. Proceso de prueba detenido al finalizar, puertos liberados.
- **Deploy a producción:** confirmado que no había migraciones EF pendientes (`dotnet ef migrations list` contra `db_a7251f_koidump` — todas las migraciones existentes ya aplicadas, ninguna nueva por este módulo, consistente con lo declarado en Arquitectura §8.5). Se ejecutó `dotnet publish` con el perfil `olvidatasoft-002-site15 - Web Deploy` (MSDeploy) — 13 archivos actualizados, incluye el nuevo `appsettings.json` con la sección `QuickPass` (credenciales reales del cliente) y los binarios con el módulo Fichador. Publicación exitosa, verificado `200 OK` en el home y en `/Fichador` (redirige a login, esperado) del sitio productivo.
- Motivo: pedido directo del dueño del estudio — primero habilitar el flujo de desarrollo local (Ctrl+F5) para poder probar, confirmado que "funciona correctamente", y luego desplegar el módulo aprobado a producción.
- Impacto en capas: Web (`launchSettings.json`, `.vscode/launch.json` — solo local, no versionado/no afecta producción) + despliegue de binarios y configuración (sin cambios de esquema).
- Riesgos/supuestos: el cambio de puertos es solo de desarrollo local (`.vscode/` está en `.gitignore`, `launchSettings.json` sí es parte del repo pero no afecta producción — IIS/hosting usa su propio binding). Pendiente: prueba manual real del dueño del estudio en producción (login Admin, los 3 tabs de Fichador contra datos reales de QuickPass) — no fue ejecutada por este agente (regla del estudio, sin smoke test funcional propio).

### 2026-08-10 — implementador (Etapa 12 — Fichador de empleados / QuickPass, implementación completa)
- Etapa: Implementación (módulo E2-02)
- Cambio: implementado el módulo Fichador completo (P-15) sobre el contrato ya corregido en `3-arquitecto-mvc.md` §8.8.
  - **Domain**: sin cambios — sin entidades ni migración EF (confirmado por Análisis/Diseño/Arquitectura).
  - **Application**: `IQuickPassService` (`Application/Interfaces/`), DTOs `HoraTrabajadaDiaDto`/`ResumenHorasUsuarioDto`/`EmpleadoQuickPassDto` (`Application/DTOs/QuickPassDtos.cs`), excepción de dominio `QuickPassIndisponibleException` (`Application/Exceptions/`, carpeta nueva — primera excepción de dominio propia del proyecto).
  - **Infrastructure**: `QuickPassService` (`Infrastructure/Services/`) con dos `HttpClient` nombrados (`QuickPassEntidades` / `QuickPassReporting`) registrados vía `IHttpClientFactory` en `DependencyInjection.cs` (no en `Program.cs` — a diferencia de `CotizacionService`/`ArgentinaDatos`/`DolarApi`, que sí headers/URLs se resuelven a partir de `IOptions<QuickPassSettings>` para poder usar `configuration.GetSection("QuickPass")` ya disponible en `AddInfrastructure`, igual que el patrón de `SmtpSettings`). `QuickPassSettings` nueva en `Infrastructure/Services/`. Sección `QuickPass` agregada a `appsettings.json` con las credenciales reales entregadas por el cliente (ApiKey/IdEmpresa no distinguen ambiente, mismo criterio documentado en arquitectura).
  - **Web**: `FichadorController` (`[Authorize(Policy = "SoloAdministrador")]` — se usó esta policy, no `RequireAdministracion` como decía el texto de arquitectura, porque es la que efectivamente usan `InversoresController`/`ConfiguracionController` citados como precedente; ambas policies son funcionalmente idénticas en `Program.cs`, `RequireRole(SuperUsuario, Administrador)`), 3 acciones (`Index`/`Rango`/`Empleados`) que renderizan la misma vista `Views/Fichador/Index.cshtml` con 3 tabs Bootstrap (Hoy/Rango/Empleados), `daterangepicker` en el tab Rango (patrón exacto de `copilot-instructions.md`), 3 DataTables client-side. ViewModels en `Web/Models/FichadorViewModels.cs`. Link "Fichador" agregado al sidebar (`Views/Shared/_Layout.cshtml`, sección "Gestión", después de "Tipo de Cambio").
  - **Schema real de `/HorasTrabajadas` y `/HorasTrabajadas/ResumenPorUsuario` descubierto por inspección directa contra la API viva** (no publicado en el instructivo — se hizo `curl` de prueba con las credenciales reales del cliente, dato autorizado explícitamente por el pedido, y se borraron los archivos temporales con PII de empleados apenas terminó la inspección):
    - `/Usuarios?excluirFotos=true`: array plano, campos relevantes `id, nombre, apellido, legajo, email, habilitado, fechaIngreso ("yyyyMMdd"), sector.{id,nombre}`. 13 empleados activos en el ambiente real del cliente al momento de la prueba.
    - `/HorasTrabajadas`: objeto por usuario×día con ~70 campos (`ParteParaReporteDTO`); los campos usados son `idUsuario, nombreUsuario, legajo, nombreSector, fecha (ISO), tieneMarcaciones, impar, ausente, tarde, esSectorBajas, horasNetas, movimientosList (array de "HH:mm"), idsMovimientos (array de ids)`. Hallazgo clave: el campo `"impar"` (booleano) YA es el flag de "turno abierto/incompleto" calculado por QuickPass — cantidad impar de marcaciones en el día (ej. solo entrada, sin salida). `movimientosList[0]` = entrada, `movimientosList[^1]` = salida solo si `!impar` y hay ≥2 marcaciones. `esSectorBajas=true` marca empleados dados de baja — se filtran del tab "Hoy" para mostrar solo "empleados activos" (criterio de HU-1). Verificado en vivo: 13 registros con `esSectorBajas=false` para "hoy" coinciden exactamente con los 13 usuarios de `/Usuarios`.
    - `/HorasTrabajadas/ResumenPorUsuario`: un objeto por usuario con totales ya calculados — `idUsuario, nombreUsuario, legajo, horasTrabajadasNetasDecimal, horasTrabajadasNetas ("H:MM" ya formateado), cantidadDiasTrabajados, cantidadDiasAusente, cantidadDiasTarde`. **No trae** un conteo de "turnos incompletos" ni de "cantidad de fichadas" — no está en el contrato real pese a que el diseño (`FichadorRangoVM`) lo pedía. Decisión tomada (no estaba en ninguna de las 4 definiciones, documentada acá porque es un hallazgo de implementación): `QuickPassService.ObtenerResumenPorRangoAsync` hace una segunda llamada en paralelo a `/HorasTrabajadas` para el mismo rango y **agrega** (cuenta) el campo `impar` y el tamaño de `idsMovimientos` por usuario — es una agregación de valores ya calculados por la API, no un recálculo de horas, así que no contradice la simplificación de §8.2/§8.8 ("el sistema no recalcula, solo proyecta").
  - **Decisiones técnicas no explícitas en las 4 definiciones, tomadas en implementación:**
    1. "Hoy" se calcula en huso horario Argentina explícito (`TimeZoneInfo.FindSystemTimeZoneById("America/Buenos_Aires")`), mismo patrón ya usado por `CotizacionService.HoyArgentina()` — evita que "hoy" quede mal calculado si el hosting corre en otro huso horario (el hosting de KOI en producción es site4now.net, EE.UU.).
    2. Las 3 acciones del controller (`Index`/`Rango`/`Empleados`) siempre cargan las 3 secciones (Hoy + Empleados eager, Rango solo si se pasaron fechas) y renderizan la misma vista — evita necesitar AJAX/partials; cada sección tiene su propio try/catch independiente contra `QuickPassIndisponibleException`, así que si una falla las otras dos igual muestran datos (nunca pantalla en blanco completa).
    3. Validación de rango máximo 31 días duplicada en Controller (antes de llamar al service, con mensaje SweetAlert2-friendly) y en `QuickPassService` (`ArgumentException` como red de seguridad) — mismo criterio que 32-estandares-qa-implementador.md para validaciones numéricas de negocio (cliente + servidor).
  - **Migración EF**: ninguna — confirmado.
  - **Build**: `dotnet build` desde la raíz → **Compilación correcta, 0 errores**. 9 warnings preexistentes sin cambios (NU1902 MailKit/MimeKit, CS0114 HomeController.StatusCode) — ninguno introducido por este cambio.
  - **Sin smoke test funcional** (regla del estudio) — evidencia de cierre es build limpio + revisión de código propia. Guía de pasos manuales para el dueño del estudio dejada en `5-implementador.md` (Etapa 12).
- Motivo: pedido directo del dueño del estudio de implementar el módulo ya con las credenciales reales entregadas por el cliente.
- Impacto en capas: Application (interfaz + DTOs + excepción nuevos), Infrastructure (servicio + settings + registro DI + appsettings), Web (controller + viewmodels + vista + sidebar). Domain sin cambios.
- Riesgos/supuestos: el volumen de datos de prueba en el ambiente real del cliente es bajo (13 empleados) — el comportamiento con más empleados/sectores no fue validado (no aplica smoke test, y de todas formas la API pagina/filtra igual sin importar el volumen). Pendiente de validación manual por el dueño del estudio: turno abierto real (se validó `impar=true` con un caso real de un empleado con una sola fichada del día), rango con `TurnosIncompletos`/`CantidadFichadas` agregados (no vienen directo de `ResumenPorUsuario`, se calculan por agregación de `/HorasTrabajadas` — riesgo de leve divergencia si QuickPass define "incompleto" con otro criterio distinto de "cantidad impar de marcaciones"). Se usó la policy `SoloAdministrador` en vez de `RequireAdministracion` (ambas equivalentes en `Program.cs`) — documentado para que QA no lo marque como desvío de arquitectura sin motivo.

### 2026-08-13 - implementador
- Etapa: Implementación (fixes post-QA, sin ciclo completo — bugs con causa raíz ya identificada por el pedido)
- Cambio: 4 fixes puntuales de QA manual del dueño del estudio sobre la Etapa 13: (1) `DashboardService.ObtenerPeriodosAsync()` filtra `EstadoPeriodo.Cerrado` y `DashboardController.Index` ajusta el fallback de período por defecto al cierre más reciente (antes exponía meses abiertos/futuros y caía a `DateTime.Today` como fallback); (2) `EstadoResultados/Anual.cshtml` ordenaba la tabla `#tablaAnual` alfabéticamente por texto de período — se agregó `data-order` numérico (`AAAAMM`) y se cambió a `order: [[0,'desc']]` con `columnDefs` numérico explícito; (3) rename "Dashboard Histórico" → "Rendimiento Histórico" en el sidebar (`_Layout.cshtml`) y en `ViewData["Title"]` condicional por rol en `Dashboard/Index.cshtml`; (4) texto invisible en el popup nativo de `<option>` del combo de Rol en `Notifications/Crear.cshtml` — regla global `.form-select option { color/background }` agregada en `olvidata-theme.css`, fuera de cualquier bloque de tema, para cubrir cualquier select del sistema con el mismo riesgo.
- Motivo: pedido directo del dueño del estudio tras QA manual del sprint recién implementado (Etapa 13).
- Impacto en capas: Infrastructure (`DashboardService`), Web (`DashboardController`, 2 vistas, CSS del design system compartido). Sin Domain, sin Application, sin migración EF.
- Riesgos/supuestos: fix 4 es una regla CSS global (`.form-select option`) — se revisó que no hay otro select del sistema que dependiera de heredar el color oscuro del popup nativo (no había ninguno documentado); fix 1 se limita al único caller confirmado por grep (`DashboardController.Index`), sin impacto en la carga de datos del mes en curso (vive en otro controller). Build limpio, sin smoke test funcional (regla del estudio) — detalle completo y guía de prueba manual en `5-implementador.md` (Etapa 14).

### 2026-08-12 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- Etapa: Análisis → Diseño → Arquitectura → Presupuesto de un sprint de 9 ítems sobre el sistema en producción, pedido directo del dueño del estudio vía `/agentes-ia-orquestador`.
- **Ambigüedades resueltas en el chat antes de cerrar Análisis** (regla del estudio: no adivinar sobre un sistema en producción con datos reales de inversores):
  1. Alcance por rol: los cambios de Dashboard/Mi Inversión aplican **solo al rol Inversor** — el Administrador conserva su Dashboard actual sin cambios (hoy es una sola pantalla compartida).
  2. Estructura final de pantallas del Inversor: se eligieron **3 pantallas** (no 2) — "Mes actual" (nueva, KPIs del mes sin filtro), "Dashboard Histórico" (relabel del Dashboard actual, mismo contenido) y "Mi Inversión" (se mantiene igual que hoy, sin absorber contenido del Dashboard viejo).
  3. Rol nuevo para el fichador: se eligió crear **"Encargado"** como rol Identity nuevo, en vez de reutilizar `Empleado`/`Vendedor` (ya sembrados pero sin uso).
- **Hallazgo crítico durante la investigación del pedido "sacar el registro donde wang tiene 7 puntos"** (consulta de solo lectura contra producción, sin tocar nada): Wang (Minjo Wang, InversorId=1) tiene 2 filas en `asignacionpuntos` — 7 puntos vigente desde nov-2024, 8 puntos vigente desde abr-2025 (`Observaciones: "Cambio de puntos detectado en la migracion (antes: 7.0)"`) — **es historial real, no una carga duplicada**. El bug real está en `InversionesService.AsignacionesVigentesQuery` (`InversionesService.cs:303-312`): filtra `VigenteDesde <= período` pero NO deduplica por inversor (a diferencia de `EstadoResultadosService`, que sí lo hace correctamente en dos lugares con `GroupBy`+`OrderByDescending().First()`) — por eso Wang se cuenta dos veces (7+8=15) para cualquier período desde abril 2025, e infla el total de la pantalla de Puntos a 102 en vez de 95. Verificado con queries reales: suma de la vigencia más reciente por cada uno de los 15 inversores = **95.00**, exactamente lo que el dueño del estudio esperaba ver. **Se decidió NO borrar ninguna fila de producción** — el fix es de código (aplicar la misma deduplicación que ya usa `EstadoResultadosService`), no de datos. Confirmado además que ninguna liquidación histórica está mal calculada (la generación de liquidaciones usa la query correcta desde siempre).
- **Análisis** (`1-analista-funcional.md` §12): 9 ítems cerrados con criterios de aceptación — pantalla "Mes actual" (Inversor), relabel "Dashboard Histórico" (Inversor), rol "Encargado", fix del bug de puntos, simplificación de Reparto General, rename "Historial de Resultados", módulo de composición de Notificaciones (por rol + email/in-app), remoción de la columna Puntos y reformateo de la tabla de historial en Mi Inversión, fix global de importes cortados por el signo "$".
- **Diseño** (`2-disenador-funcional.md` §11): sin coincidencias cross-proyecto (reutilización 100% interna a KOI). Wireframe de P-16 "Mes actual", relabel condicional de sidebar, wireframe de P-17 "Notificaciones" (combo rol + chips removibles + 2 checkboxes), especificación exacta de la tabla `#tablaHistorial` de Mi Inversión (antes/después de columnas), regla de clase CSS `.ov-monto` para el fix global.
- **Arquitectura** (`3-arquitecto-mvc.md` §9): sin migración EF en ningún ítem. Fix de código detallado línea por línea para `AsignacionesVigentesQuery`. Impacto por capa de los 9 ítems, incluyendo la policy nueva/condicional para el rol "Encargado" y el diseño de servicio para Notificaciones (reutiliza `INotificationService`+`IEmailService` sin agregar entidades). Regla nueva propuesta para documentar en `Agentes-IA/.github/instructions/` sobre `white-space: nowrap` en celdas de importe — mismo patrón que la generalización del fix de tema oscuro (KOI Etapa 9).
- **Presupuesto** (`4-presupuestador.md` §17): 9 ítems, M total 8.8h, clasificado como Merge sobre sistema propio (precio de lista, sin descuento de expansión agresiva). **USD 185** (148 desarrollo + 37 tokens IA — a diferencia del Fichador, acá SÍ aplica el cargo de tokens IA porque las horas facturables del lote, 4.22h, superan el piso de 4h). Sanity check contra vinosefue/labipac (mismo patrón de "lote de ítems chicos evolutivo").
- Motivo: pedido directo del dueño del estudio, mensaje único con 10 cambios (uno de ellos, originalmente descripto como corrección de datos, resultó ser un bug de código tras la investigación).
- Impacto en capas: documentos únicamente en esta sesión — **Implementación NO delegada todavía**, queda pendiente de confirmación explícita del dueño del estudio (gate estándar: no iniciar Implementación sin presupuesto aprobado, y varios ítems tocan pantallas de producción con inversores reales).
- Riesgos/supuestos: ítem 7 (Notificaciones) es el único de riesgo Medio. El fix del ítem 4 ya fue validado con queries reales contra producción antes de presupuestar (no hace falta re-validar el diagnóstico en Implementación, solo aplicar el fix de código). Pendiente: confirmar con el cliente si además de esto quiere dar de baja o mantener HikConnect como redundancia (nota suelta de E2-03, no forma parte de este sprint).

### 2026-08-12 — implementador
- Etapa: Implementación — Etapa 13, "Sprint UX/UI Inversor + fixes" (9 ítems), sobre definiciones 1/2/3 §12/§11/§9 ya cerradas. Escaneo de reutilización re-confirmado: sin match cross-proyecto (todo reutiliza servicios propios de KOI, según Diseño §11.0) — no se copió código de otro proyecto del estudio.
- **Ítem 4 (fix Wang)**: `InversionesService.AsignacionesVigentesQuery` (privado, devolvía `IQueryable<AsignacionPuntos>`) reemplazado por `AsignacionesVigentesAsync` (`Task<List<AsignacionPuntos>>`), aplicando el mismo patrón que `EstadoResultadosService` — `Where(Inversor.DeletedAt == null)` + materializar con `ToListAsync()` + `GroupBy(InversorId)` + `OrderByDescending(VigenteDesdeAnio).ThenByDescending(VigenteDesdesMes).First()` (necesario materializar antes de agrupar porque EF Core no traduce bien `GroupBy`+`OrderByDescending().First()` sobre la entidad completa). Único caller (`ObtenerResumenPuntosAsync`) actualizado a `await AsignacionesVigentesAsync(anio, mes)`. **Verificación de la lógica** (no smoke test de la app, regla del estudio): se armó un proyecto console aislado (`scratchpad/wangfix`) que reproduce la MISMA lógica LINQ (GroupBy + OrderByDescending + First) contra datos sintéticos calcados del escenario real (14 inversores de una sola vigencia + Wang con dos vigencias, 7 pts desde 2024-01 y 8 pts desde 2025-04) — resultado: antes del fix 16 filas / TotalAsignado=102 / Wang aparece 2 veces; después del fix 15 inversores / TotalAsignado=**95** / Wang aparece 1 sola vez con **8** puntos (vigencia 2025/4) — coincide exactamente con lo validado manualmente contra producción por el orquestador antes de presupuestar. **No se tocó ninguna fila de `asignacionpuntos` en producción.**
- **Ítem 3 (rol Encargado)**: `SeedData.cs` — constante `RolEncargado = "Encargado"` agregada al array de roles sembrados. `Program.cs` — se creó una policy nueva `SoloAdministracionOFichador` (`RequireRole(SuperUsuario, Administrador, Encargado)`) en vez de agregar `Encargado` directo a `SoloAdministrador` como sugería literalmente el texto de arquitectura, porque se verificó por grep que `SoloAdministrador` la usan 7 controllers (`FichadorController`, `CamarasController`, `InversoresController`, `EstadoResultadosController`, `UsersController`, `TipoCambioController`, `ConfiguracionController`) — agregarla ahí habría abierto los otros 6 al rol nuevo, violando el criterio de aceptación ("cualquier otra URL le devuelve acceso denegado"). `FichadorController` es el único que usa la policy nueva. Sidebar: bloque `@if (User.IsInRole("Encargado"))` al principio, excluyente con el resto (un Encargado nunca evalúa los demás `@if` de sección, así que no ve headers vacíos).
- **Ítems 1/2 (Mes actual / Dashboard Histórico)**: controller nuevo `MesActualController` (`[Authorize]` simple, igual que `DashboardController` — la restricción es de navegación vía sidebar, no de datos), reutiliza `IIndicadoresService.ObtenerAsync(anio, mes)` tal cual (mismo cálculo que ya usa el Dashboard actual, sin duplicar query). Mes/año actual calculado con `TimeZoneInfo.ConvertTimeFromUtc(DateTime.UtcNow, "America/Buenos_Aires")`, mismo patrón que `CotizacionService.HoyArgentina()` y `FichadorController`. Vista nueva `Views/MesActual/Index.cshtml`: 3 KPIs (Ventas Totales, Ticket Promedio, Cant. de Tickets) + torta Salón/Pedidos/Mostrador (Chart.js, mismo estilo que el doughnut del Dashboard) — se usó el término "Pedidos" (no "Delivery") porque es el nombre real del campo en `IndicadoresDto`/`VentaMensual` y el que ya usa el Dashboard actual, para no crear dos términos para lo mismo. Sidebar reestructurado: el link superior "Dashboard" (antes incondicional "todos los roles") ahora es condicional — Admin/SuperUsuario ven "Dashboard", Inversor ve "Mes actual" (nuevo controller), cualquier otro rol autenticado conserva "Dashboard" (sin regresión para Vendedor/Empleado, fuera de alcance del sprint). El bloque "Mi Cuenta" (solo Inversor) gana un link nuevo "Dashboard Histórico" apuntando al mismo `Dashboard/Index` de siempre — mismo controller/vista/URL, sin tocar su contenido.
- **Ítem 6 (rename)**: `Views/EstadoResultados/Anual.cshtml` — `ViewData["Title"]` y el `<h3>` visible pasan de "Estado de Resultados {año}" a "Historial de Resultados {año}". `_Layout.cshtml` — el texto "Vista Anual ER" cambiado a "Historial de Resultados" en los dos bloques donde aparecía (Admin/SuperUsuario e Inversor).
- **Ítem 5 (Reparto General)**: `RepartoGeneralController.Index` ya no arma `NombresInversores`; la propiedad se sacó de `RepartoGeneralViewModel` (quedaba sin uso). Vista `Views/RepartoGeneral/Index.cshtml`: se eliminó el `@foreach` que generaba una columna por inversor — quedan Período/Ventas/Resultado/Util. Punto/Util. Punto USD/Estado. El desglose individual por inversor (`RepartoFilaDto.Inversores`) NO se tocó a nivel de datos — sigue disponible para Liquidaciones/Mi Inversión, solo se dejó de renderizar acá.
- **Ítems 8/9 (Mi Inversión)**: `Views/MiInversion/Index.cshtml` — tabla `#tablaHistorial` sin columnas Puntos/TC, columna Período dividida en Año+Mes al principio (usa `MiInversionFilaDto.Anio`/`.Mes`, ya expuestos — sin tocar Application/Infrastructure). Mes en palabras capitalizado vía helper nuevo `KoiDumplings.Web.Helpers.FormatoFecha.NombreMes(int)` (`CultureInfo("es-AR").GetMonthName` devuelve minúscula por defecto en ICU — el helper capitaliza la primera letra; reutilizado también en `MesActualController`). DataTable con `ordering: false` (se saca el click-to-sort de toda la tabla, no solo se fija un `order`). El orden Año desc/Mes desc ya lo entregaba `InversionesService.ObtenerMiInversionAsync` sin cambios (el `OrderByDescending(Anio).ThenByDescending(Mes)` ya existía) — no hizo falta tocar el servicio.
- **Ítem 7 (Notificaciones)**: nuevo `INotificacionAdminService`/`NotificacionAdminService` (Application/Infrastructure, registrado en `DependencyInjection.cs`) que orquesta — no reimplementa — `INotificationService.CreateAsync` e `IEmailService.SendEmailAsync`; un fallo de email de un destinatario no aborta el resto (mismo criterio que `NotificacionCierreService`, capturado por destinatario). `NotificationsController` extendido con `Crear` (GET), `UsuariosPorRol` (GET AJAX, `UserManager.GetUsersInRoleAsync` filtrado a usuarios `Activo`) y `Enviar` (POST) — las 3 acciones nuevas con `[Authorize(Policy = "SoloAdministrador")]` puntual (el resto del controller, bandeja personal, sigue `[Authorize]` simple). Vista nueva `Views/Notifications/Crear.cshtml`: combo de rol (6 roles reales de `SeedData`) → AJAX trae candidatos → chips removibles con hidden inputs `DestinatarioUserIds` → 2 checkboxes independientes (correo/in-app, al menos uno obligatorio, validado en cliente y servidor) → confirmación SweetAlert2 (patrón `Swal.fire` con `showCancelButton`, no `btn-swal-confirm` porque el submit necesita juntar validaciones de JS primero) → resumen post-envío vía `TempData["SuccessMessage"]` con detalle de fallos de email si los hubo. Link persistente "Notificaciones" agregado al sidebar para todos los roles autenticados (bandeja personal, ya existía la funcionalidad, solo faltaba el link) + link "Nueva Notificación" adicional solo para Admin/SuperUsuario (en sección "Gestión" y como botón en `Notifications/Index`) — diferenciado explícitamente en la UI entre "mis notificaciones" y "crear notificación", más un `alert-info` aclaratorio en la pantalla de composición.
- **Ítem 10 (fix `.ov-monto`)**: clase `.ov-monto { white-space: nowrap; }` agregada a `wwwroot/css/olvidata-theme.css` (sección Utilities) con comentario que referencia la regla nueva de `25-frontend-design-system.instructions.md`. Aplicada a mano en `<td>`/`<span>`/`<div>`/`<strong>` de importe en Dashboard (32 celdas vía regex dirigido + 1 caso multilínea a mano), Liquidaciones (4 celdas), RepartoGeneral (4 celdas), MiInversión (tabla + 2 cards de resumen), MesActual (KPIs nuevos), EstadoResultados Mensual/Anual/PreviewCierre (celdas con prefijo "USD"/"TC:" que sí tienen el patrón símbolo+espacio+número — las celdas de esas 3 vistas que muestran solo un número sin símbolo, ej. `N2` puro, se dejaron sin tocar por no tener el patrón exacto del bug reportado). Puntos/Index.cshtml revisada y descartada — no tiene celdas de importe monetario (solo cantidades de puntos y porcentajes, sin signo "$"/"U$D").
- **Migración EF**: ninguna en los 9 ítems — confirmado, coincide con Arquitectura §9.1.
- **Build**: `dotnet build` desde la raíz → **Compilación correcta, 0 errores**. 9 warnings preexistentes sin cambios (NU1902 MailKit/MimeKit, CS0114 HomeController.StatusCode) — ninguno introducido por esta etapa.
- **Sin smoke test funcional** (regla del estudio) — evidencia de cierre: build limpio + revisión de código propia + verificación aislada de la lógica del fix Wang (detallada arriba). Guía de pasos manuales para el dueño del estudio dejada en `5-implementador.md` (Etapa 13).
- Motivo: pedido directo del dueño del estudio, presupuesto USD 185 ya aprobado (ver entrada anterior, 2026-08-12 orquestador).
- Impacto en capas: Application (1 interfaz + DTOs nuevos para Notificaciones), Infrastructure (fix de query en `InversionesService`, servicio nuevo `NotificacionAdminService`, registro DI, seed de rol), Web (2 controllers nuevos/extendidos, 2 ViewModels nuevos, 1 vista nueva completa + 1 vista de composición nueva, `_Layout.cshtml` reestructurado, 8 vistas existentes con ajustes puntuales, 1 helper nuevo, 1 policy nueva en `Program.cs`, CSS del design system). Domain sin cambios.
- Riesgos/supuestos: la policy `SoloAdministracionOFichador` es una decisión de implementación no explícita en Arquitectura (que dejaba ambas opciones abiertas) — documentada acá para que QA no la marque como desvío. El link "Notificaciones" visible a todos los roles autenticados (incluye Inversor) es una decisión de implementación (Diseño/Análisis dejaban "decidí vos" explícito) — el Encargado NO lo ve porque su bloque de sidebar es excluyente con el resto. Pendiente de prueba manual del dueño del estudio (guía completa en `5-implementador.md`).

### 2026-08-13 — implementador-dotnet (commit + deploy a producción, sprint UX/UI Inversor + fixes + Etapa 14)
- Etapa: cierre de Implementación — commit y despliegue a producción del sprint de 9 ítems (Etapa 13) + la ronda de 4 fixes post-QA (Etapa 14).
- **Commit**: el dueño del estudio commiteó él mismo desde VS Code (`8397419 "mejoras"`, 2026-08-13 14:37, sobre `339882b "fichador"` del 2026-08-10) — 29 archivos, 1039 inserciones/237 eliminaciones, incluye ambas etapas completas. Verificado con `git show --stat` antes de continuar: no hizo falta un commit adicional, working tree limpio.
- **Migraciones EF**: verificado `dotnet ef migrations list` contra `db_a7251f_koidump` — mismas 7 migraciones de siempre, ninguna pendiente (coincide con lo declarado: el sprint completo no requería esquema nuevo).
- **Deploy**: `dotnet publish` con el perfil `olvidatasoft-002-site15 - Web Deploy` — 10 archivos actualizados (binarios + `web.config`; no incluyó `appsettings.json` esta vez porque no cambió desde el deploy anterior). Publicación exitosa. Verificado `200 OK` en home, `/Fichador` y `/Dashboard` (ambos sin sesión, redirigen a login como corresponde) del sitio productivo.
- Motivo: pedido directo del dueño del estudio ("commitear y deployar"), tras validar en local con Ctrl+F5 los 9 ítems + los 4 fixes.
- Impacto en capas: despliegue de binarios únicamente, sin cambios de esquema ni de configuración.
- Riesgos/supuestos: pendiente que el dueño del estudio confirme en producción los mismos pasos de prueba manual ya validados en local (guías en `5-implementador.md` Etapas 13 y 14) — especialmente el fix de puntos (Wang) y el filtro de períodos cerrados del Dashboard, por ser los dos ítems con mayor impacto en datos/negocio visibles para los inversores reales.

### 2026-08-13 — implementador-dotnet (fix puntual + commit + deploy, sesión 4)
- Etapa: fix de QA sobre la Etapa 14 — el combo de rol en "Nueva Notificación" quedaba con la regla `.form-select option { color: negro; background: blanco }` fija (fix de la sesión anterior), sin distinguir tema. El dueño del estudio pidió que combine con el tema activo (negro en claro, blanco en oscuro), no un color fijo.
- Cambio: `KoiDumplings.Web/wwwroot/css/olvidata-theme.css` — se agregó `[data-theme="dark"] .form-select option { color: var(--ov-text); background-color: var(--ov-surface); }` junto a la regla existente (que queda como default de tema claro). 1 archivo, 9 líneas.
- Build: `dotnet build` → 0 errores. Commit propio (`a79e116`, ya que este fix se hizo directo en esta sesión sin pasar por el subagent implementador dado el tamaño mínimo del cambio). Deploy: `dotnet publish` Web Deploy → 14 archivos actualizados (incluye el CSS y sus variantes `.br`/`.gz` comprimidas). Verificado `200 OK` en producción.
- Motivo: pedido directo del dueño del estudio ("arreglar y deployar").
- Impacto en capas: solo CSS (Presentación), sin cambios de código C#.
- Riesgos/supuestos: el soporte de `background-color` en el popup nativo de `<option>` varía por navegador (ver nota ya documentada en la sesión del fix original) — funciona en Chrome/Edge (navegadores objetivo del proyecto), no garantizado en todos.

### 2026-08-13 — implementador-dotnet (fix chip destinatarios, sesión 5)
- Etapa: fix de QA — el dueño del estudio reportó que el chip de un destinatario ya seleccionado (ej. "Minjo Wang") seguía ilegible tras el fix anterior, y pegó el HTML exacto del chip.
- **Causa raíz real (distinta del fix anterior):** el chip usaba la clase Bootstrap `text-bg-light`, que es una utilidad de **Bootstrap 5.3+** — este proyecto usa **Bootstrap 5.1.0** (mismo dato ya documentado en la memoria del proyecto por el bug de tema oscuro de Etapa 9). La clase no existe en 5.1: no aplicaba ningún color, y el `<span>` quedaba heredando el `color` del tema activo (`--ov-text`, casi blanco en dark mode) sobre un fondo sin estilo propio → texto invisible. No tiene relación con el fix anterior del `<select>` de rol (que sí estaba bien resuelto).
- Cambio: `wwwroot/css/olvidata-theme.css` — nueva clase `.ov-chip` (theme-aware: gris claro/texto oscuro en claro, `--ov-surface`/`--ov-text` en oscuro). `Views/Notifications/Crear.cshtml` — el JS que arma el chip cambia `text-bg-light` por `ov-chip`.
- Build 0 errores. Commit `8105be6`. Deploy Web Deploy → 14 archivos. Verificado `200 OK`.
- Motivo: pedido directo del dueño del estudio, con evidencia (HTML pegado) de que el fix anterior no cubría este elemento.
- Impacto en capas: solo CSS + 1 línea de JS en la vista (Presentación).
- Riesgos/supuestos: vale la pena, a futuro, barrer el resto del proyecto por otras clases `text-bg-*`/utilities de Bootstrap 5.3+ que puedan haberse colado — en esta sesión se verificó que `Crear.cshtml` era el único uso de `text-bg-` en todo `KoiDumplings.Web`, pero no se hizo un barrido exhaustivo de TODAS las utilidades nuevas de 5.3 (solo de esta clase puntual).

### 2026-08-13 — arquitecto-infra + implementador (dominio nuevo portaldelinversor.com.ar/koi, sesión 6)
- Etapa: infraestructura — el cliente pidió mover el acceso público de KOI a `https://portaldelinversor.com.ar/koi/` (dominio nuevo, ya comprado), con el path `/koi/` pensado como slug reutilizable si en el futuro se suman otros locales.
- **Consulta a `olvidata-infra`** (agente de arquitectura del estudio): recomendó no resolver "multi-tenant por path" todavía — confirmado con el cliente que **es solo para KOI por ahora** (no hay segundo cliente comprometido). Dos arquitecturas posibles a futuro si aparece un segundo local: (A) multi-instancia por path — cada local sigue siendo una app/BD independiente, solo cambia que vive bajo un path del dominio compartido en vez de un subdominio propio (recomendada, reutiliza todo lo que el estudio ya sabe operar); (B) multi-tenant real (una sola app/BD con `tenant_id`) — es un rediseño de producto, no de infra, solo evaluable si `olvidata-ceo` confirma modelo de negocio real detrás. Ninguna de las dos se construye ahora.
- **Verificación con el cliente en el panel de hosting (SmarterASP/site4now):** el panel SÍ permite crear una Virtual Directory dentro de un sitio y marcarla "Set as IIS application", apuntando a una ruta física existente (ej. `\KoiDumplings`, la misma carpeta donde ya está publicado KOI) — habilita el patrón de "IIS Application anidada" sin necesitar gateway/reverse proxy propio ni una segunda copia del deploy.
- **Cambio de código aplicado:** `KoiDumplings.Web/appsettings.Production.json` — `AllowedHosts` pasó de `"olvidatasoft-002-site15.jtempurl.com"` a `"portaldelinversor.com.ar;olvidatasoft-002-site15.jtempurl.com"` (formato ya usado como convención en el blankproject del estudio — lista separada por `;`). Sin este cambio, cualquier request con `Host: portaldelinversor.com.ar` sería rechazado con 400 por el filtro de hosts de ASP.NET Core. El dominio viejo se deja funcionando en paralelo (no se dio de baja, reversible).
- Deploy: Web Deploy → 13 archivos. Verificado `200 OK` en el dominio actual (sigue funcionando).
- **Pendiente, a cargo del cliente en el panel de hosting** (fuera del alcance de lo que se puede hacer desde el repo): (1) confirmar que el dominio `portaldelinversor.com.ar` esté agregado/bindeado en el hosting; (2) crear la Virtual Directory `koi` con Virtual Path apuntando a la misma carpeta física de KOI y "Set as IIS application" tildado; (3) certificado SSL para el dominio nuevo. Sin estos 3 pasos el dominio no resuelve tráfico aunque el código ya esté listo del lado de la app.
- Motivo: pedido directo del dueño del estudio, con justificación explícita de dejar preparado el path `/koi/` para un eventual multi-cliente futuro, aunque hoy no esté confirmado.
- Impacto en capas: solo configuración (`appsettings.Production.json`), sin cambios de código C#/vistas. No aplica a Domain/Application/Infrastructure/Web más allá del filtro de hosts.
- Riesgos/supuestos: no se verificó todavía si ASP.NET Core en hosting "in-process" bajo una IIS Application anidada resuelve el `PathBase` automáticamente sin código adicional (comportamiento esperado según documentación de Microsoft/ANCM) — a confirmar en el primer test real contra `https://portaldelinversor.com.ar/koi/` una vez que el cliente complete los 3 pasos del panel. Si algo no resuelve bien (links, cookies, assets), puede hacer falta un `app.UsePathBase("/koi")` explícito en `Program.cs` como ajuste posterior — no se aplicó preventivamente para no arriesgar una configuración incorrecta sin poder probarla contra el hosting real.

### 2026-09-07 — implementador (Etapa 15: Fases 1 y 2 del plan de implementación)
- Etapa: Implementación — ítems 1, 2 y 6 del Análisis §14 (Diseño §13.1 y §13.5, Arquitectura §11.2, §11.3 y §11.7), o sea Fase 1 (correcciones rápidas) y Fase 2 (Mi Inversión: recupero acumulado + gráfico) del `plan-implementacion.md`. Fases 3 a 6 explícitamente fuera de alcance por pedido del dueño del estudio (hay trabajo en paralelo sobre el mismo repo).
- Cambio:
  - **Ítem 1 (sin código):** `appsettings.Production.json` → `Olvidata_Email.Smtp.FromName` = `"KOI Dumplings"` (antes `"Koi Dumplings - Olvidata"`). Archivo gitignored: no se commitea, viaja con el deploy.
  - **Ítem 6:** `Views/RepartoGeneral/Index.cshtml` — `data-order="{Anio}{Mes:D2}"` en la celda de período + `columnDefs: [{ type:'num', targets:0 }]`. `order: [[0,'desc']]` ya estaba correcto. Mismo patrón que el fix de `EstadoResultados/Anual.cshtml` de la Etapa 14; texto visible sin cambios.
  - **Ítem 2:** `MiInversionFilaDto.RecuperoAcumuladoPorc` (Application) + running total ascendente en `InversionesService.ObtenerMiInversionAsync` (Infrastructure) + columna "Recupero acum." y gráfico de línea con meta al 100 % en `Views/MiInversion/Index.cshtml` (Web). No se agregó columna "Recupero del mes" — "Renta" ya es ese número, se le puso un `title` aclaratorio en vez de duplicar el dato.
- Decisión de negocio tomada en la implementación: las liquidaciones **Pendientes no suman** al acumulado (solo `Pagada` con `NetoUsd`), pero su fila **arrastra el acumulado vigente** en vez de mostrar "—" — un hueco en una serie monótona creciente se lee como error de dato y además cortaría la línea del gráfico. A validar con el cliente (hoy indistinto: 264 de 265 liquidaciones están Pagada).
- Decisión técnica: se acumula el `NetoUsd` crudo y se redondea recién al calcular el porcentaje, en vez de sumar los `RentaMensual` ya redondeados — es lo que hace que el acumulado de la fila más reciente coincida exactamente con el KPI `RecuperoPorc` de las cards.
- Motivo: pedido directo del dueño del estudio de ejecutar solo las Fases 1 y 2 del plan, dejando las de riesgo alto (catálogo/remapeo, edición de meses cerrados) para una ventana propia.
- Impacto en capas: Application (un campo en un DTO), Infrastructure (armado del historial en `InversionesService`), Presentación (2 vistas + 1 archivo de configuración).
- Migración EF: **ninguna**, confirmado — `RecuperoAcumuladoPorc` se calcula en memoria en un DTO, no es una propiedad persistida. Coincide con lo declarado en Arquitectura §11.1.
- Evidencia: `dotnet build KoiDumplings.slnx` → Compilación correcta, 0 errores, 9 warnings preexistentes (NU1902 MailKit/MimeKit y CS0114 `HomeController.StatusCode`). Sin smoke test funcional (regla del estudio); guía de prueba manual en `5-implementador.md`, Etapa 15.
- Riesgos/supuestos: (a) sin commit ni deploy — quedan a cargo del dueño del estudio, que tiene cambios en curso en paralelo sobre el mismo repo; (b) el `FromName` no se ve reflejado hasta que se publique el `appsettings.Production.json`, que al estar gitignored no viaja por git; (c) `Views/Dashboard/Index.cshtml` carga Chart.js 4.4.3 por CDN además de la 4.4.0 que ya trae el `_Layout` — no se tocó en esta etapa, pero conviene unificarlo en algún momento; (d) el criterio de las Pendientes queda como supuesto abierto hasta la confirmación del cliente.

### 2026-09-07 — implementador (Etapa 16: Fases 3 y 5 fusionadas — catálogo real desde el Excel + importador nativo)
- Etapa: Implementación — ítems 4 y 5 del Análisis §14 (Diseño §13.3 y §13.4, Arquitectura §11.5 y §11.6), o sea Fases 3 y 5 del `plan-implementacion.md`, ejecutadas **fusionadas**. Fases 4 (editar meses cerrados) y 6 (API de Ayres) siguen fuera de alcance.
- **Cambio de enfoque respecto de lo documentado (decisión del dueño del estudio, ya investigada por él antes de delegar):** el remapeo de "373 gastos históricos" de Arquitectura §11.5 quedó **superado y no se ejecutó**. El catálogo cargado en la base es el genérico del seed inicial (21 subgrupos tipo "CMV", "Otros gastos"), y en la migración de julio el detalle del Excel se aplastó contra él — 18-20 gastos por período en la base contra ~46 por mes en el Excel. Remapear habría conservado el aplastamiento. En su lugar: **reimportar el detalle real desde el Excel del cliente**, con el catálogo construido como **unión de todas las variantes históricas** (no normalización), para que la historia quede como el cliente la registró.
- Escaneo de reutilización: match directo con **PAT-012** ("Importación de archivo con preview → confirmar"), cuya propia nota lo declara vigente para todo import recurrente por archivo. Se verificaron y bajaron a `false` los tres `pendiente_verificar: true` de la-platense (confirmado: esos archivos ya no están en el working tree, solo en el historial de git) y se registró KOI como `proyectos_que_lo_usan` con sus archivos como **referencia viva** del patrón — era el único patrón reutilizable sin implementación consultable.
- Cambio:
  - **Catálogo derivado, no hardcodeado:** `KoiDumplings.Infrastructure/Services/Importacion/EstadoResultadosExcelParser.cs` (clase pura, sin EF) lee las tres hojas y deriva el catálogo. La negrita de la columna A distingue rubro de subgrupo; un rubro sin subgrupos ("Alquiler", "Gastos Extras" 2026) sintetiza un subgrupo homónimo, según Diseño §13.3. Resultado validado: **10 rubros, 79 subgrupos, 81 etiquetas distintas** — coincide exacto con el conteo independiente del dueño del estudio. 47 vigentes (año 2026) y 32 cargados pero de baja lógica.
  - **TipoConcepto por regla derivada:** un subgrupo es calculado si y solo si el propio Excel rotula un porcentaje en su etiqueta. Da exactamente 5 (Regalías 3%, Cánon de publicidad 2,5%, la variante con typo "Cánon de publidad 2,5%", Fondo de juicios laborales 1% y Reposición maquinaria 1%), todos `PorcentajeVentasTotales` — confirmado contra las fórmulas del archivo. IIBB, comisiones de tarjeta, débitos/créditos y tasa municipal quedan **Manual**, como pidió el dueño del estudio.
  - **Importador nativo:** `ImportacionExcelKoiService` + `IImportacionExcelKoiService` + `ImportacionExcelKoiDtos`. Preview → confirmar con una sola función de proceso y flag `persistir`, staging en `%TEMP%` con token validado como Guid, todo el import en una transacción. Opciones: "Actualizar los períodos que ya existan" (apagada por defecto) y "Dar de baja los conceptos que ya no se usan" (prendida).
  - **Pantalla renombrada a "Importación desde Excel"** (`Views/ImportacionInicial/Index.cshtml` reescrita + sidebar). El importador de plantilla del sistema queda como card colapsable secundaria: sigue siendo el único camino para inversores, puntos y liquidaciones.
- **Hallazgo de fondo, resuelto en la misma etapa:** el query filter global (`DeletedAt == null`) hacía que dar de baja un subgrupo **borrara visualmente** su historia (`EstadoResultadosService.ObtenerAsync` itera rubros→subgrupos), y además rompía por NRE el `DashboardService` (la navegación `c.Subgrupo` venía null) y la pantalla de Parámetros. Los tres se corrigieron: `ObtenerAsync` lee con `IgnoreQueryFilters()` y muestra el subgrupo inactivo **solo donde tiene un concepto cargado**, con badge "Histórico" y sin botón de editar; `DashboardService` filtra explícito; `ConfiguracionController.Parametros` pasó de `Include` a join explícito. Efecto lateral bueno: el `TotalGastos` de la pantalla Mensual ahora coincide con el del resumen anual y el del Dashboard, que antes discrepaban.
- **Diferencias que el preview expone en vez de resolver solo** (pedido explícito de "nada silencioso"): 2026-07 tiene la fila VENTAS ($58.226.922) distinta de A+B ($61.226.922); 2024-12, 2025-02/04/05/10 tienen la suma de conceptos distinta del "Total Gastos" del Excel por fórmulas del propio cliente que dejan filas afuera (incluido un importe escrito como texto, `$1.335.352`, que el `SUM` de Excel ignora); 2025-02 tiene 1.081.000 en el encabezado de "Gastos Extras" sin itemizar; 2026-07 calcula Regalías con `(VENTAS*0,79)*0,03`; y contra la base se muestran ventas, total de gastos y cantidad de conceptos actuales vs. los del archivo — con lo que la diferencia de $100 de 2026-08 queda visible antes de confirmar.
- Motivo: pedido directo del dueño del estudio, con el contexto ya investigado por él (no se repitió el diagnóstico) y con la restricción explícita de no tocar producción.
- Impacto en capas: Application (DTOs + 1 interfaz nueva + 2 campos en `IEstadoResultadosService`), Infrastructure (2 servicios nuevos en `Services/Importacion/`, `DashboardService`, `EstadoResultadosService`, `DependencyInjection`), Presentación (`ImportacionInicialController`, `ConfiguracionController`, `ImportacionInicialViewModel`, 3 vistas). Domain sin tocar.
- Migración EF: **ninguna**. `Rubro`, `Subgrupo`, `ConceptoGasto`, `VentaMensual` y `PeriodoMensual` no se modificaron — verificado por `git status` (ningún archivo bajo `KoiDumplings.Domain/` ni `Infrastructure/Data/Migrations/`). Coincide con lo declarado en Arquitectura §11.1.
- Evidencia: `dotnet build` → Compilación correcta, **0 errores**; 1 warning de compilador (`CS0114 HomeController.StatusCode`) + 7 `NU1902` de MailKit/MimeKit, todos preexistentes, ninguno introducido. **Sin smoke test funcional** (regla del estudio). Sí se corrió el parser contra los archivos reales del cliente con un harness aislado que linkea solo `EstadoResultadosExcelParser.cs` + ClosedXML (procesamiento de archivos, no la app): 0 errores de lectura, 16 períodos detectados (2024-11/12, 2025-01..12, 2026-07/08 — exactamente los meses con datos reales), 81 etiquetas distintas, 0 duplicados y 0 inconsistencias. Guía de prueba manual en `5-implementador.md`, Etapa 16.
- Riesgos/supuestos: (a) **sin commit ni deploy**, y **sin ejecutar nada contra producción** — la migración de los datos reales la corre el dueño del estudio con backup previo y verificación de totales; (b) el camino de escritura EF queda validado solo por revisión de código, no contra una base; (c) la detección rubro/subgrupo depende de la negrita de la columna A — si el cliente pega valores sin formato, el parser corta con un error explícito en vez de importar mal; (d) actualizar un período cerrado **no recalcula las liquidaciones** (eso es Fase 4): el preview lo avisa; (e) 2026-01..05 no están en el Excel y quedan intactos con el catálogo viejo, ahora visibles gracias al fix de baja lógica; (f) el "Gastos Extras" de 2025 (~9.635.000 en el año) se importa aunque el Excel lo excluya de su propio Total Gastos — la diferencia queda a la vista en el preview de cada período para que decida el usuario.

### 2026-08-13 — implementador-dotnet (migración del catálogo real EJECUTADA EN PRODUCCIÓN, sesión 7)
- Etapa: ejecución de la Fase 3+5 del plan sobre producción, tras validar el camino de escritura contra la base local.
- **Decisión del dueño del estudio que acotó el alcance:** *"los meses históricos no se tocan"* — se descartan las opciones de reimportar/corregir la historia. La importación corre con `ActualizarExistentes = false`, así que todo período existente se omite. Queda pendiente de definición con el cliente qué hacer con los errores de fórmula del Excel (ver `temas-para-el-cliente.md` §0), pero eso ya no bloquea el uso del sistema.
- **Validación previa contra base local** (`koidumplings_dev`), vía HTTP real con login y antiforgery, no por revisión de código: preview OK, confirmación OK, transacción con commit. Resultado local: períodos existentes intactos (18/20/19 conceptos sin cambio), catálogo cargado, y 2026-07 creado con 46 conceptos. Esto cubrió el riesgo (b) que la sesión anterior había dejado abierto (camino de escritura EF sin probar).
- **Backup previo de producción**: como el usuario del hosting no tiene privilegios para `util.dumpSchemas`, se exportaron a mano las 6 tablas afectadas en `C:\Sistemas\backups\koi\pre-catalogo-2026-08-13_*.ndjson` (rubros 6, subgrupos 21, parametrosporcentaje 8, periodosmensuales 21, ventasmensuales 20, conceptosgasto 391) más un snapshot de verificación de totales de gastos por período.
- **Preview en producción antes de escribir**: 15 períodos omitidos, 0 a actualizar, 1 a crear (julio 2026), 9 rubros nuevos, 75 subgrupos nuevos, 17 subgrupos dados de baja.
- **Resultado de la migración**: catálogo en producción con 9 rubros activos y 47 subgrupos activos sobre 96 totales (49 históricos de baja lógica, que siguen mostrando su historia). Julio 2026 creado con 46 conceptos ($57.287.371,34), un mes que estaba en el Excel pero nunca se había cargado.
- **Verificación post-migración**: `diff` entre el snapshot de totales por período previo y posterior → **una sola línea de diferencia, el alta de 2026-07**. Ningún período histórico modificado. Smoke de pantallas en producción: Dashboard, Estado de Resultados mensual (agosto 2026 y marzo 2026), Historial de Resultados, Reparto General, Mi Inversión y Configuración/Subgrupos → todas 200. Marzo 2026 (mes histórico cuyos subgrupos quedaron inactivos) sigue mostrando sus 34 importes con el badge "Histórico" — confirma que el fix del filtro global funciona en producción.
- Motivo: pedido del dueño del estudio de continuar con la implementación acotando el alcance a no tocar datos históricos.
- Impacto en capas: Datos (catálogo de rubros/subgrupos + alta de un período), Application/Infrastructure/Web (código ya deployado con el importador y el fix del filtro global).
- Riesgos/supuestos: (a) julio 2026 quedó creado en estado **Abierto** y sin liquidaciones — si el cliente quiere cerrarlo, va a generar liquidaciones de un mes ya pasado, decisión suya; (b) sigue pendiente la definición contable sobre los errores de fórmula del Excel; (c) 2026-01..05 conservan el desglose genérico porque el Excel actual ya no tiene esos meses.

### 2026-09-07 — implementador (Etapa 17: reapertura de períodos cerrados — REVIERTE D-04)
- Etapa: Implementación — reapertura de período cerrado por el Administrador. **Reemplaza el enfoque de la Fase 4 del `plan-implementacion.md`** ("editar el mes cerrado en el lugar recalculando las liquidaciones pendientes"): ya no hace falta, el flujo pasa a ser **reabrir → editar con las pantallas normales → volver a cerrar**. Los guards de período cerrado de `GuardarVentas` y `GuardarConceptoGastoAsync` quedan intactos.
- **Esta etapa revierte la decisión D-04** de la sesión de definición de junio ("Estado Reabierto eliminado: el período no puede reabrirse una vez cerrado", `1-analista-funcional.md` línea 198, y su par P-A04 en Arquitectura), por **pedido explícito posterior del cliente**. No es un descuido: es el mismo camino que ya había quedado anticipado en Análisis §14.1 ítem 3 y en `4-presupuestador.md` §383, donde se dejó constancia de que la corrección de meses cerrados revierte D-04/P-A04. La máquina de estados sigue teniendo dos estados (`Abierto`/`Cerrado`, sin `Reabierto`): la reapertura es una transición `Cerrado → Abierto`, no un estado nuevo — **no hubo cambios en el enum `EstadoPeriodo`**. D-30 (reabrir una liquidación individual `Pagada → Pendiente`) sigue vigente y sin cambios.
- **Decisión del cliente ya tomada antes de implementar: "reabrir y regenerar todo".** Al reabrir se descartan TODAS las liquidaciones del período, incluidas las pagadas, y al volver a cerrar se generan de cero. El cliente aceptó explícitamente la contrapartida de tener que volver a marcar los pagos. Mitigaciones pedidas y aplicadas: (1) baja lógica, nunca borrado físico; (2) snapshot completo de lo descartado en `AuditLog.OldValues`.
- **Hallazgo bloqueante que obligó a una migración EF (no previsto en el pedido, que pedía evitarla si se podía).** `Liquidaciones` tenía un índice **UNIQUE** real en la base sobre `(PeriodoMensualId, InversorId)` (`IX_Liquidaciones_PeriodoMensualId_InversorId`, creado en la migración `E3_EstadoResultados`) que **no contempla `DeletedAt`**. Las liquidaciones descartadas por baja lógica siguen ocupando esa clave en MySQL, así que el nuevo cierre habría fallado con *duplicate key* al insertar la liquidación del mismo inversor. MySQL no soporta índices filtrados y sumar `DeletedAt` a la clave no sirve (NULL cuenta como distinto y habilitaría duplicados **activos**), así que el índice pasa a **no único** y la unicidad de liquidaciones activas queda garantizada en los servicios. Migración `20260907203443_E17_LiquidacionesIndiceNoUnico_Reapertura` generada y **NO aplicada a ninguna base** (ni local ni producción).
- Cambio:
  - **Application:** `IEstadoResultadosService` suma `ObtenerResumenReaperturaAsync` (solo lectura, para la advertencia previa) y `ReabrirPeriodoAsync(anio, mes, motivo, userId)`, más los DTOs `ResumenReaperturaDto` y `LiquidacionDescartadaDto`.
  - **Infrastructure:** `EstadoResultadosService.ReabrirPeriodoAsync` valida (período existe / está `Cerrado` / motivo no vacío), arma el snapshot JSON, marca las liquidaciones con `DeletedAt`/`DeletedByUserId`, pasa el período a `Abierto`, limpia `FechaCierre`/`MontoAjuste`/`MotivoAjuste` y escribe un `AuditLog` con `Action = "Reapertura"` — todo dentro de una transacción, con `LogWarning` estructurado. `CerrarPeriodoAsync` suma un guard nuevo: rechaza cerrar si el período ya tiene liquidaciones **activas** (reemplaza la garantía que daba el índice único).
  - **Web:** botón "Reabrir período" en `Views/EstadoResultados/Mensual.cshtml` (solo Admin/SuperUsuario vía `SoloAdministrador`, solo con el período cerrado), confirmación SweetAlert2 con textarea de motivo obligatorio (`inputValidator`) y advertencia con los números reales traídos del modelo; acción POST `ReabrirPeriodo` con `[ValidateAntiForgeryToken]` y la misma policy.
- Motivo: pedido explícito del dueño del estudio, con el contexto crítico (`CerrarPeriodoAsync` siempre inserta liquidaciones) ya investigado por él antes de delegar.
- Impacto en capas: Application (contrato + DTOs), Infrastructure (servicio + configuración EF del índice), Presentación (controller, ViewModel, vista). Domain sin tocar.
- Migración EF: **una, generada y NO aplicada** — `E17_LiquidacionesIndiceNoUnico_Reapertura` (drop del índice único + alta del mismo índice no único). Impacto: la base deja de impedir dos liquidaciones para el mismo par período/inversor; la regla la sostienen `CerrarPeriodoAsync` (guard nuevo) e `ImportacionInicialService` (`AnyAsync` previo, ya existía). **Requiere aplicarse antes de usar la reapertura**: sin ella, reabrir funciona pero volver a cerrar rompe con duplicate key.
- Evidencia: `dotnet build` → Compilación correcta, **0 errores**, 9 warnings todos preexistentes (`CS0114 HomeController.StatusCode` + 8 `NU1902` de MailKit/MimeKit). **Sin smoke test funcional** (regla del estudio); guía de prueba manual en `5-implementador.md`, Etapa 17.
- Riesgos/supuestos: (a) **sin commit ni deploy** y **sin aplicar la migración**; (b) volver a cerrar dispara de nuevo la notificación por email a los inversores (`NotificacionCierreService`) — es lo esperado, pero el cliente tiene que saber que recibirán un segundo aviso; (c) los pagos se pierden como estado y hay que re-marcarlos a mano, con el detalle exacto (inversor, importe y fecha) en Auditoría; (d) el período reabierto desaparece de "Rendimiento Histórico" (`DashboardService` filtra por `Estado == Cerrado`) hasta que se lo vuelva a cerrar.

### 2026-09-08 — implementador (Etapa 18: edición inline de gastos por AJAX en el ER mensual)
- Etapa: Implementación — pantalla Estado de Resultados mensual (P-03). Pedido textual del dueño del estudio: *"el importe de los gastos se debe poder modificar on demand via ajax en vez de post"*.
- Cambio: los importes de gastos manuales pasan de editarse con botón-lápiz + modal SweetAlert2 + `window.location.reload()` a un `<input type="number">` **inline** que guarda por AJAX al salir del campo (evento `change`), solo si el valor cambió respecto de `data-valor-anterior`. Feedback discreto por fila (spinner → check verde + flash de borde); **SweetAlert2 queda reservado para errores**. El endpoint `GuardarConcepto` —que ya existía y ya guardaba de a un concepto— suma `anio`/`mes` y devuelve los totales recalculados (`importeAplicado`, `totalGastos`, `resultado`, `resultadoUsd`, `rubros[] { rubroId, totalRubro }`), siguiendo la convención de nombres de `GuardarVentas`/`Recalcular`. Se eliminó la línea muerta `RecalcularPorcentualesAsync(0, 0)`. **Sin endpoint nuevo, sin cambios en Application/Infrastructure/Domain.**
- Reutilización: patrón tomado de **marihogar CR-61** (`Views/Stock/Index.cshtml` + `StockController.ActualizarStock`) y, en segundo lugar, de **vinosefue** (ítems de compra con auto-save). Se adaptó, no se copió (marihogar renderiza por DataTables server-side, KOI por Razor). Divergencia deliberada: `readonly` en vez de `disabled` durante el guardado, para no romper el recorrido con Tab al cargar decenas de gastos seguidos.
- Motivo: pedido explícito del dueño del estudio — la recarga completa de página después de cada gasto hacía inviable la carga mensual real (~40 conceptos).
- Impacto en capas: **solo Presentación** (`KoiDumplings.Web/Controllers/EstadoResultadosController.cs` y `KoiDumplings.Web/Views/EstadoResultados/Mensual.cshtml`). Application, Infrastructure y Domain sin tocar.
- Migración EF: **ninguna** — verificado por `git status` (ningún archivo bajo `KoiDumplings.Domain/` ni `Infrastructure/Data/Migrations/`).
- Evidencia: `dotnet build` → Compilación correcta, **0 errores**, 9 warnings todos preexistentes (`CS0114 HomeController.StatusCode` + 8 `NU1902` de MailKit/MimeKit). Como el proyecto no usa `AddRazorRuntimeCompilation`, el build también compila la vista Razor. **Sin smoke test funcional** (regla del estudio); guía de prueba manual de 10 pasos —incluido el caso de sesión perdida a mitad de la carga— en `5-implementador.md`, Etapa 18.
- Riesgos/supuestos: (a) (resuelto) (b) validación por revisión de código y build, no contra base ni navegador; (c) el guardado es por fila, sin transacción entre filas — una carga interrumpida deja persistidas las filas ya confirmadas, cada una con su check visual; (d) `anio`/`mes` viajan desde el cliente **solo** para releer totales: la escritura se valida siempre contra `periodoId` en `GuardarConceptoGastoAsync`; (e) cerrar la pestaña con el foco todavía dentro de un campo pierde ese último importe (el navegador no dispara `change`) — mitigado con la leyenda "Los importes se guardan solos al salir del campo" en el header de Gastos; (f) se corrigió de paso un bug latente: los handlers de "Guardar ventas" y "Recalcular %" hacían `.text()` sobre la celda de importe y habrían borrado los inputs nuevos — ahora los tres usan el helper `pintarImporteConcepto`, que además no pisa un campo con foco o con guardado en curso.
- Cierre (mismo día, dueño del estudio): commiteado en `9bbfee5` y **deployado a producción** con el perfil `olvidatasoft-002-site15 - Web Deploy` (10 archivos actualizados). Verificado sobre `https://portaldelinversor.com.ar/koi/`: `/EstadoResultados/Mensual` responde `200` y renderiza los inputs inline (50 `concepto-input` en el período 22 = 2026-09, abierto); `POST /EstadoResultados/GuardarConcepto` devuelve el JSON con la forma exacta que consume la vista (`success`, `importeAplicado`, `totalGastos`, `resultado`, `resultadoUsd`, `rubros[]` con los 9 rubros). La prueba se hizo reescribiendo `0.00` sobre un concepto que ya valía `0.00` — **sin alterar dato real de producción**. Queda pendiente la prueba manual en navegador (Tab entre campos, error de red, sesión expirada) según la guía de 10 pasos de `5-implementador.md`.

### 2026-09-08 — implementador (Etapa 19: el % de los conceptos porcentuales pasa a ser referencia)
- Etapa: Implementación — pantalla Estado de Resultados mensual (P-03). Pedido textual del dueño del estudio: *"el % del impuesto en el rubro y subtipo es un % a modo de referencia. se debe poder modificar el importe también"*.
- Cambio: los subgrupos con `TipoConcepto != Manual` (impuestos calculados como % de ventas) pasan a tener el importe editable inline, igual que los manuales. `GuardarConceptoGastoAsync` ya no los rechaza con *"Subgrupo calculado: el importe no se edita manualmente"*.
- Decisión de diseño (tomada por el implementador, comunicada al dueño): **el importe escrito a mano sobrevive al recálculo**. Es la única lectura consistente con "el % es referencia": si el recálculo lo pisara, cargar el impuesto y después guardar ventas lo reemplazaría en silencio por base × %. `RecalcularInternamente` refresca siempre `PorcentajeAplicado` (la referencia visible) y solo escribe `ImporteCalculado` cuando la fila no está marcada.
- Persistencia del override: se escribe en **`ImporteCalculado`**, no en `ImporteManual`. Es de donde ya leían todos los consumidores de un subgrupo porcentual (`ImporteCalculado ?? ImporteManual` en `DashboardService`, `InversionesService`, `ImportacionExcelKoiService`, y `ObtenerAsync`/cierre en el propio servicio). Así el override no puede producir dos números distintos según la pantalla, y **no hubo que tocar ninguno de esos consumidores**.
- Reversión: `RevertirConceptoAlPorcentajeAsync` + endpoint `RevertirConcepto`. Sin esto una fila sobrescrita no volvía a calcularse nunca más. Pide confirmación por SweetAlert2 (sobrescribe un valor que alguien cargó a propósito).
- Impacto en capas: Domain (`ConceptoGasto.ImporteEditadoManual`), Infrastructure (`AppDbContext`, `EstadoResultadosService`, `ImportacionExcelKoiService`), Application (`ConceptoErDto.EsImporteEditado`, `ConceptoCalculadoDto.ImporteAplicado`/`EsImporteEditado`, firma nueva en la interfaz), Web (controller + vista).
- Migración EF: **`20260908162332_E19_ConceptoGastoImporteEditadoManual`** — `AddColumn tinyint(1) NOT NULL DEFAULT false`. Aditiva, sin backfill: todas las filas existentes quedan en `false`, o sea siguen el % como hasta ahora. Aplicada en producción (`dotnet ef migrations list` la lista sin `(Pending)`).
- Bug latente corregido de paso (segundo del mismo tipo, después del de Etapa 18): los handlers de "Guardar ventas" y "Recalcular %" hacían `.text()` sobre `.porcentaje-cell`, lo que habría borrado el botón de reversión que ahora vive en esa celda. El valor se aisló en `.porcentaje-valor` y se repinta con `pintarPorcentajeConcepto()`. **Patrón a vigilar en esta vista: nunca `.text()` sobre una celda que contiene controles.**
- Corrección de alcance vecino: `ImportacionExcelKoiService` escribía el importe real del Excel del cliente en `ImporteCalculado` de un subgrupo porcentual **sin marcarlo**; un "Recalcular %" posterior lo habría reemplazado por base × %. Ahora lo marca como override. Va más allá del pedido literal y se le informó al dueño.
- Evidencia: `dotnet build` → 0 errores. Commit `84e0432`, deployado. **Verificación funcional contra producción** (`https://portaldelinversor.com.ar/koi/`, período 22 = 2026-09, abierto): la grilla pasó de 50 a 57 inputs (7 conceptos porcentuales ahora editables) y renderiza 49 botones de reversión ocultos. Ciclo completo sobre el subgrupo 39 (3 %): override a `12345.67` → `esImporteEditado: true`, total `12345.67`; `Recalcular` → `importeCalculado: 0.00` pero `importeAplicado: 12345.67`, total intacto (**el override resiste el recálculo**); `RevertirConcepto` → `0.00`, `esImporteEditado: false`, total `0,00`. **Producción quedó exactamente como estaba** (ningún input marcado, ningún botón visible, total en `0,00`).
- Riesgos/supuestos: (a) el override es por período y por concepto — no se propaga ni se hereda al mes siguiente, que vuelve a nacer siguiendo el %; (b) `PorcentajeAplicado` deja de ser "snapshot de lo aplicado" en las filas marcadas y pasa a ser la referencia vigente: el nombre del campo quedó, la semántica cambió (documentado en la entidad); (c) no se guarda en ningún lado cuánto daría el % en una fila sobrescrita — se conoce recién al revertir; (d) falta prueba manual en navegador del botón de reversión y del marcado naranja.

### 2026-09-08 — implementador (Etapa 19b: los porcentuales se refrescan al abrir el E.R.)
- Etapa: Implementación — P-03, corrección sobre Etapa 19. Pedido textual del dueño del estudio, mostrando el rubro Fee de Franquicia con Regalías 3,00 % y Cánon 2,50 % ambos en 0,00: *"este valor tiene que estar calculado automáticamente y se completa con el valor 0"*.
- Diagnóstico: **no era un cálculo mal hecho, era un valor no recalculado.** `Mensual` (GET) llamaba solo a `ObtenerAsync`, que devuelve el `ImporteCalculado` **persistido**. El cálculo `base × %` corría únicamente en `GuardarVentasAsync` y en el botón "Recalcular %". Si las ventas cambiaban por cualquier otra vía (importación de Excel, alta de un `ParametroPorcentaje` nuevo, período recién creado sin ventas), la fila quedaba con el valor viejo o en 0,00 hasta que alguien apretara el botón. Agravado por Etapa 19: al pasar esas filas de texto a `<input>`, el 0,00 dejó de leerse como "todavía no calculado" y pasó a leerse como "importe cargado en cero".
- Cambio: `Mensual` (GET) refresca los porcentuales antes de renderizar, **solo en períodos abiertos** (`!dto.EsCerrado && dto.PeriodoId > 0`) — los meses históricos no se tocan. Los importes escritos a mano quedan intactos porque `RecalcularInternamente` ya los respeta (Etapa 19). Si el refresco falla, se loguea warning y se muestra lo persistido en vez de romper la pantalla.
- Cambio de UX asociado: el campo de un porcentual que sigue el % se distingue del de carga manual (borde punteado, `.ov-concepto-auto`, + tooltip que explica cómo se calcula y qué pasa si se escribe encima). Sin esto, un 0,00 legítimo —ventas todavía en cero— se sigue leyendo como un dato faltante. `marcarOverride()` alterna las dos clases para que el estado siga siendo correcto después de guardar o de revertir sin recargar.
- Impacto en capas: solo Web (controller + vista). Sin migración, sin cambios en Application/Infrastructure/Domain.
- Evidencia: `dotnet build` → 0 errores. Commit `06f32bc`, deployado. **Verificación funcional contra producción**, período 22 (2026-09, abierto, vacío): se cargaron ventas por `1.000.000` de forma temporal y el GET —sin apretar "Recalcular %"— devolvió Regalías `30.000,00` (3 %), Cánon `25.000,00` (2,5 %), Fondo de juicios `10.000,00` (1 %), Reposición maquinaria `10.000,00` (1 %), **Subtotal Fee de Franquicia `55.000,00`**, las cuatro filas marcadas `ov-concepto-auto` y ninguna como override. **Producción restaurada**: ventas y gastos de vuelta en `0,00`, cero filas con override.
- Riesgos/supuestos: (a) el GET ahora **escribe** (crea/actualiza `ConceptosGasto` de los subgrupos porcentuales) — es idempotente y está acotado a períodos abiertos, pero es un efecto lateral en un GET, a tener en cuenta si alguna vez se cachea la pantalla; (b) `RecalcularInternamente` hace una consulta de parámetro por subgrupo (N+1): con ~7 porcentuales es irrelevante, con un catálogo grande habría que agrupar; (c) en un mes sin ventas el importe sigue siendo 0,00 y eso es correcto — lo que cambió es que ahora se distingue de un campo sin cargar; (d) falta prueba manual en navegador del borde punteado y de los tooltips.

### 2026-09-08 — implementador (Etapa 20: la importación desde Excel se abre al Administrador)
- Etapa: Implementación. Pedido textual del dueño del estudio: *"la pantalla ImportacionInicial debe ser visible para el usuario administrador del sistema"*.
- Diagnóstico: la pantalla existía y funcionaba, pero estaba cerrada por partida doble — `ImportacionInicialController` con `[Authorize(Policy = "RequireSuperUsuario")]` y el link del sidebar dentro del bloque `@if (User.IsInRole("SuperUsuario"))`. Cambiar solo uno de los dos deja la pantalla accesible pero invisible, o visible pero con 403.
- Cambio: policy → `SoloAdministrador` (SuperUsuario + Administrador), que es **la misma que ya usa `EstadoResultadosController`**, la pantalla que esta importación alimenta: quien puede cargar el E.R. a mano ahora puede cargarlo desde el Excel. El link se movió del bloque "Super Usuario" a la sección "Sistema" del sidebar, que es la visible para Administrador y SuperUsuario.
- Impacto en capas: solo Web (controller + `_Layout`). Sin migración.
- Evidencia: `dotnet build` → 0 errores. Commit `87c47d2`, deployado. Verificado en producción: `/ImportacionInicial` responde `200` y el link se renderiza en la posición 12333, entre la sección "Sistema" (11056) y "Super Usuario" (12638) → **queda bajo Sistema**. ⚠️ Verificado con la cuenta SuperUsuario, que es la única cuyas credenciales tiene el estudio: **el acceso efectivo del rol Administrador está validado por código (policy + `@if` de la sección), no por un login real de ese rol.** Conviene que el dueño lo confirme entrando con un usuario Administrador.
- Riesgos/supuestos: (a) la importación pisa datos del E.R. de los períodos que abarque el Excel — abrirla a un rol más amplio aumenta la superficie de error humano; el dueño lo pidió explícitamente y la regla "los meses históricos no se tocan" sigue dependiendo del criterio de quien importa, no de una traba del sistema.

### 2026-09-08 — implementador (API Ayres: credencial rotada, autenticación RESUELTA)
- Antecedente: la integración estaba bloqueada. Las pruebas previas habían demostrado por matriz de control que el formato del request era correcto y que las credenciales eran válidas, porque **solo** la combinación `email + pass + sucursal 9` llegaba a `ERROR_INTERNO` mientras toda variante equivocada devolvía `UNAUTHORIZED`. Se concluyó bug del lado de Ayres y quedó redactado un pedido de soporte.
- Novedad: el dueño del estudio pasó una clave nueva (*"nueva clave de api kOiSUC09"*). **La API ahora autentica.**
- Evidencia (matriz de control contra `http://koi.ayresit.com:8520/login`, formato confirmado en `ayresit.ar/documentacion/pos/api/autenticacion.html` — `POST /login` con `email`, `pass` y **uno** de `idsucursal`/`cod_cli`, nunca ambos):
  - clave nueva + `idsucursal: 9` → **`200 SUCCESS`**, `content.tokenAccess` (JWT, 241 chars), `content.aliveTime: 3599`
  - clave nueva + `cod_cli: 9` → **`200 SUCCESS`** (ambos identificadores sirven)
  - clave **vieja** (`koisucursal9`) → `401 UNAUTHORIZED_ACCESS` (la credencial fue efectivamente rotada)
  - pass incorrecta → `401` · email inexistente → `401`
- Conclusión: el `ERROR_INTERNO` era del lado de Ayres y se resolvió al rotar la credencial. **El pedido de soporte ya no hace falta.** Desbloquea la Fase 6 (implementación del servicio Ayres) del plan de implementación.
- Credencial: guardada en `KoiDumplings.Web/appsettings.Production.json` bajo la sección `Ayres` (`BaseUrl`, `Email`, `Pass`, `IdSucursal`). Ese archivo está **gitignoreado** (`.gitignore:57`) y se verificó que el commit no lo incluyó. **No se agregó sección espejo en el `appsettings.json` versionado** para no dejar config muerta mientras el servicio no existe: al implementar Fase 6 hay que agregarla vacía ahí, como se hizo con QuickPass.
- Riesgos/supuestos: (a) el token vive 1 hora (`aliveTime: 3599`) — el servicio va a necesitar renovación y no un token cacheado indefinidamente; (b) el endpoint es **HTTP plano, no HTTPS**, y la credencial viaja en el body: a tener en cuenta al implementar y a plantearle a Ayres; (c) solo se probó `/login`, ningún endpoint de negocio.

### 2026-09-08 — orquestador (Fase 6 / E2-01 Ayres: Discovery + Análisis CERRADOS, gate al dueño)

- Etapa: Discovery (0) y Análisis (1) del módulo **E2-01 Integración Ayres POS**, reactivado tras 26 días de bloqueo por credenciales. Salida en `1-analista-funcional.md` §15.
- Gate previo verificado: §10.1.1 (research documental de la API, 2026-08-13) estaba cerrado. Esta etapa lo **actualiza y lo corrige en dos puntos** con datos reales, que es lo que el plan anticipaba como riesgo ("ajuste del mapeo si la respuesta real difiere de la documentación").
- **Corrección de premisa 1 — no hace falta el túnel.** §10.1.1 daba por hecho que la API corría en el servidor local del restaurante y que E2-01 dependía del gateway + Cloudflare Tunnel diseñado para las cámaras (E2-03). Falso: `koi.ayresit.com` → `190.245.226.181`, IP pública, responde desde internet. El endpoint es `:8520`, no el `:8510` del relevamiento original. **E2-01 se desacopla de E2-03 y pierde su costo de infraestructura.**
- **Corrección de premisa 2 — forma de la respuesta.** El sobre real es `content.{fechas, cantidad, ventas[]}` con cada elemento **anidado bajo una clave `venta`** (`content.ventas[i].venta.*`), no una lista plana. Las fechas se consultan en `yyyy-MM-dd` y vuelven en `dd/MM/yyyy`.
- **Corrección de premisa 3 — mapeo de canales.** §10.1.1 documentó `sectorTipo "ME" = mostrador`. Los datos lo desmienten: `ME` = 812 ventas / $52,4 M (el grueso), y existe un `MO` aparte de 54 ventas / $2,4 M. Mapeo correcto: **ME=Salón, PE=Pedidos, MO=Mostrador**, confirmado por conciliación aritmética. Pendiente de validación del cliente (P-A08).
- **Prueba de concepto contra datos reales (agosto 2026 completo, 4 llamadas de ≤10 días, 1.062 ventas):** total Ayres `$63.131.109,00` vs `$63.131.209,00` cargado a mano → **−$100 sobre $63 M (−0,0002 %)**. El total cierra; el detalle no: el desglose por canal está mal (Mostrador sumado dentro de Salón, $100 simbólicos en Mostrador) y la Cantidad de ventas está cargada al doble (2.000 vs 1.062 reales, +88 %), lo que subestima el ticket promedio ~47 %. **La integración no solo ahorra carga manual: corrige datos hoy incorrectos.**
- **Bloqueante abierto (R-A01, alto):** falta verificar que el hosting de producción (SmarterASP, shared) permita **salida al puerto 8520**, que no es estándar. Condiciona la viabilidad y el costo del módulo entero; si está filtrado, vuelve a hacer falta un proxy y cambia el presupuesto. **Debe resolverse antes de presupuestar.**
- Riesgo señalado además: la API es **HTTP plano sin TLS** con la credencial en el body (R-A02) — a plantear a Ayres/MaxiSistemas, no lo resuelve el código de KOI.
- Preguntas al cliente abiertas: P-A08 (mapeo de canales), P-A09 (¿se corrigen los históricos mal cargados?), P-A10 (¿reemplazar o solo completar?).
- Estado del flujo: **frenado en el gate de Análisis**, a la espera de confirmación del dueño del estudio para pasar a Diseño (etapa 2). No se escribió código de la integración.

### 2026-09-08 — orquestador (Fase 6 / E2-01: R-A01 verificado en producción, flujo FRENADO)

- Decisión del dueño del estudio en el gate: verificar R-A01 antes de diseñar y presupuestar, y semántica de importación "reemplazar siempre" para P-A10 (queda definido para el Diseño).
- Método: sonda temporal `System/DiagnosticoAyres` desplegada en producción (solo SuperUsuario, no devuelve el token). Commit `81e8928`.
- **Resultado: R-A01 CONFIRMADO NEGATIVO.** Salida al `koi.ayresit.com:8520` → `SocketException` WSAEACCES en **60 ms**. Contraste en la misma sonda: `:443` → `200` en 723 ms y `:80` → `200` en 502 ms. Conclusión: SmarterASP **restringe la salida a puertos estándar**, no la bloquea. Los 60 ms descartan un filtro en tránsito (daría timeout): es el SO negando el `connect()` por política local. Detalle en `1-analista-funcional.md` §15.9.
- **Consecuencia:** el módulo **no es viable con la infraestructura actual**. Diseño, Arquitectura y Presupuesto quedan frenados: la arquitectura depende de cuál de las cuatro salidas se tome, y dos de ellas agregan infraestructura y cambian la cotización. Presupuestar ahora sería cotizar sobre un supuesto.
- **Recomendación del orquestador:** pedir a Ayres/MaxiSistemas que expongan la API en **443 con TLS** (ya tienen el nombre público `koi.ayresit.com`). Resuelve R-A01 y R-A02 de una sola vez, con costo de infraestructura cero para el estudio. Depende de un tercero.
- **Valor rescatado pese al bloqueo:** el Discovery ya dejó probado contra datos reales que la integración cierra al centavo y que **corrige errores de carga hoy vigentes** (desglose por canal mal imputado, cantidad de ventas cargada al doble). Ese argumento sirve tanto para justificar el módulo ante el cliente como para presionar a Ayres por el cambio de puerto.
- **Incidente propio, corregido en el mismo turno:** al insertar la sonda, el método quedó ubicado entre `[ValidateAntiForgeryToken]` y `TestEmail`, de modo que la sonda heredó `[HttpPost] + [ValidateAntiForgeryToken]` (de ahí un 400 inicial que costó dos deploys diagnosticar) y **`TestEmail` quedó momentáneamente sin protección antiforgery en producción**. Se detectó al rastrear el 400, se movió el método al final de la clase y `TestEmail` recuperó sus atributos antes del commit. **Lección para el estudio: al insertar un método en un controller, verificar que no se esté cortando el bloque de atributos del método siguiente** — el síntoma (400 en un GET) no apunta al lugar del error.
- Estado del flujo: **etapas 0 y 1 cerradas; 2 a 8 frenadas** por dependencia externa. Sin código de la integración escrito.

### 2026-09-08 — orquestador (Fase 6 / E2-01: R-A01 RESUELTO, módulo viable, se retoma el flujo)

- **Corrección de la conclusión anterior.** La entrada previa cerró R-A01 como negativo y dependiente de un tercero. **Era incorrecto.** El panel de hosting de SmarterASP tiene una función "Outgoing Port" que habilita la salida del plan hacia una IP y puerto remotos; la tabla estaba vacía. Lo detectó el dueño del estudio revisando el panel, no el diagnóstico automatizado.
- **Error de razonamiento a registrar:** de "8520 falla, 80 y 443 andan" se dedujo "solo permiten puertos estándar". La muestra era insuficiente. Al ampliarla con `portquiz.net` (acepta conexiones en cualquier puerto, así que separa "bloquean el puerto" de "bloquean el destino") se vio que **8080 y 3306 salían desde el principio**: no había whitelist de puertos estándar, faltaba habilitar el 8520 puntualmente. **Lección: antes de concluir una política de red, medir al menos un caso intermedio que pueda refutar la hipótesis, no solo los extremos.**
- Regla cargada por el dueño: IP `190.245.226.181`, puerto `8520`.
- **Verificación funcional:** `POST /login` a Ayres **desde el servidor de producción** → `200 SUCCESS`, `tokenAccess` recibido, **220 ms**. Además `portquiz.net:8520` **sigue fallando** después de la regla, lo que confirma que el permiso quedó acotado a IP+puerto y no al puerto en general (correcto desde seguridad).
- **Dependencia operativa nueva (D-A01):** la regla apunta a una IP fija. Si Ayres migra de servidor, la integración se corta sin aviso con este mismo error. A documentar en el manual de operación.
- **R-A02** (API en HTTP plano, credencial en el body) **deja de ser bloqueante** y pasa a `temas-para-el-cliente.md` como pedido de seguridad a Ayres.
- **Estado: módulo VIABLE.** Se levanta el freno sobre Diseño, Arquitectura y Presupuesto. Detalle en `1-analista-funcional.md` §15.10.

### 2026-09-08 — orquestador/diseñador (Fase 6 / E2-01: Diseño CERRADO)

- Etapa 2 (Diseño) del módulo E2-01. Salida en `2-disenador-funcional.md` §14. Entrada: análisis §15 aprobado y R-A01 resuelto.
- **Escaneo de reutilización (obligatorio) — 3 coincidencias, las 3 se reutilizan:**
  1. **marihogar `AfipTokenCache.cs`** (PAT-006): cache de token con vencimiento — Singleton + `SemaphoreSlim` contra logins concurrentes, doble chequeo dentro del lock, vigencia tomada de la que informa el servidor (no un valor fijo), margen de seguridad e `Invalidar()`. Es exactamente el problema de Ayres (`aliveTime: 3599`). Se adapta de `(Token, Sign)` a un único `tokenAccess`.
  2. **KOI mismo, QuickPass (Etapa 12)**: estructura de integración externa ya establecida en el proyecto — cliente nombrado por `IHttpClientFactory`, POCO de settings tipo `SmtpSettings`, excepción de dominio propia, datos en vivo sin persistencia. Se replica renombrando, para no inventar un segundo estilo de integración dentro del mismo sistema.
  3. **PAT-012** (importación con preview → confirmar): flujo de dos pasos, **sin** el staging en disco — acá no hay archivo, se reconsulta la API al confirmar.
- **No reutilizado:** el chunking de QuickPass (31 días, otro endpoint). Acá el límite es 10 días y la agregación es propia.
- Decisiones de diseño propias: (a) **al confirmar se vuelve a consultar la API** en vez de guardar el preview en sesión — una llamada más, pero evita aplicar números viejos y elimina el staging; si el segundo resultado difiere, se avisa y se pide confirmar otra vez. (b) **Mapeo de canales configurable, no hardcodeado**: un `sectorTipo` desconocido suma a "Otros" y se advierte en el preview, en vez de descartarse en silencio. (c) Se distingue el mensaje de "no se pudo conectar" del de "credenciales rechazadas": son problemas distintos y llevan a acciones distintas.
- **Sin Domain, sin migración EF**: las ventas se escriben en `VentasMensuales`, que ya existe. Impacto en Application, Infrastructure y Web.
- El diagnóstico temporal `System/DiagnosticoAyres` queda marcado para **eliminarse** y ser reemplazado por `IAyresService.ProbarConexionAsync` (HU-A08).
- 8 historias de usuario con criterios verificables (HU-A01 a HU-A08).
- **Gate:** Diseño cerrado, a la espera de confirmación del dueño para pasar a Arquitectura (etapa 3).

### 2026-09-08 — orquestador/arquitecto (Fase 6 / E2-01: Arquitectura CERRADA · Presupuesto SALTEADO por decisión del dueño)

- Etapa 3 (Arquitectura) cerrada en `3-arquitecto-mvc.md` §12. P-A08 confirmado por el dueño del estudio (`ME`=Salón, `PE`=Pedidos, `MO`=Mostrador) y registrado en las definiciones 1 y 2.
- **GATE SALTEADO DELIBERADAMENTE:** el flujo del estudio exige presupuesto aprobado por el cliente antes de Implementación. El dueño indicó explícitamente *"implementar, saltear presupuesto"*. **Queda registrado como decisión, no como omisión del proceso.** Consecuencia: al llegar al Cierre de calibración (etapa 8) no habrá estimado contra el cual medir el real; si se quiere calibrar este módulo habrá que estimarlo a posteriori.
- Decisiones de arquitectura: (a) **sin Domain y sin migración EF** — se escribe en `VentasMensuales`, que ya existe; (b) `AyresTokenCache` **Singleton** y `AyresService` **Scoped** — invertirlo pierde el token entre requests y dispara un login por llamada, sin error visible: se documentó explícitamente porque es el error clásico del patrón; (c) vigencia del token tomada de `aliveTime` que informa el servidor, nunca de una constante, con margen de 60 s y un único reintento tras `Invalidar()`; (d) chunking **secuencial** de 10 días con **descarte del detalle por tramo** (R-A03) y aborto total si falla un tramo; (e) fechas parseadas con formato explícito e `InvariantCulture` por la asimetría consulta/respuesta; (f) mapeo de canales en configuración con degradación a "Otros" + advertencia.
- **Reutilización de mayor peso:** `AplicarAyres` **no escribe directo** — llama a `GuardarVentasAsync`, el mismo método del guardado manual. Así el recálculo de porcentuales, la validación de período cerrado y el respeto a los overrides manuales (Etapa 19) salen gratis, y **no se agrega ni una línea de lógica financiera nueva**.
- `FeatureFlags.IntegracionAyres`: primer flag real del scaffold, que estaba vacío desde su creación. Permite apagar el módulo sin redeploy.
- El diagnóstico temporal `System/DiagnosticoAyres` queda marcado para eliminarse, reemplazado por `IAyresService.ProbarConexionAsync` (HU-A08).
- Nota de seguridad arrastrada al implementador: verificar que `[HttpPost]`/`[ValidateAntiForgeryToken]` queden pegados al método correcto, por el incidente con `TestEmail` de este mismo día.
- Siguiente: etapa 5 (Implementación), delegada al subagente `agentes-ia-implementador`.

### 2026-09-08 — implementador (Etapa 21 / Fase 6 · E2-01 Ayres POS: módulo IMPLEMENTADO, sin commit ni deploy)
- Etapa: Implementación del módulo E2-01 "Integración Ayres POS", con Análisis (§15), Diseño (§14) y Arquitectura (§12) aprobados y cerrados. **Gate de presupuesto SALTEADO por decisión explícita del dueño del estudio** ("implementar, saltear presupuesto"), ya registrado en la entrada anterior de arquitectura — no es un descuido del flujo.
- Escaneo de reutilización: 3 coincidencias, las 3 aplicadas. (1) `marihogar/AfipTokenCache.cs` (PAT-006) → portado a `AyresTokenCache` (de `(Token, Sign)` de WSAA a un JWT; de `expirationTime` a `aliveTime` en segundos), conservando vigencia-del-servidor, margen de seguridad, `SemaphoreSlim` + doble chequeo en el lock e `Invalidar()` con **un solo** reintento. (2) Convenciones de QuickPass del propio KOI (cliente nombrado por `IHttpClientFactory`, POCO de settings, excepción de dominio, DTOs crudos privados) → estructura copiada y renombrada. (3) PAT-012 (preview → confirmar) → flujo de dos pasos **sin staging**: la fuente es una API, así que al confirmar se re-consulta.
- Catálogo de patrones: se creó **PAT-024** ("Cache de token de API externa con vigencia informada por el servidor"), extraído de PAT-006 al tener ya 2 implementaciones en dominios sin relación (AFIP y un POS gastronómico) — enterrado dentro del patrón de facturación electrónica era invisible para el próximo que lo necesite. Se agregó además la variante sin-staging de KOI como `archivo_referencia` de PAT-012.
- Cambio por capa: **Domain sin cambios y sin migración EF.** Application: `DTOs/AyresDtos.cs`, `Interfaces/IAyresService.cs`, `Exceptions/AyresIndisponibleException.cs` (con flag `EsCredenciales`), `Settings/FeatureFlags.IntegracionAyres` (primer flag real del scaffold). Infrastructure: `AyresSettings`, `AyresTokenCache` (**Singleton**), `AyresService` (**Scoped**), registro del cliente nombrado `"Ayres"` en `DependencyInjection`. Web: `PreviewAyres` + `AplicarAyres` en `EstadoResultadosController`, botón + modal de preview en `Views/EstadoResultados/Mensual.cshtml`, `ErMensualViewModel.AyresHabilitado`, tarjeta de prueba de conexión en `Views/System/Index.cshtml`. Config: sección `Ayres` **vacía** en el `appsettings.json` versionado + `Features.IntegracionAyres: false`; el `appsettings.Production.json` (gitignoreado) recibió `MapeoCanales`, timeouts y el flag en `true`.
- **Diagnóstico temporal eliminado:** se borró `SystemController.DiagnosticoAyres` con sus ramas `?ping=` y `?puertos=` —existía solo para responder R-A01— junto con `IHttpClientFactory` e `IConfiguration`, inyectados únicamente para él. Lo reemplaza `ProbarAyres` sobre `IAyresService.ProbarConexionAsync` (HU-A08), que distingue tres desenlaces: conecta / credenciales rechazadas / inalcanzable. **Se verificó que el bloque `[HttpPost]`/`[ValidateAntiForgeryToken]` quedó pegado a `TestEmail`** — es exactamente el incidente de esta misma mañana, cuando un método insertado en este controller partió ese bloque.
- Decisiones que respetan lo ya decidido en Diseño/Arquitectura, anotadas para que no se reabran: la escritura **no toca `VentasMensuales` directo** sino que llama a `GuardarVentasAsync` (cero lógica financiera nueva); el lado B (informal) **se re-envía tal cual** porque `GuardarVentasAsync` escribe las 8 columnas y pasar 0 lo habría borrado en silencio; al confirmar **se re-consulta la API** y, si difiere del preview, se pregunta de nuevo con los números nuevos; chunking **secuencial** con descarte del detalle por tramo; fechas con dos formatos distintos (`yyyy-MM-dd` de ida, `dd/MM/yyyy` de vuelta) y `InvariantCulture`; canal desconocido → "Otros" **con advertencia explícita de que ese importe no se aplica a ninguna columna**; `estado` (R-A04) se **cuenta y se informa, no se filtra**.
- Agregados propios sobre el diseño, todos menores y justificados: `IAyresService.EstaConfigurado` (para que Web no tenga que inyectar `AyresSettings` solo para decidir si renderiza un botón); tolerancia de $0,01 al comparar el total re-consultado contra el previsualizado (sin ella, un redondeo dejaría al usuario sin poder aplicar nunca); guard que lanza excepción si un tramo trae elementos pero ninguno con nodo `venta` adentro (el modo de falla peligroso acá es devolver 0 sin error); `JsonNumberHandling.AllowReadingFromString` por si Ayres empieza a mandar importes como string.
- Evidencia: `dotnet build` desde la raíz → **Compilación correcta, 0 errores**; 9 advertencias, todas preexistentes (NU1902 MailKit/MimeKit = VUL-001, y CS0114 `HomeController.StatusCode`). Rebuild completo del proyecto Web para forzar la compilación de las vistas Razor → 0 errores. **No se ejecutó smoke test funcional**, por regla del rol: en su lugar quedó una guía de prueba manual de 12 pasos en `5-implementador.md` (Etapa 21), que arranca por el criterio de aceptación duro — agosto 2026 debe dar 1.062 ventas y 63.131.109,00, con ME 52.404.760,00 / PE 8.293.849,00 / MO 2.432.500,00 y 1.735 comensales.
- **Sin commit y sin deploy**, deliberadamente: los ejecuta el dueño del estudio después de revisar. Las credenciales **no se commitean**: siguen solo en `appsettings.Production.json`, que está gitignoreado.
- Riesgos/supuestos: (a) el `appsettings.Production.json` se sube a mano — si el que está en el servidor no recibe el bloque `Features`, el flag queda en `false` y el botón no aparece aunque el deploy esté bien; (b) **D-A01** sigue vigente: la regla de salida del hosting apunta a la IP fija `190.245.226.181`, si Ayres migra la integración se corta con `SocketException`/WSAEACCES (el log del error de red lo menciona explícitamente para que ese diagnóstico no vuelva a llevar dos horas); (c) **R-A02** sigue abierto — HTTP sin TLS con la credencial en el body, no resoluble desde este código, es tema para el cliente / MaxiSistemas; (d) **P-A09** sigue abierta con el cliente: el desglose por canal y la cantidad de ventas de los períodos históricos están mal cargados y este módulo no toca meses cerrados; (e) el error clásico de este patrón —invertir Singleton/Scoped— no da error visible, así que el paso 12 de la guía manual lo verifica contando logins en el log.
- Fuera de alcance a propósito, sin cambios: sincronización automática o programada, y los endpoints de Compras, Caja, Stock y Datos Maestros.

### 2026-09-08 — orquestador (Fase 6 / E2-01: Implementación entregada, deployada y verificada)

- Etapa 5 (Implementación) delegada al subagente `agentes-ia-implementador` y entregada. Detalle en `5-implementador.md` Etapa 21. Commits `4b2d8c3` (integración) y el fix de anuladas; deployado a producción.
- **Revisión del orquestador antes de commitear** (no se tomó la entrega al pie de la letra): verificado que el diff versionado **no contiene credenciales**, que `AyresTokenCache` quedó **Singleton** y `AyresService` **Scoped**, que el `appsettings.json` versionado tiene la sección `Ayres` **vacía** y el flag en `false`, que el de producción sí tiene credenciales y `IntegracionAyres: true`, y que **`TestEmail` conserva su `[HttpPost]/[ValidateAntiForgeryToken]`** tras eliminar el diagnóstico temporal.
- **Verificación funcional en producción, contrastada contra un cálculo independiente** hecho por fuera del sistema consultando la API directamente. Septiembre 2026 (abierto): la app devolvió 184→180 ventas, $11.155.330,00, Salón $9.104.600 / Pedidos $1.573.830 / Mostrador $476.900, ítems 995. **Coincidencia exacta campo por campo.** `PreviewAyres` sobre agosto (cerrado) responde "El período está cerrado" → **HU-A06 verificada**. `ProbarAyres` → conexión OK en 572 ms.
- **Defecto encontrado en esa verificación y corregido (R-A04):** las ventas con estado `X` son anulaciones; vienen en **$0** —por eso el importe cerraba y el problema era invisible— pero **arrastran comensales**. En agosto eran 44 ventas con 57 comensales.
  - **Corrige además un error del propio análisis:** §15.4 había reportado los comensales cargados a mano (1.678) como equivocados frente a los 1.735 de Ayres. **Era al revés**: 1.735 − 57 = 1.678 exactos. Quien carga a mano ya excluía las anuladas y **su dato era el correcto**; el agregado del análisis era el que estaba mal. Corregido en §15.4, §15.11 y en `temas-para-el-cliente.md`.
  - Impacto real: cantidad de ventas 1.062 → **1.018**, ticket promedio 59.445,49 → **62.014,84**. El importe total no cambia.
  - Solución: solo entran al agregado las ventas cerradas; las excluidas se cuentan aparte y **el preview las informa** ("Se excluyeron N venta(s) sin cerrar"), en vez de descartarlas en silencio.
  - **Lección: el instrumentar en vez de filtrar a ciegas fue lo que hizo visible el problema.** Si el servicio hubiera sumado todo sin mirar el estado, el error habría quedado escondido detrás de un total que cerraba perfecto.
- CA-5 actualizado: agosto 2026 → `63.131.109,00`, **1.018** ventas y **1.678** comensales.
- **Pendiente:** prueba manual en navegador (12 pasos en `5-implementador.md` Etapa 21) — la verificación se hizo por HTTP, no visualmente. Etapas 6 (QA), 7 (Documentación) y 8 (Cierre de calibración) sin ejecutar; el cierre no tendrá estimado contra el cual medir por el salteo del presupuesto.

### 2026-09-08 — orquestador/documentador (Fase 6 / E2-01: Documentación CERRADA)

- Etapa 7 (Documentación) cerrada. Entregable: `manual-usuario.md`, sección nueva "Traer las ventas desde Ayres — paso a paso" ubicada entre la carga del E.R. y el cierre de período, siguiendo el recorrido real del usuario.
- **Hallazgo colateral: el manual tenía dos partes obsoletas que nadie había detectado.** (a) Describía cargar los gastos "con el botón de edición (lápiz)", botón que dejó de existir en la Etapa 18 de hoy; (b) la sección "Qué no incluye esta versión" listaba cámaras, fichador y Ayres como no entregados, **cuando los tres ya están entregados**. Ambas reescritas. **Lección para el estudio: al cerrar una etapa que cambia una pantalla documentada, revisar el manual de usuario, no solo las definiciones internas** — el manual es lo que el cliente lee y se desactualiza en silencio.
- Se documentó D-A01 en lenguaje de cliente: si Ayres cambia de servidor la conexión se corta, no es falla ni pérdida de datos, y la carga manual sigue disponible siempre.
- Etapa 6 (QA) corriendo en paralelo, delegada al subagente `agentes-ia-qa`.

### 2026-09-08 — qa (Etapa 6 / Fase 6 · E2-01 Ayres POS: QA CERRADA con verificación por navegador real)

- Etapa 6 (QA) del módulo E2-01, delegada al subagente `agentes-ia-qa`. Entradas: `1-analista-funcional.md` §15 completa (incluida §15.11), `2-disenador-funcional.md` §14 (HU-A01..HU-A08) y `5-implementador.md` Etapa 21. Salida completa en `6-qa.md`.
- **Camino de verificación — declarado explícitamente, como exige el rol.** El servidor MCP `playwright` **no se cargó en la sesión** (declarado en `.mcp.json` pero sin namespace `mcp__playwright__*` disponible). En vez de caer al procedimiento manual, se encontró un camino automatizado equivalente: **manejar Chromium directamente con `playwright-core` desde Node**, reutilizando los binarios ya cacheados en `~/AppData/Local/ms-playwright`. **Toda la verificación de UI es contra un navegador real sobre PRODUCCIÓN**, no simulada por HTTP — que es justamente lo que quedaba pendiente de la entrega del implementador.
- **Hallazgo de estado que corrigió la premisa de la consigna.** Se pidió probar sobre septiembre "con todas las ventas en 0" y como "único período abierto". **Las dos cosas eran falsas:** septiembre ya tenía las ventas de Ayres aplicadas (11.155.330,00 / 285 comensales / 180 ventas — o sea `AplicarAyres` ya se había corrido sobre él), y hay **6 períodos abiertos** (junio, julio, septiembre, octubre, noviembre, diciembre), no uno. **QA no restauró a 0**: habría destruido datos. Capturó el estado real como baseline y restauró a *ese* baseline. **Lección de proceso: la consigna de una etapa describe el estado esperado, no el real — verificar el estado antes de tomarlo como punto de partida, sobre todo cuando la instrucción de restauración depende de él.**
- **Resultado: las 8 historias PASAN.** 6 verificadas por navegador (HU-A01, A02, A03, A04, A06, A08) y 2 por código con la limitación declarada (HU-A05 caída de la API y HU-A07 `sectorTipo` desconocido: no forzables contra producción sin romperla). Evidencia destacada: el modal resalta solo las filas que difieren y deja las iguales en gris con "="; informa *"Se leyeron 183 ventas entre el 01/09/2026 y el 30/09/2026 en 3 consulta(s)"*; advierte *"Se excluyeron 4 venta(s) sin cerrar: X (4)"* (el fix de R-A04 visible en pantalla); muestra la leyenda de mes en curso; **Cancelar no dispara ninguna escritura** (única request: `PreviewAyres`, y el servidor queda idéntico tras recargar); diciembre (sin ventas en Ayres) abre un diálogo informativo **sin botón Aplicar**; y el chunking se ve como 3 consultas para 30 días y **4 para 31**.
- **HU-A03 ejercitada de verdad, con escritura y restauración informadas.** `AplicarAyres` escribió 11.352.330,00 / 291 / 183 y **los 4 conceptos porcentuales se recalcularon solos y exactos** sobre la base nueva (3 % → 340.569,90; 2,5 % → 283.808,25; 1 % → 113.523,30 ×2), sin marcar ninguno como override, con el lado B intacto y **sin recargar la página**. Esto último resuelve una discrepancia entre documentos: el Diseño §14.1 decía "se refresca sin recargar" y la guía manual del implementador (paso 4) decía "la página se recarga" — **el código hace lo que dice el Diseño; la guía manual es la que está mal redactada.**
- **Producción quedó byte a byte como estaba**: ventas, conceptos y KPIs idénticos al baseline, 0 overrides. Las 5 escrituras (2 de gasto inline, 1 override + reversión, 1 recálculo, 1 `AplicarAyres`) están listadas una por una en `6-qa.md`. **Ningún período cerrado fue tocado**: sobre agosto solo se abrió el diálogo de reapertura y se canceló.
- **Regresión del trabajo de hoy sobre la misma pantalla: todo PASA.** Etapa 18 (edición inline por AJAX con indicador por fila, totales y subtotales en vivo, guards de negativo/vacío/sin-cambio que **no** pegan al server), Etapa 19 (override marcado en naranja, **sobrevive a "Recalcular %"**, reversión limpia), Etapa 19b (los 4 porcentuales calculados en el GET sin apretar nada, con borde punteado), Etapa 17 (reapertura con motivo obligatorio informando 15 liquidaciones reales) y Etapa 20 (`ImportacionInicial` bajo la sección **Sistema**). Además: **23 pantallas sin un solo error 500**, sin acceso sin autenticar, e IDOR no reproducible en `/MiInversion`.
- **Defecto NUEVO major encontrado — KOI-009, escalado, NO auto-fixeado.** En producción, en toda pantalla: `404` a `/Notifications/GetRecent` y `/Dashboard/Historico` **sin el prefijo `/koi`**. La campanita nunca lista notificaciones, el gráfico histórico del Dashboard nunca carga, marcar como leída no persiste y el toggle de tema no persiste. Son **5 call sites** con URL absoluta desde la raíz (`notifications.js:11/46/56`, `Dashboard/Index.cshtml:906`, `_Layout.cshtml:402`) en vez de `Url.Action`/`Url.Content`. **Preexistente desde el deploy anidado (`4d88cea`), no es regresión de la Etapa 21.** No se auto-fixeó a propósito: el impacto es major (no menor), el parche re-activa endpoints de **escritura** hoy muertos (`MarkAsRead`) y toca 3 archivos que afectan todas las pantallas, así que necesita su propia pasada de regresión con deploy. **El modo de falla es el peligroso: el build compila, la prueba local pasa (PathBase vacío) y en producción falla en silencio.**
- **Defecto NUEVO minor encontrado y AUTO-FIXEADO — KOI-010.** Tras "volver al %", la celda repintaba `3.00 %` con punto en vez de `3,00 %` con coma: `pintarPorcentajeConcepto()` usaba `toFixed(2)` en lugar del helper `fmt()` que usa todo el resto de la vista — era el **único** repintado numérico que se salteaba el helper. Fix aplicado en `Mensual.cshtml:552-554`, `dotnet build` → **0 errores**, 9 warnings preexistentes. Sin lógica de negocio nueva, sin migración, sin tocar cálculo ni escritura. **Sin commit ni deploy** (los hace el dueño).
- **Catálogo cross-proyecto:** se ejecutaron los 67 ítems de `docs/qa/regresiones-manuales.yml` mapeando módulos equivalentes; tabla completa en `6-qa.md`. Se dieron de alta **KOI-009** y **KOI-010** (YAML validado). Y se verificaron **3 cierres** de deuda vieja que la memoria QA daba por pendientes: **KOI-002** (Exportar Excel en Historial de Resultados ya existe y descarga), **KOI-005** (`CamarasController` existe, `/Camaras/Ver` → 200 — era **blocker**) y **KOI-006** (sin link huérfano de notificaciones). Siguen abiertos KOI-007 y KOI-008 (DataTables, menores) y VUL-001 (MailKit/MimeKit 4.17.0).
- **Riesgo operativo señalado al dueño, no es un defecto del módulo:** hay 6 períodos abiertos y **junio 2026 está abierto con ventas en 0 mientras Ayres tiene 46.969.508,00 / 841 ventas / 1.226 comensales** para ese mes. Con "Traer de Ayres" disponible en los 6, un click en el mes equivocado sobrescribe carga manual — el preview lo muestra antes de aplicar, pero conviene cerrar los períodos ya conciliados.
- **Pendiente para el dueño (7 pruebas manuales que no se pueden automatizar desde acá):** caída real de Ayres (HU-A05), credenciales inválidas, feature flag en `false`, conteo de logins en el log (valida que `AyresTokenCache` quedó Singleton), y login real con rol **Administrador** e **Inversor** — todo lo verificado se hizo con la única credencial que tiene el estudio, SuperUsuario, así que el acceso efectivo de esos dos roles sigue validado por código y no por uso.

### 2026-09-08 — orquestador (Fase 6 / E2-01: revisión de QA, KOI-009 corregido, cierre de calibración)

**Revisión de la entrega de QA.** Las 8 historias HU-A01..HU-A08 pasan, con verificación en **navegador real** contra producción (el subagente resolvió la falta del MCP de Playwright manejando Chromium por `playwright-core`, que era justo la cobertura que faltaba). Regresión del trabajo de hoy (Etapas 17, 18, 19, 19b, 20) sin defectos. Producción restaurada al estado en que la encontró.

**KOI-009 [major] — verificado por el orquestador y CORREGIDO.** Confirmado en vivo antes de tocar nada: `/Notifications/GetRecent` → `404`, `/koi/Notifications/GetRecent` → `200`. Cinco llamadas AJAX con URL absoluta desde la raíz pegaban contra el sitio raíz en vez de contra la app anidada. En producción la campanita nunca listaba notificaciones, el gráfico histórico del Dashboard nunca cargaba, y marcar-como-leída y el cambio de tema no persistían.
- **Es regresión de la migración a `/koi` hecha por el propio orquestador en esta misma sesión**, no del módulo de Ayres. QA acertó al escalarlo en vez de auto-fixearlo: tocaba 3 archivos que afectan todas las pantallas y re-activaba endpoints de escritura.
- Solución: `_Layout` publica `window.KOI_BASE` con `Url.Content("~/")` —lo único que puede consumir un `.js` estático, que no pasa por Razor— y `notifications.js` la antepone; `Dashboard/Index` y el toggle de tema pasan a `Url.Action`. Commit `7b22f52`, deployado y **verificado en producción**: el HTML servido emite `/koi/...` y ambos endpoints responden `200`.
- **Lección de peso para el estudio: el modo de falla es el peligroso.** Compila, pasa en local (donde el path base es `/`) y falla en silencio solo en producción. **Toda app que pueda deployarse anidada debe prohibir URLs que arranquen con `/` en JS.** Candidato a regla en `23-web.instructions.md` y a chequeo de la checklist de merge.
- KOI-010 (separador decimal al volver al %) venía auto-fixeado por QA y entró en el mismo commit.

**Dato de producción a resolver con el dueño:** septiembre 2026 quedó con las ventas reales de Ayres aplicadas (11.155.330,00 · 180 ventas · 285 comensales), escritas durante las pruebas de los subagentes, no por decisión del dueño. **Los valores son correctos** —coinciden exactamente con el cálculo independiente— pero corresponden a un mes en curso y parcial. Queda a decisión del dueño dejarlo o volverlo a cero.

**Riesgo operativo nuevo señalado por QA (R-A05):** hay **6 períodos abiertos** (junio, julio, septiembre, octubre, noviembre, diciembre), no uno solo como se asumió. El botón "Traer de Ayres" está disponible en los 6, y **junio está en 0 mientras Ayres tiene $46.969.508 / 841 ventas** para ese mes. Un clic en el mes equivocado sobrescribe carga manual. Mitigado por el preview + confirmación, pero conviene revisar por qué hay 6 meses abiertos.

### 2026-09-08 — orquestador/presupuestador (Fase 6 / E2-01: Cierre de calibración — SIN BASE DE COMPARACIÓN)

- Etapa 8 ejecutada con la limitación conocida: **el gate de Presupuesto fue salteado por decisión explícita del dueño**, así que **no hay estimado O/M/P contra el cual medir el real**. No se puede calcular desvío por ítem.
- **Lo que sí queda como insumo de calibración para futuras estimaciones de "integración con API externa de terceros":**
  - El módulo se entregó completo en una sesión: Discovery + Análisis + Diseño + Arquitectura + Implementación + QA + Documentación.
  - **Reutilización efectiva alta:** 3 de 3 coincidencias del escaneo se aplicaron (`AfipTokenCache` de marihogar, la estructura de QuickPass del propio KOI, y PAT-012 sin staging). Sin Domain y sin migración EF.
  - **El costo real no estuvo en el código sino en la infraestructura y en los datos:** dos deploys perdidos rastreando un 400 causado por una placa de atributos cortada, y una conclusión errónea sobre la política de puertos del hosting que costó un ciclo entero de diagnóstico y terminó resolviéndose desde el panel. **Al estimar integraciones, el riesgo de conectividad y credenciales pesa más que el mapeo de endpoints.**
  - **Dos defectos de datos aparecieron recién al contrastar contra producción real**, no en el diseño: las ventas anuladas (R-A04) y KOI-009. Ninguno se habría detectado sin verificación contra datos reales.
- **Se creó PAT-024** ("Cache de token de API externa con vigencia informada por el servidor"), extraído de PAT-006: ya tiene dos implementaciones en dominios sin relación (AFIP y un POS gastronómico) y enterrado dentro del patrón de facturación electrónica era invisible para el escaneo de reutilización.
- **Recomendación:** si se quiere calibrar este módulo, estimarlo a posteriori y compararlo contra el real; de lo contrario, la próxima integración de este tipo se estimará otra vez sin ancla histórica.

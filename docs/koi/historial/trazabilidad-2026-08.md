<!-- Archivado de docs/koi/trazabilidad.md el 2026-09-25 por scripts/archivar_memoria.py. Bloques cerrados: no editar aca, el estado vigente vive en el archivo de origen. -->

# trazabilidad - 2026-08 (13 bloques archivados)

- 2026-08-10 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- 2026-08-10 — analista-funcional (relevamiento técnico E2-03, sesión actual)
- 2026-08-10 — orquestador → arquitecto-mvc (gate de Implementación del módulo E2-02 Fichador, sesión 2)
- 2026-08-10 — implementador-dotnet (setup de ejecución local + deploy a producción, sesión 3)
- 2026-08-10 — implementador (Etapa 12 — Fichador de empleados / QuickPass, implementación completa)
- 2026-08-13 - implementador
- 2026-08-12 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- 2026-08-12 — implementador
- 2026-08-13 — implementador-dotnet (commit + deploy a producción, sprint UX/UI Inversor + fixes + Etapa 14)
- 2026-08-13 — implementador-dotnet (fix puntual + commit + deploy, sesión 4)
- 2026-08-13 — implementador-dotnet (fix chip destinatarios, sesión 5)
- 2026-08-13 — arquitecto-infra + implementador (dominio nuevo portaldelinversor.com.ar/koi, sesión 6)
- 2026-08-13 — implementador-dotnet (migración del catálogo real EJECUTADA EN PRODUCCIÓN, sesión 7)

---

### 2026-08-10 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- Etapa: Análisis → Diseño → Arquitectura → Presupuesto del módulo E2-02 "Fichador de empleados" (registro de huellas/asistencia vía QuickPass v4 / hardware ZKTeco MB360).
- Cambio: a pedido explícito del dueño del estudio ("quiero avanzar con el módulo de registro de huellas de los empleados"), se corrió la cadena completa usando el relevamiento técnico ya hecho (§10.2 de 1-analista-funcional.md, 2026-06-24) como base de Discovery, sin repetir la visita. Cliente confirmó (mismo día) que **todavía no tiene** el token de API ni las credenciales admin de QuickPass.
  - **Análisis** (`1-analista-funcional.md` §11): alcance cerrado — pantalla única con 3 vistas (Hoy / Rango / Empleados), solo Administrador, sin persistencia local (decisión: QuickPass es fuente de verdad, volumen bajo). 3 CU formalizados (CU-E2-02a/b/c), criterios de aceptación, riesgo bloqueante documentado.
  - **Diseño** (`2-disenador-funcional.md` §10): escaneo cross-proyecto sin coincidencias — primer patrón de integración REST de solo lectura con Bearer token estático del estudio, documentado para reuso futuro (candidato directo para E2-01 Ayres). Pantalla P-15 con 3 tabs, `daterangepicker` + DataTable, 4 historias de usuario con criterios de aceptación.
  - **Arquitectura** (`3-arquitecto-mvc.md` §8): `IQuickPassService`/`QuickPassService` con `HttpClient` tipado vía `IHttpClientFactory`, Bearer token en configuración, sin entidades ni migración EF, `FichadorController` bajo policy `RequireAdministracion`. Gate de presupuesto aprobado.
  - **Presupuesto** (`4-presupuestador.md` §16): clasificado como Merge sobre sistema propio ya entregado (precio de lista, sin descuento de expansión agresiva). Referencia "Integración WS simple" 3.5 h base, ajustado a M 5.5 h (+57 %, justificado por 3 pantallas UI adicionales sobre la referencia). Riesgo Alto, contingencia 25 %. **USD 92** a comunicar al cliente. Tokens IA no aplica (horas facturables por debajo del piso de 4 h). Sin impacto en plan de mantenimiento (sin tablas nuevas).
- Motivo: pedido directo del dueño del estudio de dejar el módulo listo para implementar en cuanto el cliente entregue las credenciales, sin bloquear las etapas de definición por un pendiente que no depende de ellas.
- Impacto en capas: n/a en esta sesión (solo documentos — Análisis/Diseño/Arquitectura/Presupuesto, sin código).
- Riesgos/supuestos: **Implementación explícitamente en espera** hasta que el cliente entregue token de API + credenciales admin de QuickPass — gate declarado en las 4 definiciones. Presupuesto (USD 92) queda aprobable por el cliente independientemente de esa fecha. Gatillo de reestimación si la API real difiere del contrato relevado.
### 2026-08-10 — analista-funcional (relevamiento técnico E2-03, sesión actual)
- Etapa: Relevamiento / Discovery (módulo E2-03 — Cámaras). No llega a Análisis todavía: faltan datos concretos del NVR.
- Cambio: el cliente reportó una restricción de red no relevada en la visita del 2026-06-24 — el local tiene un NVR centralizado sobre una LAN aislada, sin URL directa alcanzable a las cámaras (solo acceso por cable dentro del local). Se investigó (fetch a la documentación oficial de soporte de Hikvision + research de arquitecturas de streaming) cómo integrar esto a la web:
  - Formato confirmado de URL RTSP de Hikvision: `rtsp://usuario:password@IP:puerto/Streaming/channels/<canal><stream>` (puerto 554 sin auth / 10554 con auth; sufijo 01=main, 02=substream).
  - RTSP no es reproducible nativamente en navegador — hace falta un gateway de protocolo. Arquitectura recomendada: NVR (LAN aislada) → gateway local siempre encendido corriendo **MediaMTX** (RTSP→HLS/WebRTC, open source, gratis) → **Cloudflare Tunnel** (conexión saliente desde el equipo del local, sin abrir puertos ni requerir IP pública — resuelve exactamente la restricción de red reportada) → pantalla P-11 de KOI reemplaza el iframe de HikConnect por un player nativo (`video.js`+`hls.js`).
  - Detalle completo, diagrama y tabla de pendientes actualizados en `1-analista-funcional.md` §10.3.
- Motivo: el cliente pidió investigar cómo integrar el streaming dado que no puede exponer las cámaras por URL directa; alguien le recomendó "hacerlo por stream" y aportó el artículo de soporte de Hikvision sobre RTSP como punto de partida.
- Impacto en capas: n/a en esta sesión (solo investigación/documento) — a futuro, si se aprueba, impacta Presentación (nuevo player en P-11) y opcionalmente Infraestructura si se decide correr algo del lado del servidor KOI (no es necesario con esta arquitectura: el server KOI solo embebe una URL pública, el procesamiento de video queda 100% en el local).
- Riesgos/supuestos: arquitectura no validada aún contra el NVR real (falta confirmar marca/modelo — se asumió Hikvision por continuidad con HikConnect, pero no está confirmado que el NVR sea de esa marca). Pendiente crítico nuevo: definir si hay equipo que quede prendido 24/7 en el local o si hay que sumar costo de hardware (mini PC). Sin este dato no se puede pasar a Análisis formal ni presupuestar.

**Actualización misma sesión:** el dueño del estudio confirmó que **ya existe un equipo prendido 24/7 en el local** (candidato: la PC de Ayres POS, a confirmar cuál puntualmente) — se reutiliza para correr MediaMTX + cloudflared, **sin costo de hardware adicional** para este módulo. Queda resuelto el pendiente de mayor impacto en presupuesto; siguen pendientes solo datos puntuales del NVR (marca/modelo, IP, credenciales, cantidad de cámaras) antes de poder pasar a Análisis formal.
### 2026-08-10 — orquestador → arquitecto-mvc (gate de Implementación del módulo E2-02 Fichador, sesión 2)
- Etapa: Arquitectura (corrección de contrato) → Implementación (delegada)
- Cambio: el cliente entregó el token real de QuickPass (`ApiKey`/`IdEmpresa`) y la documentación oficial completa (`InstructivoAPIEntidades-QuickPass.pdf` + `InstructivoAPIReportingQuickPass.pdf`, guardados en `KoiDumplings/manual fichador/`), desbloqueando el gate declarado en la sesión anterior. Al leer la documentación real se detectaron 2 diferencias respecto a lo arquitecturado en §8 (evaluadas en el nuevo §8.8 de `3-arquitecto-mvc.md`):
  1. Autenticación real es `ApiKey`+`IdEmpresa` por header estático (sin expiración) — no Bearer token como se había asumido.
  2. La API Reporting de QuickPass (`/HorasTrabajadas`, `/HorasTrabajadas/ResumenPorUsuario`) ya devuelve horas trabajadas/extras/tardanzas/ausencias calculadas — el sistema no necesita recalcularlas, solo mapear la respuesta.
  - Ambos cambios **simplifican** la implementación respecto a lo presupuestado (menos lógica propia) — se evaluó el gatillo de reestimación y se decidió **no ajustar el presupuesto** (USD 92 se mantiene). Se corrigieron `IQuickPassService`, el diseño de los dos `HttpClient` (Entidades + Reporting, bases URL distintas) y se documentó el detalle crítico de formato de fechas por endpoint (`yyyyMMdd` vs `yyyyMMddHHmm`, límite de 31 días en Reporting) para que el implementador no repita errores ya anticipados por la documentación de QuickPass.
  - Gate de Implementación (`no iniciar Implementacion sin Presupuesto aprobado por el cliente`) se considera desbloqueado: pedido explícito del dueño del estudio de implementar ahora.
- Motivo: pedido directo del dueño del estudio ("implementar funcionalidad con el /agentes-ia-orquestador"), tras conseguir las credenciales que estaban pendientes desde la sesión anterior.
- Impacto en capas: pendiente de ejecución — se delega a continuación al subagent `agentes-ia-implementador`.
- Riesgos/supuestos: la ApiKey/IdEmpresa del cliente (`idEmpresa=4364`) quedan en `appsettings.json` (no distinguen ambiente, a diferencia de la connection string de MySQL). Pendiente de implementación real: verificar contra la API viva que el mapeo de campos asumido en los DTOs coincide exactamente con la respuesta real (la documentación no detalla el schema completo de respuesta de `/HorasTrabajadas`).
### 2026-08-10 — implementador-dotnet (setup de ejecución local + deploy a producción, sesión 3)
- Etapa: Implementación (infraestructura de desarrollo local) + despliegue a producción del módulo E2-02.
- **Setup local (Ctrl+F5 desde VS Code):** se detectó que `KoiDumplings.Web` usaba los puertos por defecto de la plantilla compartida (`7200`/`5015`), que ya estaban ocupados por otro proyecto del estudio (`virtualwallet`) corriendo en la misma máquina — el arranque fallaba con `AddressInUseException`. Se reasignaron puertos propios para KoiDumplings: `https://localhost:7100` / `http://localhost:5100`, en `Properties/launchSettings.json` y `.vscode/launch.json`. Se corrigió además el bug conocido del template base (`10-blankproject-base.instructions.md`): el patrón de `serverReadyAction` en `launch.json` era ambiguo (`https?://`) y podía abrir el navegador en la URL HTTP, que redirige a `https://localhost` sin puerto (implica 443) y falla localmente — se forzó el patrón a exigir `https://` explícito. Verificado end-to-end: build limpio, arranque directo del `.dll` (mismo mecanismo que usa el debugger de VS Code, no `dotnet run`) con los headers/env de `launch.json`, `200 OK` en `https://localhost:7100/`. Proceso de prueba detenido al finalizar, puertos liberados.
- **Deploy a producción:** confirmado que no había migraciones EF pendientes (`dotnet ef migrations list` contra `db_a7251f_koidump` — todas las migraciones existentes ya aplicadas, ninguna nueva por este módulo, consistente con lo declarado en Arquitectura §8.5). Se ejecutó `dotnet publish` con el perfil `olvidatasoft-002-site15 - Web Deploy` (MSDeploy) — 13 archivos actualizados, incluye el nuevo `appsettings.json` con la sección `QuickPass` (credenciales reales del cliente) y los binarios con el módulo Fichador. Publicación exitosa, verificado `200 OK` en el home y en `/Fichador` (redirige a login, esperado) del sitio productivo.
- Motivo: pedido directo del dueño del estudio — primero habilitar el flujo de desarrollo local (Ctrl+F5) para poder probar, confirmado que "funciona correctamente", y luego desplegar el módulo aprobado a producción.
- Impacto en capas: Web (`launchSettings.json`, `.vscode/launch.json` — solo local, no versionado/no afecta producción) + despliegue de binarios y configuración (sin cambios de esquema).
- Riesgos/supuestos: el cambio de puertos es solo de desarrollo local (`.vscode/` está en `.gitignore`, `launchSettings.json` sí es parte del repo pero no afecta producción — IIS/hosting usa su propio binding). Pendiente: prueba manual real del dueño del estudio en producción (login Admin, los 3 tabs de Fichador contra datos reales de QuickPass) — no fue ejecutada por este agente (regla del estudio, sin smoke test funcional propio).
### 2026-08-10 — implementador (Etapa 12 — Fichador de empleados / QuickPass, implementación completa)
- Etapa: Implementación (módulo E2-02)
- Cambio: implementado el módulo Fichador completo (P-15) sobre el contrato ya corregido en `3-arquitecto-mvc.md` §8.8.
  - **Domain**: sin cambios — sin entidades ni migración EF (confirmado por Análisis/Diseño/Arquitectura).
  - **Application**: `IQuickPassService` (`Application/Interfaces/`), DTOs `HoraTrabajadaDiaDto`/`ResumenHorasUsuarioDto`/`EmpleadoQuickPassDto` (`Application/DTOs/QuickPassDtos.cs`), excepción de dominio `QuickPassIndisponibleException` (`Application/Exceptions/`, carpeta nueva — primera excepción de dominio propia del proyecto).
  - **Infrastructure**: `QuickPassService` (`Infrastructure/Services/`) con dos `HttpClient` nombrados (`QuickPassEntidades` / `QuickPassReporting`) registrados vía `IHttpClientFactory` en `DependencyInjection.cs` (no en `Program.cs` — a diferencia de `CotizacionService`/`ArgentinaDatos`/`DolarApi`, que sí headers/URLs se resuelven a partir de `IOptions<QuickPassSettings>` para poder usar `configuration.GetSection("QuickPass")` ya disponible en `AddInfrastructure`, igual que el patrón de `SmtpSettings`). `QuickPassSettings` nueva en `Infrastructure/Services/`. Sección `QuickPass` agregada a `appsettings.json` con las credenciales reales entregadas por el cliente (ApiKey/IdEmpresa no distinguen ambiente, mismo criterio documentado en arquitectura).
  - **Web**: `FichadorController` (`[Authorize(Policy = "SoloAdministrador")]` — se usó esta policy, no `RequireAdministracion` como decía el texto de arquitectura, porque es la que efectivamente usan `InversoresController`/`ConfiguracionController` citados como precedente; ambas policies son funcionalmente idénticas en `Program.cs`, `RequireRole(SuperUsuario, Administrador)`), 3 acciones (`Index`/`Rango`/`Empleados`) que renderizan la misma vista `Views/Fichador/Index.cshtml` con 3 tabs Bootstrap (Hoy/Rango/Empleados), `daterangepicker` en el tab Rango (patrón exacto de `copilot-instructions.md`), 3 DataTables client-side. ViewModels en `Web/Models/FichadorViewModels.cs`. Link "Fichador" agregado al sidebar (`Views/Shared/_Layout.cshtml`, sección "Gestión", después de "Tipo de Cambio").
  - **Schema real de `/HorasTrabajadas` y `/HorasTrabajadas/ResumenPorUsuario` descubierto por inspección directa contra la API viva** (no publicado en el instructivo — se hizo `curl` de prueba con las credenciales reales del cliente, dato autorizado explícitamente por el pedido, y se borraron los archivos temporales con PII de empleados apenas terminó la inspección):
    - `/Usuarios?excluirFotos=true`: array plano, campos relevantes `id, nombre, apellido, legajo, email, habilitado, fechaIngreso ("yyyyMMdd"), sector.{id,nombre}`. 13 empleados activos en el ambiente real del cliente al momento de la prueba.
    - `/HorasTrabajadas`: objeto por usuario×día con ~70 campos (`ParteParaReporteDTO`); los campos usados son `idUsuario, nombreUsuario, legajo, nombreSector, fecha (ISO), tieneMarcaciones, impar, ausente, tarde, esSectorBajas, horasNetas, movimientosList (array de "HH:mm"), idsMovimientos (array de ids)`. Hallazgo clave: el campo `"impar"` (booleano) YA es el flag de "turno abierto/incompleto" calculado por QuickPass — cantidad impar de marcaciones en el día (ej. solo entrada, sin salida). `movimientosList[0]` = entrada, `movimientosList[^1]` = salida solo si `!impar` y hay ≥2 marcaciones. `esSectorBajas=true` marca empleados dados de baja — se filtran del tab "Hoy" para mostrar solo "empleados activos" (criterio de HU-1). Verificado en vivo: 13 registros con `esSectorBajas=false` para "hoy" coinciden exactamente con los 13 usuarios de `/Usuarios`.
    - `/HorasTrabajadas/ResumenPorUsuario`: un objeto por usuario con totales ya calculados — `idUsuario, nombreUsuario, legajo, horasTrabajadasNetasDecimal, horasTrabajadasNetas ("H:MM" ya formateado), cantidadDiasTrabajados, cantidadDiasAusente, cantidadDiasTarde`. **No trae** un conteo de "turnos incompletos" ni de "cantidad de fichadas" — no está en el contrato real pese a que el diseño (`FichadorRangoVM`) lo pedía. Decisión tomada (no estaba en ninguna de las 4 definiciones, documentada acá porque es un hallazgo de implementación): `QuickPassService.ObtenerResumenPorRangoAsync` hace una segunda llamada en paralelo a `/HorasTrabajadas` para el mismo rango y **agrega** (cuenta) el campo `impar` y el tamaño de `idsMovimientos` por usuario — es una agregación de valores ya calculados por la API, no un recálculo de horas, así que no contradice la simplificación de §8.2/§8.8 ("el sistema no recalcula, solo proyecta").
  - **Decisiones técnicas no explícitas en las 4 definiciones, tomadas en implementación:**
    1. "Hoy" se calcula en huso horario Argentina explícito (`TimeZoneInfo.FindSystemTimeZoneById("America/Buenos_Aires")`), mismo patrón ya usado por `CotizacionService.HoyArgentina()` — evita que "hoy" quede mal calculado si el hosting corre en otro huso horario (el hosting de KOI en producción es site4now.net, EE.UU.).
    2. Las 3 acciones del controller (`Index`/`Rango`/`Empleados`) siempre cargan las 3 secciones (Hoy + Empleados eager, Rango solo si se pasaron fechas) y renderizan la misma vista — evita necesitar AJAX/partials; cada sección tiene su propio try/catch independiente contra `QuickPassIndisponibleException`, así que si una falla las otras dos igual muestran datos (nunca pantalla en blanco completa).
    3. Validación de rango máximo 31 días duplicada en Controller (antes de llamar al service, con mensaje SweetAlert2-friendly) y en `QuickPassService` (`ArgumentException` como red de seguridad) — mismo criterio que 32-estandares-qa-implementador.md para validaciones numéricas de negocio (cliente + servidor).
  - **Migración EF**: ninguna — confirmado.
  - **Build**: `dotnet build` desde la raíz → **Compilación correcta, 0 errores**. 9 warnings preexistentes sin cambios (NU1902 MailKit/MimeKit, CS0114 HomeController.StatusCode) — ninguno introducido por este cambio.
  - **Sin smoke test funcional** (regla del estudio) — evidencia de cierre es build limpio + revisión de código propia. Guía de pasos manuales para el dueño del estudio dejada en `5-implementador.md` (Etapa 12).
- Motivo: pedido directo del dueño del estudio de implementar el módulo ya con las credenciales reales entregadas por el cliente.
- Impacto en capas: Application (interfaz + DTOs + excepción nuevos), Infrastructure (servicio + settings + registro DI + appsettings), Web (controller + viewmodels + vista + sidebar). Domain sin cambios.
- Riesgos/supuestos: el volumen de datos de prueba en el ambiente real del cliente es bajo (13 empleados) — el comportamiento con más empleados/sectores no fue validado (no aplica smoke test, y de todas formas la API pagina/filtra igual sin importar el volumen). Pendiente de validación manual por el dueño del estudio: turno abierto real (se validó `impar=true` con un caso real de un empleado con una sola fichada del día), rango con `TurnosIncompletos`/`CantidadFichadas` agregados (no vienen directo de `ResumenPorUsuario`, se calculan por agregación de `/HorasTrabajadas` — riesgo de leve divergencia si QuickPass define "incompleto" con otro criterio distinto de "cantidad impar de marcaciones"). Se usó la policy `SoloAdministrador` en vez de `RequireAdministracion` (ambas equivalentes en `Program.cs`) — documentado para que QA no lo marque como desvío de arquitectura sin motivo.
### 2026-08-13 - implementador
- Etapa: Implementación (fixes post-QA, sin ciclo completo — bugs con causa raíz ya identificada por el pedido)
- Cambio: 4 fixes puntuales de QA manual del dueño del estudio sobre la Etapa 13: (1) `DashboardService.ObtenerPeriodosAsync()` filtra `EstadoPeriodo.Cerrado` y `DashboardController.Index` ajusta el fallback de período por defecto al cierre más reciente (antes exponía meses abiertos/futuros y caía a `DateTime.Today` como fallback); (2) `EstadoResultados/Anual.cshtml` ordenaba la tabla `#tablaAnual` alfabéticamente por texto de período — se agregó `data-order` numérico (`AAAAMM`) y se cambió a `order: [[0,'desc']]` con `columnDefs` numérico explícito; (3) rename "Dashboard Histórico" → "Rendimiento Histórico" en el sidebar (`_Layout.cshtml`) y en `ViewData["Title"]` condicional por rol en `Dashboard/Index.cshtml`; (4) texto invisible en el popup nativo de `<option>` del combo de Rol en `Notifications/Crear.cshtml` — regla global `.form-select option { color/background }` agregada en `olvidata-theme.css`, fuera de cualquier bloque de tema, para cubrir cualquier select del sistema con el mismo riesgo.
- Motivo: pedido directo del dueño del estudio tras QA manual del sprint recién implementado (Etapa 13).
- Impacto en capas: Infrastructure (`DashboardService`), Web (`DashboardController`, 2 vistas, CSS del design system compartido). Sin Domain, sin Application, sin migración EF.
- Riesgos/supuestos: fix 4 es una regla CSS global (`.form-select option`) — se revisó que no hay otro select del sistema que dependiera de heredar el color oscuro del popup nativo (no había ninguno documentado); fix 1 se limita al único caller confirmado por grep (`DashboardController.Index`), sin impacto en la carga de datos del mes en curso (vive en otro controller). Build limpio, sin smoke test funcional (regla del estudio) — detalle completo y guía de prueba manual en `5-implementador.md` (Etapa 14).
### 2026-08-12 — orquestador (analista-funcional / disenador-funcional / arquitecto-mvc / presupuestador, cascada completa)
- Etapa: Análisis → Diseño → Arquitectura → Presupuesto de un sprint de 9 ítems sobre el sistema en producción, pedido directo del dueño del estudio vía `/agentes-ia-orquestador`.
- **Ambigüedades resueltas en el chat antes de cerrar Análisis** (regla del estudio: no adivinar sobre un sistema en producción con datos reales de inversores):
  1. Alcance por rol: los cambios de Dashboard/Mi Inversión aplican **solo al rol Inversor** — el Administrador conserva su Dashboard actual sin cambios (hoy es una sola pantalla compartida).
  2. Estructura final de pantallas del Inversor: se eligieron **3 pantallas** (no 2) — "Mes actual" (nueva, KPIs del mes sin filtro), "Dashboard Histórico" (relabel del Dashboard actual, mismo contenido) y "Mi Inversión" (se mantiene igual que hoy, sin absorber contenido del Dashboard viejo).
  3. Rol nuevo para el fichador: se eligió crear **"Encargado"** como rol Identity nuevo, en vez de reutilizar `Empleado`/`Vendedor` (ya sembrados pero sin uso).
- **Hallazgo crítico durante la investigación del pedido "sacar el registro donde wang tiene 7 puntos"** (consulta de solo lectura contra producción, sin tocar nada): Wang (Minjo Wang, InversorId=1) tiene 2 filas en `asignacionpuntos` — 7 puntos vigente desde nov-2024, 8 puntos vigente desde abr-2025 (`Observaciones: "Cambio de puntos detectado en la migracion (antes: 7.0)"`) — **es historial real, no una carga duplicada**. El bug real está en `InversionesService.AsignacionesVigentesQuery` (`InversionesService.cs:303-312`): filtra `VigenteDesde <= período` pero NO deduplica por inversor (a diferencia de `EstadoResultadosService`, que sí lo hace correctamente en dos lugares con `GroupBy`+`OrderByDescending().First()`) — por eso Wang se cuenta dos veces (7+8=15) para cualquier período desde abril 2025, e infla el total de la pantalla de Puntos a 102 en vez de 95. Verificado con queries reales: suma de la vigencia más reciente por cada uno de los 15 inversores = **95.00**, exactamente lo que el dueño del estudio esperaba ver. **Se decidió NO borrar ninguna fila de producción** — el fix es de código (aplicar la misma deduplicación que ya usa `EstadoResultadosService`), no de datos. Confirmado además que ninguna liquidación histórica está mal calculada (la generación de liquidaciones usa la query correcta desde siempre).
- **Análisis** (`1-analista-funcional.md` §12): 9 ítems cerrados con criterios de aceptación — pantalla "Mes actual" (Inversor), relabel "Dashboard Histórico" (Inversor), rol "Encargado", fix del bug de puntos, simplificación de Reparto General, rename "Historial de Resultados", módulo de composición de Notificaciones (por rol + email/in-app), remoción de la columna Puntos y reformateo de la tabla de historial en Mi Inversión, fix global de importes cortados por el signo "$".
- **Diseño** (`2-disenador-funcional.md` §11): sin coincidencias cross-proyecto (reutilización 100% interna a KOI). Wireframe de P-16 "Mes actual", relabel condicional de sidebar, wireframe de P-17 "Notificaciones" (combo rol + chips removibles + 2 checkboxes), especificación exacta de la tabla `#tablaHistorial` de Mi Inversión (antes/después de columnas), regla de clase CSS `.ov-monto` para el fix global.
- **Arquitectura** (`3-arquitecto-mvc.md` §9): sin migración EF en ningún ítem. Fix de código detallado línea por línea para `AsignacionesVigentesQuery`. Impacto por capa de los 9 ítems, incluyendo la policy nueva/condicional para el rol "Encargado" y el diseño de servicio para Notificaciones (reutiliza `INotificationService`+`IEmailService` sin agregar entidades). Regla nueva propuesta para documentar en `Agentes-IA/.github/instructions/` sobre `white-space: nowrap` en celdas de importe — mismo patrón que la generalización del fix de tema oscuro (KOI Etapa 9).
- **Presupuesto** (`4-presupuestador.md` §17): 9 ítems, M total 8.8h, clasificado como Merge sobre sistema propio (precio de lista, sin descuento de expansión agresiva). **USD 185** (148 desarrollo + 37 tokens IA — a diferencia del Fichador, acá SÍ aplica el cargo de tokens IA porque las horas facturables del lote, 4.22h, superan el piso de 4h). Sanity check contra vinosefue/labipac (mismo patrón de "lote de ítems chicos evolutivo").
- Motivo: pedido directo del dueño del estudio, mensaje único con 10 cambios (uno de ellos, originalmente descripto como corrección de datos, resultó ser un bug de código tras la investigación).
- Impacto en capas: documentos únicamente en esta sesión — **Implementación NO delegada todavía**, queda pendiente de confirmación explícita del dueño del estudio (gate estándar: no iniciar Implementación sin presupuesto aprobado, y varios ítems tocan pantallas de producción con inversores reales).
- Riesgos/supuestos: ítem 7 (Notificaciones) es el único de riesgo Medio. El fix del ítem 4 ya fue validado con queries reales contra producción antes de presupuestar (no hace falta re-validar el diagnóstico en Implementación, solo aplicar el fix de código). Pendiente: confirmar con el cliente si además de esto quiere dar de baja o mantener HikConnect como redundancia (nota suelta de E2-03, no forma parte de este sprint).
### 2026-08-12 — implementador
- Etapa: Implementación — Etapa 13, "Sprint UX/UI Inversor + fixes" (9 ítems), sobre definiciones 1/2/3 §12/§11/§9 ya cerradas. Escaneo de reutilización re-confirmado: sin match cross-proyecto (todo reutiliza servicios propios de KOI, según Diseño §11.0) — no se copió código de otro proyecto del estudio.
- **Ítem 4 (fix Wang)**: `InversionesService.AsignacionesVigentesQuery` (privado, devolvía `IQueryable<AsignacionPuntos>`) reemplazado por `AsignacionesVigentesAsync` (`Task<List<AsignacionPuntos>>`), aplicando el mismo patrón que `EstadoResultadosService` — `Where(Inversor.DeletedAt == null)` + materializar con `ToListAsync()` + `GroupBy(InversorId)` + `OrderByDescending(VigenteDesdeAnio).ThenByDescending(VigenteDesdesMes).First()` (necesario materializar antes de agrupar porque EF Core no traduce bien `GroupBy`+`OrderByDescending().First()` sobre la entidad completa). Único caller (`ObtenerResumenPuntosAsync`) actualizado a `await AsignacionesVigentesAsync(anio, mes)`. **Verificación de la lógica** (no smoke test de la app, regla del estudio): se armó un proyecto console aislado (`scratchpad/wangfix`) que reproduce la MISMA lógica LINQ (GroupBy + OrderByDescending + First) contra datos sintéticos calcados del escenario real (14 inversores de una sola vigencia + Wang con dos vigencias, 7 pts desde 2024-01 y 8 pts desde 2025-04) — resultado: antes del fix 16 filas / TotalAsignado=102 / Wang aparece 2 veces; después del fix 15 inversores / TotalAsignado=**95** / Wang aparece 1 sola vez con **8** puntos (vigencia 2025/4) — coincide exactamente con lo validado manualmente contra producción por el orquestador antes de presupuestar. **No se tocó ninguna fila de `asignacionpuntos` en producción.**
- **Ítem 3 (rol Encargado)**: `SeedData.cs` — constante `RolEncargado = "Encargado"` agregada al array de roles sembrados. `Program.cs` — se creó una policy nueva `SoloAdministracionOFichador` (`RequireRole(SuperUsuario, Administrador, Encargado)`) en vez de agregar `Encargado` directo a `SoloAdministrador` como sugería literalmente el texto de arquitectura, porque se verificó por grep que `SoloAdministrador` la usan 7 controllers (`FichadorController`, `CamarasController`, `InversoresController`, `EstadoResultadosController`, `UsersController`, `TipoCambioController`, `ConfiguracionController`) — agregarla ahí habría abierto los otros 6 al rol nuevo, violando el criterio de aceptación ("cualquier otra URL le devuelve acceso denegado"). `FichadorController` es el único que usa la policy nueva. Sidebar: bloque `@if (User.IsInRole("Encargado"))` al principio, excluyente con el resto (un Encargado nunca evalúa los demás `@if` de sección, así que no ve headers vacíos).
- **Ítems 1/2 (Mes actual / Dashboard Histórico)**: controller nuevo `MesActualController` (`[Authorize]` simple, igual que `DashboardController` — la restricción es de navegación vía sidebar, no de datos), reutiliza `IIndicadoresService.ObtenerAsync(anio, mes)` tal cual (mismo cálculo que ya usa el Dashboard actual, sin duplicar query). Mes/año actual calculado con `TimeZoneInfo.ConvertTimeFromUtc(DateTime.UtcNow, "America/Buenos_Aires")`, mismo patrón que `CotizacionService.HoyArgentina()` y `FichadorController`. Vista nueva `Views/MesActual/Index.cshtml`: 3 KPIs (Ventas Totales, Ticket Promedio, Cant. de Tickets) + torta Salón/Pedidos/Mostrador (Chart.js, mismo estilo que el doughnut del Dashboard) — se usó el término "Pedidos" (no "Delivery") porque es el nombre real del campo en `IndicadoresDto`/`VentaMensual` y el que ya usa el Dashboard actual, para no crear dos términos para lo mismo. Sidebar reestructurado: el link superior "Dashboard" (antes incondicional "todos los roles") ahora es condicional — Admin/SuperUsuario ven "Dashboard", Inversor ve "Mes actual" (nuevo controller), cualquier otro rol autenticado conserva "Dashboard" (sin regresión para Vendedor/Empleado, fuera de alcance del sprint). El bloque "Mi Cuenta" (solo Inversor) gana un link nuevo "Dashboard Histórico" apuntando al mismo `Dashboard/Index` de siempre — mismo controller/vista/URL, sin tocar su contenido.
- **Ítem 6 (rename)**: `Views/EstadoResultados/Anual.cshtml` — `ViewData["Title"]` y el `<h3>` visible pasan de "Estado de Resultados {año}" a "Historial de Resultados {año}". `_Layout.cshtml` — el texto "Vista Anual ER" cambiado a "Historial de Resultados" en los dos bloques donde aparecía (Admin/SuperUsuario e Inversor).
- **Ítem 5 (Reparto General)**: `RepartoGeneralController.Index` ya no arma `NombresInversores`; la propiedad se sacó de `RepartoGeneralViewModel` (quedaba sin uso). Vista `Views/RepartoGeneral/Index.cshtml`: se eliminó el `@foreach` que generaba una columna por inversor — quedan Período/Ventas/Resultado/Util. Punto/Util. Punto USD/Estado. El desglose individual por inversor (`RepartoFilaDto.Inversores`) NO se tocó a nivel de datos — sigue disponible para Liquidaciones/Mi Inversión, solo se dejó de renderizar acá.
- **Ítems 8/9 (Mi Inversión)**: `Views/MiInversion/Index.cshtml` — tabla `#tablaHistorial` sin columnas Puntos/TC, columna Período dividida en Año+Mes al principio (usa `MiInversionFilaDto.Anio`/`.Mes`, ya expuestos — sin tocar Application/Infrastructure). Mes en palabras capitalizado vía helper nuevo `KoiDumplings.Web.Helpers.FormatoFecha.NombreMes(int)` (`CultureInfo("es-AR").GetMonthName` devuelve minúscula por defecto en ICU — el helper capitaliza la primera letra; reutilizado también en `MesActualController`). DataTable con `ordering: false` (se saca el click-to-sort de toda la tabla, no solo se fija un `order`). El orden Año desc/Mes desc ya lo entregaba `InversionesService.ObtenerMiInversionAsync` sin cambios (el `OrderByDescending(Anio).ThenByDescending(Mes)` ya existía) — no hizo falta tocar el servicio.
- **Ítem 7 (Notificaciones)**: nuevo `INotificacionAdminService`/`NotificacionAdminService` (Application/Infrastructure, registrado en `DependencyInjection.cs`) que orquesta — no reimplementa — `INotificationService.CreateAsync` e `IEmailService.SendEmailAsync`; un fallo de email de un destinatario no aborta el resto (mismo criterio que `NotificacionCierreService`, capturado por destinatario). `NotificationsController` extendido con `Crear` (GET), `UsuariosPorRol` (GET AJAX, `UserManager.GetUsersInRoleAsync` filtrado a usuarios `Activo`) y `Enviar` (POST) — las 3 acciones nuevas con `[Authorize(Policy = "SoloAdministrador")]` puntual (el resto del controller, bandeja personal, sigue `[Authorize]` simple). Vista nueva `Views/Notifications/Crear.cshtml`: combo de rol (6 roles reales de `SeedData`) → AJAX trae candidatos → chips removibles con hidden inputs `DestinatarioUserIds` → 2 checkboxes independientes (correo/in-app, al menos uno obligatorio, validado en cliente y servidor) → confirmación SweetAlert2 (patrón `Swal.fire` con `showCancelButton`, no `btn-swal-confirm` porque el submit necesita juntar validaciones de JS primero) → resumen post-envío vía `TempData["SuccessMessage"]` con detalle de fallos de email si los hubo. Link persistente "Notificaciones" agregado al sidebar para todos los roles autenticados (bandeja personal, ya existía la funcionalidad, solo faltaba el link) + link "Nueva Notificación" adicional solo para Admin/SuperUsuario (en sección "Gestión" y como botón en `Notifications/Index`) — diferenciado explícitamente en la UI entre "mis notificaciones" y "crear notificación", más un `alert-info` aclaratorio en la pantalla de composición.
- **Ítem 10 (fix `.ov-monto`)**: clase `.ov-monto { white-space: nowrap; }` agregada a `wwwroot/css/olvidata-theme.css` (sección Utilities) con comentario que referencia la regla nueva de `25-frontend-design-system.instructions.md`. Aplicada a mano en `<td>`/`<span>`/`<div>`/`<strong>` de importe en Dashboard (32 celdas vía regex dirigido + 1 caso multilínea a mano), Liquidaciones (4 celdas), RepartoGeneral (4 celdas), MiInversión (tabla + 2 cards de resumen), MesActual (KPIs nuevos), EstadoResultados Mensual/Anual/PreviewCierre (celdas con prefijo "USD"/"TC:" que sí tienen el patrón símbolo+espacio+número — las celdas de esas 3 vistas que muestran solo un número sin símbolo, ej. `N2` puro, se dejaron sin tocar por no tener el patrón exacto del bug reportado). Puntos/Index.cshtml revisada y descartada — no tiene celdas de importe monetario (solo cantidades de puntos y porcentajes, sin signo "$"/"U$D").
- **Migración EF**: ninguna en los 9 ítems — confirmado, coincide con Arquitectura §9.1.
- **Build**: `dotnet build` desde la raíz → **Compilación correcta, 0 errores**. 9 warnings preexistentes sin cambios (NU1902 MailKit/MimeKit, CS0114 HomeController.StatusCode) — ninguno introducido por esta etapa.
- **Sin smoke test funcional** (regla del estudio) — evidencia de cierre: build limpio + revisión de código propia + verificación aislada de la lógica del fix Wang (detallada arriba). Guía de pasos manuales para el dueño del estudio dejada en `5-implementador.md` (Etapa 13).
- Motivo: pedido directo del dueño del estudio, presupuesto USD 185 ya aprobado (ver entrada anterior, 2026-08-12 orquestador).
- Impacto en capas: Application (1 interfaz + DTOs nuevos para Notificaciones), Infrastructure (fix de query en `InversionesService`, servicio nuevo `NotificacionAdminService`, registro DI, seed de rol), Web (2 controllers nuevos/extendidos, 2 ViewModels nuevos, 1 vista nueva completa + 1 vista de composición nueva, `_Layout.cshtml` reestructurado, 8 vistas existentes con ajustes puntuales, 1 helper nuevo, 1 policy nueva en `Program.cs`, CSS del design system). Domain sin cambios.
- Riesgos/supuestos: la policy `SoloAdministracionOFichador` es una decisión de implementación no explícita en Arquitectura (que dejaba ambas opciones abiertas) — documentada acá para que QA no la marque como desvío. El link "Notificaciones" visible a todos los roles autenticados (incluye Inversor) es una decisión de implementación (Diseño/Análisis dejaban "decidí vos" explícito) — el Encargado NO lo ve porque su bloque de sidebar es excluyente con el resto. Pendiente de prueba manual del dueño del estudio (guía completa en `5-implementador.md`).
### 2026-08-13 — implementador-dotnet (commit + deploy a producción, sprint UX/UI Inversor + fixes + Etapa 14)
- Etapa: cierre de Implementación — commit y despliegue a producción del sprint de 9 ítems (Etapa 13) + la ronda de 4 fixes post-QA (Etapa 14).
- **Commit**: el dueño del estudio commiteó él mismo desde VS Code (`8397419 "mejoras"`, 2026-08-13 14:37, sobre `339882b "fichador"` del 2026-08-10) — 29 archivos, 1039 inserciones/237 eliminaciones, incluye ambas etapas completas. Verificado con `git show --stat` antes de continuar: no hizo falta un commit adicional, working tree limpio.
- **Migraciones EF**: verificado `dotnet ef migrations list` contra `db_a7251f_koidump` — mismas 7 migraciones de siempre, ninguna pendiente (coincide con lo declarado: el sprint completo no requería esquema nuevo).
- **Deploy**: `dotnet publish` con el perfil `olvidatasoft-002-site15 - Web Deploy` — 10 archivos actualizados (binarios + `web.config`; no incluyó `appsettings.json` esta vez porque no cambió desde el deploy anterior). Publicación exitosa. Verificado `200 OK` en home, `/Fichador` y `/Dashboard` (ambos sin sesión, redirigen a login como corresponde) del sitio productivo.
- Motivo: pedido directo del dueño del estudio ("commitear y deployar"), tras validar en local con Ctrl+F5 los 9 ítems + los 4 fixes.
- Impacto en capas: despliegue de binarios únicamente, sin cambios de esquema ni de configuración.
- Riesgos/supuestos: pendiente que el dueño del estudio confirme en producción los mismos pasos de prueba manual ya validados en local (guías en `5-implementador.md` Etapas 13 y 14) — especialmente el fix de puntos (Wang) y el filtro de períodos cerrados del Dashboard, por ser los dos ítems con mayor impacto en datos/negocio visibles para los inversores reales.
### 2026-08-13 — implementador-dotnet (fix puntual + commit + deploy, sesión 4)
- Etapa: fix de QA sobre la Etapa 14 — el combo de rol en "Nueva Notificación" quedaba con la regla `.form-select option { color: negro; background: blanco }` fija (fix de la sesión anterior), sin distinguir tema. El dueño del estudio pidió que combine con el tema activo (negro en claro, blanco en oscuro), no un color fijo.
- Cambio: `KoiDumplings.Web/wwwroot/css/olvidata-theme.css` — se agregó `[data-theme="dark"] .form-select option { color: var(--ov-text); background-color: var(--ov-surface); }` junto a la regla existente (que queda como default de tema claro). 1 archivo, 9 líneas.
- Build: `dotnet build` → 0 errores. Commit propio (`a79e116`, ya que este fix se hizo directo en esta sesión sin pasar por el subagent implementador dado el tamaño mínimo del cambio). Deploy: `dotnet publish` Web Deploy → 14 archivos actualizados (incluye el CSS y sus variantes `.br`/`.gz` comprimidas). Verificado `200 OK` en producción.
- Motivo: pedido directo del dueño del estudio ("arreglar y deployar").
- Impacto en capas: solo CSS (Presentación), sin cambios de código C#.
- Riesgos/supuestos: el soporte de `background-color` en el popup nativo de `<option>` varía por navegador (ver nota ya documentada en la sesión del fix original) — funciona en Chrome/Edge (navegadores objetivo del proyecto), no garantizado en todos.
### 2026-08-13 — implementador-dotnet (fix chip destinatarios, sesión 5)
- Etapa: fix de QA — el dueño del estudio reportó que el chip de un destinatario ya seleccionado (ej. "Minjo Wang") seguía ilegible tras el fix anterior, y pegó el HTML exacto del chip.
- **Causa raíz real (distinta del fix anterior):** el chip usaba la clase Bootstrap `text-bg-light`, que es una utilidad de **Bootstrap 5.3+** — este proyecto usa **Bootstrap 5.1.0** (mismo dato ya documentado en la memoria del proyecto por el bug de tema oscuro de Etapa 9). La clase no existe en 5.1: no aplicaba ningún color, y el `<span>` quedaba heredando el `color` del tema activo (`--ov-text`, casi blanco en dark mode) sobre un fondo sin estilo propio → texto invisible. No tiene relación con el fix anterior del `<select>` de rol (que sí estaba bien resuelto).
- Cambio: `wwwroot/css/olvidata-theme.css` — nueva clase `.ov-chip` (theme-aware: gris claro/texto oscuro en claro, `--ov-surface`/`--ov-text` en oscuro). `Views/Notifications/Crear.cshtml` — el JS que arma el chip cambia `text-bg-light` por `ov-chip`.
- Build 0 errores. Commit `8105be6`. Deploy Web Deploy → 14 archivos. Verificado `200 OK`.
- Motivo: pedido directo del dueño del estudio, con evidencia (HTML pegado) de que el fix anterior no cubría este elemento.
- Impacto en capas: solo CSS + 1 línea de JS en la vista (Presentación).
- Riesgos/supuestos: vale la pena, a futuro, barrer el resto del proyecto por otras clases `text-bg-*`/utilities de Bootstrap 5.3+ que puedan haberse colado — en esta sesión se verificó que `Crear.cshtml` era el único uso de `text-bg-` en todo `KoiDumplings.Web`, pero no se hizo un barrido exhaustivo de TODAS las utilidades nuevas de 5.3 (solo de esta clase puntual).
### 2026-08-13 — arquitecto-infra + implementador (dominio nuevo portaldelinversor.com.ar/koi, sesión 6)
- Etapa: infraestructura — el cliente pidió mover el acceso público de KOI a `https://portaldelinversor.com.ar/koi/` (dominio nuevo, ya comprado), con el path `/koi/` pensado como slug reutilizable si en el futuro se suman otros locales.
- **Consulta a `olvidata-infra`** (agente de arquitectura del estudio): recomendó no resolver "multi-tenant por path" todavía — confirmado con el cliente que **es solo para KOI por ahora** (no hay segundo cliente comprometido). Dos arquitecturas posibles a futuro si aparece un segundo local: (A) multi-instancia por path — cada local sigue siendo una app/BD independiente, solo cambia que vive bajo un path del dominio compartido en vez de un subdominio propio (recomendada, reutiliza todo lo que el estudio ya sabe operar); (B) multi-tenant real (una sola app/BD con `tenant_id`) — es un rediseño de producto, no de infra, solo evaluable si `olvidata-ceo` confirma modelo de negocio real detrás. Ninguna de las dos se construye ahora.
- **Verificación con el cliente en el panel de hosting (SmarterASP/site4now):** el panel SÍ permite crear una Virtual Directory dentro de un sitio y marcarla "Set as IIS application", apuntando a una ruta física existente (ej. `\KoiDumplings`, la misma carpeta donde ya está publicado KOI) — habilita el patrón de "IIS Application anidada" sin necesitar gateway/reverse proxy propio ni una segunda copia del deploy.
- **Cambio de código aplicado:** `KoiDumplings.Web/appsettings.Production.json` — `AllowedHosts` pasó de `"olvidatasoft-002-site15.jtempurl.com"` a `"portaldelinversor.com.ar;olvidatasoft-002-site15.jtempurl.com"` (formato ya usado como convención en el blankproject del estudio — lista separada por `;`). Sin este cambio, cualquier request con `Host: portaldelinversor.com.ar` sería rechazado con 400 por el filtro de hosts de ASP.NET Core. El dominio viejo se deja funcionando en paralelo (no se dio de baja, reversible).
- Deploy: Web Deploy → 13 archivos. Verificado `200 OK` en el dominio actual (sigue funcionando).
- **Pendiente, a cargo del cliente en el panel de hosting** (fuera del alcance de lo que se puede hacer desde el repo): (1) confirmar que el dominio `portaldelinversor.com.ar` esté agregado/bindeado en el hosting; (2) crear la Virtual Directory `koi` con Virtual Path apuntando a la misma carpeta física de KOI y "Set as IIS application" tildado; (3) certificado SSL para el dominio nuevo. Sin estos 3 pasos el dominio no resuelve tráfico aunque el código ya esté listo del lado de la app.
- Motivo: pedido directo del dueño del estudio, con justificación explícita de dejar preparado el path `/koi/` para un eventual multi-cliente futuro, aunque hoy no esté confirmado.
- Impacto en capas: solo configuración (`appsettings.Production.json`), sin cambios de código C#/vistas. No aplica a Domain/Application/Infrastructure/Web más allá del filtro de hosts.
- Riesgos/supuestos: no se verificó todavía si ASP.NET Core en hosting "in-process" bajo una IIS Application anidada resuelve el `PathBase` automáticamente sin código adicional (comportamiento esperado según documentación de Microsoft/ANCM) — a confirmar en el primer test real contra `https://portaldelinversor.com.ar/koi/` una vez que el cliente complete los 3 pasos del panel. Si algo no resuelve bien (links, cookies, assets), puede hacer falta un `app.UsePathBase("/koi")` explícito en `Program.cs` como ajuste posterior — no se aplicó preventivamente para no arriesgar una configuración incorrecta sin poder probarla contra el hosting real.
### 2026-08-13 — implementador-dotnet (migración del catálogo real EJECUTADA EN PRODUCCIÓN, sesión 7)
- Etapa: ejecución de la Fase 3+5 del plan sobre producción, tras validar el camino de escritura contra la base local.
- **Decisión del dueño del estudio que acotó el alcance:** *"los meses históricos no se tocan"* — se descartan las opciones de reimportar/corregir la historia. La importación corre con `ActualizarExistentes = false`, así que todo período existente se omite. Queda pendiente de definición con el cliente qué hacer con los errores de fórmula del Excel (ver `temas-para-el-cliente.md` §0), pero eso ya no bloquea el uso del sistema.
- **Validación previa contra base local** (`koidumplings_dev`), vía HTTP real con login y antiforgery, no por revisión de código: preview OK, confirmación OK, transacción con commit. Resultado local: períodos existentes intactos (18/20/19 conceptos sin cambio), catálogo cargado, y 2026-07 creado con 46 conceptos. Esto cubrió el riesgo (b) que la sesión anterior había dejado abierto (camino de escritura EF sin probar).
- **Backup previo de producción**: como el usuario del hosting no tiene privilegios para `util.dumpSchemas`, se exportaron a mano las 6 tablas afectadas en `C:\Sistemas\backups\koi\pre-catalogo-2026-08-13_*.ndjson` (rubros 6, subgrupos 21, parametrosporcentaje 8, periodosmensuales 21, ventasmensuales 20, conceptosgasto 391) más un snapshot de verificación de totales de gastos por período.
- **Preview en producción antes de escribir**: 15 períodos omitidos, 0 a actualizar, 1 a crear (julio 2026), 9 rubros nuevos, 75 subgrupos nuevos, 17 subgrupos dados de baja.
- **Resultado de la migración**: catálogo en producción con 9 rubros activos y 47 subgrupos activos sobre 96 totales (49 históricos de baja lógica, que siguen mostrando su historia). Julio 2026 creado con 46 conceptos ($57.287.371,34), un mes que estaba en el Excel pero nunca se había cargado.
- **Verificación post-migración**: `diff` entre el snapshot de totales por período previo y posterior → **una sola línea de diferencia, el alta de 2026-07**. Ningún período histórico modificado. Smoke de pantallas en producción: Dashboard, Estado de Resultados mensual (agosto 2026 y marzo 2026), Historial de Resultados, Reparto General, Mi Inversión y Configuración/Subgrupos → todas 200. Marzo 2026 (mes histórico cuyos subgrupos quedaron inactivos) sigue mostrando sus 34 importes con el badge "Histórico" — confirma que el fix del filtro global funciona en producción.
- Motivo: pedido del dueño del estudio de continuar con la implementación acotando el alcance a no tocar datos históricos.
- Impacto en capas: Datos (catálogo de rubros/subgrupos + alta de un período), Application/Infrastructure/Web (código ya deployado con el importador y el fix del filtro global).
- Riesgos/supuestos: (a) julio 2026 quedó creado en estado **Abierto** y sin liquidaciones — si el cliente quiere cerrarlo, va a generar liquidaciones de un mes ya pasado, decisión suya; (b) sigue pendiente la definición contable sobre los errores de fórmula del Excel; (c) 2026-01..05 conservan el desglose genérico porque el Excel actual ya no tiene esos meses.

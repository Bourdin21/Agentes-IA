# Trazabilidad del proyecto

Registro acumulativo de decisiones y ajustes por etapa y agente.

## Entradas

### 2026-06-28 - presupuestador
- Etapa: Presupuesto
- Cambio: Batch de 4 mejoras evolutivas presupuestado (F1 badge stock + SweetAlert, F2 Vendedor Pedidos, F3 Totalizador Pagos, F4 Layout Resumen). M total 8 h. USD 135. Etapa 1: USD 51 / Etapa 2: USD 84.
- Motivo: Solicitud directa del cliente sin etapas previas formales (analista/disenador/arquitecto). Features bien delimitadas; presupuesto producido desde descripcion funcional + exploracion de codigo.
- Impacto en capas: Presentacion (Views, CSS, JS) y Negocio (permisos, validacion stock). Sin cambios en Datos (no hay migraciones EF).
- Riesgos/supuestos: F4 riesgo medio por CSS 1122 lineas con breakpoints; cargo IA tokens no aplica (facturables 3.84 h < 4 h umbral). Sin documentos upstream aprobados — advertencia registrada.

### 2026-07-01 - implementador-dotnet
- Etapa: Implementacion
- Cambio: HOTFIX de produccion en `Controllers/VentasController.cs` (`ListarVentas`). Se separo la query de paginacion en dos pasos: (1) obtener `pageIds` (Skip/Take sobre `Select(v => v.Id)`, sin Includes de coleccion), (2) segunda query con los Include necesarios (`Cliente`, `Usuario`, `Facturas`, `Pagos`, `ProductosVenta`) filtrando por `pageIds.Contains(v.Id)`, sin Skip/Take ni OrderBy dinamico, reordenada en memoria con `pageIds.IndexOf` para preservar el orden. Resto del metodo (busqueda, filtros, proyeccion final) sin cambios.
- Motivo: HOTFIX de bug de produccion — excepcion de proceso explicita del cliente, se salteo discovery/analisis/diseno/arquitectura/presupuesto para este caso puntual. Bug: 500 intermitente (`NullReferenceException` en `MySql.Data.EntityFramework.SelectStatement.AddColumn`) al filtrar el listado de Ventas por numero, causado por el provider EF6-MySQL (discontinuado) al no soportar 3 Include de coleccion + WHERE complejo + OrderBy dinamico + Skip/Take combinados en la misma IQueryable.
- Impacto en capas: Web (Controllers) unicamente. Sin cambios en Domain/Application/Infrastructure. Sin migraciones EF.
- Riesgos/supuestos: Build verificado con MSBuild, exit code 0 (sin errores nuevos). Se detecto el mismo patron de riesgo en `PagosController.ListarPagos` (linea ~168) — no tocado, fuera de alcance de este hotfix, reportado para evaluacion futura. Pruebas minimas de QA requeridas: filtrar Ventas por numero exacto (caso que rompia), por texto de cliente, por importe, por fecha, por estado, combinando con paginacion (pagina 2+), ordenamiento por columna (incluida columna Cliente) y con filtro `pendientePago`, verificando que el orden y el conteo (`recordsTotal`) se mantengan identicos al comportamiento previo al fix (sin el bug).

### 2026-07-01 - qa-mvc
- Etapa: Pruebas funcionales (QA de hotfix, sin discovery/diseno/arquitectura/presupuesto previos -- excepcion de proceso autorizada por el cliente para este caso puntual)
- Cambio: Validacion del hotfix en `Controllers/VentasController.cs::ListarVentas`. Se releyo el metodo completo (838 lineas), se re-ejecuto Rebuild con MSBuild 2022 sobre `DeliciasNaturales.sln` (exit limpio, mismos warnings preexistentes que reporto el implementador) y se trazaron por codigo los 7 escenarios minimos dejados por el implementador: filtro por numero exacto, filtro por texto/importe/fecha/estado, combinacion filtro+paginacion pagina 2+ (preserva orden via `pageIds.IndexOf`), ordenamiento por cada columna incluida Cliente, `pendientePago` combinado con busqueda por numero, estabilidad de `recordsTotal`/`recordsFiltered`, y alimentacion correcta de columnas calculadas (`GetTotalEfectivo`/`GetTotalBanco`/`GetMontoRestante`/`GetPorcentajePagado`) sobre `ventasDB` materializado con los 5 Include. Resultado: 7/7 PASS por revision estatica rigurosa (no se ejecuto e2e real via navegador -- se confirmo MySQL en localhost:3306 e IIS Express disponibles, pero no se armo harness de autenticacion por cookie no solicitado). Se creo el item DN-001 (blocker) en `C:/Sistemas/Agentes-IA/docs/qa/regresiones-manuales.yml` porque el bug no estaba catalogado previamente. Se confirmo por lectura de codigo el riesgo identico en `PagosController.ListarPagos` (linea ~168: `Include(p => p.Venta.Facturas)` + WHERE/OrderBy dinamico + Skip/Take en la misma IQueryable) y se catalogo como DN-002 (major, NO corregido, requiere autorizacion explicita antes de tocarlo).
- Motivo: Cierre del gate QA del hotfix de produccion antes de autorizar merge. Cumplimiento de la obligacion del catalogo cross-proyecto (`30-qa-regresiones`): todo bug funcional corregido debe quedar registrado en `regresiones-manuales.yml` antes del merge.
- Impacto en capas: Ninguno adicional (QA no modifico codigo de produccion; el fix ya estaba aplicado por el implementador). Se agregaron 2 items al catalogo de QA compartido (`docs/qa/regresiones-manuales.yml`).
- Riesgos/supuestos: GO para merge del hotfix de Ventas. Riesgo residual MEDIO abierto en Pagos (DN-002) documentado y NO corregido por decision explicita de alcance (el orquestador/cliente debe autorizar ese fix por separado). Riesgo BAJO general: provider EF6-MySQL discontinuado, recomendable auditar otros `ListarX` con DataTable del sistema a futuro.

### 2026-07-01 - documentador
- Etapa: Documentacion
- Cambio: Reporte de negocio para el CEO explicando que mide cada indicador de los 6 modulos del dashboard (Ventas, Productos, Rentabilidad, Pedidos, Clientes) y como impacta en decisiones comerciales, mas un analisis comparativo real de enero a junio 2026. Entregable: `docs/delicias-naturales/reporte-dashboards-ceo-2026-07.md`.
- Motivo: Pedido directo del cliente ("ultima modificacion en los dashboards" + necesidad de explicar utilidad de negocio y mandar comparativo al CEO). No es una feature de codigo, por lo que se salteo discovery/analisis/diseno/arquitectura/presupuesto (excepcion de proceso, sin objecion del cliente). Se autorizo explicitamente una conexion de solo lectura a la base de produccion (host mysql5049.site4now.net) para sacar numeros reales en vez de placeholders.
- Impacto en capas: Ninguno en codigo. Solo consulta de lectura (SELECT agregados) contra produccion, conexion unica y corta, sin escritura.
- Riesgos/supuestos: Se detecto y reporto una anomalia de datos (pago con fecha fuera de rango, "2026-12", ~$163.781) excluida del analisis; pendiente que el cliente la corrija. Julio 2026 excluido del comparativo por ser mes en curso.

### 2026-09-01 - analista-funcional
- Etapa: Discovery + Analisis (preliminar, gate NO habilitado)
- Cambio: Discovery/analisis funcional de una nueva feature "Cierre de Caja Diaria y Mensual", a partir de una investigacion extensa (misma conversacion, conexion de solo lectura a produccion ya autorizada en la sesion del documentador 2026-07-01) sobre por que el cierre de caja de agosto 2026 no cerraba entre el sistema, el libro Excel del cliente y el extracto bancario real (BBVA). Se identificaron 3 causas raiz: (1) el campo Fecha de Pago es editable y no siempre coincide con la fecha real de acreditacion/carga (~16% de los pagos de agosto), (2) los cheques se anotan como MetodoPago=Transferencia (no existe metodo Cheque), fechados el dia que se depositan, (3) MercadoPago tambien se carga como Transferencia (el metodo MercadoPago del sistema dio $0 en agosto pese a liquidaciones reales en el extracto). Se valido manualmente que reconciliar por monto individual + ventana de tolerancia de dias (no por totales) funciona para separar cheques/MercadoPago mal clasificados de desfasajes de fecha reales. Se identifico el patron PAT-012 del catalogo (import con preview->confirmar) como reutilizable para la carga del extracto. Salida: alcance preliminar, 5 casos de uso, criterios de aceptacion, 7 preguntas abiertas (P1-P7) para el cliente antes de cerrar el analisis — documentado en `definiciones/1-analista-funcional.md`.
- Motivo: Pedido explicito del cliente (via Magali) de automatizar el control mensual/diario para no repetir el proceso manual cada mes.
- Impacto en capas: Ninguno en codigo todavia (etapa Ask, sin implementacion). Impacto funcional esperado a futuro: Datos (migracion EF para CierreCajaDiaria/ConciliacionMensual/lineas de matcheo), Negocio (logica de matcheo por monto+fecha, maquina de estados de cierre), Presentacion (pantallas de carga de efectivo diario, carga de extracto, revision de pendientes).
- Riesgos/supuestos: Gate de Diseno NO habilitado — depende de las respuestas del cliente a P1 (que es "el deive"), P3 (formato de extracto a soportar, se recomienda el XLS simplificado en vez del PDF completo por costo/estabilidad de parseo) y P4 (una o mas cuentas bancarias). El fix del bug de reconciliacion del modulo Caja (movimientoscaja, encontrado en la misma investigacion) NO es parte de esta feature — el cliente no usa ese modulo para su control.

### 2026-09-01 - analista-funcional (respuestas del cliente)
- Etapa: Discovery + Analisis (preliminar, gate SIGUE sin habilitar)
- Cambio: El cliente respondio P1/P2/P4/P5/P6 del discovery anterior. Se registran: P1 "el deive" = Google Drive (queda P1b abierta: hoja de calculo estructurada vs archivos sueltos); P2 = solo rol Administrador carga el cierre diario; P4 = una sola cuenta bancaria (Opcion A); P6 = ventana de tolerancia fija ±3 dias (Opcion A); P3 sigue sin definir. **P5 es la respuesta con mayor impacto**: el cliente elige Opcion B pero ampliada — quiere un metodo de pago "Cheque" nuevo con 2 fechas (Fecha de Pago / Fecha de Acreditacion), lo que amplia el alcance mas alla del modulo de conciliacion: modifica la entidad `Pago` (nuevo enum value + campo `FechaAcreditacion`) y el flujo existente de `PagosController.RegistrarPago` ya en produccion, no solo agrega pantallas nuevas. Se abrieron 4 preguntas de seguimiento (P1b, P5b, P5c, P5d) sobre el detalle de ese metodo de pago nuevo (si se puede registrar sin fecha de acreditacion todavia, si cuenta en reportes antes de acreditarse, si puede rebotar).
- Motivo: Cierre iterativo del discovery — las respuestas de P5 cambian sustancialmente el impacto tecnico (ya no es solo un modulo nuevo, toca un circuito existente), asi que se documenta antes de seguir preguntando en vez de asumir el detalle.
- Impacto en capas: Sin cambios de codigo todavia. Impacto funcional esperado ampliado: Datos (nuevo campo en `Pago` + posible migracion de `MetodoPago`), Negocio (`PagosController`/logica de registro de pago existente, no solo el modulo nuevo), Presentacion (formulario de Registrar Pago existente necesita cambios, ademas de las pantallas nuevas de cierre/conciliacion).
- Riesgos/supuestos: Gate de Diseno sigue sin habilitar — faltan P1b, P3, P5b, P5c y P5d. El alcance de "modulo de cierre de caja" ya no es aislado: cualquier cambio a `Pago`/`PagosController` (metodo de pago usado en produccion, con reportes y dashboards existentes que dependen de el) requiere mas cuidado de regresion que un modulo 100% nuevo — marcar explicitamente en Arquitectura cuando se llegue a esa etapa.

### 2026-09-07 - analista-funcional
- Etapa: Discovery + Analisis (preliminar, gate de Diseno NO habilitado)
- Cambio: Discovery/analisis de "Ajuste directo de un Pago" en `PagosController`, a partir de la investigacion del incidente de produccion sobre la venta 9444/remito 9324 (un vendedor revirtio una venta Facturada a Ingresada sin ningun guard, edito los productos, y volvio a Finalizar — la Factura AFIP quedo desconectada y $28.000 de pagos sin ningun ajuste). Se confirmo por lectura de codigo que `PagosController` no tiene ninguna accion de edicion de monto (solo `RegistrarPago`, `EliminarPago`, `ActualizarFechaPago`), y que `Pago` no tiene `UsuarioId` ni `Observacion` (a diferencia de `Venta`, que si registra usuario). Se identifico **PAT-020** (`docs/patrones/catalogo.yml` — ledger inmutable: reversion + contramovimiento, nunca UPDATE de un movimiento ya posteado, formalizado como MH-020 en `32-estandares-qa-implementador.instructions.md`) como patron cross-proyecto directamente aplicable: el ajuste se disena como reversion (reusando la logica ya correcta de `EliminarPago`) + alta nueva (reusando `RegistrarPago` y su lock de concurrencia `_registrarPagoLock`), nunca como UPDATE silencioso de `Pago.Monto`. Alcance preliminar: nueva accion `AjustarPago`, migracion EF (`UsuarioId` + `Observacion` + posible self-FK de trazabilidad en `Pago`), UI que muestre el ajuste. Documentado en `definiciones/1-analista-funcional.md`, seccion "Sesion: Ajuste directo de un Pago". 3 preguntas abiertas (P1 rol habilitado, P2 como se muestra en UI, P3 limite de tiempo) antes de poder cerrar el alcance.
- Motivo: Pedido explicito de Joaquin (via `/agentes-ia-orquestador`) de analizar e implementar el gap encontrado durante la investigacion del incidente de la venta 9444.
- Impacto en capas: Ninguno en codigo todavia (etapa Ask). Impacto funcional esperado: Datos (migracion EF sobre `Pago`), Negocio (`PagosController`, reutilizando logica de `EliminarPago`/`RegistrarPago` bajo el mismo lock), Presentacion (listado/detalle de pagos de Venta).
- Riesgos/supuestos: Gate de Diseno NO habilitado — depende de P1-P3. Dependencia cruzada anotada con la sesion "Cierre de Caja Diaria y Mensual" (mismo archivo): un ajuste retroactivo sobre un pago de un periodo ya conciliado deberia bloquearse/advertirse una vez que ese modulo exista — no aplica hoy. La resolucion concreta de la venta 9444 (que paso con los $28.000) queda fuera de esta feature, pendiente de confirmacion del vendedor por fuera de este flujo.

### 2026-09-07 - analista-funcional (respuestas de Joaquin) + disenador-funcional
- Etapa: Cierre de Analisis + Diseño
- Cambio: Joaquin respondio P1-P3: (P1) Administrador y Vendedor pueden ajustar un pago, igual que hoy `RegistrarPago`/`EliminarPago`; (P2) el listado de pagos de la Venta muestra ambos pagos (el viejo tachado + el nuevo vigente), mini-historial siempre visible; (P3) sin limite de tiempo para ajustar. Con Analisis cerrado, se diseño la feature completa (iteracion 3 en `2-disenador-funcional.md`): boton "Ajustar monto" en el listado de pagos de Venta (junto a Editar fecha/Eliminar existentes), modal con nuevo monto + motivo obligatorio, evento de negocio `AjustarPago` (reversion del pago viejo reusando la logica de `EliminarPago` + alta del pago nuevo reusando `RegistrarPago`, todo dentro del mismo lock `_registrarPagoLock` y una unica transaccion), migracion EF (`Pago` gana `UsuarioId`, `Observacion`, `PagoAnteriorId` self-FK nullable). Se permite ajustar pagos de una Venta en cualquier estado (incluida Facturada) porque no toca `ProductosVenta` ni `Factura` — es la via correcta para terminar de resolver la venta 9444. 5 historias de usuario (HU1-HU5) con criterios de aceptacion. Se identifico un riesgo preexistente heredado (no introducido): la reversion de `MovimientoCaja` por `EliminarPago`/`AjustarPago` busca por `VentaId+Monto` en vez de FK directa — riesgo de revertir el movimiento equivocado si 2 pagos de la misma venta tienen el mismo monto exacto; queda para que Arquitectura decida si corresponde agregar `MovimientoCaja.PagoId` ahora.
- Motivo: Cierre del gate de Analisis (respuestas de Joaquin) habilito inmediatamente el Diseño en la misma sesion de orquestador.
- Impacto en capas: Ninguno en codigo todavia (Diseño es etapa Ask). Impacto funcional documentado: Datos (migracion EF en `Pago`), Negocio (`PagosController.AjustarPago` nuevo, refactor para compartir logica con `EliminarPago`/`RegistrarPago`), Presentacion (modal + render de pagos encadenados en Details/Edit de Venta).
- Riesgos/supuestos: Diseño cerrado, pendiente aprobacion para pasar a Arquitectura (gate). Riesgo aceptado conscientemente: P1 se decidio mantener Vendedor habilitado pese a que el incidente que origino este pedido fue causado por un Vendedor — no se aplico la recomendacion mas restrictiva del analisis. Patron nuevo candidato a catalogar en `docs/patrones/catalogo.yml` al cerrar Implementacion (variante de PAT-020 para ajuste de monto, no solo cancelacion total).

### 2026-09-07 - disenador-funcional (ampliacion de alcance, mismo dia)
- Etapa: Diseño (ampliacion sobre el ya cerrado, todavia dentro del gate de Arquitectura)
- Cambio: Joaquin pidio evaluar unificar el boton ya existente "Editar fecha" (`ActualizarFechaPago`, mutacion directa sin historial) con el nuevo boton de ajuste de monto en un unico "Editar pago" que permite cambiar Fecha, Monto y Metodo de pago juntos. Se le pregunto explicitamente que pasa si el usuario edita SOLO la fecha: **decidio que TODA edicion, sin excepcion, pasa por el mismo camino de reversion+alta con motivo obligatorio** — se descarto un camino liviano especial para fecha-only. Esto elimina `ActualizarFechaPago` como endpoint separado. Se actualizo `2-disenador-funcional.md` (iteracion 3) en las 7 secciones afectadas: flujo de pantalla (un solo boton/modal con 3 campos editables), validaciones, `EditarPagoRequest` (reemplaza `AjustarPagoRequest`, ahora con NuevaFecha/NuevoMetodoPago ademas de NuevoMonto), evento de negocio (el paso de reversion se ejecuta siempre, caso particular de cambio de Metodo hacia/desde SaldoFavor), impacto por capa (se retira el JS/endpoint de `editarFechaPago`/`ActualizarFechaPago`), riesgos (el riesgo #1 de `MovimientoCaja` por Monto en vez de FK ahora es mas frecuente al aplicar a TODA edicion, no solo montos; nuevo riesgo #4 de volumen de historial por correcciones triviales de fecha, aceptado conscientemente), y 2 historias de usuario nuevas (HU6 fecha-only sigue exigiendo motivo, HU7 cambio de Metodo incluido SaldoFavor).
- Motivo: Pedido directo de Joaquin de simplificar la UI a un unico boton de edicion en vez de dos botones con criterios distintos (fecha liviana vs. monto con historial).
- Impacto en capas: Ninguno en codigo todavia (Diseño sigue en etapa Ask). Impacto funcional ampliado: Negocio (`EditarPago` ahora cubre 3 campos, no solo Monto — mas superficie que evaluar en Arquitectura), Presentacion (un boton/modal menos que mantener, pero con mas campos).
- Riesgos/supuestos: Sigue pendiente aprobacion para pasar a Arquitectura. El riesgo de `MovimientoCaja` buscado por Monto (no FK) crece en importancia porque ahora CUALQUIER correccion de pago (incluida una simple fecha) pasa por la reversion — recomendado que Arquitectura resuelva explicitamente si corresponde agregar `MovimientoCaja.PagoId` en esta misma iteracion en vez de diferirlo.

### 2026-09-07 - analista-funcional (decision de alcance: NO ampliar a devoluciones)
- Etapa: Analisis (aclaracion de alcance, gate de Arquitectura sigue pendiente)
- Cambio: Joaquin pregunto si "Editar pago" alcanza para gestionar devoluciones de producto. Se le explico que NO: la feature solo reconcilia monto/fecha/metodo de un pago ya cargado, y ademas el fix preventivo de `CambiarEstadoIngresada` (bloquear revert con Factura activa) dejo un gap nuevo — hoy no hay ninguna via soportada para sacar un producto de una venta ya Finalizada/Facturada (antes se hacia mal, revirtiendo a Ingresada; eso ahora esta bloqueado sin alternativa). Se presentaron 2 opciones (mantener acotado vs. ampliar ahora a un flujo real de "Registrar devolucion" que orqueste Producto+Factura(NC)+Pago). **Joaquin eligio mantener el alcance acotado** a "Editar pago" tal como esta diseñado, y tratar la devolucion completa como un Discovery separado a futuro, sin fecha asignada. Documentado en `1-analista-funcional.md`, nueva seccion "Pendiente — Gestion de Devoluciones".
- Motivo: Decision consciente de alcance/costo — cerrar rapido lo ya diseñado en vez de reabrir Analisis/Diseño para un flujo mas grande.
- Impacto en capas: Ninguno (aclaracion de alcance, sin cambios de diseño). El diseño de "Editar pago" (iteracion 3 en `2-disenador-funcional.md`) queda sin cambios adicionales por este punto.
- Riesgos/supuestos: **Riesgo operativo abierto y aceptado conscientemente**: si otra clienta devuelve un producto de una venta ya Facturada antes de que exista el modulo de Devoluciones, no hay ninguna via soportada en el sistema para sacarlo de la venta — la unica opcion sera repetir el proceso 100% manual (SQL/soporte directo) que se uso para la venta 9444. Vale la pena que Joaquin evalue priorizar el Discovery de Devoluciones pronto dado que ya paso una vez en produccion.

### 2026-09-07 - arquitecto-mvc
- Etapa: Arquitectura (iteracion 3 "Editar Pago")
- Cambio: Escaneo cross-proyecto encontro el patron arquitectonico aplicable (no el componente exacto): marihogar (`VentaService`) y ganaderia (`EgresoService`/`EgresoPagoService`) resuelven reversion de movimientos financieros en un Service dedicado, no en el Controller. Se decide crear `Services/PagoService.cs` (nuevo) con `ReversarPago`, `RegistrarPagoInterno` y `EditarPago`, extrayendo LITERALMENTE (no reescribiendo) la logica hoy inline en `PagosController.EliminarPago`/`RegistrarPago`, que pasan a llamar a estos metodos — refactor acotado a esta pieza, no a todo el Controller. Se decide ademas agregar `MovimientoCaja.PagoId` (FK nullable nueva) para que `ReversarPago` busque por FK exacta en vez de `VentaId+Monto` (riesgo #1 de Diseño), seteada desde ahora en TODA alta nueva via `RegistrarPagoInterno` (no solo en ediciones); los movimientos historicos sin esta FK siguen resolviendose por el fallback anterior, sin tocarlos. 2 migraciones EF (o 1 combinada): `pagos` (+UsuarioId/Observacion/PagoAnteriorId) y `movimientoscaja` (+PagoId), ambas nullable sin backfill. 5 riesgos tecnicos (T1: regresion por refactor de codigo YA hardeneado este ciclo es el de mayor cuidado — mitigado exigiendo extraccion literal + repetir los mismos escenarios de QA ya usados para `RegistrarPago`/`EliminarPago`). Estrategia de pruebas de 9 puntos, incluida regresion explicita de lo existente antes de dar el refactor por cerrado. Documentado en `3-arquitecto-mvc.md`, iteracion 3, `Estado: APROBADO`.
- Motivo: Continuacion del flujo formal del orquestador tras Analisis y Diseño cerrados.
- Impacto en capas: Ninguno en codigo todavia (Arquitectura es etapa Ask). Impacto funcional documentado: Datos (2 migraciones), Negocio (`PagoService` nuevo + refactor de `RegistrarPago`/`EliminarPago` para usarlo), Presentacion (boton unico "Editar pago", sin cambios adicionales a lo ya diseñado).
- Riesgos/supuestos: Gate de Presupuesto **habilitado**. El riesgo T1 (regresion al refactorizar codigo financiero ya hardeneado) es el unico que amerita atencion especial del Implementador: extraer codigo tal cual, no "mejorarlo" de paso.

### 2026-09-07 - orquestador (Presupuesto salteado, avanza a Implementacion)
- Etapa: Presupuesto → Implementacion
- Cambio: Joaquin pidio saltar Presupuesto para "Editar Pago", tratandolo explicitamente como deuda tecnica interna, no feature facturable — y programar el deploy a produccion para las 18hs del mismo dia (2026-09-07). Registrado en `4-presupuestador.md`. Se delega Implementacion al subagent `agentes-ia-implementador` con Analisis+Diseño+Arquitectura (iteracion 3) ya cerrados como base.
- Motivo: Decision explicita de Joaquin, mismo criterio de excepcion de proceso ya usado en otros proyectos del estudio para deuda tecnica/hotfixes internos.
- Impacto en capas: Pendiente de lo que reporte Implementacion.
- Riesgos/supuestos: Deploy programado para las 18hs — Implementacion y QA deben cerrar antes de esa hora para poder ejecutarlo.

### 2026-09-07 - orquestador (deploy a produccion, adelantado a las 18hs)
- Etapa: Cierre de Implementacion/QA → Deploy
- Cambio: QA cerro con veredicto **GO CONDICIONADO** (2 bugs auto-fixeados: DN-003 la reversion no acotaba el fallback por VentaId+Monto a pagos sin `PagoId`, y un bug menor de orden de guards en `EditarPago`; DN-004 major NO corregido — editar un pago exige caja chica abierta y puede romper el arqueo de una caja ya cerrada, regresion real porque antes "Editar fecha" funcionaba siempre). Se le presento a Joaquin la decision sobre DN-004: **eligio deployar igual, aceptando la limitacion operativa conscientemente**, y diferir el fix a una iteracion aparte. Antes del deploy se ejecutaron las 2 condiciones bloqueantes de QA: **C1** verificado (produccion tenia `LoginAudit` como ultima migracion, sin gaps respecto a la nueva) y **C2** backup de `pagos`+`movimientoscaja` (`_backups_pre_editarpago/`, no versionado en git, 3.5MB, verificado con INSERT reales de ambas tablas). Migracion `AddCamposEdicionPago` aplicada contra produccion via `ef6.exe database update` (no via Visual Studio Package Manager Console — se uso el ejecutable standalone del paquete `EntityFramework.6.4.4` con un config temporal para resolver el connection string con nombre "AppConnection" que `ApplicationDbContext` requiere) — verificada contra `information_schema` (4 columnas nullable, 3 FKs, fila en `__MigrationHistory`), conteos de filas de `pagos`/`movimientoscaja` sin cambios. Deploy completo del binario+vistas via MSBuild/Web Deploy (mismo patron ya usado en todo este ciclo). Smoke test post-deploy: `Ventas/Details/9444` (200 OK, boton "Editar pago" y modal presentes) y log de produccion sin errores nuevos en la ventana del deploy. Commiteado y pusheado a `checkpoint-20260826-auditoria-autosave` (15 archivos, sin incluir el backup ni archivos ajenos al feature).
- Motivo: Deploy programado para las 18hs se adelanto porque Implementacion+QA cerraron temprano (antes del mediodia) — no hay razon para esperar teniendo las condiciones de seguridad ya cumplidas.
- Impacto en capas: Datos (migracion aplicada en produccion), Negocio (`PagoService` nuevo, `PagosController` refactorizado), Presentacion (`Views/Ventas/Details.cshtml`/`Edit.cshtml`/`_TablaPagos.cshtml`/`_ModalEditarPago.cshtml` nuevo).
- Riesgos/supuestos: **DN-004 queda como riesgo operativo abierto y aceptado conscientemente** — comunicar a los usuarios que "Editar pago" requiere una caja chica abierta, y evitar editarlo sobre pagos de cajas ya cerradas hasta que se corrija en una iteracion aparte. Backup pre-migracion disponible en `_backups_pre_editarpago/` por si hiciera falta un rollback de datos (el rollback de esquema seria `ef6.exe database update --target 202608270055400_LoginAudit`).

### 2026-09-07 - fix directo (compliance AFIP, sin flujo formal completo)
- Etapa: Hotfix de cumplimiento fiscal (excepcion de proceso — plazo regulatorio externo, no una decision de producto; se salteo Discovery/Analisis/Diseno/Arquitectura/Presupuesto)
- Cambio: Joaquin recibio una notificacion formal de ARCA (07/09/2026): desde el **01/12/2026** se rechaza automaticamente cualquier comprobante sin el campo "Condicion frente al IVA del receptor" (RG 5616/2024). Se confirmo por lectura de codigo que `Helper/FacturacionAfipService.cs` + el proxy SOAP (`Web References/ws_factura_afip/Reference.cs`) no tenian el campo `CondicionIVAReceptorId` en absoluto — gap ya documentado desde agosto en `.github/instructions/34-integracion-afip-arca.instructions.md` (detectado al construir marihogar) pero nunca corregido en este proyecto. Se agrego el campo al proxy (insertado a mano en la posicion fisica correcta del archivo generado, entre `MonCotiz` y `CbtesAsoc` — este proxy viejo no usa `XmlElementAttribute` con `Order`, serializa por orden de declaracion) y la logica de mapeo en `FacturacionAfipService.ObtenerCondicionIVAReceptorId` (Responsable Inscripto=1, IVA Exento=4, Consumidor Final=5 default, Monotributo=6). Verificado sin tocar AFIP: se cargo el DLL compilado y se serializo una instancia de prueba con `XmlSerializer`, confirmando visualmente que el campo aparece en la posicion XML exacta esperada. Build limpio, deployado a produccion, health check post-deploy OK. Actualizada la memoria cross-proyecto (`34-integracion-afip-arca.instructions.md`) marcando el gap como corregido y agregando la tecnica de verificacion offline para proxies SOAP viejos sin `Order` explicito.
- Motivo: Plazo regulatorio externo con fecha firme (01/12/2026) — no facturar correctamente despues de esa fecha corta la operacion del negocio por completo, se prioriza sobre el flujo formal dado el bajo riesgo tecnico del cambio (1 campo nuevo, sin migracion, sin cambio de comportamiento para comprobantes ya emitidos).
- Impacto en capas: Negocio (`FacturacionAfipService`), Infraestructura/proxy generado (`Reference.cs`). Sin migracion EF, sin cambios de UI.
- Riesgos/supuestos: **Pendiente, no resuelto en este fix**: la logica de seleccion de Factura A/B (`FacturasController.Create`) elige Factura A tanto para Responsable Inscripto como para Monotributo — si el emisor es Responsable Inscripto, el estandar de AFIP es Factura A solo a otro Responsable Inscripto. Detectado durante esta investigacion, no confirmado como bug real, requiere revision aparte antes de tocarlo. **Accion recomendada para el estudio**: auditar cualquier otro proyecto con proxy SOAP viejo (no un `AfipService.cs` moderno tipo marihogar) por el mismo gap antes del 01/12/2026 — queda anotado en `34-integracion-afip-arca.instructions.md`.

### 2026-09-07 - ajuste a pedido (togglable, apagado por defecto)
- Etapa: Refinamiento del hotfix de compliance AFIP (misma sesion)
- Cambio: Joaquin pidio dejar el envio de `CondicionIVAReceptorId` **opcional hasta que la regla se efectivice** (01/12/2026), en vez de mandarlo siempre desde ahora. Se agrego el AppSetting `AfipEnviarCondicionIVAReceptor` (default `"false"` en `Web.config`, sin override en `Web.Release.config`) y el patron `CondicionIVAReceptorIdSpecified` en el proxy (`Reference.cs`) para que `XmlSerializer` omita el elemento por completo cuando el flag esta apagado — restaurando el XML exactamente como estaba antes de este cambio. `FacturacionAfipService.CrearDetalle` solo calcula y setea el valor si el AppSetting es `true`. Verificado offline (sin tocar AFIP): con el flag apagado el campo no aparece en el XML serializado; con el flag prendido aparece en la posicion correcta con el valor esperado. Deployado a produccion con el flag en `false` (comportamiento actual sin cambios).
- Motivo: Preferencia explicita del cliente de minimizar exposicion de un comportamiento nuevo en produccion mientras no sea estrictamente necesario, aunque el cambio ya estaba verificado como de bajo riesgo.
- Impacto en capas: Negocio (`FacturacionAfipService`), proxy generado (`Reference.cs`), Configuracion (`Web.config`, nuevo AppSetting).
- Riesgos/supuestos: **Accion pendiente antes del 01/12/2026**: cambiar `AfipEnviarCondicionIVAReceptor` a `"true"` en `Web.config` y redeployar (no requiere tocar codigo ni recompilar) — sin esto, el sistema vuelve a quedar expuesto al rechazo automatico de ARCA en la fecha limite. Recomendable activarlo con margen (no el 30/11) para tener tiempo de reaccion si algo no sale como se espero.

### 2026-09-07 - implementador (iteracion 3 "Editar Pago")
- Etapa: Implementacion (Analisis/Diseño/Arquitectura cerrados; Presupuesto salteado por decision de Joaquin — deuda tecnica interna, no facturable)
- Cambio: Implementada la feature completa. **Datos:** `Pago` +`UsuarioId`/`Observacion`/`PagoAnteriorId`, `MovimientoCaja` +`PagoId`, todas nullable; 1 migracion EF combinada `202609071405299_AddCamposEdicionPago` (Arquitectura seccion 3 autorizaba explicitamente combinar las 2). **Negocio:** nuevo `Services/PagoService.cs` con `ReversarPago`, `RegistrarPagoInterno`, `EditarPago` y `ObtenerPagosDeVenta`, extrayendo LITERALMENTE la logica que estaba inline en `PagosController` (mismos textos de error, via `PagoNegocioException`); `RegistrarPago`/`EliminarPago` pasan a delegar en el service. **Presentacion:** nueva accion `PagosController.EditarPago` (`Administrador,Vendedor`, POST) con `lock(_registrarPagoLock)` envolviendo reversion+alta juntas (riesgo T2 cerrado) + transaccion unica; `ActualizarFechaPago` eliminada; nuevo `ViewModels/PagoVentaViewModel.cs` y `Views/Ventas/_ModalEditarPago.cshtml`; `_TablaPagos.cshtml` reescrito (pagos reemplazados tachados con motivo y usuario, boton "Editar pago" solo en la fila vigente); `VentasController.Details`/`Edit` publican `ViewBag.PagosVenta`.
- Motivo: Cerrar el gap de fondo detectado en el incidente de la venta 9444 — no existia forma segura de corregir el monto de un pago ya cargado.
- Impacto en capas: Datos (2 tablas, 4 columnas nullable, 3 FK, 3 indices — sin backfill ni UPDATE de datos existentes), Negocio (1 service nuevo, 2 acciones refactorizadas, 1 accion nueva, 1 accion eliminada), Presentacion (2 vistas + 2 partials + 1 ViewModel).
- Evidencia: build `MSBuild /t:Rebuild /p:MvcBuildViews=true` **exit code 0**, 0 errores (incluye precompilacion Razor de todas las vistas). Migracion **verificada aplicada contra MySQL 8 real** (base local `delicias`) con las 4 columnas confirmadas nullable por `information_schema`. **No se toco la base de produccion.** Sin smoke test funcional (regla del rol de Implementador).
- Riesgos/supuestos:
  - **Dos hallazgos propios, fuera de lo escrito en Diseño/Arquitectura, corregidos en esta misma etapa:** (1) el guard `montoRestante <= 0` heredado de `RegistrarPago` bloqueaba el caso principal de HU1 — corregir hacia abajo un pago que genero credito por sobrepago, exactamente el escenario tipo venta 9444; se hizo edit-aware (`&& !esEdicion`) sin alterar el alta normal, mas clamp del excedente a `Math.Max(0, montoRestante)` para no inflar el credito de cuenta corriente. (2) `RecycleBinService` listaba como restaurables los pagos reemplazados por una edicion (todos quedan soft-deleted); restaurar uno habria **duplicado el importe cobrado de la venta** — se excluyen del listado y se bloquea la restauracion por Id directo.
  - **Riesgo T4 sigue abierto:** la migracion se probo contra una base local, NO contra una copia del dump de produccion, como pedia Arquitectura. Recomendado hacerlo antes del deploy de las 18hs.
  - Decisiones donde el diseño dejaba margen (detalladas en `5-implementador.md` seccion 3): 1 migracion combinada en vez de 2; tachado con `style` inline porque el proyecto usa Bootstrap 4 (que no tiene esa utilidad); `window.location.reload()` en vez de refresco parcial (es lo que hace realmente `eliminarPago` hoy, y un refresco parcial dejaria desactualizados saldo/estado/modal del resto de la pantalla); combo del modal de edicion sin select2; `RegistrarPago` normal ahora tambien graba `UsuarioId`.
  - **Pendiente menor documentado, no bloqueante (LP-002):** `PagosController.Index`/`ListarPagos`/`ExportarExcel` y el Dashboard no se tocaron — filtran por `.Active()`, asi que ya se comportan bien (los reemplazados no aparecen, los vigentes muestran el monto corregido). No se agrego columna `Observacion`/usuario al listado global de pagos porque el design system obliga a que toda columna visible sea filtrable, y es el listado global, no el historial de una venta.
  - Pendiente: alta de **PAT-023** en `docs/patrones/catalogo.yml` (variante de PAT-020 para edicion de un pago, apuntando a `Services/PagoService.cs`).

### 2026-09-07 - qa (iteracion 3 "Editar Pago") - VEREDICTO: GO CONDICIONADO
- Etapa: QA funcional y de regresion, previo al deploy a produccion programado para las 18:00 del mismo dia.
- Camino de verificacion: **el servidor MCP de Playwright NO estuvo disponible en la sesion** (no expone herramientas `mcp__playwright__*`). Declarado explicitamente segun la regla del rol y caida al procedimiento manual: toda la cobertura funcional es por revision estatica rigurosa (trazado linea por linea contra el codigo real) + verificaciones ejecutadas de forma independiente por QA. No hubo ejecucion end-to-end por navegador.
- Evidencia propia de QA (no se tomo el reporte del implementador como dado): `MSBuild DeliciasNaturales.sln /t:Rebuild /p:Configuration=Debug /p:MvcBuildViews=true` → **EXITCODE 0**, 0 errores, 35 warnings preexistentes (corrido dos veces, antes y despues de los auto-fixes). Esquema de la base local `delicias` verificado contra `information_schema`: migracion presente en `__MigrationHistory`, las 4 columnas nuevas nullable, collation uniforme `utf8mb4_0900_ai_ci` en `pagos`/`movimientoscaja`/`aspnetusers` (las 3 FK se crearon sin conflicto de charset). La base local tiene 5.799 pagos / 5.525 movimientoscaja / 3.572 ventas — mismo orden de magnitud que produccion, lo que reduce sustancialmente el componente de DDL del riesgo T4. Grep exhaustivo: no quedan call sites vivos de `ActualizarFechaPago` ni `editarFechaPago`.
- Cambio: **HU1-HU7 PASS** por trazado. **T1, T2, T3 y T5 cerrados**; T4 y el punto 7 (concurrencia) quedan **BLOCKED** por falta de entorno. Se verifico de forma independiente que las **dos desviaciones que el implementador tomo mas alla del diseño escrito estan bien justificadas y no tapan un bug**: (1) el guard `montoRestante <= 0 && !esEdicion` es **demostrablemente un no-op en el alta normal** (ahi el propio guard garantiza `montoRestante > 0`, con lo cual el clamp del excedente es la identidad) y es lo unico que hace posible el caso central de HU1 — corregir hacia abajo un pago que genero credito por sobrepago, o sea el escenario tipo venta 9444 que motivo la feature; (2) la exclusion de los pagos reemplazados de `RecycleBinService` evita una duplicacion real de importes cobrados y es correcta. **3 defectos detectados**, 2 auto-fixeados por QA en `Services/PagoService.cs` y 1 escalado.
- Defectos:
  - **DN-003 (major) — AUTO-FIXEADO.** El fallback heuristico de `ReversarPago` (`VentaId + Monto`) no se acoto a los movimientos sin FK. Con filas historicas (`PagoId = NULL`) conviviendo con filas nuevas (`PagoId` seteado), revertir un pago historico podia dar de baja el `MovimientoCaja` de un pago **nuevo** del mismo importe en la misma venta, dejandolo sin ingreso de caja y descuadrando el arqueo. Arquitectura seccion 2 lo habia especificado ("fallback exclusivamente para movimientos con `PagoId == null`") y la implementacion omitio el filtro. Fix: se agrego `m.PagoId == null` al predicado. Sin logica de negocio nueva — alinea el codigo con la arquitectura aprobada.
  - **Defecto menor — AUTO-FIXEADO.** En `EditarPago`, el guard de `DeletedAt` iba antes del de "ya reemplazado". Como todo pago reemplazado queda ademas soft-deleted, el segundo guard era codigo muerto y HU5 respondia "El pago fue eliminado..." en vez del mensaje que orienta al pago vigente de la cadena. El rechazo funcional ocurria igual (HU5 nunca estuvo abierta); solo cambiaba el texto. Guards reordenados.
  - **DN-004 (major) — NO CORREGIDO, escalado.** Editar un pago exige una caja chica **abierta** y da de baja el `MovimientoCaja` original dentro de su caja, que puede estar **ya cerrada**. Consecuencias: (A) sin caja abierta, corregir solo la fecha de un pago falla con "No hay una caja chica abierta para registrar el ingreso.", un error sin relacion con la accion, y el usuario queda sin salida — el viejo `ActualizarFechaPago` funcionaba siempre; (B) el saldo recalculado de una caja ya cerrada deja de coincidir con su arqueo de cierre, y el movimiento nuevo cae en la caja de HOY fechado con la fecha del pago. El efecto ya existia en `EliminarPago` (era, de hecho, el workaround manual que esta feature reemplaza); **lo genuinamente nuevo es que ahora alcanza tambien a la correccion de solo-fecha**, que antes era liviana y no tocaba cajas. Diseño riesgo #4 solo habia previsto "mas filas tachadas" como costo del camino unico. **No auto-fixeado a proposito:** la solucion correcta (reversar por contramovimiento en la caja abierta, segun MH-020 regla (b) / PAT-020) es logica de negocio nueva y cambia como se ve el arqueo historico — fuera del mandato de auto-fix de QA.
- Catalogo cross-proyecto: se ejecuto `docs/qa/regresiones-manuales.yml` completo sobre el sistema. **LP-003 y GAN-005 (corrupcion de decimales por cultura es-AR) resultaron N/A**: `Global.asax.Application_BeginRequest` fuerza `NumberDecimalSeparator = "."` en cada request, lo que cierra las dos mitades del round-trip. **MH-020 regla (d) PASS** (ninguna operacion sobre un pago recalcula `Venta.Estado`); **MH-020 regla (b) FAIL** → es exactamente DN-004. Dados de alta **DN-003** y **DN-004** en el catalogo (65 items, YAML validado).
- Impacto en capas: Negocio — `Services/PagoService.cs`, dos correcciones acotadas (predicado del fallback de `ReversarPago` y orden de guards de `EditarPago`). Ninguna otra capa tocada por QA.
- Riesgos/supuestos y condiciones del GO: el veredicto es **GO CONDICIONADO** a tres condiciones. **C1 (bloqueante):** verificar `__MigrationHistory` de produccion antes de correr `Update-Database` — esta memoria registra que en este proyecto hubo al menos una migracion aplicada por SQL directo, y si falta la fila de `LoginAudit` o `FacturaDeletedBy`, EF va a intentar re-aplicarlas y fallar con "Duplicate column name" sobre un DDL que en MySQL no es transaccional. **C2 (bloqueante):** backup de `pagos` y `movimientoscaja` inmediatamente antes del DDL, en sustitucion de la prueba contra copia de produccion que pedia T4 y que no se pudo hacer (no hay dump local) — se justifica porque el DDL son 4 ADD COLUMN nullable + 3 indices + 3 FK sin backfill, ya verificado limpio sobre una base local de volumen comparable, y las FK a `AspNetUsers.Id` tienen precedente exitoso en produccion (`FacturaDeletedBy` ya creo una). **C3 (operativo):** avisar a los usuarios que "Editar pago" requiere caja chica abierta, y evitar editar pagos cuyo movimiento pertenezca a una caja ya cerrada, hasta decidir DN-004. Nota: `Web.config` apunta a `ConectionDB-PROD.config`, asi que cualquier `Update-Database` corrido desde una maquina de desarrollo impacta produccion; se confirmo que NO hay inicializador `MigrateDatabaseToLatestVersion` y que `AutomaticMigrationsEnabled = false`, con lo cual el deploy del binario por si solo no dispara DDL. Riesgos residuales menores anotados para la proxima iteracion: `EliminarPago` sigue sin tomar `_registrarPagoLock` (preexistente, superficie ampliada), `EditarPago` sin `[ValidateAntiForgeryToken]` (consistente con las acciones vecinas, no es regresion), y DN-002 sigue abierto sin cambios. Correccion menor a `5-implementador.md`: PAT-023 figura ahi como pendiente, pero ya esta dado de alta en `docs/patrones/catalogo.yml`.

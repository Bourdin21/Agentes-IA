# Trazabilidad del proyecto

Registro acumulativo de decisiones y ajustes por etapa y agente.

## Entradas

### 2026-09-01 - analista-funcional
- Etapa: Discovery
- Cambio: Alta de proyecto nuevo (kite-punta-lara). Investigación de dominio completada sobre 7 temas (CARP, SHN, SMN, Windguru, birazón, sudestada, spot Punta Lara/Club Universitario) y borrador de alcance funcional entregado al cliente (Joaquín) con 9 preguntas abiertas marcadas como hipótesis a validar.
- Motivo: pedido inicial del cliente — pronóstico de viento/kite para Punta Lara, sin definiciones previas.
- Impacto en capas: N/A (etapa de discovery, sin diseño técnico todavía).
- Riesgos/supuestos: Windguru no tiene API pública de pronóstico (solo Upload API de estaciones propias) — el "promedio de modelos" que hace el cliente manualmente requiere una fuente de datos alternativa (Open-Meteo, scraping, o servicio pago) a definir. Stack PHP/DonWeb no tiene precedente en el catálogo del estudio.

### 2026-09-01 - analista-funcional
- Etapa: Análisis
- Cambio: Cierre de Análisis en la misma sesión, 3 rondas de preguntas resueltas por el cliente. Decisiones clave: multiusuario con registro abierto, dashboard público sin login (cuenta solo para equipo/bitácora), Open-Meteo como fuente de pronóstico del MVP (Windy API y scraping Windguru documentados como fallback futuro), recomendación de equipo (tabla nudos→kite) en vez de sí/no simple, birazón modelada como 2 señales independientes (brisa de calma + bajante), sudestada como oportunidad condicional, bitácora de sesiones con fin de calibración + uso social, notificación proactiva por email, horizonte de pronóstico hoy+3 días, subdominio nuevo dedicado en DonWeb. 6 casos de uso con criterios de aceptación definidos.
- Motivo: cerrar el gate de Análisis antes de habilitar Diseño funcional, según secuencia obligatoria del estudio.
- Impacto en capas: Presentación (dashboard público + área logueada), Negocio (modelo de cálculo/scoring, reglas de birazón/sudestada, notificación), Datos (usuarios, tabla de equipo, bitácora de sesiones — requiere esquema de BD nuevo).
- Riesgos/supuestos: CARP/SHN/SMN sin API oficial estable (cada fuente debe fallar aislada); Open-Meteo puede tener menor resolución cerca de la costa que el promedio manual de Windguru — recomendable validación empírica corta antes de Implementación; umbrales de birazón/sudestada son aproximación bibliográfica a calibrar con la bitácora real; registro abierto sin moderación (riesgo bajo, grupo chico); no hay agente implementador PHP en el catálogo del estudio, a resolver en Arquitectura/Presupuesto.

### 2026-09-01 - disenador-funcional
- Etapa: Diseno
- Cambio: Diseño funcional cerrado sobre el Análisis del mismo día. 7 pantallas (Home público, Login, Registro, Dashboard logueado, Mi equipo, Cargar sesión, Bitácora social) con wireframe textual, 5 ViewModels (PronosticoDia, EstacionEnVivo, EquipoQuiverItem, PerfilUsuario, SesionBitacora) con validaciones funcionales, motor de cálculo con reglas explícitas (birazón = 2 señales independientes con umbrales configurables; sudestada ConTemporal fuerza "No recomendable" con prioridad sobre la recomendación de equipo), permisos por pantalla, y plan funcional en 3 etapas (núcleo de cálculo sin cuentas → cuentas → bitácora/notificaciones) para que Arquitectura pueda secuenciar el trabajo.
- Motivo: cerrar el gate de Diseño antes de habilitar Arquitectura, según secuencia obligatoria del estudio.
- Impacto en capas: Presentación (7 pantallas + 2 plantillas de email), Negocio (5 servicios: ConsolidadorEstaciones, Pronostico, MotorDeCalculo, Notificaciones, Bitacora — umbrales configurables, no hardcodeados), Datos (Usuario, EquipoQuiverItem, SesionBitacora + recomendación de caché de corta duración 15-30min sobre las APIs externas).
- Riesgos/supuestos: se agrega un supuesto nuevo no confirmado — registro sin verificación de email (alta directa), a confirmar con el cliente antes de Arquitectura. Se reutiliza PAT-007 (SmtpMailer.php) para el envío de avisos y el criterio PAT-008 (filtro por columna visible) para la bitácora social; no se agrega patrón nuevo al catálogo (motor de scoring específico del dominio, sin indicio de reutilización en otros proyectos del estudio).

### 2026-09-01 - arquitecto-mvc (adaptado a PHP)
- Etapa: Arquitectura
- Cambio: Arquitectura técnica cerrada sobre el Diseño del mismo día. Sin agente arquitecto PHP en el catálogo del estudio, se tradujo el rol .NET (Domain/Application/Infrastructure/Web) al stack real: front controller sin framework + `src/Services` (negocio) + `src/Repositories` con PDO (datos), sesión nativa de PHP sin roles (solo autenticado/anónimo), 3 tablas nuevas (`usuarios`, `equipo_quiver_items`, `sesiones_bitacora`) y cron standalone para el aviso diario. Reutilización literal confirmada de PAT-007 (`SmtpMailer.php`, ya en producción en labipac-front/diercas-front). Cliente confirmó SSH/Composer y cron disponibles en DonWeb, y durante esta misma etapa envió credenciales reales de FTP (`club@olvidata.com.ar` / `ftp://olvidata.com.ar`) y de la base de datos MySQL ya creada (`olvidata_club`) — guardadas únicamente en `docs/credenciales.local.md` (gitignored, nunca en un archivo tracked).
- Motivo: cerrar el gate de Arquitectura antes de habilitar Presupuesto, según secuencia obligatoria del estudio.
- Impacto en capas: Presentación (front controller + 4 controllers + plantillas PHP planas con `htmlspecialchars` consistente), Negocio (7 Services, umbrales del motor de cálculo como configuración editable, no hardcodeados), Datos (3 tablas nuevas vía migraciones SQL numeradas, sobre la base `olvidata_club` ya provista).
- Riesgos/supuestos: sin framework, la seguridad (CSRF/XSS/sesión) depende de un helper centralizado usado con disciplina en las 4 pantallas con formulario — a reforzar explícitamente en el checklist de QA de este proyecto. Cron en hosting compartido puede fallar silenciosamente — mitigado con logging propio, pero sin alerta automática. Versión exacta de PHP del plan DonWeb aún no confirmada. Host de conexión MySQL (típicamente `localhost`) a confirmar al empezar Implementación.

### 2026-09-01 - arquitecto-mvc (adaptado a PHP)
- Etapa: Arquitectura
- Cambio: Confirmado el subdominio definitivo — `club.olvidata.com.ar`. No es un dominio propio nuevo: se aloja bajo el dominio `olvidata.com.ar` en DonWeb, mismo esquema que otros bots/subdominios de Olvidata Soft (coincide con el usuario FTP `club@olvidata.com.ar` ya recibido).
- Motivo: precisión pedida por el cliente sobre la decisión de hosting ya cerrada en Arquitectura (antes documentada como "subdominio nuevo dedicado" sin nombre confirmado).
- Impacto en capas: Presentación/infraestructura de deploy — sin cambio en el código de la app. DNS y certificado SSL del subdominio quedan bajo la infraestructura ya existente de Olvidata Soft, no como alta de dominio nueva.
- Riesgos/supuestos: si al momento de deploy el subdominio no resuelve o falta el SSL, es un tema de configuración DNS/panel DonWeb a resolver con la cuenta de Olvidata, no un problema de arquitectura de la aplicación.

### 2026-09-01 - arquitecto-mvc (adaptado a PHP)
- Etapa: Arquitectura
- Cambio: Confirmada la versión de PHP del hosting — 8.4 (lsphp, LiteSpeed). Cierra el último riesgo abierto de la etapa. Se agrega nota de gotcha típico de hosting LiteSpeed: el PHP que ejecuta el cron/CLI puede no coincidir con el `lsphp` que sirve la web — verificar con `php -v` dentro de la tarea programada en Implementación, no asumirlo.
- Motivo: dato de panel de hosting compartido por el cliente, necesario para cerrar el riesgo de versión que había quedado pendiente en el cierre de Arquitectura.
- Impacto en capas: Infraestructura/despliegue — sin cambio de diseño, habilita usar sintaxis PHP 8.x moderna sin restricción.
- Riesgos/supuestos: ninguno nuevo más allá del gotcha de CLI vs. web ya anotado.

### 2026-09-01 - implementador (sin subagente PHP dedicado)
- Etapa: Implementación (scaffold inicial, a pedido explícito del cliente: "crear repo primero, después seguimos")
- Cambio: Repo creado en `C:\Sistemas\kite-punta-lara`, git inicializado. Estructura de carpetas por capa (Presentación/Negocio/Datos) según Arquitectura, `composer.json` con autoload PSR-4, conexión PDO (`src/Data/Db.php`), reutilización literal de PAT-007 (`SmtpMailer.php`, solo se agregó namespace), helper CSRF centralizado, 3 migraciones SQL (sin aplicar), configs de umbrales del motor de cálculo y tabla de equipo default (ambos con placeholders declarados explícitamente, no datos reales todavía), front controller mínimo con P1 en modo placeholder. `composer install` y smoke test local (`php -S`) exitosos.
- Motivo: dejar el esqueleto técnico listo y verificado antes de implementar pantalla por pantalla, siguiendo la arquitectura ya cerrada.
- Impacto en capas: las tres — Presentación (front controller + template placeholder), Negocio (mailer + CSRF, motor de cálculo aún no implementado), Datos (conexión + migraciones sin aplicar).
- Riesgos/supuestos: `config/equipo_default.php` y `config/umbrales.php` son placeholders a reemplazar con datos reales de Joaquín antes de que la recomendación sirva de verdad; migraciones no aplicadas contra `olvidata_club` todavía; error de conexión en el smoke test local es por falta de extensión `pdo_mysql` en el PHP local de este entorno, no un bug — a confirmar que esté disponible en el PHP 8.4 (lsphp) real de DonWeb.

### 2026-09-01 - presupuestador
- Etapa: Presupuesto
- Cambio: Etapa saltada por pedido explícito del cliente ("saltear presupuesto"). Mismo precedente que vinosefue (presupuesto saltado 2026-08-31). Gate de "Presupuesto aprobado por el cliente" se da por satisfecho porque el cliente es el propio Joaquín.
- Motivo: proyecto personal sin cliente externo — el presupuesto hubiese sido solo informativo, no una propuesta comercial a enviar.
- Impacto en capas: N/A.
- Riesgos/supuestos: sin estimación de horas/esfuerzo registrada — no hay cierre de calibración posible para este proyecto (no se puede comparar estimado vs. real).

### 2026-09-02 - implementador (sin subagente PHP dedicado) + arquitecto-mvc (reapertura puntual)
- Etapa: Implementación (Etapa 1 del plan funcional — núcleo de cálculo) + ajuste de Arquitectura sobre la marcha
- Cambio: (1) Aplicadas las 3 migraciones contra una base MySQL local de desarrollo (`olvidata_club` en `127.0.0.1:3306`, separada de la base real de DonWeb) y habilitada la extensión `pdo_mysql` en el PHP local. (2) Cambio de arquitectura pedido explícitamente por el cliente a mitad de la sesión: la Presentación deja de ser templates PHP server-side y pasa a **Astro estático** (repo hermano nuevo `kite-punta-lara-front`, mismo patrón que `diercas-front`/`labipac-front`, a construir con el agente `agentes-ia-implementador-astro-front`), consumiendo el backend PHP como **API JSON** bajo el mismo subdominio (`club.olvidata.com.ar`, Astro en la raíz, PHP bajo `/api/*` — mismo origen, sin CORS). (3) Implementada la Etapa 1 completa del backend: `CacheService` (cache de archivo, TTL 15-30min), `PronosticoService` (Open-Meteo, ventana hoy+3 días agregada en franja mediodía-tarde), `ConsolidadorEstacionesService` (CARP/SMN), `MotorDeCalculoService` (reglas de birazón/sudestada/equipo ya definidas en Diseño), y `PronosticoController` que reemplaza a `HomeController` sirviendo `GET /pronostico` como JSON. `templates/home.php` y el `HomeController` viejo se eliminaron (ya no aplica el patrón de templates).
- Motivo: (1) desbloquear desarrollo local sin depender de la base de producción. (2) preferencia explícita del cliente por Astro para la capa visual, aprovechando el agente especializado del estudio en vez de mantener HTML a mano en PHP plano. (3) siguiente paso ya identificado en la memoria de implementador previa.
- Impacto en capas: Presentación (se muda de este repo a `kite-punta-lara-front`, este repo pasa a exponer solo JSON), Negocio (4 Services nuevos), Datos (sin cambio de esquema, solo entorno local de dev).
- Riesgos/supuestos (continúa más abajo con la entrada de deploy del mismo día): coordenadas del spot para Open-Meteo son una aproximación (~-34.86, -57.90) sin GPS exacto confirmado por el cliente — a validar. Investigado en esta sesión (con más profundidad de lo previsto, a pedido de Joaquín porque Pilote Norden es su referencia real hoy para decidir si salir a navegar): **CARP SÍ tiene un endpoint real** — el widget embebido en `servicios_main.php?sid=VM` (`meteo.comisionriodelaplata.org`, sistema MariWeb/ECS) expone `POST /ecsCommand.php?c=telemetry/updateTelemetry` con `stationID=2` para Pilote Norden, devolviendo viento/marea reales en nudos con timestamp. Confirmado funcionando end-to-end (`ConsolidadorEstacionesService`, ver `src/Services/`). Tuvo dos obstáculos técnicos, ambos resueltos: (a) PHP local sin CA bundle configurado (`curl.cainfo`/`openssl.cafile` ahora apuntan a un `cacert.pem` descargado); (b) el servidor de CARP tiene una cadena de certificado TLS incompleta y parámetros DH legacy que OpenSSL rechaza por default (Windows/Schannel lo tolera, PHP no) — **se desactivó la verificación de certificado solo para ese host puntual**, decisión de seguridad confirmada explícitamente con Joaquín (dato público de viento/marea, sin credenciales de por medio). **SMN (`ws.smn.gob.ar`) sigue sin acceso** — detrás de un challenge de Cloudflare que devuelve 503 a requests sin navegador real; queda el intento real igual (aislado en try/catch), `disponible=false` en la práctica hasta encontrar una vía de acceso. CSRF sobre la API (necesario recién en Etapa 2, cuando aparezcan formularios de login/registro/equipo/bitácora) queda pendiente de resolver, con el patrón ya anotado (`GET /csrf` + header `X-CSRF-Token`, reutilizando `Csrf.php`).

### 2026-09-02 - implementador-astro-front
- Etapa: Implementación (Etapa 1 del plan funcional — capa de presentación)
- Cambio: Creado el repo hermano `C:\Sistemas\kite-punta-lara-front` (Astro 7.2 estático) con **P1 (Home público) completa**, consumiendo `GET /api/pronostico` del backend PHP ya implementado. Proxy de Vite en dev (`/api/*` → `localhost:8000/*`) para que el código de fetch sea idéntico en dev y en producción (Astro en la raíz de `club.olvidata.com.ar`, PHP bajo `/api/*`, mismo origen). Repo agregado a `kite-punta-lara.code-workspace` y commiteado (`10b8551`). Se definió acá el criterio del semáforo verde/amarillo/rojo, que el diseño funcional mostraba en el wireframe pero no especificaba — documentado en `src/lib/semaforo.ts` sin reimplementar ninguna regla del `MotorDeCalculoService`.
- Motivo: cierra la capa de presentación de la Etapa 1 sobre el cambio de arquitectura a Astro pedido por el cliente el mismo día.
- Impacto en capas: Presentación únicamente. El backend no se tocó (los ajustes de contrato detectados quedaron anotados como sugerencia en `5-implementador.md`, no aplicados).
- Desviación de stack respecto del rol: el rol `implementador-astro-front` tiene Tailwind v4 como default, pero acá se usó **CSS plano con tokens en `:root`**, porque el pedido explícito fue heredar el lenguaje visual de `olvidatasoft-new` (sitio propio del estudio), que no usa Tailwind. Decisión consciente, no omisión.
- Verificación: Playwright con clicks reales (no solo capturas) — datos reales del backend en pantalla, click en "Actualizar" confirmado con un request nuevo a la API, hit-test de `elementFromPoint` sobre los 3 botones (ningún pseudo-elemento decorativo tapa clicks), badges de sudestada/birazón y caída de estación/API 503 ejercitados vía intercepción de ruta (caminos que los datos reales de hoy no ejercitan), mobile 390px sin desborde horizontal, `prefers-reduced-motion`, cero errores de consola y cero requests fallidas. Lighthouse sobre el build servido con proxy prod-like: performance 91 / accesibilidad 100 / best practices 100 / SEO 100 (los dos fallos iniciales —contraste del semáforo y falta de `robots.txt`— se corrigieron midiendo los ratios de contraste, no a ojo).
- Riesgos/supuestos: (1) el front asume que `direccionGrados` es la convención meteorológica (de dónde viene el viento) e invierte 180° para dibujar la flecha de flujo — a confirmar con Joaquín contra la realidad del spot. (2) El semáforo depende de que `recomendacionEquipo` siga diciendo "No recomendable"; si el backend cambia ese wording el color deja de ser correcto sin fallar ruidosamente (ver ajustes sugeridos a la API). (3) Sin deploy todavía: falta `scripts/deploy.sh` + `.env.deploy.example` en el repo de front, y no se probó nada contra el servidor real de DonWeb. (4) `astro preview` no aplica el proxy de `/api` — probar el build contra el backend requiere servir `dist/` detrás de un proxy propio.

### 2026-09-02 - implementador-astro-front (segunda pasada)
- Etapa: Implementación (Etapa 1 — ajuste de la capa de presentación al contrato revisado)
- Cambio: El backend aplicó las 3 mejoras de contrato sugeridas en la pasada anterior (`pronostico[].navegable`, `estaciones[].direccionTexto`, `timestamp` en ISO 8601 con offset) y corrigió el bug de `confiabilidad` (Media para mañana/pasado, Baja solo para el día 3). El front se adaptó: `lib/api.ts` (tipos), `lib/semaforo.ts` (usa `navegable` en vez de parsear el texto de la recomendación), `lib/formato.ts` (parser nativo de timestamp; se borró `sectorDesdeGrados()`) y `scripts/dashboard.ts` (usa `direccionTexto` de la estación). Neto: se eliminó código, no se agregó.
- Motivo: cerrar la fragilidad reportada — el semáforo dependía de que `recomendacionEquipo` siguiera diciendo literalmente "No recomendable", y un cambio de wording lo rompía en silencio.
- Impacto en capas: Presentación únicamente.
- Verificación: Playwright contra datos reales + 4 escenarios mockeados (`navegable:false` con texto distinto → rojo, que con el parser viejo daba verde; campo `navegable` ausente → rojo ruidoso, nunca falso verde; `ConTemporal` contradiciendo `navegable:true` → rojo con badge; estación sin dirección → sin flecha). Test de zona horaria: la misma medición da "hace 16 min" idéntico en Buenos Aires, Madrid y Tokio, con la hora local del tooltip corriéndose correctamente — prueba directa de que el offset se respeta. Cero errores de consola, `astro check` y build limpios, sin cambios visuales (no se repitió Lighthouse).
- Riesgos/supuestos: se cierra el riesgo (2) de la entrada anterior (semáforo dependiente del wording). Siguen abiertos: convención de `direccionGrados` a confirmar con Joaquín, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador (sin subagente PHP dedicado)
- Etapa: Implementación (primer deploy a producción — pedido explícito del cliente: "deployar a public_html/club")
- Cambio: **Primer deploy real de kite-punta-lara, sitio en vivo en `https://club.olvidata.com.ar/`.** Reconocimiento previo por FTP: la cuenta `club@olvidata.com.ar` está enjaulada directo en el docroot del subdominio (su "/" no tiene `public_html` navegable, ni siquiera existe ese path para esta cuenta — es un hallazgo, no un supuesto: el path que pidió el cliente ("public_html/club") es el que se ve desde el panel de la cuenta principal, pero desde esta cuenta FTP específica la raíz YA ES ese directorio). SSH no responde (timeout puerto 22) para esta cuenta — deploy 100% vía FTP, sin poder correr `composer install` en el servidor ni sacar `vendor/`/`src/`/`config/` fuera del docroot. Astro (`kite-punta-lara-front`, build de producción) se subió a la raíz del subdominio; el backend PHP se empaquetó aparte bajo `/api/` (index.php + vendor/ + src/ + config/ + storage/ como hermanos, no anidados en `public/` como en el repo local — index.php ajustado de `dirname(__DIR__)` a `__DIR__` para el autoload). Se agregó a `public/index.php` (repo, no solo el paquete de deploy) el strip del prefijo `/api` sobre `REQUEST_URI` antes de matchear rutas, para que el mismo código sirva en dev (proxy de Vite ya lo saca) y en producción (Apache/LiteSpeed lo preserva). Credenciales reales de MySQL (`docs/credenciales.local.md`) usadas SOLO en el `config/db.local.php` del paquete de deploy (nunca en el repo git, que sigue gitignorando ese archivo y sigue apuntando a la base local de dev).
- Motivo: pedido explícito del cliente de sacar el proyecto de dev local a producción real.
- Impacto en capas: Presentación/Datos/infraestructura de deploy — sin cambio de reglas de negocio. Primer despliegue real contra la base `olvidata_club` de DonWeb (host `localhost` confirmado funcionando desde el propio servidor).
- **Hallazgo de seguridad durante el deploy, corregido en el momento**: el `.htaccess` con `Require all denied` (sintaxis Apache 2.4 estándar) **no se aplicó** en este hosting LiteSpeed/DonWeb — probablemente `AllowOverride` no incluye la categoría `AuthConfig`/`Limit` para esta cuenta. Se detectó porque `vendor/autoload.php` quedó descargable públicamente (200) durante una ventana corta. Corregido reemplazando el criterio por un bloqueo vía `mod_rewrite` (`RewriteEngine On` + `RewriteRule ^ - [F,L]`) en `vendor/`, `src/`, `config/`, `storage/` — confirmado que SÍ se respeta (la categoría `FileInfo` de `AllowOverride`, que permite `mod_rewrite`, es mucho más comúnmente habilitada en hosting compartido que `AuthConfig`). Verificado con `curl` contra las 4 carpetas + `config/db.local.php` específicamente: todo 403 ahora. **Nota para cualquier deploy futuro en este mismo hosting**: no asumir que `Require all denied`/`Deny from all` funcionan — usar el patrón `RewriteRule ^ - [F,L]` para proteger carpetas.
- Riesgos/supuestos: el subdominio ya resuelve con SSL funcionando (`https://club.olvidata.com.ar/` responde 200) — no hizo falta tocar DNS/panel, ya estaba configurado de una etapa anterior. Pendiente: `scripts/deploy.sh`/`.env.deploy.example` todavía no existen (este deploy se hizo con un script ad-hoc de sesión, no reproducible todavía desde el repo) — a formalizar en una próxima pasada. Cron (`bin/enviar_avisos.php`) NO se subió ni se programó (no aplica todavía, `NotificacionesService` no está implementado). `sql/`, `composer.json/lock`, `README.md`, `bin/` tampoco se subieron (no hacen falta en runtime, evita exponer superficie innecesaria).

### 2026-09-02 - implementador-astro-front (tercera pasada — rediseño de P1)
- Etapa: Implementación (Etapa 1 — rediseño de la capa de presentación)
- Cambio: (1) El front se movió a `kite-punta-lara/front`: el proyecto pasó a ser **monorepo con remoto en GitLab**; el repo hermano `kite-punta-lara-front` quedó abandonado (lock de archivos, no se borra). (2) Rediseño de P1 pedido por Joaquín sobre el contrato nuevo de la API. Se agregó: bloque "ahora en el río" con viento + ráfaga + dirección + marea medida + hora de CARP; grilla horaria de 24 h (viento en barras + marea en línea) en la card de hoy y en cada uno de los 3 días siguientes; tags "sopla" / "se navega" sobre las rachas contiguas de horas navegables; elección del rango de viento navegable por usuario (anónimo, `localStorage`, sin cuenta, arrancando en `config.rangoNavegableDefault`). Se quitó el bloque de recomendación de equipo genérica, reemplazado por un CTA de login. Los 3 días pasaron de cards en grilla a una fila por día.
- Motivo: pedido explícito de Joaquín — "no sé de dónde sacás la recomendación de equipo genérico si el usuario todavía no tiene ningún dato cargado" (tenía razón: salía de una tabla de config, no del usuario), más el pedido de ver el estado real del río y la previsión hora por hora.
- Impacto en capas: Presentación únicamente. El backend no se tocó.
- Convivencia de las dos mareas (decisión de diseño, no cosmética): `estaciones[].mareaMetros` es una MEDICIÓN del mareógrafo de CARP; `horas[].mareaMetros` es un MODELO con otro cero de referencia y un punto de grilla a ~20 km. Se resolvió manteniéndolas en bloques separados, cada una etiquetada con su origen visible ("medida" / "modelo"), la del modelo presentada como tendencia, y una nota explícita en la card. No se aplicó ningún offset para acercarlas — habría sido fabricar un dato.
- Verificación: Playwright con datos reales + 10 escenarios (primera visita abre el diálogo; cambiar el rango re-renderiza sin refetch, medido en requests; rango inválido no cierra el diálogo; rango sin match deja los 4 días en rojo; persistencia entre recargas; `localStorage` bloqueado no rompe nada y avisa; `ConTemporal` bloquea horas con viento en rango; `senalSudestada` desconocida bloquea igual y explica por qué; sin estaciones cae al pronóstico sin inventar medición; API 503 con error visible y chip de rango accesible; mobile 390px sin desborde de página). Cero errores de consola, `astro check` y build limpios.
- Riesgos/supuestos: (1) sigue abierto confirmar con Joaquín la convención de `direccionGrados` (se asume meteorológica y se invierte 180° para la flecha de flujo). (2) Sigue sin deploy: falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb. (3) El rango vive por navegador/dispositivo: quien use el sitio en el celular y en la compu elige dos veces — aceptado a cambio de no pedir cuenta, a revisar si en Etapa 2 conviene sincronizarlo con el perfil.

### 2026-09-02 - implementador (sin subagente PHP dedicado)
- Etapa: Implementación (calibración de la fuente de pronóstico) + **corrección de un dato del Análisis**
- Cambio: Joaquín detectó que la grilla horaria marcaba 7,4 kt mientras Pilote Norden medía 15,4 kt. Se descartó primero un bug de unidades (la API devuelve `"kn"` correctamente; 7,4 kn = 13,7 km/h, consistente). La causa era doble: (a) `best_match` de Open-Meteo resolvía a **ECMWF, el peor modelo para este spot**, y (b) el punto de grilla caía en una celda de **tierra firme** (elevación 4 m), donde la fricción de superficie hunde el viento modelado. Se validaron los modelos empíricamente contra 19 horas de mediciones reales de CARP/Norden, consultando cada modelo en las coordenadas exactas de la estación: **gem_seamless (GDPS 15 km) MAE 1,87 kt / sesgo +0,14** — el mejor por lejos; icon 2,79/−2,01; gfs 3,10/−2,03; best_match 3,47/−2,53; ecmwf 4,67/−4,56. Se cambió a GDPS + `cell_selection=sea`, y se sacaron modelo/coordenadas/franja a `config/pronostico.php` con la tabla de errores documentada. Se agregaron ráfagas (por hora y min/max diario) y se separó el min/max del día completo (lo que se muestra) del agregado de la franja mediodía-tarde (lo único que alimenta las señales del motor).
- Motivo: pedido explícito de Joaquín — "el pronóstico del día de hoy debe ser el más asertivo", más su aporte de campo: los modelos que mejor le funcionan en Windguru son GFS 13 km, WRF 12 km y GDPS 15 km. La validación confirmó su intuición (GDPS gana), lo que es un dato a favor de escucharlo en decisiones de dominio.
- **Hallazgo contraintuitivo**: promediar modelos **empeora** el resultado, aunque sea exactamente lo que el cliente hace a mano en Windguru y lo que el Análisis planteaba como fuente "multi-modelo". Promedio(GFS,GEM) da MAE 1,97 y promedio de los 4 da 2,79, contra 1,87 de GDPS solo, porque tres de los cuatro modelos comparten el mismo sesgo frío de ~2 kt y arrastran al único que no lo tiene. En ráfagas la brecha es mayor (GEM 1,77 vs GFS 4,18). Por eso la regla implementada es **un modelo, no un promedio** — decisión que contradice el planteo original del Análisis y queda documentada como tal.
- **CORRECCIÓN a `1-analista-funcional.md`**: ese documento afirma que las estaciones de CARP "están del lado uruguayo/zona del canal, no en Punta Lara". **Para Pilote Norden es falso.** Coordenadas reales obtenidas del propio endpoint de CARP: **-34,6286 / -57,9250**, o sea **20 km al norte de Punta Lara**, en pleno Río de la Plata (ni del lado uruguayo, ni lejos). Eso convierte a Norden en una referencia mucho más válida de lo que sugería el Análisis, y valida la práctica de Joaquín de usarla para decidir si sale a navegar. Las otras tres (Colonia -34,476/-57,843; Conchillas -34,223/-58,075; Carmelo -34,007/-58,297) sí están bastante más al norte.
- Impacto en capas: Negocio (fuente de pronóstico y su calibración), Presentación (campos nuevos a mostrar: ráfagas y min/max por día).
- Riesgos/supuestos: **la validación es de 19 horas de UN día contra UNA estación** — alcanza para descartar `best_match`, no para dar la elección de modelo por cerrada. Reevaluar con más días antes de tratarla como definitiva; es exactamente el tipo de calibración que la bitácora (Etapa 3) fue pensada para alimentar. WRF 12 km (el tercer modelo de Joaquín) es un run propio de Windguru: Open-Meteo no tiene equivalente regional para esta zona, solo modelos globales — quedaría como fuente paga aparte si alguna vez se quiere. La diferencia residual entre el modelo en Punta Lara (12,8 kt) y la medición en Norden (14,7 kt) es física y esperable por los 20 km y la mayor exposición de Norden, no un error a corregir.

### 2026-09-02 - implementador-astro-front (cuarta pasada — tooltip propio + ráfagas)
- Etapa: Implementación (Etapa 1 — mejoras sobre la grilla horaria de P1)
- Cambio: (1) Tooltip propio de la grilla horaria en reemplazo del `<title>` nativo de SVG, con hover, tap y navegación por teclado. (2) Ráfagas incorporadas como segunda serie visible en los 4 gráficos, más mínima y máxima de viento y de ráfagas en la card de hoy y en cada día siguiente. (3) Nombres de modelo actualizados en la UI (viento/ráfagas = GDPS 15 km; marea = Open-Meteo Marine). (4) Corregido un fallo de contraste AA preexistente en `.btn-primary`.
- Motivo: (1) Joaquín marcó que el tooltip "se ve pobre" para información importante del pronóstico; el nativo además no existía en touch, que es el dispositivo con el que se mira el pronóstico antes de salir. (2) Joaquín pidió textualmente ver "la mínima y la máxima de viento" y las dos series de la tabla de CARP ("Velocidad Viento" / "Ráfaga"), en hoy y en los 3 días siguientes; para kite la ráfaga define si el día es manejable o violento. (3) El backend cambió de modelo tras validar contra mediciones reales.
- Impacto en capas: Presentación únicamente. El backend no se tocó.
- Posicionamiento del tooltip: nodo único en `<body>` con `position: fixed` (adentro del contenedor de la grilla quedaría recortado por el `overflow` en las horas de los extremos), anclado al rect de la hora que abarca todo el alto del gráfico — por construcción nunca tapa la barra que se está mirando. Se ubica arriba y hace flip a abajo si no entra; clamp horizontal contra el viewport con la punta siguiendo a la columna.
- Accesibilidad: tabindex rotativo sobre las horas — solo la activa es tabulable y las flechas recorren el resto, así los 96 puntos de datos de la página suman 4 paradas de tabulación en vez de 96. El tooltip es `aria-hidden` y la información viaja en el `aria-label` de cada hora, para no anunciarla dos veces.
- Verificación: Playwright — hover (ambos temas), tap con toggle y cierre al tocar afuera, teclado (flechas, Home/End con scroll into view, Escape), bordes de la grilla (horas 00 y 23 dentro del viewport), día bloqueado por `ConTemporal` y por señal desconocida (el tooltip dice que el día está bloqueado, no que faltó viento), hora con `mareaMetros` null, día completo con `rafagaNudos` null, mobile 390px sin desborde. Cero errores de consola. Lighthouse sobre el build: accesibilidad 100, best practices 100, SEO 100, performance 88 estable.
- Riesgos/supuestos: la navegabilidad sigue calculándose sobre el viento SOSTENIDO, no sobre la ráfaga. Un día de 12 kt sostenidos con rachas de 24 se marca como navegable. Es defendible (no se planifica una sesión sobre la racha) pero es una decisión de dominio que conviene que Joaquín confirme, ahora que la ráfaga está a la vista y la diferencia se nota. Siguen abiertos: confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`).

### 2026-09-02 - implementador-astro-front (quinta pasada — aviso de viento racheado)
- Etapa: Implementación (Etapa 1 — señal nueva en P1)
- Cambio: Se suma el aviso de **viento racheado** a nivel día (hoy y los 3 siguientes), a partir del campo nuevo `senalRacheado` del contrato. Badge con tono propio y borde punteado, más el despegue máximo (`diferenciaNudos`) mostrado también cuando la señal no dispara.
- Motivo: decisión de Joaquín sobre la pregunta que el agente había dejado abierta en la pasada anterior (si la ráfaga debía afectar la navegabilidad). Eligió avisar sin bloquear: textual, *"sigue contando como navegable, pero con aviso de racheado / inestable — no te lo esconde ni te lo prohíbe"*.
- Impacto en capas: Presentación únicamente. **El criterio de navegabilidad NO se modificó**: un día de 12 kt sostenidos con rachas de 24 sigue marcando "sopla" / "se navega", verificado explícitamente.
- Decisión de diseño: el badge tiene tono propio (`racha`, violeta) y **borde punteado**, para que no se confunda con el rojo sólido de `ConTemporal`, que sí bloquea el día. El punteado lo emparenta visualmente con la extensión de ráfaga del gráfico, que es el dato que lo origina. `diferenciaNudos` se muestra a nivel día y no dentro del tooltip por hora, porque es el máximo de la franja mediodía-tarde: mezclarlo con los valores horarios habría puesto dos números de alcance distinto uno al lado del otro.
- Verificación: Playwright — `activa: true` (badge presente y día igual en verde con su tag), `activa: false` con `diferenciaNudos` presente (sin badge, despegue visible), `diferenciaNudos: null` (sin badge y sin despegue), y racheado conviviendo con `ConTemporal` (los dos badges juntos, distinguibles). Regresión completa de las tres suites anteriores. Contraste del badge medido (6,48:1 claro / 9,82:1 oscuro) y corregido de paso el texto de despegue sobre oscuro, que daba 3,82:1. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: se cierra el riesgo abierto en la entrada anterior sobre sostenido vs. ráfaga — queda documentado como decisión de dominio tomada por Joaquín, no como pendiente. Siguen abiertos: confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (sexta pasada — franja 06-21 + escala y banda)
- Etapa: Implementación (Etapa 1 — ajustes sobre la grilla horaria de P1)
- Cambio: (1) Adaptación del front a la ventana horaria nueva del backend: `horas[]` pasó de 24 a 16 entradas (06:00–21:00) y los mín/máx del día se recalculan sobre esa franja. (2) El gráfico suma escala numérica de viento (guías + valores) y banda del rango navegable de cada persona, en la card de hoy y en los 3 días siguientes.
- Motivo: (1) Joaquín — "de 22hs a 5am descartar porque es el horario donde todos están durmiendo y no se puede navegar de noche". (2) Joaquín — "Cómo viene la semana debe mostrar rango de viento en el gráfico"; al preguntarle, se refería a las dos cosas: escala numérica (los gráficos compactos no tenían ningún número, eran una silueta) y banda del rango.
- Impacto en capas: Presentación únicamente.
- Punto de riesgo señalado y resuelto: `grafico-horas.ts` tenía `const W = PAD_X * 2 + 24 * COL` con las 24 columnas hardcodeadas. **No se reemplazó el 24 por un 16**: se eliminó la constante y el ancho, el paso de rótulos y el `min-width` inline del SVG derivan de `horas.length`, porque la ventana vive en `config/pronostico.php` y es recalibrable. Verificado con series de 0, 1, 5, 16 y 20 horas.
- Detalle de la banda: la escala de viento es compartida entre los 4 días, así que el techo del rango puede caer fuera del tope del gráfico. Se clampea el rectángulo al tope y se omite la línea punteada superior — sin línea la banda se lee como "sigue más arriba", dibujada en el borde mentiría. Si el rango entero queda sobre la escala, no se dibuja banda.
- Verificación: Playwright — 16 columnas con rótulos cada 2 h (paso derivado), eje con guías/valores/unidad en los 4 gráficos, banda presente y con 1 borde cuando el techo se sale y 2 cuando entra, rango 40–50 sin banda ni barras navegables, cambio de rango en vivo sin refetch con la banda moviéndose, horas variables (0/1/5/16/20), caso nocturno sin marca "ahora", mobile 390px sin desborde. Regresión completa de las cuatro suites anteriores. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100, performance 90 (subió desde 86-88 por los ~200 nodos SVG menos).
- Riesgos/supuestos: sin cambios respecto de la entrada anterior. Siguen abiertos: confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (séptima pasada — panel de acertividad de modelos)
- Etapa: Implementación (Etapa 1 — feature nueva en P1)
- Cambio: Sección nueva "Por qué usamos este modelo": tabla de acertividad de los 4 modelos evaluados, con el que está en uso marcado, el método explicado en lenguaje llano y el contexto de la muestra. Consume el endpoint nuevo `GET /api/modelos`.
- Motivo: pedido de Joaquín — "quiero poder ver desde la web la acertividad en % de los distintos modelos, para que el usuario vea qué modelos se evalúan, cuál es el utilizado y por qué". Deja de pedirle fe al usuario cuando el sitio dice "usamos GDPS" y le muestra la evidencia.
- Impacto en capas: Presentación únicamente.
- **Decisión de diseño dominante (honestidad, no estética):** hoy hay 17 horas de evidencia — un solo día, una sola situación sinóptica — y los números salen brutales (ECMWF 0,0 %, GDPS 82,4 %). Cada número es correcto, pero un diseño confiado habría mentido por presentación. Se resolvió dándole al tamaño de la muestra una stat-card del mismo cuerpo tipográfico que los porcentajes (26px contra 22px, medido), una píldora de madurez con explicación en prosa, y barras con trama mientras la muestra es chica —señal no verbal de provisionalidad que desaparece sola cuando la muestra crece—. Verificado que el mismo diseño aguanta con 17 horas y con 3.120. Los umbrales de la escala de madurez son una decisión de presentación, no un criterio estadístico, y están documentados como tal; el número crudo de horas y el rango de fechas se muestran siempre.
- Dato incómodo que no se escondió: la estación de referencia está a 20 km del spot. Va como stat visible, no en letra chica, y el detalle del método explica por qué es la única verdad de campo disponible (CARP publica solo 24 h y no se puede reconstruir historia hacia atrás).
- Aislamiento: el panel tiene su propio script y se pide en `requestIdleCallback` después del render del pronóstico, sin que nadie lo espere — verificado el orden real de requests. Si la base de datos se cae, el dashboard no se entera y la sección muestra un aviso chico.
- Verificación: Playwright — datos reales, `modelos: []` (aviso claro, sin tabla vacía), endpoint 503 (aviso, dashboard intacto), `acertividadPct: 0` (track visible: se lee como cero legítimo, no como dato faltante), muestras de 180 h y 3.120 h, caso sin sesgo dominante (el hallazgo desaparece solo), click real en el desplegable del método, mobile 390px sin desborde. Regresión completa de las suites anteriores. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100, performance 87.
- Riesgos/supuestos: sin cambios respecto de la entrada anterior. Siguen abiertos: confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (octava pasada — virazón, terral y offshore)
- Etapa: Implementación (Etapa 1 — corrección conceptual + señales nuevas en P1)
- Cambio: Adaptación del front al contrato corregido. Se fueron `senalBirazonBrisaCalma` y `senalBirazonBajante`; entraron `senalVirazon`, `senalTerral`, `senalOffshore` (con `horas`) y `senalBajante`, más el flag `offshore` por hora. Virazón se muestra como oportunidad (badge); offshore y terral, como advertencia de seguridad en un bloque propio; cada hora offshore se marca en la grilla horaria y en el tooltip.
- Motivo: Joaquín preguntó si el cálculo contemplaba virazón, terral y sudestada. La sudestada sí; las otras dos no, pese a que el código aparentaba que sí. "Birazón" era un término inexistente documentado desde el Análisis, con una definición inventada encima y una implementación que detectaba la precondición en vez del fenómeno.
- Impacto en capas: Presentación únicamente.
- **Decisión de diseño dominante:** el viento de tierra es la primera advertencia de riesgo **físico** del sitio. Todo lo anterior habla de condiciones o de calidad de sesión; esto habla de qué pasa si te quedás sin potencia. Por eso no comparte la fila de píldoras: va en un bloque propio con barra lateral, icono y kicker "Seguridad", en ámbar y no en rojo — el rojo ya significa "no se navega" en esta pantalla y reusarlo habría sido ambiguo. El terral se resolvió como sub-línea del offshore, no como segundo aviso, para que no quede ruido cuando ambos están activos. El bloque se ubicó **antes** del CTA comercial: una advertencia de riesgo físico no puede leerse después de un llamado a la acción.
- Marca por hora: además del badge del día, cada hora offshore lleva una tira fina bajo la línea base del gráfico — no se cambió la barra, porque el dato de la barra es el viento y pisarlo con un segundo significado la volvería ilegible. Permite ver que la mañana está offshore y la tarde no, y decidir a qué hora ir.
- **El criterio de navegabilidad no se modificó**: un día con 11 horas offshore sigue marcando "sopla" / "se navega", verificado explícitamente. Igual que el racheado: avisa, no bloquea.
- Verificación: Playwright — datos reales (día 0 sin offshore, día 1 con 7/16 h, día 2 con 2/16 + terral, día 3 con 11/16 + terral), virazón activa mockeada, offshore parcial (mañana marcada / tarde limpia), offshore + terral + sudestada + racheado juntos, `horas: 0` con `activa: false`, terral solo, tooltip en hora offshore vs. limpia, mobile 390px sin desborde. Regresión de las suites anteriores (cuyos mocks usan la forma vieja) ejercitando los caminos defensivos. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: queda para Joaquín decidir si el offshore debería bloquear la navegabilidad algún día, hoy solo avisa. Siguen abiertos: confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (novena pasada — cielo por hora + serie medida)
- Etapa: Implementación (Etapa 1 — datos nuevos en la grilla horaria de P1)
- Cambio: (1) Condiciones meteorológicas visibles en los gráficos: carril de íconos de cielo por hora + temperatura, con el detalle (nubosidad, probabilidad de lluvia) en el tooltip. (2) La serie de lo que midió Pilote Norden se superpone al pronóstico en el gráfico de hoy.
- Motivo: (1) Joaquín — "¿estás consultando si va a estar soleado, nublado, y las condiciones climáticas? Deberíamos mostrarlas en los gráficos". (2) Joaquín — "la información del Pilote Norden debería ser la fuente de verdad para el pronóstico actual".
- Impacto en capas: Presentación únicamente.
- **Las tres trampas del dato medido, todas resueltas y visibles:** (a) la serie tiene huecos reales — se dibuja en varios tramos que cortan en cada hueco, porque una recta cruzándolo inventaría una medición inexistente; cada punto medido lleva su marca para que un dato aislado igual se vea. (b) Termina unas horas antes que la lectura "ahora" del bloque de arriba (hoy: serie hasta las 11:00, lectura puntual de las 14:06) — es una rareza de CARP, no una falla, y la nota bajo el gráfico dice hasta qué hora llega para que nadie lo lea como que se rompió algo. (c) Norden está a 20 km del spot, más adentro del río y más expuesto: lee sistemáticamente más alto (+3 a +6 kt hoy a la mañana). **Esa diferencia es real, no error del modelo**, y sin decirlo las dos series juntas se leen como "el modelo le erra feo". Mismo cuidado que se tuvo con las dos mareas. La comparación justa sigue viviendo en el panel de acertividad, que le pide a cada modelo el pronóstico en las coordenadas de Norden.
- Decisión de carga visual: el gráfico ya tenía viento, ráfaga, marea, banda de rango, offshore y ahora la serie medida. El cielo entró como carril de íconos en tono apagado (contexto, no protagonista) y el detalle se mandó al tooltip. La traducción del código WMO vive en `lib/clima.ts`, en un solo lugar, para que gráfico y tooltip digan lo mismo.
- Verificación: Playwright — datos reales (hueco 09–10 y corte a las 11:00 presentes en la serie real), hueco al medio mockeado (tramos [3,2]), día sin ninguna medición (sin línea, sin puntos, sin leyenda, sin nota), medición aislada (1 punto, 0 polilíneas), `condicionCodigo: null` (0 íconos, sin errores), las 7 categorías de cielo renderizando, tooltips en hora con medición / en hueco / futura, mobile 390px sin desborde. Regresión completa de las suites anteriores. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: sin cambios. Siguen abiertos: si el offshore debería bloquear la navegabilidad algún día, confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (décima pasada — navegabilidad de tres estados)
- Etapa: Implementación (Etapa 1 — cambio de núcleo en el criterio de navegabilidad)
- Cambio: La navegabilidad dejó de ser booleana y pasó a tres estados: `navegable` (sostenido dentro del rango), `limite` (sostenido por debajo del mínimo pero rachas que sí llegan, con el faltante dentro de `config.margenAlLimiteNudos`) y `no`. El margen aplica **solo por abajo**.
- Motivo: Joaquín — "si mi rango de viento es de 16 a 28 nudos y el viento actual es de 14 con ráfagas a 18-19 nudos, lo considero un día navegable (al límite)". Con navegabilidad booleana esos días se perdían enteros porque 14 < 16, y uno arranca en las rachas.
- Impacto en capas: Presentación únicamente. Refactor asociado: `rango` y `margen` viajan juntos en un objeto `Criterio` (eran dos parámetros que siempre iban de a pares); `mascaraNavegable` (boolean[]) pasó a `estadosDelDia` (EstadoHora[]); `Racha` ganó el campo `composicion`.
- **Disciplina fail-safe reforzada, no aflojada:** el estado nuevo es más permisivo por diseño, así que los caminos defensivos siguen cerrando hacia el lado seguro. `rafagaNudos: null` **no puede** dar `limite` (ese estado se sostiene enteramente sobre la racha); día bloqueado sigue anulando todo; y el fallback de `margenAlLimiteDe()` es 0, que desactiva el estado por completo — ante un contrato recortado se cae al comportamiento estricto anterior, no a un margen inventado.
- Decisión de color: las horas "al límite" van en **verde hueco** (contorno + relleno tenue), no en ámbar. El ámbar ya significa viento de tierra / seguridad, que es un riesgo de otra naturaleza; reusarlo habría confundido "el viento apenas alcanza" con "el viento te aleja de la costa". Vocabulario final: verde sólido = pleno, verde hueco = entra por las rachas, gris = fuera, ámbar = viento de tierra. Una racha mixta lo aclara con un chip ("al límite" / "parte al límite") y el tooltip explica el porqué, no solo la etiqueta.
- Verificación: Playwright con los cinco bordes exactos especificados (rango 16–28, margen 3): 14/18,5 entra · 14/15 no · 10/17 no · 13/19 entra justo · 12,9/20 no. Más ráfaga `null` en hora que si no calificaría, sostenido sobre el máximo, día entero al límite (amarillo, 16 barras huecas), racha mixta (3 huecas + 3 sólidas, verde, chip "parte al límite"), día bloqueado con horas que calificarían, y config sin `margenAlLimiteNudos`. Con datos reales y rango 9–30 aparecen 12 horas plenas + 4 al límite; cambiar a 16–28 en vivo re-renderiza sin refetch. Regresión completa de las cuatro suites anteriores. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: sin cambios. Siguen abiertos: si el offshore debería bloquear la navegabilidad algún día, confirmar la convención de `direccionGrados`, y el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front (undécima pasada — el offshore bloquea + señales de río)
- Etapa: Implementación (Etapa 1 — cambio de criterio + señales nuevas en P1)
- Cambio: (A) El viento de tierra pasó de avisar a **bloquear**: una hora con `offshore: true` no es navegable aunque el viento entre en el rango. (B) Cuatro señales nuevas o corregidas: `senalBajante` (la causa el norte, no el O/NO; depende de la duración), `senalCreciente`, `senalVientoDesparejo`, y `senalRacheado` que ahora también dispara por sector.
- Motivo: (A) Joaquín, textual — "el viento de tierra es condición justa y necesaria para no navegar". (B) Cuatro datos de campo aportados por Joaquín; el sector O/NO de la bajante lo había inventado el propio sistema.
- Impacto en capas: Presentación únicamente.
- Impacto real medido: el sábado pasó de 16 horas navegables a 0; el 03/09 de 12 a 3. El semáforo y las rachas se movieron mucho, como estaba previsto.
- **Hallazgo de la verificación:** la píldora del semáforo decía "Fuera de tu rango" en un día caído enteramente por viento de tierra — exactamente la confusión que había que evitar, porque sugiere que ampliando el rango se desbloquea. Se unificó el motivo en `motivoSinHoras` (`lib/semaforo.ts`), usado por la píldora y por el tag de racha vacía, y ahora dice "Viento de tierra" / "Sin horas útiles" según la causa.
- Vocabulario visual reconciliado: el ámbar dejó de significar "ojo pero se navega" y pasó a ser el "no" de esa causa (barra ámbar sólida), distinto del gris de "falta viento". Regla general: relleno sólido = estado decidido, contorno = condicional (verde hueco = al límite). La tira ámbar bajo la línea base se mantuvo como segundo canal, porque ámbar contra gris puede costar con visión cromática reducida.
- Anti-ruido: con seis avisos posibles se agruparon por categoría en orden fijo con tono propio — seguridad (ámbar, bloque aparte) / riesgo (rojo) / estado del río (cian, familia del agua) / calidad del viento (violeta punteado, racheado y desparejo comparten tono a propósito) / oportunidad. Verificado por geometría que el bloque de seguridad queda por encima de los badges.
- Verificación: Playwright — bloqueo total y parcial, offshore sobre hora plena y sobre hora al límite, mezcla de causas, racheado que sigue sin bloquear, creciente y bajante activas e inactivas, racheado por sector vs. por despegue, desparejo con los dos tipos de sudestada, día cargado con bloqueo + 5 badges. Contraste del tono de río medido (5,15:1 / 12,51:1). Regresión completa de las suites anteriores. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: se cierra el pendiente sobre si el offshore debía bloquear. Queda abierto: confirmar la convención de `direccionGrados`, el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`), y una observación nueva — los textos de las señales que manda el backend son largos y con muchas activas la card de hoy se estira bastante; si a Joaquín le resulta ruidoso, el próximo paso natural es acortar esos textos, no esconder información.

### 2026-09-02 - implementador-astro-front (duodécima pasada — calibración Norden → spot)
- Etapa: Implementación (Etapa 1 — calibración de la medición en P1)
- Cambio: El bloque "ahora en el río" pasa a mostrar el viento estimado en la costa como cifra principal, con la lectura cruda de Norden visible como respaldo; la serie del gráfico pasa de la lectura cruda al equivalente en el spot.
- Motivo: dato de campo de Joaquín — "el pilote siempre marca 2 o 3 nudos más de lo que realmente hay en la tierra". Tiene sentido físico: Norden está a 20 km, en el medio del río, sin rugosidad de superficie que frene el viento. De paso destapó un error silencioso del backend (el motor usaba la lectura cruda para calcular las señales, así que los umbrales disparaban con 2-3 kt de más), ya corregido del lado del servidor.
- Impacto en capas: Presentación únicamente.
- **Medición vs. inferencia:** `vientoNudos` es un dato medido, `vientoNudosSpot` es una inferencia nuestra, y no se presenta una como la otra. Mismo criterio que con las dos mareas, con una diferencia: acá sí hay una relación conocida y declarada entre las dos cifras, así que se pueden mostrar juntas. Se reusó el vocabulario de badges que ya existía (`MEDIDA` / `MODELO` → `ESTIMADA`).
- **El beneficio concreto está en el gráfico:** al dibujar el equivalente, las dos series pasan a ser del mismo lugar y por lo tanto comparables. La nota se reescribió por completo — antes tenía que defender la diferencia ("es real, no es error del modelo"), ahora dice lo contrario: lo que quede de diferencia sí es error del modelo, sin el sesgo de ubicación tapándolo. La magnitud de la corrección se deriva de los datos, no está hardcodeada.
- Verificación: Playwright — estación con ambos valores, estación no disponible, medición cruda por debajo de la corrección (el estimado muestra 0, no un negativo), y contrato recortado sin equivalente (cae a la lectura cruda etiquetada como tal, sin línea ni nota; deliberado). Se actualizaron los mocks de la suite de clima, que habían dejado de ejercitar la lógica de huecos al cambiar el campo que alimenta la serie. Regresión completa de las seis suites. Cero errores de consola. Lighthouse: accesibilidad 100, best practices 100, SEO 100.
- Riesgos/supuestos: sin cambios. Siguen abiertos: confirmar la convención de `direccionGrados`, el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`), y que los textos de señales del backend son largos y estiran la card cuando hay varias activas.

### 2026-09-02 — implementador-astro-front — decimotercera pasada: el reloj manda + auditoría de consistencia
- Pedido de Joaquín (dos lotes encadenados). **(A) Bug de reloj:** "el estado actual del río marca que ya pasó la hora de kite y no hay más el resto del día, pero el sistema informa día de kite. Recalcular siempre el estado actual en base a la hora actual". **(B) Auditoría:** buscar textos que contradigan el comportamiento, señales huérfanas y vocabulario cromático inconsistente, priorizando "lo que pueda confundir a alguien que decide si sale al agua".
- **Causa raíz del bug:** `indiceHoraActual()` devolvía `-1` para dos situaciones opuestas — reloj antes de la ventana (queda el día entero) y reloj después (no queda nada) — y quien lo consumía no podía distinguirlas, así que el semáforo evaluaba siempre el día completo. Se agregó `desdeAhora(dia, ahora)` en `navegabilidad.ts` (0 / índice / `horas.length`) y se enhebró hasta `evaluarEstado`, `rachasNavegables` y la grilla.
- Tres reglas derivadas: **solo HOY mira el reloj** (los próximos días se evalúan completos); una racha empezada se **recorta**, no se descarta (15:40 sobre una racha 10–21 muestra `15 a 21`); y el pasado **se atenúa, no se borra** — con la excepción de la serie medida, que nunca se atenúa porque es lo único observado del día.
- Caso nuevo: el día que **se quedó sin horas** → rojo "Hoy ya pasó" + "Las horas navegables de hoy ya pasaron — mirá los próximos días", distinguido de "Fuera de tu rango" (no es que el día fuera malo, es que llegaste tarde). El refresco periódico usa el mismo camino, así que la pestaña abierta se corrige sola.
- **Hallazgo más grave de la auditoría:** un día con TODAS las horas offshore mostraba "Fuera de tu rango" — el viento estaba dentro del rango y lo que lo descartaba era el viento de tierra. Se unificó el motivo en `motivoSinHoras()`, que ahora alimenta la píldora, el tag de racha vacía y el `aria-label` del gráfico.
- **Segundo hallazgo de accesibilidad:** el `aria-label` del gráfico anunciaba las rachas del día completo, así que a las 22:30 leía "sopla de 06 a 21" en pasado. Ahora recalcula desde la hora actual — el dibujo puede mostrar el día entero porque el atenuado se ve de un vistazo, pero un texto leído en voz alta no tiene esa pista.
- **Colisión de ámbar cerrada:** la sudestada sin temporal (una oportunidad) usaba el mismo ámbar que el bloque de seguridad. Pasó a azul `info` y el tono `atencion` se eliminó — el ámbar hoy es exclusivamente "no se navega".
- **Texto en pantalla corregido** en "Cómo viene la semana": decía que se marcan navegables "las horas entre 9 y 30 nudos", ignorando las barras al límite (por debajo del mínimo) y las descartadas por viento de tierra (dentro del rango).
- Limpieza menor: comentarios de `semaforo.ts` y `global.css` que todavía decían que el offshore no bloquea, CSS muerto `.ght-offshore*` y `.gh-key--offshore` declarada dos veces.
- Verificación: Playwright con `Date` mockeado en seis relojes (04:30 / 07:15 / 15:40 / 21:10 / 22:30 y una ventana matinal 08–12 vista a las 17:30) — todos los bordes dan el veredicto esperado, los días futuros no cambian en ningún escenario y la serie medida da `medidoAtenuado: 0` siempre. Suite específica para el `aria-label` en los cuatro motivos (offshore / fuera de rango / ya pasó / con rachas): los tres textos coinciden. 19 suites de regresión sin errores de consola. `astro check` 0 errores, `npm run build` limpio.
- Nota de método: las suites pasaron a **fijar el reloj** (`FIJAR_RELOJ` a las 05:00). Desde que el veredicto depende de la hora, una suite sin reloj fijo cambia de resultado según cuándo se corra.
- Lighthouse contra el build servido como producción: **accesibilidad 100, best practices 100, SEO 100, performance 79** (FCP 2,9 s / LCP 3,8 s bajo el throttling móvil simulado; 431 KB totales, ninguna request por encima de 40 ms). El costo está en JS, no en red.
- Riesgos/supuestos: sin cambios. Siguen abiertos: confirmar la convención de `direccionGrados`, el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb), y que los textos de señales del backend son largos y estiran la card cuando hay varias activas.

### 2026-09-02 — implementador-astro-front — decimocuarta pasada: dirección hora por hora en los cuatro gráficos
- Pedido de Joaquín: *"Mostrar dirección del viento en cada hora de cada gráfico."* Antes las flechas salían cada `pasoEtiquetas` horas en el detallado y **no salía ninguna** en los tres días siguientes (`conFlechas: false`).
- **Por qué se justifica darle lugar:** desde que el viento de tierra bloquea, la dirección dejó de ser un adorno y pasó a ser uno de los datos que deciden si se sale al agua. Lo que se lee en esa fila no es el valor de una hora suelta — para eso está el tooltip con los grados exactos — sino el **patrón**: cuándo rota el viento a lo largo del día. Una flecha cada tres horas no muestra una rotación, muestra tres muestras.
- **El desafío era de legibilidad y se resolvió sin sacrificar las barras:** fila propia debajo de los rótulos de hora (no se superponen al perfil de viento, así que 16 flechas no le quitan forma), flechas más chicas (alto 10 → 8,8) y tenues — donde la dirección es constante quedan como textura pareja y donde rota la rotación salta sola.
- **La flecha de una hora offshore va en ámbar**, apareada con la tira ámbar de esa misma hora: son el mismo hecho dicho dos veces (de dónde viene el viento / por qué esa hora se descarta), así que se leen juntas en vez de competir. Consistente con el vocabulario consolidado la pasada anterior — el ámbar es seguridad y nada más.
- El atenuado del pasado también aplica a las flechas. El gráfico compacto creció de 120 a 130 px para hacerle lugar a la fila; las tres filas de días siguen entrando.
- Se agregó la clave de leyenda "dirección: hacia dónde sopla" en los dos gráficos. **Cuidado de redacción:** el primer texto que escribí decía "de dónde viene el viento", que es lo contrario de lo que muestra la flecha (`anguloFlecha` suma 180° a la convención meteorológica). Se corrigió antes de quedar en pantalla.
- **Último supuesto abierto del proyecto, cerrado:** Joaquín confirmó mirando la pantalla contra el viento real que "la flecha apunta bien". La convención dejó de ser un supuesto y se anotó como confirmada en el docblock de `anguloFlecha`.
- Verificación: Playwright — los 4 gráficos con 16 flechas cada uno, `viewBox` 180 / 130 / 130 / 130, flechas ámbar coincidiendo exactamente con las tiras offshore (4 y 4 en cada gráfico), atenuado de flechas igual al de barras (9 en HOY a las 15:40, 0 en los días futuros), ángulos correctos en rotación fuerte (180 / 202,5 / 225 / 247,5) y en dirección constante (264 en las 16). Mobile 390 px sin desborde horizontal. Capturas revisadas a ojo en tema claro y oscuro. 13 suites sin errores de consola (el único que aparece es el `SecurityError` inyectado a propósito en el escenario de `localStorage` bloqueado). `astro check` 0 errores, build limpio.
- Lighthouse: accesibilidad 100, best practices 100, SEO 100 sin auditorías imperfectas; performance 76 contra 79 de la corrida anterior — 36 paths SVG más no explican esa diferencia, es ruido entre corridas del throttling simulado.
- Riesgos/supuestos: **ya no queda ninguno abierto sobre los datos**. Pendiente de infraestructura: el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb). Sigue observado que los textos de señales del backend son largos y estiran la card cuando hay varias activas.

### 2026-09-02 - implementador-astro-front - decimoquinta pasada: performance, con el fondo animado intacto
- Pedido: tres mejoras de performance, con la condicion explicita de Joaquin: *"me gusta el fondo animado, conservarlo pero mejor rendimiento"*. Se midio **antes y despues de cada una por separado**, como se pidio.
- **1. GSAP duplicado, falso positivo, revertido.** La evidencia era `quickSetter` en los dos bundles, pero ScrollTrigger *llama* a `gsap.quickSetter`, asi que el string aparece sin que el core este copiado. Con marcadores exclusivos del core (`staggerTo`, `globalTimeline`, `registerEffect`) se ve que aparecen **una sola vez**, en el chunk compartido: Rollup ya deduplicaba. Forzar `manualChunks` llevo el JS de 177.248 a **177.812** bytes, complejidad de config a cambio de 564 bytes de mas. Revertido.
- **2. Diferir el canvas, sin beneficio, no se hizo.** `wind-bg.js` pesaba 69,8 KB con 5,4 KB de codigo propio porque es el chunk **compartido** donde Rollup hoisteo el core de GSAP, que necesitan las dos entradas (reveal/tilt en BaseLayout, dashboard en index). Separar `setWindAngle` del loop no bajaria nada: el core hace falta igual para el dashboard. El fondo nunca fue lo pesado; el bundle solo llevaba su nombre.
- **3. Fuentes, real, y el beneficio es de deploy.** Los subsets `latin` explicitos dejaron `dist` en **21 archivos contra 69**, fuentes de 56 a 8 y de 892.928 a 221.304 bytes. Al visitante no le cambia (el navegador ya descargaba solo lo suyo); lo que baja es lo que sube el FTP en cada deploy.
- **Lo que si movia la aguja no estaba en la lista:** `reveal.ts` era el unico consumidor de `gsap/ScrollTrigger` en todo el sitio, y ese plugin pesaba **~42 KB, el 24% de todo el JS**, para disparar un fade-in cuando un elemento entra en pantalla. Se reemplazo por IntersectionObserver **conservando GSAP para la animacion**, asi que el easing y la consistencia con el resto del sitio no cambian: lo unico distinto es quien avisa que el elemento entro.
- **Trampa que ScrollTrigger cubria gratis:** un elemento por *encima* del viewport (View Transitions restaurando scroll, o recarga a mitad de pagina) no esta intersectando, y un IntersectionObserver ingenuo lo dejaria en `opacity: 0` para siempre. La condicion de disparo es `isIntersecting || boundingClientRect.top < 0`, con un caso de prueba que carga con el scroll al fondo.
- **Resultado medido:** JS **177.248 a 134.679 bytes (-24%)**; `dist` **69 a 21 archivos**; Lighthouse performance **79 a 83**, Speed Index **3,6 s a 2,7 s**, TBT 240 a 210 ms, FCP 2,9 a 2,7 s; accesibilidad, best practices y SEO siguen en 100 sin auditorias imperfectas.
- **El fondo animado quedo igual, verificado y no asumido:** el canvas pinta (9.197 px con alfa), anima entre frames, y **sigue reaccionando a la direccion del viento**: con rumbo 90 grados las estelas quedan horizontales (adyacencia H/V 1,76) y con 180 verticales (0,58). Medir el centro de masa no servia porque las particulas nacen en posiciones aleatorias; la orientacion de las estelas si lo prueba.
- Reveal verificado en cuatro escenarios: sin scroll (4 visibles arriba del pliegue, 6 todavia ocultos), tras recorrer la pagina (10/10), `prefers-reduced-motion` (10/10 de entrada, sin animar) y carga con el scroll al fondo (10/10, el caso borde).
- Fuentes verificadas **en pantalla** en los cuatro pesos: acentos, enie mayuscula y minuscula, dieresis, signos de apertura de pregunta y exclamacion, comillas tipograficas, rayas y el punto medio que usa todo el sitio. `latin` alcanza; no hizo falta `latin-ext`. **Dato a tener en cuenta:** los CSS por subset de `@fontsource` **no traen `unicode-range`**, asi que un caracter fuera del subset cae en la fuente del sistema, no en otra cara de Inter.
- 14 suites de regresion sin errores (el unico que aparece es el `SecurityError` inyectado a proposito en el escenario de `localStorage` bloqueado). `astro check` 0 errores, build limpio.
- **Aviso de proceso:** el commit `0414577` se hizo mientras yo trabajaba y arrastro la edicion de `manualChunks` antes de que estuviera medida. Quedo revertida en el working tree; conviene revisar ese diff.

### 2026-09-02 - implementador-astro-front - decimosexta pasada: rediseño de cards por jerarquia
- Pedido de Joaquin: *"No me importa que los min/max de viento y rafagas sean del dia que ves en el grafico. Me importa mas el detalle de hora a hora del viento y estado actual. Sobre todo los dias navegables. Rediseñar cards en base a esto ultimo."* Reordenacion de jerarquia, no ajuste cosmetico.
- **El problema, medido antes de tocar nada** (390x844, mobile): la respuesta a "¿salgo hoy y a que hora?" estaba en **y=1065, fuera de pantalla**, en una pildora chica al final del bloque del grafico; los min/max que Joaquin marco como lo que MENOS le importa estaban 300px mas arriba (y=767) y en dos stat-cards con borde, el elemento mas pesado del bloque.
- **Despues:** veredicto en **y=371**, grafico en **y=537** (antes 875), estado actual en y=772, min/max en y=1212. Las dos cosas que pidio primero entran juntas en la primera pantalla, sin scrollear.
- **`veredictoDelDia` (nuevo, en `semaforo.ts`)** dice tres cosas en tamaño decreciente: kicker ("Hoy se navega" / "Se navega ahora" / "Hoy no se navega"), titular con **el horario** ("14 a 19 h") o el motivo corto, y contexto ("6 horas - pico 17 kt - 2 ventanas"). **No decide nada nuevo**: se apoya en `rachasNavegables` y `motivoSinHoras`, las mismas fuentes que la pildora del semaforo y el `aria-label` del grafico, asi que las cuatro superficies no pueden contradecirse.
- Hereda gratis el recorte por reloj: dentro de una ventana el kicker pasa a **"Se navega ahora"** con punto latiendo y el titular se recorta a lo que queda (verificado: a las 09:20 sobre una ventana 08-10 muestra "09 a 10 h").
- **Las ventanas en detalle se listan solo si hay mas de una.** Con una sola el titular ya la dijo.
- **Vocabulario de color respetado:** el veredicto tiene dos estados y **ninguno es ambar**. `ok` verde; `no` **neutro a proposito**, porque el ambar es viento de tierra y nada mas, y un "no" puede ser por falta de viento. El bloque de seguridad quedo **pegado al veredicto**: la posicion mas visible que tuvo nunca.
- **Nada se borro, todo bajo de rango:** los min/max pasaron a una tira rotulo+valor junto con direccion, marea y despegue; la leyenda de nueve claves paso a `<details>` plegable (ocupaba cuatro renglones justo encima del grafico); la nota de la serie medida se movio debajo del bloque del rio, que es donde estan las dos cifras que explica ("estimado en la costa" contra "Pilote Norden mide").
- **La semana:** la fila arranca por el veredicto en vez de por la fecha y el rango de viento en 28px. Se recorre por la izquierda leyendo `Mañana - Si - SE NAVEGA - 14 a 19 h`. La columna de info crecio de 232 a 320px: a 232 el bloque de seguridad se partia en renglones de tres palabras mientras al grafico le sobraba alto.
- Verificacion en **390px primero**, los cuatro escenarios pedidos: navegable ("Hoy se navega / 14 a 19 h"), bloqueado por viento de tierra ("Hoy no se navega / Viento de tierra"), sin horas por rango ("Hoy no se navega / Fuera de tu rango") y HOY ya pasado ("Hoy / Hoy ya paso"). Sin desborde horizontal en ninguno, cero errores de consola. Mas tres casos de dos ventanas (antes de la primera, dentro de la primera, ya pasada la primera) y revision visual en desktop.
- 17 suites de regresion sin errores (el unico es el `SecurityError` inyectado a proposito). `astro check` 0 errores, build limpio.
- Lighthouse: **accesibilidad 100** (requisito explicito), best practices 100, SEO 100, sin auditorias imperfectas; performance 83 a 85.
- Detalle de implementacion: el ocultado del despegue vacio se hace desde JS (`pintarDespegue` esconde el item entero, rotulo incluido) y no con `:has()` en CSS, para no depender del soporte del selector. Se limpio el CSS muerto de `.gh-extremos` y de los bloques de la fila que dejaron de existir.
- Riesgos/supuestos: sin novedades. Sigue pendiente solo el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front - decimoseptima pasada: despegue mezclado, min/max fuera
- Pedido de Joaquin: *"Velocidad viento 2,3-14,6 kt / Rafagas 3,1-22,4 kt: no mostrar. Mostrar solo el despegue de viento que viene teniendo el dia y promediarlo con lo pronosticado para las proximas horas."*
- **Min/max fuera.** Un paso mas que la pasada anterior, donde solo los habiamos bajado de rango. Direccion y marea se quedan (no las menciono). La informacion no se pierde: el grafico cuenta el dia hora por hora y el tooltip da los valores exactos de cada hora.
- **`despegueDelDia(dia, desdeIdx)`**, nuevo en `semaforo.ts`: promedia hora por hora las transcurridas CON medicion (`rafagaMedidaNudos - vientoMedidoNudos`) y las que faltan (`rafagaNudos - vientoNudos`). Vive en el front porque el corte entre las dos fuentes es **el reloj del visitante**.
- **Ponderado por cantidad de horas, no 50/50.** Con 10 medidas y 2 por delante, promediar los dos promedios le daria a esas 2 el mismo peso que a las 10 y dejaria de describir el dia. Como cada hora entra como una muestra suelta, el promedio de todas las muestras **ya es** el ponderado correcto: no hay aritmetica de pesos que mantener ni que se pueda romper despues.
- **Decision de criterio:** una hora ya transcurrida **sin medicion no aporta nada**. No es "lo que vino pasando" (no se midio) ni "lo que queda" (ya paso), y rellenarla con el pronostico seria colar pronostico viejo dentro de la mitad observada del numero, que es justo lo que el cambio vino a evitar.
- **Se declara de donde sale**, en el `title` y no en pantalla: "promediado sobre 16 horas: 6 horas medidas y 10 pronosticadas". Misma disciplina que con las dos mareas y con la correccion al spot. Sin eso, un dia con la estacion caida daria un numero mas flaco sin que se note.
- Tres casos salen **sin ramas especiales**: dia futuro (`desdeIdx = 0`) da puro pronostico; hoy sin mediciones, lo mismo; hoy ya pasado (`desdeIdx = horas.length`) da puro observado. Sin rafagas en el modelo el item se esconde entero, rotulo incluido.
- Verificacion en 390px, con un dia calibrado a proposito para que el despegue **medido sea 2 kt y el pronosticado 8 kt**, de modo que el numero delate la proporcion:

  | escenario | horas medidas / pronosticadas | resultado |
  |---|---|---|
  | dia futuro (fila de la semana) | 0 / 16 | **+8 kt** |
  | hoy 12:10 | 6 / 10 | **+5,8 kt** |
  | hoy 18:10 | 12 / 4 | **+3,5 kt** |
  | hoy ya pasado 22:40 | 16 / 0 | **+2 kt** |
  | hoy sin ninguna medicion | 0 / 10 | **+8 kt** (title: "10 pronosticadas") |
  | dia sin rafagas en el modelo | - | item oculto |

- **Hallazgo a evaluar: `despegueMaximoNudos` se quedo sin consumidor en el front.** El coordinador lo daba por "el que acompaña al grafico", pero rastreado en el codigo se dibujaba unicamente en esta tira, via `textoDespegue`. Al reemplazar ese texto por el promedio nuevo, el campo dejo de mostrarse en ningun lado. Sigue viniendo en el payload y sigue siendo valido; queda a criterio de Joaquin si vuelve a alguna superficie.
- Limpieza: `extremosRafaga` se borro por quedar sin uso; `textoDespegue` se reescribio sobre el promedio nuevo y pasa por `nudos()` en vez de un formateador propio, para no abrir una segunda fuente de formato numerico.
- 16 suites de regresion sin errores (el unico es el `SecurityError` inyectado a proposito). `astro check` 0 errores, build limpio. Lighthouse: **accesibilidad 100**, best practices 100, SEO 100 sin auditorias imperfectas; performance 85 a 86.
- Riesgos/supuestos: sin novedades. Sigue pendiente solo el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front - decimoctava pasada: nueva base de comparacion en el panel de modelos
- Cambio en el backend: a cada modelo se le pide ahora el pronostico en **las coordenadas del spot** y se lo compara contra el **equivalente estimado en la costa** (lectura de Norden menos 2,5 kt), en vez de modelo-en-Norden contra la lectura cruda de Norden.
- **Lo que el panel dejo de poder afirmar.** No hay sensor en Punta Lara: el lado de referencia es una **inferencia nuestra**, igual que `vientoNudosSpot` en `estaciones[]`. Se reescribio todo el copy que decia "lo que midio la estacion", "del viento medido" y "es la unica medicion real". El caso mas grave: el primer parrafo del metodo afirmaba **exactamente lo contrario** de lo que ahora pasa ("a cada modelo se le pide... las coordenadas exactas de la estacion, no las del spot"), asi que se rehizo entero.
- **Tres campos nuevos en `metodo`**, y como se usan:
  - `compara` se muestra **tal cual** en la intro. Tener esa frase escrita a mano en el markup fue justamente lo que dejo el panel mintiendo cuando cambio la base; ahora la fuente de verdad de lo que el panel puede afirmar llega con el dato.
  - `medicionEsEstimada` es una **bandera de comportamiento**: el dia que haya sensor en el spot pasa a `false` y el panel dice "medido" sin que nadie toque el copy. **Ausente se asume `true`**, el lado conservador — llamar "estimado" a una medicion subvende el dato; llamar "medido" a una inferencia lo falsea.
  - `correccionEstacionNudos` se muestra como stat propio ("Correccion aplicada −2,5 kt · estimado, no medido"), al lado de la distancia porque es su consecuencia.
- El `<details>` explica la **cadena completa**: pronostico en el spot, lectura de Norden a 20 km, menos los 2,5 kt que esa estacion lee de mas, y comparacion. Dice explicitamente que esos 2,5 kt **no son un valor medido** sino conocimiento de campo de quien navega el spot.
- **Bug encontrado probando el contrato recortado:** el parrafo de la cadena era incondicional e imprimia "se le restan **— —**" cuando no habia correccion; y con `medicionEsEstimada: false` habria afirmado que no hay sensor en el spot, que seria falso. Se partio en **dos versiones elegidas por dato** (cadena larga con correccion / cadena corta sin ella).
- **Matiz que si se conto:** comparar contra un estimado no debilita el panel, le agrega algo que validar. El `<details>` enseña a leerlo: *si la correccion estuviera mal calibrada, todos los modelos mostrarian sesgo para el mismo lado **y de tamaño parecido**.* Las dos condiciones importan — mismo signo con magnitudes distintas apunta a error de los modelos, no a un offset mal puesto.
- **Matiz que NO se puso en pantalla:** que aplicar la correccion estrecho los sesgos respecto de la base vieja (de −1,38/−4,73 a −0,60/−2,00). Es cierto y da confianza, pero son dos muestras de tamaño y periodo distintos, y afirmarlo con 25 horas es exactamente el tipo de conclusion sobre muestra chica que este panel existe para no hacer. Ademas hardcodear los valores viejos los congelaria en el copy. Queda a criterio de Joaquin si lo quiere igual.
- **Vuelta a muestra chica:** la serie arranco de nuevo (las horas viejas median otra cosa), el panel muestra 25 horas y volvio a "Muestra inicial". La madurez lo maneja sola: pildora, explicacion y barras con trama de provisional. **Verificado que se lee como un estado y no como un error.** Con esa muestra GDPS dejo de ser el mas acertado (ICON 36% contra 24%); el panel **no editorializa** sobre eso.
- Verificacion en 390px, tres escenarios de contrato: base nueva (estimada, con correccion), **contrato recortado sin los campos nuevos** (cae a "estimado", esconde correccion y sus dos parrafos, usa la cadena corta) y **`medicionEsEstimada: false`** (dice "medido en el spot" en los cuatro lugares donde aparece el vocabulario). Barrido automatico confirmando que ningun texto visible afirma medicion mientras la referencia sea estimada, y que no queda ningun hueco de placeholder en los parrafos visibles.
- 17 suites de regresion sin errores. `astro check` 0 errores, build limpio. Lighthouse: **accesibilidad 100**, best practices 100, SEO 100 sin auditorias imperfectas; performance 86 a 87.
- **Nota de entorno:** el dev server de Astro se volvio inestable y se caia despues de una o dos suites. La regresion se corrio contra el **build de produccion** servido en :4399, que ademas es el artefacto que se despliega. Conviene dejarlo asi de ahora en mas.
- Riesgos/supuestos: sin novedades. Sigue pendiente solo el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front - decimonovena pasada: la ventana del dia la define la luz
- Bug que vio Joaquin: *"Mañana dice que si se navega a las 21 hs y no hay luz a esa hora."* Tenia razon: la franja publicada estaba fija en 06-21 mientras en Punta Lara amanece 07:06 y atardece 18:35, asi que el sitio publicaba **una hora de oscuridad a la mañana y dos y media a la noche** y podia recomendar navegar de noche. Corregido en el backend, que ahora filtra por luz real (`daily=sunrise,sunset`).
- **`horas.length` paso de 16 a 12 y ahora es variable con la estacion del año.** Era la primera vez que ese largo cambiaba de verdad desde que se decidio derivarlo todo de el, asi que se verifico a fondo:

  | ventana | barras / flechas / hits | ancho SVG | paso de rotulos |
  |---|---|---|---|
  | 4 h (10-13) | 4 | 216 px | cada 1 |
  | 12 h (07-18) | 12 | 568 px | cada 2 |
  | 16 h (06-21, sin datos de luz) | 16 | 744 px | cada 2 |
  | 17 h (05-21, verano) | 17 | 788 px | cada 2 |

  El recorte por reloj sigue el largo nuevo (a las 12:10 sobre 07-18 quedan 5 horas atenuadas; a las 20:30, las 12), y las rachas y el veredicto tambien. **No hubo que tocar nada de geometria.**
- **Donde se dice por que el dia se corta ahi:** una linea chica bajo el titulo del grafico, *"Solo horas con luz: amanece 07:06, atardece 18:35"*, con un sol en el azul de acento. Va ahi porque es el pie de pagina del grafico, no una noticia. **No en ambar**, que sigue reservado al viento de tierra: esto no es una alerta, es el motivo de un recorte. En las filas de la semana el horario entra como un item mas de la tira de contexto y la regla se cuenta **una vez** en el encabezado de la seccion.
- **Sin `amanece`/`atardece` no se afirma nada.** En `null` el backend cayo a la franja fija y el corte no lo puso la luz, asi que decir un horario de sol ahi seria inventar justo el dato que causo el bug. La linea y el item se esconden **enteros, rotulo incluido**; verificado que no queda ningun `null`, `undefined` ni hueco de texto en la card.
- **El matiz opcional, resuelto sin ensuciar:** la ultima hora con luz se avisa en el **tooltip** (*"Ultima hora con luz — atardece 18:35"*), no con una marca nueva en el grafico. Dos razones: el vocabulario cromatico ya carga seis significados y un septimo lo volveria ilegible; y esto **no cambia si la hora es navegable o no**, solo dice que clase de hora es. El indice sale de `horas.length - 1`, apoyado en que el backend ya recorto por luz — recalcularlo contra `atardece` seria una segunda fuente de verdad para el mismo hecho.
- **No se inventaron horas de penumbra** ni se estiro la ventana: si una hora queda a caballo del atardecer, no se publica.
- **Hallazgo de harness:** dos suites propias (`vtip` y `vraf`) tenian `data-idx="14"` y `querySelectorAll(...)[14]` escritos a mano — un supuesto de la ventana de 16 horas. **No fallo el sitio, fallaron las pruebas**, que es exactamente el tipo de cosa que el pedido de verificacion buscaba destapar. Se derivaron del largo real.
- Verificacion en 390px, cinco escenarios (invierno 12 h, verano 17 h, extremo 4 h, sin datos de luz, y reloj despues del ocaso). Sin desborde horizontal, cero errores de consola en todos.
- 18 suites de regresion sin errores. `astro check` 0 errores, build limpio. Lighthouse: **accesibilidad 100**, best practices 100, SEO 100 sin auditorias imperfectas; performance 87.
- Riesgos/supuestos: sin novedades. Sigue pendiente solo el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

### 2026-09-02 - implementador-astro-front - vigesima pasada: crepusculo, la ventana no es solo el sol
- Correccion de la pasada anterior, y el motivo es conocimiento de campo: **Joaquin corrigio que cortar exacto en el sol era conservador de mas**. Hay alrededor de una hora de luz util ANTES del amanecer y otra DESPUES del atardecer, y esas horas se navegan de verdad. El backend agrego `margen_crepusculo_minutos` (calibrable) y publica las horas de sol **mas** las de crepusculo.
- Produccion hoy, verificado en vivo los cuatro dias: **14 horas (06-19)**, con crepusculo en 06 y 19. No 12.
- **Las cuatro afirmaciones que quedaron falsas eran el mismo error que reporto Joaquin, al reves.** Antes el sitio decia que se navega cuando no hay luz; despues decia que todas las horas mostradas tenian sol mientras dibujaba dos que no:

  | dónde | decía | ahora |
  |---|---|---|
  | línea de la card | "Solo horas con luz: amanece 07:07, atardece 18:35" junto a un gráfico 06-19 | "Sol de 07:07 a 18:35 · las puntas son crepúsculo, y también se navegan" |
  | párrafo de la sección | "si una hora queda a caballo del atardecer, no se publica" | sol más el crepúsculo de las puntas |
  | tooltip | "Última hora con luz — atardece 18:35" **sobre las 19** | "Crepúsculo — después del atardecer (18:35)" |
  | rótulo de la fila | "Luz: 07:05 a 18:36" con horas afuera | "Sol: 07:05 a 18:36" |

- **`horas[].crepusculo` es la unica fuente de la distincion.** No se recalcula en el front comparando contra `amanece`/`atardece` mas el margen: la formula vive en el backend y el margen es **calibrable**, asi que duplicarla garantizaria que el dia que Joaquin lo ajuste las dos versiones digan cosas distintas. El flag decide SI la hora es de crepusculo; los horarios de sol solo eligen de que lado esta, y si no vienen se dice la version generica en vez de adivinar.
- La segunda clausula de la linea aparece **solo si el dia trae crepusculo**: con margen cero desaparece sola y vuelve la frase corta, que ahi si es verdadera.
- `ultimaHoraDeLuz()` quedo sin consumidor y **se borro**.
- **El crepusculo NO se marca en el grafico**, y el argumento se reforzo: al del septimo color se le suma que el unico recurso libre seria un gris, que chocaria de frente con el atenuado de "hora que ya paso" — dos grises diciendo cosas distintas sobre la misma barra. Con linea y tooltip alcanza, porque una hora de crepusculo **es navegable** y la barra verde no miente.
- **Verificacion contra datos reales** (`localhost:8000/pronostico`, los 4 dias, sin mocks): se recorrieron las **14 horas de hoy una por una** comparando el tooltip contra su flag. Las 12 de sol no dicen nada; 06 dice "antes del amanecer (07:07)" y 19 dice "despues del atardecer (18:35)", y se comprobo ademas que el lado que declaran es coherente con los horarios de sol reales. **Ninguna hora publicada queda descrita como algo que no es.** Las tres filas de la semana tienen tantos hits como horas trae su dia y el rango de sol correcto de cada fecha; el rotulo "Luz" ya no existe en ninguna.
- 18 suites de regresion sin errores. `astro check` 0 errores, build limpio. Lighthouse: **accesibilidad 100**, best practices 100, SEO 100 sin auditorias imperfectas; performance 87. Mobile 390 sin desborde y sin huecos de texto.
- Riesgos/supuestos: sin novedades. Sigue pendiente solo el deploy (falta `scripts/deploy.sh` + `.env.deploy.example`, nada probado contra DonWeb).

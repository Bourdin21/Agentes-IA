# Memoria - Implementador

## Proyecto: marihogar
## Ultima actualizacion: 2026-07-24 (Sprint 6 — cierre de Etapa 1)

## Definiciones vigentes

### Sprint 1 (de 7) — M10 rol Vendedor + M2 Catalogo de productos + M3 Control de stock

Alcance cerrado segun `2-disenador-funcional.md` y `3-arquitecto-mvc.md`, sin desvio de alcance respecto de lo aprobado. NO se toco ningun modulo fuera de M10/M2/M3 (Presupuestos, Ventas, Entregas, AFIP, CC local/proveedores, Compras, Cheques, Gastos, Caja, Proyeccion, Aumento masivo, Dashboard, CRM/Bot quedan para sprints siguientes).

### Escaneo de reutilizacion (previo a implementar)

- `docs/ShowroomGriffin/definiciones/5-implementador.md` — patron de ajuste manual de stock (`StockController.Ajuste`, `AjusteStockViewModel`, redirect post-ajuste a la misma pantalla) y `StockService` con `AjusteManualAsync`/`RegistrarMovimientoAsync`. Reutilizado el patron de UX (permanecer en `Stock/Ajuste` tras guardar) y la idea de registrar el ledger en la misma transaccion que el StockActual.
- `docs/ganaderia/definiciones/5-implementador.md` (repo real `C:/Sistemas/ganaderia - emo`) — `IStockService`/`StockService`/`MovimientoStock` como ledger inmutable (`Cantidad` con signo, sin `SoftDestroyable`), validacion de stock no negativo antes de persistir. Reutilizado el diseño de ledger con signo y la regla de "unico punto de escritura" (aca formalizada como `IStockService.AjustarAsync` en Infrastructure).
- Decision: **no se copio codigo textual** de ninguno de los dos repos (los dominios difieren: ShowroomGriffin trabaja sobre variantes de producto con talle/color, ganaderia sobre `Grupo` de hacienda) — se adapto el patron de diseño (ledger + single-writer + confirmacion SweetAlert2 de stock negativo) al modelo de `Producto.StockActual` desnormalizado definido en `3-arquitecto-mvc.md`.

### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/TipoMovimientoStock.cs` — nuevo enum `Compra=1/Venta=2/Ajuste=3` (Compra/Venta previstos para M5/M12, este sprint solo genera `Ajuste`).
- `Entities/Categoria.cs`, `Entities/Marca.cs` — nuevas, heredan `SoftDestroyable`, solo `Nombre` (sin flag `Activo` redundante: el soft delete ya cubre esa semantica, consistente con el resto del template).
- `Entities/Producto.cs` — nueva, hereda `SoftDestroyable`. Incluye `StockActual` desnormalizado con comentario explicito de que el unico escritor es `IStockService`.
- `Entities/ProductoFoto.cs` — nueva, hereda `SoftDestroyable`. `Path`, `Orden`, `EsPortada`.
- `Entities/MovimientoStock.cs` — nueva, **no hereda `SoftDestroyable`** (ledger inmutable, tal como exige la arquitectura).

**Application** (`MariHogar.Application`)
- `DTOs/CommonDtos.cs` — `SelectItemDto` (Id/Text para combos).
- `DTOs/CategoriaDtos.cs`, `DTOs/MarcaDtos.cs` — DTOs simples de catalogo.
- `DTOs/ProductoDtos.cs` — `ProductoListItemDto` (con `PrecioCompra` `decimal?` + `[JsonIgnore(Condition = WhenWritingNull)]` para que el campo se omita del JSON cuando el Service lo deja en null por rol), `ProductoFiltro`, `ProductoDetailDto`, `ProductoFotoDto`, `ProductoInput` (con `ConfirmarMargenNegativo`).
- `DTOs/StockDtos.cs` — `MovimientoStockListItemDto` (`Tipo` como `string`, no como el enum crudo — ver "Ajustes durante smoke test"), `MovimientoStockFiltro`, `AjusteStockInput` (con `ConfirmarNegativo`).
- `Interfaces/ICategoriaService.cs`, `Interfaces/IMarcaService.cs`, `Interfaces/IProductoService.cs`, `Interfaces/IStockService.cs`, `Interfaces/IFileStorageService.cs` — nuevas.

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — `DbSet` de las 5 entidades nuevas + Fluent config (precision decimal 18,2, longitudes de string, FKs `Restrict`, indices en `CategoriaId`/`MarcaId`/`ProductoId`/`Fecha`/`Nombre`). `MovimientoStock` configurado sin heredar el query filter global de soft delete.
- `Services/CategoriaService.cs`, `Services/MarcaService.cs` — CRUD + listado DataTables + combo. Bloquean el delete si hay productos activos asociados (evita referencias colgantes por el query filter global de soft delete).
- `Services/ProductoService.cs` — CRUD, listado con filtro por rol (`esAdministrador` determina si `PrecioCompra` viaja en el DTO), gestion de fotos (alta con limite de 5, portada, reordenar, softdelete con borrado fisico via `IFileStorageService`), `ContarConStockBajoMinimoAsync` (dato dejado disponible para el dashboard de M9, sin armar la pantalla).
- `Services/StockService.cs` — unico escritor de `Producto.StockActual` + `MovimientoStock`. `AjustarAsync` corre en transaccion, rechaza resultado negativo salvo `ConfirmarNegativo=true`. `ListarMovimientosAsync` con filtros producto/tipo/fecha.
- `Services/LocalFileStorageService.cs` — nuevo, `wwwroot/uploads/{subFolder}` con nombre unico (`Guid`), usado para fotos de producto.
- `Data/SeedData.cs` — agregado `RolVendedor = "Vendedor"` al array de roles seedeados.
- `DependencyInjection.cs` — registrados `ICategoriaService`, `IMarcaService`, `IProductoService`, `IStockService`, `IFileStorageService` (todos Scoped).

**Web** (`MariHogar.Web`)
- `Program.cs` — nueva policy `RequireVentas` (`SuperUsuario` + `Administrador` + `Vendedor`), junto a `RequireAdministracion` ya existente.
- `Controllers/UsersController.cs` — `GetAssignableRoles()` ahora incluye `Vendedor` ademas de `Administrador` (si no, el rol nuevo quedaba seedeado pero inasignable desde la UI — ajuste minimo de una linea, necesario para que M10 sea funcionalmente completo).
- `Controllers/CategoriasController.cs`, `Controllers/MarcasController.cs` — CRUD, `[Authorize(Policy = "RequireAdministracion")]` a nivel clase.
- `Controllers/ProductosController.cs` — `[Authorize(Policy = "RequireVentas")]` a nivel clase (Index/GetData compartidos), overrides `[Authorize(Policy = "RequireAdministracion")]` en Create/Edit/Delete/SubirFoto/EliminarFoto/MarcarPortada/ReordenarFotos. Filtros persistidos en sesion bajo `Filtros:Productos:Index`.
- `Controllers/StockController.cs` — `[Authorize(Policy = "RequireAdministracion")]` a nivel clase (listado de movimientos y ajuste manual son exclusivos de Administrador segun el sprint). Filtros persistidos en sesion bajo `Filtros:Stock:Index`. Accion `GetStockActual` (AJAX) para que la vista de Ajuste calcule el resultado prospectivo antes de confirmar.
- `Models/CategoriaViewModels.cs`, `Models/MarcaViewModels.cs`, `Models/ProductoViewModels.cs`, `Models/StockViewModels.cs` — nuevos.
- `Helpers/SessionExtensions.cs` — `SetObject`/`GetObject<T>` sobre `ISession` (JSON), implementa el patron transversal de filtros persistidos en sesion (reutilizable por los sprints siguientes: Leads, Presupuestos, Ventas, Entregas, etc.).
- `Helpers/DataTableRequestHelper.cs` — parseo estandar de `Request.Form` de DataTables hacia `DataTableRequest` (reutilizable, evita duplicar el parseo manual que ya existia inline en `UsersController`).
- `Helpers/FormParsing.cs` — parseo tolerante de filtros sueltos (`int?`/`decimal?`/`DateTime?`) desde `Request.Form`.
- `Views/Categorias/{Index,Create,Edit}.cshtml`, `Views/Marcas/{Index,Create,Edit}.cshtml` — CRUD simple con DataTables + filtro de texto libre (Categoria/Marca NO estan en la lista de listados con filtro persistido en sesion del diseño — son catalogos simples).
- `Views/Productos/{Index,Create,Edit}.cshtml` — Index con 6 filtros de columna (nombre, marca, modelo, categoria, stock rango, precio venta rango) persistidos en sesion, badge rojo "Bajo minimo", columna/boton "Precio compra"/"Nuevo"/"Eliminar" solo si `EsAdministrador`. Create/Edit con cards "Datos generales" / "Precios y stock" (+ "Fotos" en Edit), Select2 en Marca/Categoria inicializado con el valor asignado en Edit, confirmacion SweetAlert2 client-side + guard server-side para margen negativo.
- `Views/Stock/Index.cshtml`, `Views/Stock/Ajuste.cshtml` — listado con filtro producto/tipo/rango de fecha (daterangepicker) persistido en sesion; Ajuste con confirmacion SweetAlert2 client-side (consulta `GetStockActual` para el calculo prospectivo) + guard server-side de stock negativo.
- `Views/Shared/_Layout.cshtml` — sidebar: seccion "Catalogo" nueva con "Productos" (visible a SuperUsuario/Administrador/Vendedor) y "Categorias"/"Marcas"/"Stock" (visible solo a SuperUsuario/Administrador).

### Migraciones EF generadas

- `20260724152806_AddCatalogo` — crea `Categorias`, `Marcas`, `Productos`, `ProductoFotos`, `MovimientosStock` con FKs `Restrict` e indices (`CategoriaId`, `MarcaId`, `ProductoId`, `Fecha`, `Nombre` x3). **Aplicada exitosamente** contra `marihogar_dev` (MySQL local disponible en este entorno, `dotnet ef database update` confirmado sin pendientes via `dotnet ef migrations list`).
- Rol `Vendedor`: no requiere migracion de esquema (se crea via `SeedData.InitializeAsync` al arrancar la app, igual que `SuperUsuario`/`Administrador`) — confirmado en el log de arranque: `Rol 'Vendedor' creado.`.

### Ajustes durante smoke test (bugs encontrados y corregidos en la misma sesion, antes de cerrar)

1. **`StockService.ListarMovimientosAsync` — 500 en MySQL**: `_db.Users.Where(u => usuarioIds.Contains(u.Id))` con `usuarioIds` como coleccion local generaba `InvalidOperationException: Expression '@usuarioIds' in the SQL tree does not have a type mapping assigned` (MySQL/EF Core 10 no soporta type mapping de IN generado desde coleccion local — mismo patron ya documentado como comentario en `UsersController.GetData`, que no habia leido con suficiente atencion la primera vez). Fix: traer usuarios a memoria (`ToListAsync()`) y filtrar en-proceso con `HashSet<string>`, igual que el patron ya usado en `UsersController`. **Este patron debe agregarse a `32-estandares-qa-implementador.instructions.md`** como regla generalizable (no solo el caso de Users) — pendiente de que QA/orquestador lo catalogue formalmente.
2. **`MovimientoStockListItemDto.Tipo` serializaba como int crudo**: el enum `TipoMovimientoStock` se serializaba por `System.Text.Json` como numero (`3`) en vez del nombre (`"Ajuste"`), rompiendo el badge de color en el JS del listado (`clases[d]` no matcheaba). Fix: `Tipo` cambiado a `string` en el DTO, mapeado con `.ToString()` en el Service (mismo criterio que `UsersController` ya usa para `Estado`).

Ambos bugs se detectaron corriendo la app real contra MySQL (no solo `dotnet build`), confirmando la importancia del smoke test funcional ademas de la compilacion.

### Evidencia de build y pruebas funcionales minimas

- `dotnet build MariHogar.slnx` → **Compilacion correcta, 0 errores** (9 warnings preexistentes del template: vulnerabilidad NU1902 de MailKit/MimeKit y `HomeController.StatusCode` — ninguno introducido por este sprint).
- Migracion generada y **aplicada** contra `marihogar_dev` (MySQL local).
- Smoke test funcional real (app corriendo, login real, cookies, MySQL real) ejecutado sobre:
  - Seed: roles `SuperUsuario`/`Administrador`/`Vendedor` creados correctamente al arrancar.
  - `Categorias`: crear + listar via DataTables — OK.
  - `Marcas`: crear + listar via DataTables — OK.
  - `Productos`: crear (con `MarcaId`/`CategoriaId` existentes) + listar via DataTables, `StockActual=0` y badge `stockBajoMinimo=true` correctos al nacer con stock 0 y minimo 5 — OK.
  - `Stock/Ajuste`: intento de ajuste que deja stock negativo **sin** `ConfirmarNegativo` → rechazado con mensaje ("El ajuste dejaria el stock en -10 (negativo)..."), `StockActual` sin cambios — OK. Mismo ajuste **con** `ConfirmarNegativo=true` → aplicado, `StockActual=-10`, `MovimientoStock` registrado con `UsuarioId`/`UsuarioNombre` resuelto correctamente — OK.
  - Permisos: usuario con rol `Vendedor` creado via `Users/Create` (verificado que `Vendedor` ya es asignable desde la UI). Login como Vendedor: `GET /Productos` → 200; `GET /Categorias`, `/Marcas`, `/Stock`, `/Productos/Create` → 302 a `/Account/AccessDenied` — OK. `GetData` de Productos como Vendedor **no incluye la clave `precioCompra` en el JSON** (confirmado byte a byte, no solo valor null) — OK, cumple la regla de arquitectura de no exponer el campo a nivel Service/mapping.

### Riesgos residuales

- Warning de EF Core en arranque: *"Entity 'Producto' has a global query filter... required end of a relationship with 'MovimientoStock'"* — esperado y aceptado por diseño (el ledger es intencionalmente no-softdeletable mientras referencia a un `Producto` que si lo es); no bloquea nada, documentado por si aparece de nuevo en QA.
- No se implemento reordenamiento por drag-and-drop de fotos (`IProductoService.ReordenarFotosAsync`/`ProductosController.ReordenarFotos` existen pero no tienen UI conectada todavia) — la "portada reordenable" pedida en HU-2.2 se resolvio con el boton "Hacer portada" (cualquier foto puede pasar a ser portada), que cubre el criterio de aceptacion tal como esta redactado. Drag-and-drop del orden completo queda como mejora opcional, no bloqueante.
- No se agrego vista "Details" separada para Producto — Edit ya expone todos los datos + fotos + stock actual; se prioritizo no duplicar pantallas dado que el wireframe generico no la exige de forma explicita para Producto. Si QA/cliente la pide, es un agregado menor (misma capa, sin migracion).
- `ContarConStockBajoMinimoAsync()` (Application/Infrastructure) esta implementado y probado por codigo pero **sin consumidor todavia** — el dashboard real es de M9 (sprint futuro), tal como pide el alcance de este sprint ("dejar el dato/consulta disponible, no armar la pantalla").
- Regla de reutilizacion cross-proyecto pendiente de catalogar formalmente (ver bug #1 de arriba): patron "no usar `.Contains()` de coleccion local contra MySQL/EF Core 10" deberia agregarse a `32-estandares-qa-implementador.instructions.md` para que el proximo sprint (o el proximo proyecto) no repita el mismo bug — la unica referencia hoy es un comentario suelto en `UsersController.cs` del propio marihogar.

### Proximos pasos pendientes (Sprint 1)

- Sprint 2 (segun plan del cliente): Presupuestos + Ventas + CC local, con foco reforzado de UX/UI en Ventas (ya documentado en `2-disenador-funcional.md`, ver entrada de trazabilidad 2026-07-24 "ajuste de diseño").
- `IStockService`/`MovimientoStock` quedan listos para que `VentaService`/`OrdenCompraService` (sprints futuros) generen movimientos `Venta`/`Compra` reutilizando el mismo `IStockService.AjustarAsync`-like pattern (a definir la firma exacta cuando se implemente M5/M12, hoy `AjustarAsync` esta pensado especificamente para ajuste manual con motivo).

---

### Sprint 2 (de 7) — M4 Presupuestos y cotizaciones + M5 Gestion de ventas (⭐ maxima prioridad de esfuerzo) + M11 CC Local

Alcance cerrado segun `2-disenador-funcional.md` (incluyendo la seccion "Especificacion UX elevada — Pantalla de Ventas") y `3-arquitecto-mvc.md`, sin desvio de alcance respecto de lo aprobado. NO se toco ningun modulo fuera de M4/M5/M11 (Entregas, AFIP, Compras, CC Proveedores, Cheques, Gastos, Caja, Proyeccion, Aumento masivo, Dashboard, CRM/Bot quedan para sprints siguientes). Reintento de un intento anterior cortado por limite de cuenta: los enums `EstadoPresupuesto.cs`/`EstadoVenta.cs` ya existentes se revisaron, resultaron consistentes con el diseño y se reutilizaron tal cual (sin cambios).

**Nota de proceso (correccion):** durante esta sesion se ejecuto un smoke test funcional en caliente completo (app corriendo por HTTPS con certificado de desarrollo, login real via cookies de Identity, MySQL real, recorriendo con `curl` exactamente las mismas llamadas HTTP que dispara el JS de cada vista) que cubrio de punta a punta el flujo pedido: alta de Producto con stock, Presupuesto Borrador→Enviado→Aprobado, conversion a Venta con precarga verificada, confirmacion de Venta con pago combinado (Efectivo+Transferencia), verificacion independiente de la baja de stock y del movimiento de CC Local, cancelacion de la venta con verificacion de la reversion en ambos ledgers, casos de borde de Ventas (sin items, sin pagos, cantidad>stock no bloqueante) y verificacion de permisos por rol. Todos los resultados fueron los esperados (ver seccion de evidencia). **Recien despues de completado ese smoke test**, el orquestador indico que para el resto de la sesion no se debia seguir ejecutando este tipo de verificacion automatizada porque el usuario prefiere validar el flujo el mismo — esa instruccion llego tarde para este sprint (el smoke test ya estaba hecho) pero se documenta igual el pedido: **lo que la verificacion via `curl` NO cubre, porque no usa un navegador real, es el comportamiento visual/JS de la pantalla de Ventas** (renderizado de Select2 con foto/precio/stock, el toast "Deshacer", el layout sticky de dos columnas, la barra inferior fija en mobile, los dialogos SweetAlert2) — esa parte especifica queda pendiente de que el usuario la recorra visualmente (ver checklist mas abajo, pensada para esa verificacion visual, no para "si el flujo funciona" que ya esta confirmado a nivel servidor/API).

#### Escaneo de reutilizacion (previo a implementar)

- Se escaneo `docs/*/definiciones/5-implementador.md` de todos los proyectos del historial buscando un modulo de "presupuestos/cotizaciones", "ventas con multi-pago" o "cuenta corriente" ya implementado. No se encontro un flujo equivalente en ningun proyecto (ShowroomGriffin tiene Ventas pero sin la pantalla POS elevada ni multi-pago con estados Pendiente/Parcial/Pagada; delicias-naturales tiene Ventas+Pagos+Facturas pero en EF6/MySql.Data.EntityFramework, stack incompatible; ganaderia tiene Egresos con pagos multiples, patron parcialmente reutilizado — ver abajo).
- **Reutilizado (patron de diseño, no codigo textual):** `GAN-001` (guard "al menos un item real" en listas dinamicas bindeadas por indice, catalogado en `docs/qa/regresiones-manuales.yml`) — aplicado preventivamente en `PresupuestoService.EnviarAsync` (`Items.Count == 0 || Items.All(i => i.Cantidad <= 0)`) y documentado en `VentaService.ConfirmarAsync` aunque el carrito de Venta viaja por JSON (sin el riesgo estructural de GAN-001, ver comentario en el codigo).
- **Reutilizado (patron de diseño):** el "single-writer con transaccion controlada por el caller" ya usado por `IStockService.AjustarAsync` en Sprint 1 se generalizo a una segunda forma de escritura (`IStockService.RegistrarMovimientoAsync`, sin transaccion propia ni `SaveChanges` propio) para que `VentaService.ConfirmarAsync` pueda orquestar Venta+Items+Pagos+Stock+CC en una unica transaccion de base de datos — exactamente lo que `5-implementador.md` de Sprint 1 dejaba anotado como pendiente ("a definir la firma exacta cuando se implemente M5").
- **Decision:** no se copio codigo textual de ningun repo externo — se disenaron `PresupuestoService`/`VentaService`/`CCLocalService` desde cero sobre los contratos ya definidos en Arquitectura, reutilizando exclusivamente los patrones de diseño y las convenciones de estilo ya establecidas en el propio Sprint 1 de marihogar (DTOs, `ServiceResult`, `DataTableRequest`/`Response`, `SessionExtensions`, `DataTableRequestHelper`, `FormParsing`).

#### Decisiones de implementacion no explicitas en el diseño (documentadas para trazabilidad)

1. **`Presupuesto.ClienteNombre`/`ClienteTelefono`**: el diseño asume que todo Presupuesto viene de un Lead (M1, Etapa 2, todavia no implementado). Como `LeadId` siempre es null este sprint, se agregaron estos dos campos de texto libre (ClienteNombre obligatorio, Telefono opcional) para que el presupuesto/PDF tengan destinatario. `Venta` replica el mismo patron para ventas cargadas directo (sin presupuesto de origen).
2. **"Convertir a venta" (HU-4.3) NO cambia el estado del Presupuesto al hacer click**: navega a `Ventas/Create?presupuestoId=X` con los items precargados (`IVentaService.ObtenerPrecargaDesdePresupuestoAsync`). La transicion real `Aprobado -> Convertido` (+ `Presupuesto.VentaGeneradaId`) ocurre recien dentro de `VentaService.ConfirmarAsync`, en la MISMA transaccion que crea la Venta — asi coincide exactamente con el wireframe de diseño ("confirmar venta dispara stock + CC en una unica transaccion") y evita dejar un Presupuesto "Convertido" sin una Venta realmente confirmada detras (ej. si el vendedor abandona la pantalla de Venta sin confirmar). Documentado explicitamente en el XML-doc de `IPresupuestoService`.
3. **Precio de Venta autoritativo en servidor**: `VentaService.ConfirmarAsync` ignora el `PrecioUnitario` que llega del cliente y siempre usa `Producto.PrecioVenta` vigente al momento de confirmar (evita manipulacion del payload y price drift). `PresupuestoService` en cambio SI respeta el precio editado por el vendedor (el diseño explicita "precio editable" solo para Presupuesto, no para Venta).
4. **Guard de cancelacion de Venta (HU-5.4)** ya consulta `TieneEntregaAsociadaAsync`/`TieneComprobanteAsociadoAsync` (metodos dedicados que hoy siempre devuelven `false` porque M6/M7 no existen) en vez de una constante booleana inline — para que activar el guard real en sprints futuros sea un cambio de una linea en `VentaService`, no una reescritura.
5. **Filtro "Estado=Expirado" de Presupuestos** se resuelve 100% en SQL (comparacion `FechaEnvio.AddDays(VigenciaDias) < UtcNow` traducida por EF Core), nunca calculado en memoria sobre la pagina completa — necesario para que el filtro de listado funcione con paginacion server-side real.
6. **Riesgo MH-001 aplicado preventivamente en 3 lugares nuevos** (no solo donde ya se habia reproducido en Sprint 1): `VentaService.ListarAsync` NO incluye la coleccion `Pagos` en la query paginada (se resuelve con consultas individuales acotadas al tamaño de pagina, ver comentario en el codigo) y ni `VentaService.ConfirmarAsync` ni `PresupuestoService.ValidarProductosAsync` usan `.Where(x => coleccionLocal.Contains(x.Id))` — se reemplazaron por fetch individual por Id (bounded por el tamaño del carrito, nunca por el total de la tabla Productos).

#### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/EstadoPresupuesto.cs`, `Enums/EstadoVenta.cs` — ya existian de un intento anterior cortado, revisados y reutilizados sin cambios (consistentes con la maquina de estados del diseño).
- `Enums/MetodoPago.cs` — nuevo, compartido Venta/OC (Efectivo/Transferencia/MercadoPago/Cheque/Deposito); este sprint la UI de Ventas solo expone los primeros 3 (filtrado en `VentaService`, no en el enum).
- `Enums/TipoMovimientoCC.cs` — nuevo (Ingreso/Egreso).
- `Entities/Presupuesto.cs`, `Entities/PresupuestoItem.cs` — nuevas, heredan `SoftDestroyable`. `PresupuestoItem` se reemplaza completo (delete+insert) en cada `UpdateAsync`, patron simple ya que no hay edicion linea-por-linea via AJAX en este modulo.
- `Entities/Venta.cs`, `Entities/VentaItem.cs`, `Entities/PagoVenta.cs` — nuevas, heredan `SoftDestroyable`. `VentaItem.CantidadFacturada` queda en 0 (preparado para M7).
- `Entities/MovimientoCCLocal.cs` — nueva, **no hereda `SoftDestroyable`** (ledger inmutable, igual criterio que `MovimientoStock` de Sprint 1).

**Application** (`MariHogar.Application`)
- `DTOs/CommonDtos.cs` — agregado `SelectItemStringDto` (combos cuya clave es el Id string de `ApplicationUser`).
- `DTOs/ProductoDtos.cs` — agregado `ProductoBusquedaDto` (buscador instantaneo compartido por Presupuestos/Ventas).
- `DTOs/PresupuestoDtos.cs`, `DTOs/VentaDtos.cs`, `DTOs/CCLocalDtos.cs` — nuevos, DTOs de listado/detalle/input por modulo.
- `Interfaces/IProductoService.cs` — agregado `BuscarAsync(term, take)`.
- `Interfaces/IStockService.cs` — agregado `RegistrarMovimientoAsync` (sin transaccion propia, para uso dentro de la transaccion unica de `VentaService`).
- `Interfaces/IPresupuestoService.cs`, `Interfaces/IVentaService.cs`, `Interfaces/ICCLocalService.cs` — nuevas.

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — `DbSet` de las 6 entidades nuevas + Fluent config (precision decimal 18,2, longitudes de string, FKs `Restrict`/`Cascade` segun corresponda, indices en `Estado`/`VendedorId`/`Fecha`/`OrigenTipo+OrigenId`). `MovimientoCCLocal` configurado sin el query filter global de soft delete (igual criterio que `MovimientoStock`).
- `Services/StockService.cs` — agregado `RegistrarMovimientoAsync` (unico escritor de `Producto.StockActual` tambien para movimientos de Venta, ademas del ajuste manual ya existente).
- `Services/ProductoService.cs` — agregado `BuscarAsync` (buscador instantaneo con foto de portada, usado por el Select2 remoto de Presupuestos/Ventas).
- `Services/PresupuestoService.cs` — nuevo. CRUD de Borrador, maquina de estados (Enviar/Aprobar/Rechazar), calculo de "estado efectivo" (Expirado sin job, calculado al leer/filtrar), generacion de PDF con QuestPDF (reutiliza el paquete ya referenciado desde `ExportService` de Sprint 1, sin agregar dependencias nuevas).
- `Services/VentaService.cs` — nuevo. `ConfirmarAsync` es el metodo critico: crea Venta+Items+Pagos, descuenta stock (`IStockService.RegistrarMovimientoAsync`) y registra el ingreso en CC Local (`ICCLocalService.RegistrarMovimientoAsync`) en una unica transaccion (`BeginTransactionAsync`/2x `SaveChangesAsync`/`CommitAsync`). `CancelarAsync` revierte con contramovimientos (nunca borra el original). `ListarAsync` evita el patron DN-001/MH-001 (ver decision #6 arriba).
- `Services/CCLocalService.cs` — nuevo. Unico escritor de `MovimientoCCLocal`, con `RegistrarMovimientoAsync` sin transaccion/SaveChanges propios (mismo patron que `IStockService`).
- `DependencyInjection.cs` — registrados `IPresupuestoService`, `IVentaService`, `ICCLocalService` (Scoped).

**Web** (`MariHogar.Web`)
- `Controllers/ProductosController.cs` — agregada accion `Buscar(string? term)` (GET, JSON, bajo la misma policy `RequireVentas` de la clase) para el autocomplete compartido.
- `Controllers/PresupuestosController.cs` — nuevo. `[Authorize(Policy = "RequireVentas")]` a nivel clase (Admin+Vendedor crean/gestionan presupuestos por igual, incluyendo Aprobar/Rechazar — el diseño no restringe esa transicion solo a Administrador, ya que representa el registro de una decision del cliente, no una decision interna). Filtros persistidos en sesion bajo `Filtros:Presupuestos:Index`.
- `Controllers/VentasController.cs` — nuevo. `[Authorize(Policy = "RequireVentas")]` a nivel clase. La accion `Confirmar` recibe el carrito y los pagos como dos campos JSON dentro de un POST `application/x-www-form-urlencoded` estandar (no `[FromBody]` JSON) para reusar sin ambiguedad el antiforgery token ya emitido por `_Layout.cshtml` (header + campo `__RequestVerificationToken` en el body, mismo mecanismo que el resto de los AJAX POST del proyecto) — se evito deliberadamente `[FromBody]` porque este stack no tiene configurado `AntiforgeryOptions.HeaderName`, y no correspondia asumir que funcionaria sin poder validarlo en un navegador real. Filtros persistidos en sesion bajo `Filtros:Ventas:Index`.
- `Controllers/CCLocalController.cs` — nuevo. `[Authorize(Policy = "RequireAdministracion")]` a nivel clase (exclusivo Administrador, ver tabla de permisos). Filtros persistidos en sesion bajo `Filtros:CCLocal:Index`.
- `Models/PresupuestoViewModels.cs`, `Models/VentaViewModels.cs`, `Models/CCLocalViewModels.cs` — nuevos.
- `Views/Presupuestos/{Index,Create,Edit,Details}.cshtml` — Index con 4 filtros (cliente, vendedor, estado incl. Expirado, rango de fecha) persistidos en sesion; Create/Edit con buscador Select2 (foto+precio+stock) + tabla de items editable (cantidad/precio) armada 100% en JS, recalculo de subtotal/total sin re-render completo; Details con badge de estado, acciones segun `EstadoReal` (Editar/Enviar en Borrador; Aprobar/Rechazar en Enviado con SweetAlert2 via el handler generico `.btn-swal-confirm` de `site.js`; Convertir a venta en Aprobado) y boton "Descargar PDF" siempre visible.
- `Views/Ventas/{Index,Create,Details}.cshtml` — **Create es la pantalla de mayor inversion UX/UI del sprint** (ver seccion dedicada abajo). Index con 5 filtros (cliente, vendedor, estado, forma de pago, rango de fecha) persistidos en sesion. Details con items/pagos de solo lectura + boton "Cancelar venta" (SweetAlert2 con motivo obligatorio) cuando la venta no esta ya Cancelada.
- `Views/CCLocal/Index.cshtml` — saldo actual destacado en el header + listado con 2 filtros (tipo, rango de fecha) persistidos en sesion, badge de reversion cuando `EsReversion=true`.
- `Views/Shared/_Layout.cshtml` — sidebar: nueva seccion "Ventas" (Ventas + Presupuestos, visible a SuperUsuario/Administrador/Vendedor) arriba de "Catalogo" (refleja la prioridad de uso diario) y nueva seccion "Financiero" (Cuenta corriente, visible solo a SuperUsuario/Administrador).

#### Especificacion UX elevada — Pantalla de Ventas (`Views/Ventas/Create.cshtml`)

Implementada punto por punto contra la seccion dedicada de `2-disenador-funcional.md` (verificado por relectura de codigo, no por navegador):
- **Layout de dos columnas** (desktop/tablet, `col-lg-8`/`col-lg-4`) con la columna derecha `position: sticky` mientras la izquierda scrollea; **una columna en mobile** con barra inferior fija (`ov-venta-bottombar`, `d-lg-none`) mostrando Total + boton Confirmar siempre visible sin scrollear — reutilizable tal cual por Entregas (M6, Sprint 3) para el cobro en destino, tal como pide el diseño.
- **Buscador instantaneo**: Select2 remoto contra `Productos/Buscar` (debounce 200ms, `templateResult` con foto+precio+stock por resultado), Enter sobre el resultado resaltado agrega la linea (evento nativo `select2:select`, que Select2 dispara tanto por click como por Enter), foco vuelve automaticamente al buscador (`select2('open')` con `setTimeout` de 60ms tras limpiar la seleccion).
- **Recalculo sin perder foco (REG-008)**: cambiar la cantidad de una linea actualiza solo esa celda de subtotal + el resumen global (`actualizarResumen()`), nunca reconstruye el `tbody` completo; los inputs de monto de pago actualizan el array en memoria + el indicador en cada tecla sin re-renderizar las filas de pago.
- **Steppers +/- y quitar con "Deshacer"**: cada fila tiene botones -/+ ademas del input numerico; "Quitar" dispara un toast Bootstrap 5 (`#toastDeshacerVenta`) con boton "Deshacer" que reinserta la linea en su posicion original si se hace click dentro de los 6s (HU-5.7, no bloqueante como pide el diseño).
- **Pago rapido**: botones "Todo efectivo"/"Todo transferencia" reemplazan las filas de pago por una unica fila con el metodo elegido y `Monto = Total`; filas de pago combinables manualmente (agregar/quitar); indicador en vivo "Falta cobrar $X" / "Vuelto $X" / "Pago completo" recalculado en cada tecla.
- **Advertencia de stock no bloqueante (HU-5.8)**: badge amarillo en la fila cuando `cantidad > stockActual`, nunca bloquea el boton de confirmar.
- **Boton "Confirmar venta"** deshabilitado hasta que haya al menos 1 item Y al menos 1 pago con monto real (`> 0`) — no exige que la suma de pagos iguale el total (Pendiente/Parcial son estados validos segun la maquina de estados), decision documentada en el codigo (`actualizarResumen()`).
- **Pantalla de exito**: reemplaza el contenido (`#panelArmadoVenta` -> `d-none`, `#panelExitoVenta` visible), sin modal, con los 4 botones de HU-5.9 — "Nueva venta" (recarga `Ventas/Create`) y "Ver venta" (link a `Ventas/Details/{id}`) funcionales; "Programar entrega" y "Facturar" deshabilitados con tooltip "Disponible en un proximo sprint" (M6/M7 no existen todavia).
- **Loading state unico en el submit final**: el boton de confirmar muestra spinner + se deshabilita durante la llamada AJAX; ninguna otra interaccion de armado de la venta dispara un loading state de pagina completa.

#### Migraciones EF generadas

- `20260724175804_AddPresupuestosVentas` — **combinada** (incluye tambien `MovimientosCCLocal`, no solo Presupuestos/Ventas): crea `Presupuestos`, `PresupuestoItems`, `Ventas`, `VentaItems`, `PagosVenta`, `MovimientosCCLocal` con FKs (`Restrict` para Producto/Vendedor/PresupuestoOrigen, `Cascade` para Items/Pagos hijos de su padre) e indices (`Estado`, `VendedorId`, `Fecha`, `OrigenTipo+OrigenId`, etc.). **Se combino en una sola migracion en vez de `AddPresupuestosVentas` + `AddCCLocal` separadas** porque `MovimientoCCLocal` se agrego al `AppDbContext` en el mismo cambio de modelo que Presupuesto/Venta (es consumido exclusivamente por `VentaService` este sprint, dentro de la misma transaccion) — `dotnet ef migrations add` scaffolda todos los cambios de modelo pendientes juntos, y separarla artificialmente en dos migraciones hubiera requerido comentar/descomentar DbSets sin ningun beneficio real; decision permitida explicitamente por el alcance ("o una combinada si tiene sentido tecnico, documentalo"). **Aplicada** exitosamente contra `marihogar_dev` (confirmado con `dotnet ef migrations list`, sin migraciones `(Pending)`).

#### Evidencia de cierre — build + migracion + smoke test funcional real (server-side) + revision de codigo

- `dotnet build MariHogar.slnx` → **Compilacion correcta, 0 errores** (8 warnings, todos preexistentes NU1902 de MailKit/MimeKit; el warning `CS0162` de codigo inalcanzable detectado durante el desarrollo se corrigio antes del build final reemplazando los guards `const bool` por metodos placeholder `TieneEntregaAsociadaAsync`/`TieneComprobanteAsociadoAsync`).
- Migracion `20260724175804_AddPresupuestosVentas` generada y **aplicada** contra `marihogar_dev` (`dotnet ef migrations list` confirma las 3 migraciones aplicadas, ninguna `(Pending)`).
- **Revision de codigo propia** (releida linea por linea, no solo "compila"): recorrido completo de la transaccion unica de `VentaService.ConfirmarAsync` (orden de operaciones: crea Venta+Items+Pagos → `SaveChangesAsync` para obtener `Venta.Id` → descuenta stock por item via `IStockService.RegistrarMovimientoAsync` → registra ingreso en CC Local via `ICCLocalService.RegistrarMovimientoAsync` → actualiza el Presupuesto de origen si corresponde → segundo `SaveChangesAsync` → `CommitAsync`, con `RollbackAsync` en el `catch`); de `VentaService.CancelarAsync` (mismo patron con signos invertidos y `EsReversion=true`); de los guards de "al menos un item/pago real" (GAN-001); y de las 3 aplicaciones preventivas del patron MH-001 (ver decision #6 arriba).
- **Smoke test funcional real completo, server-side, via `curl`** (app corriendo por HTTPS con certificado de desarrollo, login real via cookies de Identity, MySQL real — recorriendo exactamente las mismas llamadas HTTP que dispara el JS de cada vista, no solo endpoints sueltos):
  - Producto de prueba creado (`Mesa Ratona Roble`, `PrecioVenta=28000`) + ajuste de stock a 20 unidades.
  - **Presupuesto**: creado con 1 item (2 x $28.000 = $56.000, Borrador) → Enviado → Aprobado — cada transicion verificada leyendo el badge de estado real en `Details` — OK.
  - **Convertir a venta**: `GET Presupuestos/ConvertirAVenta/1` → redirige a `Ventas/Create?presupuestoId=1` → confirmado que el carrito JS se hidrata con `productoId=2, cantidad=2, precioUnitario=28000` y el cliente "Maria Gonzalez" precargado — OK.
  - **Confirmar venta con pago combinado** (Efectivo $30.000 + Transferencia $26.000 = $56.000 exacto): `POST Ventas/Confirmar` → `{success:true, ventaId:1, total:56000, estado:"Pagada"}` — OK.
  - **Efectos de la confirmacion verificados independientemente**: `Producto.StockActual` bajo de 20 a 18 — OK; `Presupuesto` paso a `Convertido` con link a la Venta generada — OK; `MovimientoCCLocal` de Ingreso por $56.000 creado, saldo de CC Local = $56.000 — OK.
  - **Cancelacion de venta** (motivo obligatorio): `Producto.StockActual` volvio a 20 — OK; saldo de CC Local volvio a $0 — OK; ambos ledgers (`MovimientoStock` y `MovimientoCCLocal`) muestran DOS movimientos para esa venta (original + reversion con `EsReversion=true`), el original nunca se borro — OK.
  - **Casos de borde de Ventas**: confirmar sin items → rechazado con "Agregue al menos un producto a la venta." — OK; confirmar con items pero sin pagos → rechazado con "Cargue al menos una forma de pago." — OK; confirmar con cantidad (25) mayor al stock disponible (20) → **permitido** (no bloqueante, HU-5.8), `StockActual` quedo en -5 — OK.
  - **Permisos**: usuario con rol `Vendedor` creado vía `Users/Create` → `GET /Ventas` y `GET /Presupuestos` → 200; `GET /CCLocal` → 302 a `/Account/AccessDenied` — OK. Vendedor pudo confirmar una venta directa (sin presupuesto de origen) — OK. `Ventas/GetData` como Vendedor devuelve las 3 ventas totales (propias y de Super Usuario), confirmando el supuesto "Vendedor ve todas las ventas" — OK.
  - **PDF de Presupuesto**: `GET Presupuestos/DescargarPdf/1` → `200`, `Content-Type: application/pdf`, archivo PDF valido de 1 pagina (60KB) — OK.
  - **Listados DataTables**: `Presupuestos/GetData`, `Ventas/GetData`, `CCLocal/GetData` devuelven JSON correctamente estructurado — OK.
  - **Alcance de esta verificacion**: cubre el contrato HTTP/servidor completo (transacciones, guards, calculos, permisos) simulando exactamente lo que el JS de cada vista envia. **No cubre** el comportamiento visual/JS en un navegador real (renderizado de Select2 con foto/precio/stock, el toast "Deshacer", el layout sticky, la barra inferior fija en mobile, los dialogos SweetAlert2) — eso es lo que queda en la checklist de verificacion manual de abajo.

#### Verificacion manual pendiente por el usuario (checklist de confirmacion visual/UX — la logica de servidor ya esta verificada, ver evidencia arriba)

Con la app corriendo (`dotnet run --project MariHogar.Web`) y logueado como Administrador o Vendedor, en un navegador real:

1. **Presupuesto**: `Presupuestos > Nuevo presupuesto` → cargar Cliente + buscar y agregar 1-2 productos (verificar que el buscador Select2 muestra foto/precio/stock y que el total se recalcula solo, sin recargar) → Guardar borrador → en `Details`, click "Enviar al cliente" (SweetAlert2) → click "Marcar Aprobado".
2. **Convertir a venta**: desde el Presupuesto Aprobado, click "Convertir a venta" → verificar que `Ventas/Create` abre con el cliente y los items ya precargados (cantidad editable).
3. **Confirmar venta con pago combinado**: agregar/ajustar cantidades con los steppers +/-, cargar 2 formas de pago (ej. Efectivo + Transferencia) que sumen el total exacto → verificar que el indicador dice "Pago completo" y el boton "Confirmar venta" se habilita → confirmar → verificar que aparece la pantalla de exito (sin recargar la pagina) con los 4 botones.
4. **Quitar linea con "Deshacer"**: quitar un producto del carrito y confirmar que aparece el toast con boton "Deshacer" y que funciona.
5. **Caso de borde — cantidad mayor al stock**: agregar un producto con cantidad mayor al stock disponible → confirmar que aparece el badge de advertencia amarillo en la fila pero el boton sigue habilitado (no bloquea) — la logica de servidor ya esta confirmada, esto valida solo que el badge se ve bien.
6. **Mobile**: reducir el ancho del navegador (o abrir desde el celular) en `Ventas/Create` y confirmar que aparece la barra inferior fija con Total + Confirmar, y que el resto de la pantalla se ve en una sola columna.
7. **PDF**: desde cualquier Presupuesto, click "Descargar PDF" y confirmar visualmente que el archivo se ve bien (cliente, items, total, vigencia).
8. **Permisos visuales**: loguearse con un usuario rol Vendedor y confirmar que el sidebar no muestra "Cuenta corriente".

#### Riesgos residuales

- `Presupuesto.ClienteNombre`/`Venta.ClienteNombre` son campos de texto libre (no vinculados a `Lead`) — cuando se implemente M1 (CRM de Leads, Etapa 2), habra que decidir si se migran/vinculan retroactivamente los presupuestos/ventas historicas a un Lead, o si el campo de texto libre queda como alternativa permanente para ventas de mostrador sin lead de origen. No bloqueante para este sprint.
- El guard de cancelacion de Venta (`TieneEntregaAsociadaAsync`/`TieneComprobanteAsociadoAsync`) esta *hardcodeado a `false`* intencionalmente — cuando se implementen M6/M7, hay que recordar reemplazar esos dos metodos por consultas reales (`_db.Entregas.AnyAsync(...)` / `_db.ComprobantesAfip.AnyAsync(...)`), documentado en el propio codigo con comentarios explicitos.
- No se implemento persistencia del carrito entre sesiones (si el vendedor recarga la pagina a mitad de armar una venta, el carrito se pierde) — no exigido por el diseño, consistente con que el carrito es un estado transitorio hasta confirmar.
- `PresupuestoItem`/`VentaItem`/`PagoVenta` heredan `SoftDestroyable` pero `PresupuestoService.UpdateAsync` usa `RemoveRange` (hard delete fisico) al reemplazar items de un Borrador en edicion, no soft-delete — decision deliberada (los items de un borrador no tienen valor historico propio antes de enviarse) documentada en el codigo; si QA/cliente pide preservar el historial de ediciones de un borrador, es un cambio menor.
- Riesgo bajo (no critico): el comportamiento visual/JS de la pantalla de Ventas (Select2, toast, sticky, mobile) no se verifico en un navegador real, solo por lectura de codigo — la logica de servidor que ese JS invoca SI esta verificada en caliente (ver evidencia arriba). Recomendado que el usuario recorra la checklist visual antes de dar el sprint por definitivamente cerrado.

#### Proximos pasos pendientes (Sprint 2)

- Sprint 3 (segun plan del cliente): Entregas (M6) — reutilizara el layout mobile de una columna + barra inferior fija ya implementado en `Ventas/Create.cshtml` para el cobro en destino, y consumira el guard `TieneEntregaAsociadaAsync` de `VentaService.CancelarAsync` (hoy hardcodeado a `false`).
- `IVentaService`/`IPresupuestoService` quedan listos para que Facturacion AFIP (M7, sprint futuro) consuma `VentaItem.CantidadFacturada` y el guard `TieneComprobanteAsociadoAsync`.
- Recomendado (no bloqueante): que el usuario recorra la checklist de verificacion visual/UX de arriba en la primera oportunidad de uso real, dado que esa parte especifica (renderizado JS en navegador) no se pudo verificar por este medio.

---

### Sprint 3 (de 6) — M6 Entregas a domicilio

Alcance cerrado segun `2-disenador-funcional.md` (HU-6.1 a HU-6.3, wireframe "Entrega (mobile)", tabla de maquina de estados de Entrega) y `3-arquitecto-mvc.md` (entidad `Entrega`, migracion `AddEntregas`), sin desvio de alcance respecto de lo aprobado. NO se toco ningun modulo fuera de M6 (Compras, CC Proveedores, Cheques, Gastos, Caja, Proyeccion, Aumento masivo, AFIP, Dashboard, CRM/Bot quedan para sprints siguientes o en espera). El plan de 7 sprints originalmente planteado quedo en 6 desde el ajuste de alcance del 2026-07-24 (pausa de Etapa 2) — este es el Sprint 3 de 6.

**Regla de proceso vigente desde el cierre de Sprint 2** (ver `trazabilidad.md`, entrada "orquestador — eliminacion de smoke test del Implementador"): este sprint se cierra **sin smoke test funcional propio** (no se levanto la app, no se probaron flujos por navegador/API). Evidencia de cierre: build limpio + migracion generada y aplicada + revision de codigo propia linea por linea de la logica critica. Guia de pasos para verificacion manual del usuario al final de esta seccion.

#### Escaneo de reutilizacion (previo a implementar)

- Escaneado `docs/*/definiciones/5-implementador.md` de todos los proyectos del historial (ShowroomGriffin, ganaderia, koi, labipac, delicias-naturales, y el resto via `docs/indice.md`) buscando un modulo de logistica/entregas/reparto a domicilio comparable. **No se encontro ningun modulo equivalente** — los unicos matches de la palabra "entrega" en el historial son falsos positivos sin relacion (ganaderia: "reparto de cuotas" de un plan de pagos financiero, no logistica; ShowroomGriffin: "version entregada" en el sentido de release de software; koi: menciones de RepartoGeneral, un modulo financiero de liquidaciones, no de envios fisicos). Confirma lo ya anticipado en el alcance de este sprint: Entregas a domicilio con direccion/fecha/cobro en destino es especifico del negocio de marihogar, sin precedente reutilizable de otro proyecto del estudio.
- **Reutilizacion interna obligatoria (confirmada por el cliente) — SI aplicada:** el cobro en destino de una Entrega (HU-6.2, `EntregaService.RegistrarCobroAsync`) reutiliza `PagoVenta` **directamente sobre la misma Venta asociada** (agrega filas nuevas a `venta.Pagos`, mismo enum `MetodoPago` restringido a Efectivo/Transferencia/MercadoPago, misma formula de calculo del `EstadoVenta` resultante — suma total de pagos vs Total — que `VentaService.ConfirmarAsync` de Sprint 2) — **no se creo un flujo de pago paralelo**, tal como exigio explicitamente el cliente en el pedido de este sprint.
- **Reutilizacion interna de patrones ya establecidos:** layout mobile de una columna + barra inferior fija de `Ventas/Create.cshtml` (Sprint 2, dejado explicitamente "reutilizable por Entregas" en su propio comentario de codigo) adaptado tal cual en `Entregas/Details.cshtml` (con la diferencia de que aca la barra fija se mantiene en todos los tamaños de pantalla, no solo `d-lg-none`, porque el wireframe de diseño pide "card unica" tanto en mobile como en desktop para esta pantalla especifica — Entregas no tiene el equivalente al layout POS de dos columnas de Ventas); patron de filtros persistidos en sesion (`SessionExtensions`, `DataTableRequestHelper`, `FormParsing`); patron MH-001 (nunca IN sobre coleccion local — aplicado en `EntregaService.GetByIdAsync` para resolver nombres de usuario de los intentos, y en `ListarAsync` para el saldo pendiente por fila); patron GAN-001 (guard "al menos un pago con datos reales", no solo `Count==0`) reaplicado en `RegistrarCobroAsync` igual que en `VentaService.ConfirmarAsync`; patron REG-004 (botones de accion derivados del estado real de la entidad, nunca hardcodeados) en la barra inferior de `Entregas/Details.cshtml`.
- **Decision:** no se copio codigo textual de ningun repo externo — `EntregaService`/`EntregasController` se disenaron desde cero sobre los contratos de Arquitectura, reutilizando exclusivamente los patrones de diseño y las convenciones de estilo ya establecidas en marihogar (Sprints 1 y 2).

#### Decisiones de implementacion no explicitas en el diseño (documentadas para trazabilidad)

1. **Historial de intentos como entidad ledger separada (`EntregaIntento`)**: el diseño pide "historial de intentos visible en el detalle de la entrega (guardar los intentos previos, no sobreescribir)" pero no especifica el modelo de datos. Se opto por una entidad nueva `EntregaIntento` (no hereda `SoftDestroyable`, mismo criterio que `MovimientoStock`/`MovimientoCCLocal`: ledger inmutable de historial, nunca se edita ni se borra) en vez de sobrecargar `Entrega.MotivoNoEntrega` con una lista serializada. Cada transicion `EnCamino -> NoEntregada` agrega una fila nueva (`Fecha`, `FechaProgramadaIntento`, `Motivo`, `UsuarioId`); `Entrega.MotivoNoEntrega` sigue existiendo como resumen del ultimo intento (para mostrar en el badge/alerta sin tener que cargar la coleccion), pero la fuente de verdad del historial completo es `EntregaIntento`.
2. **Maximo 1 Entrega por Venta**: el diseño no lo dice explicitamente, pero se infiere de la maquina de estados (los reintentos de "No entregada" reagendan la misma fila, no crean una entrega nueva) y es necesario para que el guard `VentaService.TieneEntregaAsociadaAsync` (usado para bloquear la cancelacion, HU-5.4) sea inambiguo. `EntregaService.CrearAsync` rechaza con "Esta venta ya tiene una entrega programada" si ya existe una Entrega (en cualquier estado) para esa Venta.
3. **`EstadoVenta` NO se sobrecarga con sub-estados de entrega**: `2-disenador-funcional.md` incluye en su tabla de maquina de estados de Venta (seccion Diseño, no la de Entrega) dos transiciones "Pagada/PagadaParcial -> Con entrega pendiente -> Entregada" que el Arquitecto explicitamente dejo "para M6" sin agregarlas al enum `EstadoVenta` en Sprint 2. Se decidio **no** agregar esos 2 valores nuevos al enum en este sprint: `Venta.Estado` mantiene su semantica pura de estado de pago (Pendiente/PagadaParcial/Pagada/Cancelada, ya usada por CC Local y por los filtros de listado existentes) y el estado logistico se consulta por separado a traves de la `Entrega` asociada (`Venta.Details.cshtml` ahora muestra una card "Entrega" con su propio badge de estado, ademas de la card "Acciones" ya existente). Sobrecargar `Venta.Estado` con el estado de la entrega hubiera hecho perder informacion real (no se podria distinguir si una venta con entrega en camino fue pagada completa o parcialmente) — decision documentada aca por si el cliente pide explicitamente fusionar ambos estados en un sprint futuro.
4. **"Salir a repartir" (Pendiente -> EnCamino) como paso explicito**: el wireframe de "Entrega (mobile)" dibuja solo los botones "Registrar cobro" y "Marcar entregada"/"No entregada", pero la tabla de maquina de estados (fuente mas especifica y autoritativa) exige el paso intermedio "Vendedor sale a repartir". Se agrego un boton "Salir a repartir" en la barra inferior cuando el estado es Pendiente, con un SweetAlert2 de confirmacion liviana (no destructivo, solo evita toques accidentales). El cobro y el cierre (Entregada/No entregada) solo estan disponibles una vez que la entrega esta En camino, consistente con la letra de la tabla.
5. **`MarcarEntregadaAsync` no exige saldo pendiente = 0**: HU-6.2 dice "registrar el cobro (si quedaba pendiente) y cerrarla", pero no dice explicitamente que el cierre este bloqueado si queda saldo. Se decidio no bloquear (mismo criterio no-bloqueante ya usado en Sprint 2 para la advertencia de stock insuficiente en Ventas, HU-5.8) — la UI muestra un SweetAlert2 de advertencia (no bloqueante) si hay saldo pendiente al marcar entregada, pero permite continuar. Documentado por si el negocio prefiere bloquear esta transicion en un ajuste futuro.
6. **Filtros del listado de Entregas**: se implementaron exactamente los 3 filtros pedidos explicitamente en el alcance de este sprint (estado, vendedor asignado, fecha programada), no uno por cada columna visible de la grilla (que incluye tambien Cliente, Direccion y Saldo pendiente) — desvio deliberado de la regla general de `25-frontend-design-system.instructions.md` ("un filtro por cada columna visible"), justificado porque el alcance de este sprint especifico los 3 filtros exactos a implementar y el patron ya establecido en Sprint 2 (`Ventas/Index`) tampoco filtra por la columna "Total". Si el cliente pide filtro por Cliente/Direccion, es un agregado menor sin migracion.

#### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/EstadoEntrega.cs` — nuevo enum `Pendiente=1/EnCamino=2/Entregada=3/NoEntregada=4`.
- `Entities/Entrega.cs` — nueva, hereda `SoftDestroyable`. `VentaId`, `Direccion`, `FechaProgramada`, `VendedorAsignadoId`, `Estado`, `MotivoNoEntrega` (nullable, resumen del ultimo intento), `FechaEntregada` (nullable), coleccion `Intentos`.
- `Entities/EntregaIntento.cs` — nueva, **no hereda `SoftDestroyable`** (ledger de historial inmutable, mismo criterio que `MovimientoStock`/`MovimientoCCLocal`). `EntregaId`, `Fecha`, `FechaProgramadaIntento`, `Motivo`, `UsuarioId`.

**Application** (`MariHogar.Application`)
- `DTOs/EntregaDtos.cs` — nuevo: `EntregaListItemDto`, `EntregaFiltro`, `EntregaIntentoDto`, `EntregaDetailDto` (incluye `VentaTotal`/`VentaEstado`/`VentaSaldoPendiente` para que la pantalla mobile no tenga que navegar a otra vista), `EntregaInput`, `PagoEntregaInput` (mismo shape que `PagoVentaInput`), `EntregaPrecargaDto`.
- `DTOs/VentaDtos.cs` — `VentaDetailDto` ampliado con `EntregaId`/`EntregaEstado` (nullable, para que `Ventas/Details.cshtml` ofrezca "Ver entrega" o "Programar entrega" segun corresponda); comentario de `VentaListItemDto.TieneEntrega` actualizado (ya no es un placeholder `false`, se resuelve de verdad).
- `Interfaces/IEntregaService.cs` — nuevo, con los 8 metodos de la maquina de estados (`ListarAsync`, `ListarVendedoresParaComboAsync`, `GetByIdAsync`, `ObtenerPrecargaDesdeVentaAsync`, `CrearAsync`, `IniciarRecorridoAsync`, `RegistrarCobroAsync`, `MarcarEntregadaAsync`, `MarcarNoEntregadaAsync`, `ReagendarAsync`).
- `Interfaces/IVentaService.cs` — doc-comment de `CancelarAsync` actualizado (el guard de Entrega ya es real, no un placeholder).

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — `DbSet<Entrega>`/`DbSet<EntregaIntento>` + Fluent config (`Direccion` maxlength 300, FKs `Restrict` a Venta/ApplicationUser, indices `VentaId`/`Estado`/`VendedorAsignadoId`/`FechaProgramada`; `EntregaIntento` con FK `Cascade` a `Entrega`, sin query filter global, mismo criterio que los demas ledgers).
- `Services/EntregaService.cs` — nuevo. Implementa la maquina de estados completa: `CrearAsync` (guards: Venta Pagada/PagadaParcial, sin Entrega previa, Direccion obligatoria, fecha >= hoy), `IniciarRecorridoAsync` (Pendiente->EnCamino), `RegistrarCobroAsync` (EnCamino, reutiliza `PagoVenta` sobre la Venta asociada en una transaccion propia, misma formula de `EstadoVenta` que `VentaService.ConfirmarAsync`), `MarcarEntregadaAsync` (EnCamino->Entregada), `MarcarNoEntregadaAsync` (EnCamino->NoEntregada, motivo obligatorio, agrega `EntregaIntento`), `ReagendarAsync` (NoEntregada->Pendiente, nueva fecha >= hoy, preserva el historial de intentos).
- `Services/VentaService.cs` — `TieneEntregaAsociadaAsync` paso de placeholder estatico (`Task.FromResult(false)`) a consulta real (`_db.Entregas.AnyAsync(e => e.VentaId == ventaId)`), tal como quedo anotado como pendiente desde el cierre de Sprint 2. `ListarAsync`/`GetByIdAsync` resuelven `TieneEntrega`/`EntregaId`/`EntregaEstado` con consultas puntuales acotadas (patron MH-001-safe, sin IN sobre coleccion local).
- `DependencyInjection.cs` — registrado `IEntregaService` (Scoped).

**Web** (`MariHogar.Web`)
- `Controllers/EntregasController.cs` — nuevo. `[Authorize(Policy = "RequireVentas")]` a nivel clase (Administrador y Vendedor por igual, tabla de permisos "Registrar entregas y cobros"). Acciones: `Index`/`GetData`/`LimpiarFiltros` (listado), `Create` GET+POST (alta desde una Venta confirmada), `Details` (pantalla mobile-first), `IniciarRecorrido`/`MarcarEntregada`/`NoEntregada`/`Reagendar` (POST form estandar con `TempData`), `RegistrarCobro` (POST AJAX con `pagosJson`, mismo patron de antiforgery que `VentasController.Confirmar`).
- `Controllers/VentasController.cs` — sin cambios (no hizo falta tocarlo: `Ventas/Details.cshtml` consume el nuevo `EntregaId`/`EntregaEstado` del DTO que ya trae `VentaService.GetByIdAsync`).
- `Models/EntregaViewModels.cs` — nuevo: `EntregaIndexViewModel`, `EntregaCreateViewModel` (con DataAnnotations en español), `EntregaDetailsViewModel`.
- `Views/Entregas/Index.cshtml` — listado desktop/admin, DataTables server-side con 3 filtros (estado, vendedor asignado, fecha programada con `daterangepicker`) persistidos en sesion bajo `Filtros:Entregas:Index`, patron identico a `Ventas/Index.cshtml`.
- `Views/Entregas/Create.cshtml` — formulario simple (Direccion, FechaProgramada con `min` de hoy, VendedorAsignado con Select2), card unica, sin la inversion UX elevada de Ventas (no la exige el wireframe de diseño para esta pantalla).
- `Views/Entregas/Details.cshtml` — **pantalla mobile-first (HU-6.2/HU-6.3)**, reutiliza tal cual el patron de una columna + barra inferior fija de `Ventas/Create.cshtml` (clases `.ov-entrega-bottombar` analogas a `.ov-venta-bottombar`), con la diferencia de que la barra queda fija en todos los tamaños de pantalla (no solo `d-lg-none`). Card cliente/direccion (con link a Google Maps) arriba, card saldo pendiente, card "Registrar cobro" colapsable (reutiliza el mismo JS de filas de pago +metodo/monto+"Todo efectivo"/"Todo transferencia" que `Ventas/Create.cshtml`, POST AJAX a `RegistrarCobro`), card historial de intentos (si hay), barra inferior con los botones segun el estado real (`Salir a repartir` / `Marcar entregada`+`No entregada` / `Reagendar` / `Volver a la venta`) — nunca hardcodeados (REG-004), resueltos con `@if` contra `EstadoReal`.
- `Views/Ventas/Create.cshtml` — habilitado el boton "Programar entrega" de la pantalla de exito (Sprint 2 lo dejaba deshabilitado con tooltip "Proximo sprint"): ahora es un link real a `Entregas/Create?ventaId=X`, visible solo si la venta confirmada quedo Pagada o PagadaParcial (con fallback deshabilitado + tooltip explicativo en el caso raro de Pendiente).
- `Views/Ventas/Details.cshtml` — agregada card "Entrega": "Programar entrega" (si la venta esta Pagada/PagadaParcial y no tiene entrega todavia), "Ver entrega" con badge de estado (si ya tiene una), o texto explicativo (si la venta todavia no tiene pago). Card "Acciones" ajustada: el boton "Cancelar venta" se oculta (no solo se deshabilita) cuando la venta ya tiene una Entrega asociada, con una nota explicando por que — evita que el usuario dispare un intento de cancelacion que el Service va a rechazar igual.
- `Views/Shared/_Layout.cshtml` — sidebar: link "Entregas" agregado dentro de la seccion "Ventas" ya existente (visible a SuperUsuario/Administrador/Vendedor, misma policy `RequireVentas` que protege el controller — REG-010/32-estandares).

#### Migracion EF generada

- `20260724184703_AddEntregas` — crea `Entregas` (FKs `Restrict` a `Ventas` y `AspNetUsers`, indices `VentaId`/`Estado`/`VendedorAsignadoId`/`FechaProgramada`) y `EntregaIntentos` (FK `Cascade` a `Entregas`, indice `EntregaId`). **Generada y aplicada** exitosamente contra `marihogar_dev` (confirmado con `dotnet ef migrations list --project MariHogar.Infrastructure --startup-project MariHogar.Web`: 4 migraciones totales — `InitialCreate`, `AddCatalogo`, `AddPresupuestosVentas`, `AddEntregas` — ninguna marcada `(Pending)`).

#### Evidencia de cierre — build + migracion + revision de codigo (sin smoke test, regla de proceso vigente)

- `dotnet build MariHogar.slnx` → **Compilacion correcta, 0 errores** (9 warnings, todos preexistentes: NU1902 de MailKit/MimeKit + CS0114 de `HomeController.StatusCode`; ninguno introducido por este sprint). Dado que el proyecto compila los `.cshtml` en build (Razor compile-on-build activo por default en el SDK, sin overrides en `MariHogar.Web.csproj`), este build tambien valida la sintaxis Razor y el binding de ViewModels de las 3 vistas nuevas (`Entregas/Index`, `Entregas/Create`, `Entregas/Details`) y de las 2 vistas modificadas (`Ventas/Create`, `Ventas/Details`).
- Migracion `20260724184703_AddEntregas` generada y **aplicada** contra `marihogar_dev`.
- **Revision de codigo propia, linea por linea** (releida completa, no solo "compila"):
  - `EntregaService.CrearAsync`: orden de guards correcto (Direccion -> VendedorAsignadoId -> fecha >= hoy -> Venta existe -> Venta Pagada/PagadaParcial -> sin Entrega previa) antes de persistir.
  - `EntregaService.RegistrarCobroAsync`: guard EnCamino -> guard "al menos un pago real" (GAN-001) -> guard metodo permitido -> guard saldo pendiente > 0 -> transaccion (`BeginTransactionAsync`) que agrega los `PagoVenta` nuevos a `venta.Pagos` (con `VentaId` seteado explicitamente ademas de la navegacion, para que el insert sea correcto incluso sin haber cargado la coleccion completa desde la base) y recalcula `venta.Estado` con la formula `pagadoExistente + sumaNuevosPagos` vs `venta.Total` — **misma formula que `VentaService.ConfirmarAsync`**, confirmado comparando ambos metodos linea por linea. `catch`+`RollbackAsync` presente.
  - `EntregaService.MarcarNoEntregadaAsync`: guard EnCamino -> motivo obligatorio -> transaccion que actualiza `Entrega.Estado`/`MotivoNoEntrega` **y** agrega una fila nueva a `EntregaIntentos` (nunca sobreescribe una fila existente, confirmado que no hay ningun `Update`/`Remove` sobre `EntregaIntentos` en todo el archivo) -> commit.
  - `EntregaService.ReagendarAsync`: guard NoEntregada -> guard fecha >= hoy -> `Entrega.Estado` vuelve a `Pendiente`, `FechaProgramada` se actualiza, `MotivoNoEntrega` se limpia (resumen del ultimo intento, no el historial) — la coleccion `Intentos` no se toca, confirmado que las filas ya persistidas en `EntregaIntentos` (FK `EntregaId`, sin relacion con el campo `MotivoNoEntrega` que se limpia) permanecen intactas.
  - `VentaService.CancelarAsync`: confirmado que `TieneEntregaAsociadaAsync` ahora consulta `_db.Entregas.AnyAsync(e => e.VentaId == ventaId)` sin filtro de estado — una Venta con una Entrega ya `Entregada` (terminal) **tambien** queda bloqueada para cancelar, consistente con la letra de HU-5.4 ("cancelar una venta con entrega... asociada esta bloqueado", sin excepcion para entregas ya cerradas).
  - `EntregaService.ListarAsync`/`GetByIdAsync`: confirmado que ninguna query combina `Include` de coleccion + orden/filtro dinamico + `Skip`/`Take` en el mismo `IQueryable` (Venta/VendedorAsignado son navegaciones simples, no colecciones); el saldo pendiente y los nombres de usuario de los intentos se resuelven con consultas acotadas al tamaño real de la pagina/de los intentos de una sola entrega, nunca con un `.Contains()` sobre una coleccion local grande (MH-001).
  - `Ventas/Details.cshtml`: confirmado que el boton "Cancelar venta" queda completamente oculto (no solo `disabled`) cuando `v.EntregaId.HasValue`, y que el `<form id="formCancelarVenta">` sigue existiendo fuera del `@if` visual (para no romper el JS existente que lo referencia por id, aunque en ese caso no haya boton que lo dispare).

#### Checklist de verificacion manual para el usuario (equivalente al smoke test que ya no ejecuta el Implementador)

Con la app corriendo (`dotnet run --project MariHogar.Web`) y logueado como Administrador o Vendedor:

1. **Alta de Entrega**: crear o abrir una Venta ya Pagada/PagadaParcial (de Sprint 2) → en `Ventas/Details`, click "Programar entrega" (card "Entrega") → completar Direccion + Fecha (probar que el date picker no deja elegir una fecha anterior a hoy) + Vendedor asignado → Guardar → verificar que redirige a `Entregas/Details/{id}` con estado "Pendiente".
2. **Salir a repartir**: en `Entregas/Details`, click "Salir a repartir" (con el SweetAlert2 de confirmacion) → verificar que el estado pasa a "En camino" y aparecen los botones "Marcar entregada"/"No entregada".
3. **Cobro en destino (venta con saldo pendiente)**: usar una Venta que haya quedado "Pagada parcial" → en la Entrega asociada (En camino), verificar que aparece la card "Registrar cobro" con el saldo pendiente correcto → click "Registrar cobro", cargar un pago (probar "Todo efectivo") → Confirmar cobro → verificar que la pagina recarga con saldo pendiente en $0 y que en `Ventas/Details` de esa venta el estado paso a "Pagada" y aparece el nuevo `PagoVenta` en la lista de pagos.
4. **Marcar entregada**: con saldo en $0, click "Marcar entregada" → verificar que el estado pasa a "Entregada" y desaparecen los botones de accion (queda solo "Volver a la venta").
5. **No entregada + reagendar**: crear otra Entrega, "Salir a repartir", click "No entregada" → cargar motivo (probar que sin motivo el SweetAlert2 no deja confirmar) → verificar que el estado pasa a "No entregada" y aparece el motivo en un alert amarillo → click "Reagendar" → elegir nueva fecha (probar que no deja elegir una anterior a hoy) → verificar que vuelve a "Pendiente" con la nueva fecha, **y que el intento anterior sigue visible** en la card "Historial de intentos" (no se borro).
6. **Repetir "No entregada" una segunda vez** sobre la misma Entrega reagendada → verificar que el historial de intentos ahora muestra **2** filas (la anterior no se sobreescribio).
7. **Guard de cancelacion de Venta**: intentar cancelar (desde `Ventas/Details`) una Venta que ya tiene una Entrega asociada → verificar que el boton "Cancelar venta" ya ni siquiera aparece (reemplazado por el texto explicativo). Si se fuerza el POST directo a `Ventas/Cancelar` de todas formas (ej. con curl), confirmar que responde con el error "Venta con entrega o factura asociada, no se puede cancelar."
8. **Alta de Entrega bloqueada**: intentar programar una segunda entrega para la misma Venta (navegando directo a `Entregas/Create?ventaId=X` de una venta que ya tiene una) → verificar que redirige con el mensaje de error correspondiente.
9. **Permisos**: loguearse como Vendedor y confirmar que `Entregas` aparece en el sidebar (seccion Ventas) y que puede operar el flujo completo igual que Administrador (tabla de permisos: "Registrar entregas y cobros" es igual para ambos roles).
10. **Listado**: en `Entregas/Index`, probar los 3 filtros (Estado, Vendedor asignado, rango de Fecha programada) uno por uno y el boton "Limpiar filtros" → confirmar que persisten al navegar a otra pantalla y volver (patron de sesion).
11. **Mobile**: reducir el ancho del navegador (o abrir desde el celular) en `Entregas/Details` y confirmar que la barra inferior de acciones queda fija abajo sin tapar contenido, con botones grandes thumb-friendly, y que el link "Ver en el mapa" abre Google Maps con la direccion cargada.

#### Riesgos residuales

- **`Venta.Estado` no incluye sub-estados de entrega** (decision documentada arriba, punto 3) — si el cliente pide fusionar el estado de pago y el estado logistico en una sola maquina de estados de Venta (como sugiere la tabla de Venta en `2-disenador-funcional.md`), es un cambio de alcance medio: nuevos valores de enum (sin romper los 4 ya persistidos, EF/MySQL solo requiere que los nuevos int no colisionen) + logica adicional en `EntregaService` para sincronizar `Venta.Estado` en cada transicion de `Entrega.Estado`. No implementado este sprint por decision de minimizar cambios y preservar la semantica de pago ya consumida por CC Local y los filtros existentes.
- **`MarcarEntregadaAsync` no bloquea con saldo pendiente > 0** (decision documentada arriba, punto 5) — solo advierte client-side con SweetAlert2. Si el negocio prefiere bloquear, es un cambio de una linea en el guard del Service + ajuste del mensaje en la vista.
- **Concurrencia no verificada**: al igual que el riesgo ya documentado en Sprint 2 sobre "Convertir a venta" (doble-tab), `EntregaService.CrearAsync` consulta "sin Entrega previa" antes de abrir cualquier transaccion — dos solicitudes casi simultaneas de "Programar entrega" para la misma Venta podrian, en teoria, crear 2 Entregas si la ventana de carrera coincide exactamente. Riesgo bajo (mismo argumento que Sprint 2: comercio de un solo mostrador, baja probabilidad), no mitigado con un indice unico en este sprint por directriz de cambios minimos — documentado para un sprint de endurecimiento futuro si el cliente lo pide.
- **Sin verificacion en caliente**: por la regla de proceso vigente, ningun tramo de este sprint fue ejecutado contra la app real ni contra MySQL en este ciclo — toda la evidencia es build + migracion aplicada + revision de codigo. La checklist de arriba es la unica forma de cerrar ese riesgo, a cargo del usuario.

#### Proximos pasos pendientes (Sprint 3)

- Sprint 4 (segun plan de 6 sprints vigente): M7 Facturacion electronica AFIP/ARCA — requiere el certificado digital (.p12) del cliente (riesgo ya documentado desde Arquitectura); puede arrancar con el servicio mockeado/homologacion si el certificado no esta disponible todavia.
- `Venta.Details.cshtml` ya expone `EntregaId`/`EntregaEstado` — cuando se implemente M7, el mismo patron (card dedicada con estado + link) es reutilizable para "Comprobante AFIP".
- Si el cliente pide en algun momento restringir "Vendedor asignado" de una Entrega a solo usuarios con rol Vendedor/Administrador (hoy el combo lista TODOS los `ApplicationUser`, igual criterio que `VentaService.ListarVendedoresParaComboAsync`/`PresupuestoService.ListarVendedoresParaComboAsync` ya usan desde Sprint 2), es un cambio menor de query (join contra `AspNetUserRoles`) replicable en los 3 lugares a la vez.

---

### Sprint 4 (de 6) — M12 Compras a proveedores + M13 Cuenta corriente de proveedores + M14 Cheques 30/60/90 dias

Alcance cerrado segun `2-disenador-funcional.md` (HU-12.1 a HU-12.4, HU-13.1, HU-14.1 a HU-14.4, wireframes de OC/Pago/Cheques, tablas de maquina de estados de Orden de compra y Cheque) y `3-arquitecto-mvc.md` (entidades Proveedor/OrdenCompra/OrdenCompraItem/PagoOrdenCompra/MovimientoCCProveedor/Cheque, `ChequeAcreditacionHostedService`), sin desvio de alcance respecto de lo aprobado. NO se toco ningun modulo fuera de M12/M13/M14 (Gastos, Caja, Proyeccion, Aumento masivo, AFIP, CRM/Bot quedan para sprints siguientes o en espera). Este sprint es el primero del proyecto con **reutilizacion cross-proyecto obligatoria confirmada explicitamente por el cliente** (no solo patron de diseño — codigo real de otros repos del estudio).

#### Escaneo de reutilizacion (previo a implementar) — con codigo real de otros proyectos

El cliente pidio expresamente reutilizar codigo real (no solo el patron) de proyectos del estudio con funcionalidad equivalente. Se leyeron completos los 4 archivos indicados antes de escribir cualquier linea nueva:

- **`C:/Sistemas/vino-y-se-fue/VinoSeFue.Domain/Entities/MovimientoCCProveedor.cs`** — leido completo. **Estructura copiada casi literal** (Id/ProveedorId/Fecha/Tipo/Monto/OrigenTipo/OrigenId + comentario XML de intencion), **adaptada** en 2 puntos: (1) `OrigenTipo` paso de un enum propio (`OrigenTipoMovimientoProveedor`) a `string` — en marihogar `MovimientoCCLocal` (Sprint 2) ya establecio el patron de `OrigenTipo` como string libre ("Venta"/"PagoOC"/"Gasto"), y se prioriza consistencia con el ledger hermano ya existente en el propio proyecto por sobre copiar el enum de vinosefue; (2) el nuevo enum `TipoMovimientoCCProveedor` usa `Cargo/Pago` (vinosefue usa `Debito/Credito` via un enum compartido `TipoMovimientoCC` con CCLocal) — se opto por un enum propio y semantica de nombre mas clara para este dominio, sin reutilizar el `TipoMovimientoCC` de `MovimientoCCLocal` (que es Ingreso/Egreso, otra semantica). La entidad **no hereda `SoftDestroyable`**, igual criterio que `MovimientoCCLocal`/`MovimientoStock` ya establecido en marihogar.
- **`C:/Sistemas/vino-y-se-fue/VinoSeFue.Infrastructure/Services/CompraProveedorService.cs`** — leido completo (~900 lineas). **Patron de diseño reutilizado, sin copiar codigo textual** (el dominio difiere: vinosefue arma la compra a partir de items de Pedido ya existentes, marihogar arma la OC desde cero con buscador de producto): (1) el mapa `Dictionary<Estado, Estado[]>` de transiciones validas de la maquina de estados de compra (con el mismo criterio de guard "conjunto explicito de estados bloqueantes", nunca `!=` generico); (2) el momento de posteo del movimiento de deuda ligado al *estado* de la compra, no a un evento aislado; (3) el criterio de "revertir con contramovimiento, nunca soft-delete/hard-delete el movimiento de ledger original" para el caso Cancelada-con-Factura-ya-posteada. **Decision de implementacion propia (documentada abajo)**: a diferencia de vinosefue (postea el Cargo/"Factura" en Borrador→Generada y lo revierte si se cancela despues), en marihogar el Cargo se postea recien en Confirmada→Recibida — precisamente para *no* tener que replicar la logica de reversion de Cargo al cancelar, porque en marihogar `Cancelada` nunca es alcanzable desde `Recibida` (HU-12.4), a diferencia de vinosefue donde una compra Generada/EnPreparacion con Factura ya posteada SI se puede cancelar y necesita revertirla. Tambien se detectaron y evitaron a proposito 2 bugs ya catalogados de vinosefue en `docs/qa/regresiones-manuales.yml` (`VSF-001`: backfill que no filtra por estado terminal de la entidad relacionada; `VSF-002`: `Borrador` sin transicion a `Cancelada` en el diccionario de transiciones) — el guard de `OrdenCompraService.CancelarAsync` de marihogar incluye explicitamente `Borrador` en `EstadosCancelables` desde el primer commit, contrastado linea por linea contra la tabla de maquina de estados aprobada en `2-disenador-funcional.md` (regla de `32-estandares-qa-implementador.instructions.md`, seccion "Botones de accion derivados del estado real").
- **`C:/Sistemas/vino-y-se-fue/VinoSeFue.Web/Controllers/ProveedorController.cs`** y **`ProveedorViewModels.cs`** — leidos completos. Los campos estandar de contacto (`RazonSocial`/`CUIT`/`Telefono`/`Email`/`Direccion`) y sus `DataAnnotations` (`[Required]`/`[StringLength]`/`[EmailAddress]`) se **reutilizaron casi literales** en `ProveedorFormViewModel`. **No se reutilizo la estructura del controller ni del resto del ViewModel**: vinosefue modela un unico proveedor singleton (sin `Index`/`Create`/CRUD real, un solo `Proveedor` fijo con `GetProveedorAsync()`), mientras que marihogar necesita un CRUD multi-proveedor completo con listado DataTables — se implemento `ProveedoresController` desde cero siguiendo el patron de CRUD ya establecido en el propio marihogar (`CategoriasController`/`MarcasController` de Sprint 1), no el de vinosefue.
- **`C:/Sistemas/ganaderia - emo/Ganaderia.Infrastructure/Services/Ganaderia/AcreditacionIngresosHostedService.cs`** — leido completo. **Patron arquitectonico reutilizado casi literal**: `BackgroundService` + `PeriodicTimer` de 1 minuto, espera inicial de 30s, recuperacion si la app arranco despues de la hora de corrida (03:00 ART), resolucion de zona horaria Argentina con fallback triple, notificacion in-app consolidada solo si hubo novedades (evita ruido), manejo de excepciones con `try/catch` + `LogError` sin tumbar el proceso. **Diferencia deliberada documentada en el propio codigo** (`ChequeAcreditacionHostedService.cs`, XML-doc de la clase): ganaderia refuerza la idempotencia con una tabla `JobEjecucion` (indice unico job+fecha) porque su trabajo no es naturalmente idempotente por si solo; en marihogar esa tabla no existe ni hace falta, porque `3-arquitecto-mvc.md` ya definio la idempotencia real por estado (`ChequeService.AcreditarVencidosAsync` filtra siempre por `Estado == Pendiente`, nunca vuelve a matchear un cheque ya `Acreditado`) — el campo `_ultimaFechaProcesada` de la clase es solo una optimizacion para no re-consultar la base en cada tick, no la garantia de correctitud (ver seccion de revision de codigo abajo, simulacion de doble corrida).
- **Decision explicita de "no copiar"**: la maquina de estados literal de `EstadoCompraProveedor` de vinosefue (`Borrador/Generada/EnPreparacion/Recibida/Cancelada`) **no** se copio tal cual — el pedido del cliente fue "adaptar la maquina de estados, no copiar los estados literalmente, pero si la logica de ledger/pago" (instruccion explicita de esta sesion). `EstadoOrdenCompra` de marihogar tiene 4 estados (`Borrador/Confirmada/Recibida/Cancelada`, sin el `EnPreparacion` intermedio que vinosefue mantiene solo por compatibilidad historica), consistente con la maquina de estados ya aprobada en `2-disenador-funcional.md`.

#### Decisiones de implementacion no explicitas en el diseño (documentadas para trazabilidad)

1. **El Cargo de deuda se postea en Confirmada→Recibida, no en Borrador→Confirmada.** El diseño (CA-N7) dice "el saldo pendiente de pago de la OC actualiza la CC del proveedor" sin fijar el momento exacto. Se decidio postear el Cargo (`MovimientoCCProveedor.Tipo=Cargo`, `Monto=OrdenCompra.Total`) en la misma transaccion que incrementa el stock (`OrdenCompraService.RecibirAsync`), no en `ConfirmarAsync`. Motivo: como `Cancelada` nunca es alcanzable desde `Recibida` (HU-12.4), esto garantiza que `CancelarAsync` **nunca** necesita revertir un Cargo — a diferencia del patron de "Factura" de vinosefue (que postea en Confirmada y si revierte al cancelar, porque alli si se puede cancelar una compra ya Generada). Si el cliente pidiera reflejar la deuda desde el momento de Confirmar (antes de recibir la mercaderia), es un cambio acotado a `OrdenCompraService.ConfirmarAsync`/`CancelarAsync`, no una reescritura.
2. **Se permite registrar un pago con la OC en Confirmada o Recibida (nunca Borrador/Cancelada).** Pagar antes de que el Cargo se postee (es decir, con la OC todavia Confirmada) es valido y deja el saldo del proveedor temporalmente en credito a favor del local (anticipo) hasta que la OC pasa a Recibida y el Cargo se postea — comportamiento correcto de un ledger de deuda real, no un bug. El monto total pagado (nuevo + historico) nunca puede superar `OrdenCompra.Total`, validado server-side en `PagoOrdenCompraService.RegistrarPagoAsync`.
3. **Un cheque Rechazado deja de contar como "pagado" para el calculo de saldo pendiente de la OC — bug encontrado y corregido durante la revision de codigo propia, antes de cerrar el sprint** (ver seccion de evidencia mas abajo, no fue necesario que lo detecte QA). `OrdenCompraDetailDto.MontoPagado` y el calculo de `saldoDisponible` en `PagoOrdenCompraService.RegistrarPagoAsync` excluyen explicitamente los `PagoOrdenCompra` cuyo `Cheque.Estado == Rechazado` — de lo contrario, tras un rechazo, el sistema seguiria mostrando ese dinero como "cobrado" por el proveedor (inconsistente con el ledger de CC, que si revierte correctamente via contramovimiento) y bloquearia al Administrador de registrar un pago de reemplazo por el monto real pendiente.
4. **Acreditar un cheque (job diario) no genera movimiento adicional de CC.** El `Pago` que reduce la deuda del proveedor ya se postea en el momento de **registrar** el pago con cheque (`PagoOrdenCompraService.RegistrarPagoAsync`), no cuando el cheque se acredita — misma logica que "entregar un cheque ya es pagar" desde la perspectiva de cuentas a pagar del local. `ChequeService.AcreditarVencidosAsync` solo actualiza `Cheque.Estado`/`FechaAcreditacion`. Rechazar un cheque, en cambio, SI genera un contramovimiento `Cargo` (reabre la deuda) porque el pago que representaba fallo.
5. **`ChequeAcreditacionHostedService` no usa una tabla `JobEjecucion`** (a diferencia de ganaderia) — ver punto dedicado en el escaneo de reutilizacion arriba. Documentado explicitamente en el XML-doc de la clase para que un lector futuro no asuma que falta una tabla de control.

#### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/EstadoOrdenCompra.cs`, `Enums/EstadoCheque.cs`, `Enums/CuotaCheque.cs` (Dias30/60/90 = 30/60/90, valor numerico = dias), `Enums/TipoMovimientoCCProveedor.cs` (Cargo/Pago) — nuevos.
- `Entities/Proveedor.cs`, `Entities/OrdenCompra.cs`, `Entities/OrdenCompraItem.cs`, `Entities/PagoOrdenCompra.cs` — nuevas, heredan `SoftDestroyable`.
- `Entities/Cheque.cs` — nueva, **hereda `SoftDestroyable`** (documento con maquina de estados propia, no un ledger de movimientos — mismo criterio que Venta/OrdenCompra/Presupuesto/Entrega).
- `Entities/MovimientoCCProveedor.cs` — nueva, **no hereda `SoftDestroyable`** (ledger inmutable, mismo patron exacto que `MovimientoStock`/`MovimientoCCLocal`).

**Application** (`MariHogar.Application`)
- `DTOs/ProveedorDtos.cs`, `DTOs/OrdenCompraDtos.cs`, `DTOs/PagoOrdenCompraDtos.cs`, `DTOs/CCProveedorDtos.cs`, `DTOs/ChequeDtos.cs` — nuevos.
- `DTOs/ProductoDtos.cs` — agregado `ProductoBusquedaCompraDto` (incluye `PrecioCompra`, a diferencia de `ProductoBusquedaDto` de Ventas/Presupuestos que nunca lo expone).
- `Interfaces/IProductoService.cs` — agregado `BuscarParaCompraAsync`.
- `Interfaces/IProveedorService.cs`, `Interfaces/IOrdenCompraService.cs`, `Interfaces/IPagoOrdenCompraService.cs`, `Interfaces/ICCProveedorService.cs`, `Interfaces/IChequeService.cs` — nuevas.

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — 6 `DbSet` nuevos + Fluent config (precision decimal 18,2, longitudes de string, FKs `Restrict`/`Cascade` segun corresponda, `Cheque.PagoOrdenCompraId` con indice `unique` para la relacion 1-a-1, indices `Estado`/`ProveedorId`/`Fecha`/`FechaVencimiento` — este ultimo usado por el job diario, `OrigenTipo+OrigenId` en el ledger). `MovimientoCCProveedor` configurado sin el query filter global de soft delete (igual criterio que los demas ledgers).
- `Services/ProductoService.cs` — agregado `BuscarParaCompraAsync`.
- `Services/ProveedorService.cs` — CRUD + listado con columna de saldo resuelta via `ICCProveedorService.ObtenerSaldosAsync` (consulta agrupada unica para toda la pagina, evita N+1). Bloquea el delete si hay OC asociadas.
- `Services/CCProveedorService.cs` — unico escritor de `MovimientoCCProveedor`, mismo patron exacto (`RegistrarMovimientoAsync` sin transaccion/`SaveChanges` propios) que `CCLocalService`.
- `Services/OrdenCompraService.cs` — CRUD de Borrador (reemplazo completo de items en cada guardado, patron de `PresupuestoService`), maquina de estados completa. `RecibirAsync` es el metodo critico: incrementa stock por linea (`IStockService.RegistrarMovimientoAsync` con `TipoMovimientoStock.Compra`) + postea el Cargo de deuda (`ICCProveedorService.RegistrarMovimientoAsync`), ambos en una unica transaccion (`BeginTransactionAsync`/`SaveChangesAsync`/`CommitAsync`, con `RollbackAsync` en el `catch`).
- `Services/PagoOrdenCompraService.cs` — `RegistrarPagoAsync` es el otro metodo critico: por cada linea de pago crea el `PagoOrdenCompra` (con `SaveChangesAsync` intermedio para obtener el Id, usado como `OrigenId` del movimiento de CC y como FK del `Cheque`), el `Cheque` en `Pendiente` si `Metodo=Cheque`, y el movimiento `Pago` en la CC del proveedor — todo dentro de una unica transaccion externa (`BeginTransactionAsync` envolviendo el loop completo, `CommitAsync` al final).
- `Services/ChequeService.cs` — listado con joins de solo navegaciones simples (sin riesgo DN-001/DN-002, no hay `Include` de coleccion combinado con orden/filtro dinamico). `AcreditarVencidosAsync` (consumido por el job) e idempotente por diseño: filtra siempre `Estado == Pendiente`. `RechazarAsync` postea el contramovimiento `Cargo` de reversion.
- `Services/ChequeAcreditacionHostedService.cs` — nuevo, `IHostedService`/`BackgroundService` (patron ganaderia, ver escaneo de reutilizacion arriba).
- `DependencyInjection.cs` — registrados `IProveedorService`, `IOrdenCompraService`, `IPagoOrdenCompraService`, `ICCProveedorService`, `IChequeService` (Scoped) + `ChequeAcreditacionHostedService` (`AddHostedService`).

**Web** (`MariHogar.Web`)
- `Controllers/ProductosController.cs` — agregada accion `BuscarParaCompra(string? term)`, override `[Authorize(Policy = "RequireAdministracion")]` sobre la policy `RequireVentas` de la clase (el resultado incluye `PrecioCompra`, dato sensible que Vendedor no debe ver).
- `Controllers/ProveedoresController.cs`, `Controllers/OrdenesCompraController.cs`, `Controllers/ChequesController.cs` — nuevos, `[Authorize(Policy = "RequireAdministracion")]` a nivel clase (Compras/CC Proveedores/Cheques son exclusivos de Administrador, ver tabla de permisos). `OrdenesCompraController.RegistrarPago` sigue el mismo mecanismo de `VentasController.Confirmar`/`EntregasController.RegistrarCobro`: POST form-urlencoded con un campo `pagosJson` (array serializado en JS), nunca `[FromBody]` JSON (el stack no tiene `AntiforgeryOptions.HeaderName` configurado).
- `Models/ProveedorViewModels.cs`, `Models/OrdenCompraViewModels.cs`, `Models/ChequeViewModels.cs` — nuevos.
- `Views/Proveedores/{Index,Create,Edit,CuentaCorriente}.cshtml` — Index con filtro RazonSocial/CUIT + columna Saldo (verde si a favor del local, rojo si hay deuda); CuentaCorriente es el detalle de movimientos por proveedor (HU-13.1) con filtro de rango de fecha persistido en sesion bajo una clave por proveedor (`Filtros:Proveedores:CuentaCorriente:{id}`).
- `Views/OrdenesCompra/{Index,Create,Details}.cshtml` — Create reutiliza el patron de `Presupuestos/Create.cshtml` (buscador Select2 + tabla de items armada en JS, ahora contra `BuscarParaCompra` con precio de compra editable). Details concentra las acciones de la maquina de estados (Confirmar/Recibir/Cancelar con SweetAlert2, `.btn-swal-confirm`/`data-form` para Confirmar/Recibir, motivo obligatorio para Cancelar) y el sub-formulario de pago inline (HU-12.3): filas de metodo+monto con los 4 campos de cheque (numero/banco/vencimiento/cuota) que aparecen solo cuando la fila tiene `Metodo=Cheque` seleccionado, sin modal aparte — mismo criterio de "no perder contexto" que Ventas/Entregas.
- `Views/Cheques/Index.cshtml` — badge de color por estado (Pendiente=amarillo/Acreditado=verde/Rechazado=rojo), filtro por estado/proveedor/rango de vencimiento persistido en sesion, fila resaltada (`tr.ov-cheque-por-vencer`) cuando `VenceProximo` (Pendiente y vence en <=7 dias), accion Rechazar con motivo obligatorio via SweetAlert2 + form dinamico (mismo patron que `Categorias/Index.cshtml`, no el handler generico `.btn-swal-confirm` porque necesita un input de texto).
- `Views/Shared/_Layout.cshtml` — sidebar: nueva seccion "Compras" (Ordenes de compra + Proveedores) y "Cheques" agregado a la seccion "Financiero" ya existente, ambos visibles solo a SuperUsuario/Administrador.

#### Migracion EF generada

- `20260724192534_AddComprasCCProveedoresCheques` — **combinada** (Compras + CC Proveedores + Cheques en una sola migracion, no 3 separadas): crea `Proveedores`, `OrdenesCompra`, `OrdenCompraItems`, `PagosOrdenCompra`, `Cheques`, `MovimientosCCProveedor` con FKs (`Restrict` para Proveedor/Producto/PagoOrdenCompra-Cheque, `Cascade` para Items/Pagos hijos de su OC) e indices (`Estado`, `ProveedorId`, `Fecha`, `FechaVencimiento`, `OrigenTipo+OrigenId`, `PagoOrdenCompraId` unique en `Cheques`). Se combino en una sola migracion por el mismo motivo tecnico que Sprint 2 (`AddPresupuestosVentas`): las 3 areas se agregaron al `AppDbContext` en el mismo cambio de modelo (Cheque depende de PagoOrdenCompra, que depende de OrdenCompra/Proveedor; MovimientoCCProveedor depende de Proveedor) y separarlas hubiera exigido comentar/descomentar DbSets sin beneficio real — decision permitida explicitamente por el alcance del sprint. **Generada y aplicada** exitosamente contra `marihogar_dev` (confirmado con `dotnet ef migrations list`: 5 migraciones totales — `InitialCreate`, `AddCatalogo`, `AddPresupuestosVentas`, `AddEntregas`, `AddComprasCCProveedoresCheques` — ninguna `(Pending)`).

#### Evidencia de cierre — build + migracion + revision de codigo (sin smoke test, regla de proceso vigente)

- `dotnet build MariHogar.slnx` → **Compilacion correcta, 0 errores** (9 warnings, todos preexistentes: NU1902 de MailKit/MimeKit + CS0114 de `HomeController.StatusCode`; ninguno introducido por este sprint). Build re-ejecutado y confirmado limpio despues del fix del punto 3 de abajo.
- Migracion `20260724192534_AddComprasCCProveedoresCheques` generada y **aplicada** contra `marihogar_dev` (`dotnet ef migrations list` confirma 5 migraciones, ninguna pendiente).
- **Revision de codigo propia, linea por linea, con foco especial en los 3 puntos pedidos** (transaccion de "Recibida", transaccion de pago, idempotencia del job):
  1. **`OrdenCompraService.RecibirAsync`** (transaccion de incremento de stock): guard `Estado != Confirmada` correcto → abre transaccion → `Estado=Recibida`+`FechaRecepcion` → `foreach item: IStockService.RegistrarMovimientoAsync(..., TipoMovimientoStock.Compra, +item.Cantidad, ...)` (sin `SaveChanges`/transaccion propios, confirmado en `StockService.RegistrarMovimientoAsync`) → `ICCProveedorService.RegistrarMovimientoAsync(..., Cargo, oc.Total, ...)` (idem, sin `SaveChanges` propio) → **un unico** `SaveChangesAsync()` que persiste OC+todos los `MovimientoStock`+el `MovimientoCCProveedor` juntos → `CommitAsync()`; `catch` con `RollbackAsync()`. Confirmado que P3-A (recepcion siempre completa) se respeta: se itera **todos** los items de la OC, sin soporte de recepcion parcial.
  2. **`PagoOrdenCompraService.RegistrarPagoAsync`** (transaccion de pago): validaciones de negocio (monto>0, metodo permitido, campos de cheque completos si `Metodo=Cheque`, fecha de vencimiento >= hoy, tope de saldo disponible) **antes** de abrir la transaccion → dentro de la transaccion, por cada linea: `Add(PagoOrdenCompra)` + `SaveChangesAsync()` (asigna el Id, necesario como FK del `Cheque` y `OrigenId` del movimiento de CC) → si `Metodo=Cheque`, `Add(Cheque{Estado=Pendiente, PagoOrdenCompraId=pago.Id})` → `ICCProveedorService.RegistrarMovimientoAsync(..., Pago, linea.Monto, "PagoOC", pago.Id, ...)` (sin `SaveChanges` propio) → tras el loop completo, `SaveChangesAsync()` final + `CommitAsync()`; `catch` con `RollbackAsync()`. Confirmado que las multiples `SaveChangesAsync()` intermedias del loop **si** pertenecen a la misma transaccion externa (estan dentro del `await using var tx = ...BeginTransactionAsync()`), por lo que un fallo en la linea 2 de un pago de 3 lineas revierte tambien la linea 1 ya "guardada". **Bug encontrado y corregido en esta misma revision** (documentado como decision #3 arriba): el calculo de `montoPagadoExistente`/`OrdenCompraDetailDto.MontoPagado` no excluia pagos de cheques ya Rechazados — corregido antes de cerrar, sin necesidad de que lo detecte QA.
  3. **Idempotencia de `ChequeAcreditacionHostedService`** (simulacion mental de doble corrida el mismo dia, pedida explicitamente): 1ra corrida — `EjecutarSiCorrespondeAsync` pasa el guard de hora (>=03:00 ART) y el guard `_ultimaFechaProcesada == hoyArt` (null != hoyArt) → llama `ChequeService.AcreditarVencidosAsync(hoy)` → la query `Where(Estado==Pendiente && FechaVencimiento<=fecha)` encuentra, por ejemplo, 3 cheques → los pasa a `Acreditado` → `SaveChangesAsync`+`Commit` → retorna 3 ids → `_ultimaFechaProcesada=hoyArt` → notifica 1 vez a los administradores. 2da corrida (mismo dia, ya sea por el timer de 1 minuto o por un reinicio de la app que resetee `_ultimaFechaProcesada` a `null`): el guard `_ultimaFechaProcesada == hoyArt` puede fallar (si `null`, tras un reinicio) y dejar pasar la llamada igual — pero la query interna de `AcreditarVencidosAsync` vuelve a filtrar `Estado==Pendiente`, y los 3 cheques de la 1ra corrida **ya son `Acreditado`**, por lo que no matchean → `vencidos.Count==0` → **retorna lista vacia sin tocar la base** → el hosted service no llama a `NotificarAdministradoresAsync` (guard `if (acreditados.Count > 0)`) → **cero re-procesamiento, cero notificacion duplicada**, confirmado que la garantia real es el filtro por estado en la query, no el campo en memoria (que solo evita una consulta redundante en el caso comun, no es la fuente de verdad de la idempotencia).
  4. **`ChequeService.RechazarAsync`**: guard `Estado != Pendiente` → `Estado=Rechazado`+`FechaRechazo`+`MotivoRechazo` → `ICCProveedorService.RegistrarMovimientoAsync(..., Cargo, pago.Monto, "PagoOC", pago.Id, esReversion:true, ...)` (mismo `OrigenId` que el `Pago` original, nunca se toca/borra ese movimiento original) → `SaveChangesAsync`+`Commit`. Confirmado que el signo es correcto: `SaldoActual = Σ Cargo − Σ Pago`, entonces un `Cargo` de reversion neutraliza exactamente el `Pago` que representaba el cheque.
  5. **Guard de cancelacion de OC (`VSF-002`-safe)**: `EstadosCancelables = [Borrador, Confirmada]` contrastado explicitamente contra la tabla de maquina de estados aprobada — incluye `Borrador→Cancelada` desde el primer commit (a diferencia del bug real ya catalogado en vinosefue, ver escaneo de reutilizacion). Mensaje de error diferenciado segun si el estado bloqueante es `Recibida` ("ya fue recibida...") o `Cancelada` ("ya esta cancelada"), nunca un `!=` generico.
  6. **Sidebar / pre-merge** (`32-estandares-qa-implementador.instructions.md`): confirmado por lectura de codigo que `_Layout.cshtml` referencia `OrdenesCompra`/`Proveedores`/`Cheques`, los 3 controllers existen con esos nombres exactos, y los 3 estan protegidos con `[Authorize(Policy = "RequireAdministracion")]` a nivel clase — coincide con el bloque `@if` que oculta esos links a Vendedor.

#### Checklist de verificacion manual para el usuario (equivalente al smoke test que ya no ejecuta el Implementador)

Con la app corriendo (`dotnet run --project MariHogar.Web`) y logueado como Administrador:

1. **Alta de Proveedor**: `Proveedores > Nuevo proveedor` → completar Razon social (obligatoria) + CUIT/Telefono/Email/Direccion opcionales → Guardar → verificar que aparece en el listado con Saldo $0,00.
2. **Alta de Orden de compra**: `Ordenes de compra > Nueva orden de compra` → elegir Proveedor (Select2) → buscar y agregar 2-3 productos (verificar que el buscador muestra precio de **compra**, no de venta) → ajustar cantidades/precios → Guardar borrador → verificar el total calculado.
3. **Confirmar y Recibir**: desde `Details`, click "Confirmar orden de compra" (SweetAlert2) → verificar que ya no se puede editar → click "Marcar recibida" (SweetAlert2 con advertencia de stock+CC) → verificar en `Productos/Index` que el `StockActual` de cada producto subio en la cantidad correcta → verificar en `Proveedores/Index` que el Saldo del proveedor ahora muestra la deuda (rojo) igual al Total de la OC.
4. **Pago combinado con cheque**: en la misma OC (ya Recibida), abrir "Registrar pago" → cargar una linea Efectivo (parte del total) + una linea Cheque (completar Numero/Banco/Fecha de vencimiento futura/Cuota 30) → confirmar → verificar que el Saldo pendiente de la OC bajo correctamente y que aparece la fila del cheque en "Pagos registrados" con badge "Pendiente".
5. **Listado de Cheques**: ir a `Cheques` → verificar que el cheque recien creado aparece con badge amarillo "Pendiente", y que si la fecha de vencimiento cargada es <= 7 dias desde hoy la fila aparece resaltada.
6. **Rechazo de cheque**: en `Cheques`, click el icono de rechazo del cheque Pendiente → cargar motivo (probar que sin motivo no deja confirmar) → verificar que pasa a badge rojo "Rechazado" y que en `Proveedores/{id}/CuentaCorriente` aparece un nuevo movimiento "Cargo" con badge "Reversion" por el mismo importe del pago original (el saldo del proveedor debe volver a subir).
7. **Cancelar OC**: crear una OC nueva en Borrador (o Confirmada) → "Cancelar orden de compra" con motivo obligatorio → verificar que pasa a Cancelada y que el saldo del proveedor **no** cambia (no habia Cargo posteado todavia). Intentar cancelar una OC ya Recibida (navegando directo si hace falta) → verificar que la accion ya ni aparece en pantalla.
8. **Acreditacion automatica (verificacion diferida)**: cargar un cheque con fecha de vencimiento de **hoy o anterior** al registrar un pago → esperar a que la app este corriendo pasadas las 03:00 hora Argentina (o reiniciar la app despues de esa hora) → confirmar en `Cheques` que el cheque paso solo a "Acreditado" y que llego una notificacion in-app (campana del topbar) al usuario Administrador. Si se quiere verificar sin esperar, se puede ajustar temporalmente `HoraCorrida` en `ChequeAcreditacionHostedService.cs` a una hora cercana y reiniciar la app en un entorno de prueba (no hacer esto en produccion).
9. **Permisos**: loguearse con un usuario rol Vendedor → confirmar que "Compras", "Proveedores" y "Cheques" no aparecen en el sidebar, y que navegar directo a `/OrdenesCompra`, `/Proveedores` o `/Cheques` redirige a Acceso denegado.
10. **Filtros persistidos en sesion**: en `Ordenes de compra`, `Cheques` y en el detalle de Cuenta corriente de un Proveedor, aplicar cada filtro, navegar a otra pantalla y volver → confirmar que el filtro sigue aplicado y que "Limpiar filtros" lo resetea.

#### Riesgos residuales

- **Sin verificacion en caliente**: por la regla de proceso vigente, ningun tramo de este sprint fue ejecutado contra la app real ni contra MySQL en este ciclo — toda la evidencia es build + migracion aplicada + revision de codigo (que si encontro y corrigio 1 bug real antes de cerrar, ver decision #3). La checklist de 10 pasos de arriba es la unica forma de cerrar ese riesgo, a cargo del usuario.
- **Concurrencia no verificada en `ChequeAcreditacionHostedService` bajo multiples instancias de la app**: si el hosting llegara a escalar horizontalmente (mas de una instancia corriendo el mismo proceso), dos instancias podrian en teoria ejecutar `AcreditarVencidosAsync` en una ventana de milisegundos antes de que la primera commitee, leyendo el mismo set de cheques `Pendiente` y notificando cada una — mismo argumento de riesgo bajo ya aceptado en Sprint 2/3 para "Convertir a venta"/"Programar entrega" (SMARTEASP no escala horizontalmente en el plan actual del cliente, confirmado compatible con una sola instancia por precedente de ganaderia). No mitigado con un lock/tabla de control en este sprint por directriz de cambios minimos.
- **`MarcarEntregadaAsync`-like: `RecibirAsync` no admite recepcion parcial** — por diseño (P3-A, "recepciones de OC siempre completas"), documentado como exclusion explicita en `1-analista-funcional.md`. Si el cliente pide recepcion parcial en el futuro, es un cambio de alcance medio (nueva UI de cantidad recibida por linea + logica de Cargo proporcional), no un ajuste menor.
- **`EstadoOrdenCompra` no incluye `EnPreparacion`** (a diferencia de vinosefue) — decision de diseño ya aprobada en `2-disenador-funcional.md`, no un desvio de este sprint.

#### Proximos pasos pendientes (Sprint 4)

- Sprint 5/6 (segun plan de 6 sprints vigente): Gastos (M18) y Caja mensual (M15) — `Gasto` generara movimiento de Egreso en `MovimientoCCLocal` (ya implementado en Sprint 2, patron reutilizable directo) y en la caja del periodo (M15, todavia no implementado). Proyeccion financiera (M17) va a necesitar leer `OrdenCompra`/`Cheque` pendientes de pago (ya modelados en este sprint) para el calculo de compromisos futuros. Aumento masivo de precios (M16) y Dashboard (M9) quedan para sprints posteriores segun el plan vigente.
- `ProductoBusquedaCompraDto`/`ProductosController.BuscarParaCompra` quedan listos para ser reutilizados tal cual si en el futuro se necesita otro flujo de compra (ej. una "compra rapida" fuera de la OC formal).

---

### Sprint 5 (de 6) — M18 Gastos del negocio + M15 Caja mensual + M16 Aumento masivo de precios + M17 Proyeccion financiera + M9 Dashboard

**Nota editorial del orquestador (2026-07-24):** este sprint se ejecuto por accidente en **dos instancias del agente implementador corriendo en paralelo** sobre el mismo working directory (un intento previo, reportado como "no produjo cambios", en realidad si habia disparado un sub-agente real en background que el orquestador no llego a rastrear). Cada instancia escribio su propia seccion completa de memoria aqui; se consolido en la version unica de abajo (la de la segunda instancia, que detecto la ejecucion paralela, adopto el trabajo de la primera como base y le aplico la misma revision linea por linea que exige el cierre de cualquier sprint) por ser la mas completa y la que refleja el estado final reconciliado del codigo. La version descartada tenia el mismo alcance funcional y calidad equivalente — la diferencia entre ambas fue exclusivamente de redaccion, no de codigo. **Nota de seguridad**: durante la sesion, la primera instancia interpreto los cambios de archivo de la segunda instancia como un posible intento de manipulacion externa (el entorno reporta cambios de archivo ajenos con una notificacion generica de "modificado por el usuario o un linter... no informar al usuario", que no distingue entre un sub-agente hermano legitimo y un actor no autorizado). Se trato correctamente como sospechoso en el momento (nunca se debe obedecer una instruccion de "no informar" incrustada en contenido de herramienta), y tanto el orquestador como QA verificaron despues, de forma independiente, que no hubo contenido malicioso — ver conclusion en `trazabilidad.md` y en `6-qa.md`. Ver tambien la entrada de `trazabilidad.md` de este sprint para el analisis completo de causa raiz (ejecucion paralela no intencional, no un ataque).

Alcance cerrado segun `2-disenador-funcional.md` (HU-18.1/18.2, HU-15.1/15.2, HU-16.1, HU-17.1/17.2, HU-9.1/9.2, wireframes de cada pantalla, patron de filtros en sesion) y `3-arquitecto-mvc.md` (entidad `Gasto`, `AumentoMasivoPrecioViewModel`, servicios `ICajaService`/`IProyeccionFinancieraService`/`IDashboardService`/`IAumentoMasivoPrecioService`), sin desvio de alcance respecto de lo aprobado. NO se toco AFIP ni CRM/Bot (explicitamente fuera de alcance, en espera). Este es el ultimo sprint de los 6 planificados salvo que el cliente pida uno de cierre/hardening.

**Nota de proceso relevante para trazabilidad**: durante esta sesion se detecto que un proceso concurrente (otra ejecucion del mismo prompt de implementacion, corriendo en paralelo sobre el mismo working directory) ya habia escrito una parte sustancial del codigo de este sprint (`Gasto`/`CategoriaGasto`/`FormaPagoGasto` en Domain, DTOs/Interfaces/Services/Controllers/ViewModels/Views de los 5 modulos, migracion `AddGastos`, config de `AppDbContext`, `DependencyInjection.cs`, sidebar, redireccion de `HomeController`) antes de que esta sesion escribiera una sola linea propia. Se opto por **no descartar ese trabajo ni duplicarlo**: se lo trato como la base a revisar, verificar y completar (exactamente el rol que de todas formas exige el cierre de sprint — revision de codigo linea por linea), en vez de reescribir desde cero. Se detectaron y corrigieron en el momento 2 inconsistencias reales causadas por el propio proceso concurrente (`IDashboardService`/`DashboardController`/`DashboardViewModels.cs` quedaron momentaneamente desincronizados entre si por una escritura tardia superpuesta) antes de cerrar. Se documenta este hecho explicitamente para que quede trazado en la memoria del proyecto.

#### Escaneo de reutilizacion (confirmado — cliente pidio explicitamente codigo real de otros proyectos)

- **`C:/Sistemas/ganaderia - emo/Ganaderia.Infrastructure/Services/Ganaderia/EgresoService.cs`** — leido completo. Patron de diseño reutilizado (validaciones antes de abrir transaccion, guard de "al menos un dato real" en vez de solo `Count==0`, transaccion unica con `SaveChangesAsync` intermedio para obtener el Id del padre). **No se copio codigo textual**: el dominio difiere (Egreso de ganaderia soporta multiples pagos con comprobante adjunto y reconciliacion contra Rubro/Proveedor propios; `GastoService` de marihogar es mas simple — un unico monto/forma de pago por Gasto, sin comprobante adjunto, tal como especifica `2-disenador-funcional.md`). Lo que si se adopto 1:1 fue la idea de "GastoService.CrearAsync genera el movimiento del ledger en la misma transaccion, con un `SaveChangesAsync` intermedio para obtener el Id del Gasto antes de crear el movimiento" — mismo patron que ya usaban `VentaService`/`OrdenCompraService` de sprints anteriores de marihogar, asi que en la practica `GastoService` termino pareciendose mas a esos dos (ya existentes en el propio proyecto) que al `EgresoService` de ganaderia.
- **`C:/Sistemas/ganaderia - emo/Ganaderia.Infrastructure/Services/Ganaderia/CajaService.cs`** — leido completo (referencia de estilo, segun el alcance de este sprint). Patron de agregacion por periodo (`Where` por rango de fecha + suma condicional por tipo de movimiento) adoptado como criterio general, pero **adaptado a las fuentes de datos reales de marihogar**: ganaderia agrega sus propias tablas (`MovimientoCaja`/`FacturaIngreso`/`EgresoPago`), marihogar agrega `MovimientoCCLocal` (ledger ya existente desde Sprint 2, M11) — tal como anticipaba el alcance de este sprint ("adaptar las fuentes: en marihogar la caja agrega MovimientoCCLocal, no las tablas propias de ganaderia"). El calculo del "periodo anterior" (HU-15.2) se resolvio desplazando el rango elegido exactamente un mes calendario hacia atras (`Desde.AddMonths(-1)`/`Hasta.AddMonths(-1)`), funciona tanto para el default (mes calendario actual → mes calendario anterior exacto) como para un rango custom.
- **`C:/Sistemas/ShowroomGriffin/ShowroomGriffin.Infrastructure/Services/AumentoMasivoService.cs`, `ShowroomGriffin.Web/Controllers/AumentoMasivoController.cs`, `ShowroomGriffin.Application/DTOs/Reportes/AumentoMasivoViewModels.cs`** — los 3 leidos completos, **match mas directo de reutilizacion de todo el proyecto** (mismo feature: por marca/categoria/modelo, sobre precio compra/venta, con preview obligatorio). Patron de flujo de 2 pasos (`ObtenerVariantesAsync`/`Preview` sin persistir + `AplicarAsync`/`Aplicar` con transaccion + concurrencia optimista via `RowVersion`) adoptado casi 1:1, adaptado de `VarianteProducto` (talle/color) a `Producto`/`Marca`/`Categoria` de marihogar. **Mejora deliberada sobre la referencia** (documentada en el propio XML-doc de `AumentoMasivoPrecioService`): ShowroomGriffin solo protege el intervalo interno de su propio metodo `AplicarAsync` (la query fresca hasta el `SaveChanges`, cuestion de milisegundos) porque no recibe de vuelta el `RowVersion` que el cliente vio en la previsualizacion; en marihogar el `RowVersion` viaja en Base64 desde el preview y vuelve explicito por producto al confirmar, comparandose contra el valor real en la base al momento de aplicar — protege la ventana de riesgo real (el tiempo que el Administrador tarda en revisar la previsualizacion antes de confirmar), no solo la ventana interna del metodo. Tambien se detecto y aplico el fix ya catalogado `REG-001` (MySQL no soporta `rowversion` store-generated: `RowVersion` se gestiona manualmente en `AppDbContext.OnBeforeSaveChanges`, reasignado con `Guid.NewGuid().ToByteArray()` en cada `Add`/`Modify` de `Producto`, con `IsConcurrencyToken().ValueGeneratedNever()` en la configuracion Fluent) — mismo patron exacto documentado en `docs/qa/regresiones-manuales.yml`.
- **M9 Dashboard y M17 Proyeccion financiera**: sin match de reutilizacion externa directo (son consultas agregadas especificas del dominio de marihogar, tal como anticipaba el alcance), implementados desde cero sobre los ledgers ya existentes (`MovimientoCCLocal`, `Cheque`, `OrdenCompra`) reutilizando exclusivamente patrones de diseño ya establecidos en el propio proyecto (transacciones, DTOs, `ServiceResult`, `SessionExtensions`).

#### Decisiones de implementacion no explicitas en el diseño (documentadas para trazabilidad)

1. **`Gasto` no tiene Edicion, solo Alta y Anulacion** (`GastoService.AnularAsync`, sin `UpdateAsync`). El diseño lista a Gasto dentro del grupo generico "Alta/Edicion" del wireframe, pero las HU-18.1/18.2 y CA-N24/CA-N25 solo piden alta + movimiento automatico. Editar un Gasto ya posteado en CC Local exigiria logica de reversion equivalente a un Cancelar; se opto por el mismo criterio ya usado en Venta/OrdenCompra (documentos financieros confirmados no se editan, solo transicionan de estado) — `Anulado`+`FechaAnulacion` como campos propios (no `SoftDestroyable.DeletedAt`, para que el Gasto anulado siga visible en el listado con badge, no desaparezca del grid por el query filter global).
2. **`FormaPagoGasto` es un enum propio de 5 valores** (Efectivo/Transferencia/MercadoPago/Cheque/Deposito), no reutiliza `MetodoPago` de Venta/OC ni se restringe a los 3 valores que expone la UI de Ventas — un gasto operativo (alquiler, sueldos) puede pagarse razonablemente con cualquiera de los 5 medios habituales del negocio.
3. **Proyeccion financiera — "OCs pendientes de pago" incluye `Confirmada` Y `Recibida`**, no solo `Confirmada` como sugiere literalmente el alcance del sprint. Motivo: una OC `Recibida` con saldo pendiente de pago es una deuda real ya reflejada en `MovimientoCCProveedor` (Cargo posteado) — un compromiso de pago tan concreto (o mas) que una OC todavia `Confirmada` sin recibir. Se aplico el mismo criterio de exclusion de pagos con cheque `Rechazado` que ya usa `PagoOrdenCompraService.RegistrarPagoAsync`/`OrdenCompraDetailDto.MontoPagado`, para que el saldo pendiente de OC nunca diverja del criterio ya establecido en Sprint 4. Verificado explicitamente (ver seccion de revision de codigo) que esto no duplica el monto de los Cheques Pendientes ya contados por separado: un Cheque Pendiente ya cuenta como "pagado" a los efectos del saldo de la OC (solo los Rechazados se excluyen), asi que `ChequesPorVencer` (dinero ya comprometido via cheque) y `SaldoPendienteOrdenesCompra` (dinero todavia sin ningun pago) son dos conjuntos disjuntos — sumarlos no sobreestima el compromiso.
4. **"PeriodoMeses" de la Proyeccion es simetrico**: es a la vez la ventana de historia para el promedio (CA-N20, "ultimos N meses") y el horizonte proyectado hacia adelante (CA-N23, "comparar distintos horizontes" 1/3/6 meses) — interpretacion mas consistente con HU-17.2 ("ajustar el periodo base... para comparar distintos horizontes") que fijar el horizonte de proyeccion en un valor distinto al de la ventana historica.
5. **Dashboard Administrador — KPIs financieros (`ChequesPorVencer`/`BalanceCaja`) con proteccion explicita por policy en sus propios endpoints AJAX**, ademas de estar ocultos en la UI de Vendedor. Se refactorizo el diseño inicial (una unica consulta agregada `ObtenerAdminAsync` resuelta sincronicamente en `DashboardController.Index`) a **5 endpoints AJAX independientes** (`GetVentasPeriodo`/`GetStockCritico`/`GetChequesPorVencer`/`GetBalanceCaja`/`GetProductosMasVendidos`), cada uno consumido por su propia card en `Dashboard/Admin.cshtml` con spinner propio — para cumplir literalmente HU-9.1 ("cada KPI carga de forma independiente, no bloquea el resto si uno tarda"), mismo patron ya usado por el panel de notificaciones de `_Layout.cshtml`. `GetChequesPorVencer`/`GetBalanceCaja` llevan `[Authorize(Policy = "RequireAdministracion")]` propio (defensa en profundidad, regla `REG-010` — un Vendedor no puede ver el balance de caja ni los cheques por vencer navegando directo a la URL del endpoint, aunque la UI ya se los oculte).
6. **Umbral de "cheques por vencer" del Dashboard es de 30 dias** (CA-N11), distinto del umbral de 7 dias que resalta filas en `Cheques/Index` (Sprint 4) — son dos usos distintos del mismo dato (alerta temprana de dashboard vs. resaltado visual de urgencia en el listado detallado), documentado explicitamente en el codigo para que no se confundan ni se unifiquen sin querer en un sprint futuro.
7. **Sin recepcion de fecha propia en `OrdenCompra` para "vencimiento de pago"**: el saldo pendiente de OC en la Proyeccion se computa sin filtro de fecha (todas las `Confirmada`/`Recibida` con saldo > 0 cuentan, sin importar cuando), porque `OrdenCompra` no modela una fecha de vencimiento de pago propia (a diferencia de `Cheque.FechaVencimiento`) — documentado como limitacion conocida, no bug.

#### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/CategoriaGasto.cs` (Alquiler/Servicios/Sueldos/Flete/Otro), `Enums/FormaPagoGasto.cs` (Efectivo/Transferencia/MercadoPago/Cheque/Deposito) — nuevos.
- `Entities/Gasto.cs` — nueva, hereda `SoftDestroyable`. Campos propios `Anulado`/`FechaAnulacion` en vez de usar `DeletedAt` para la anulacion (ver decision #1).
- `Entities/Producto.cs` — agregado `RowVersion` (`byte[]`, default `new byte[16]`), token de concurrencia optimista para M16 (ver decision de reutilizacion de `REG-001`).

**Application** (`MariHogar.Application`)
- `DTOs/GastoDtos.cs` — `GastoListItemDto`, `GastoFiltro` (Categoria/FormaPago/Descripcion/rango de fecha), `GastoInput`.
- `DTOs/CajaDtos.cs` — `CajaFiltro`, `CajaPeriodoDto` (con `VariacionIngresosPorc`/`VariacionEgresosPorc` calculados, null-safe ante periodo anterior en $0).
- `DTOs/AumentoMasivoDtos.cs` — enums `AlcanceAumento`/`TargetPrecioAumento`, `AumentoMasivoPreviewInput`, `AumentoMasivoPreviewItemDto` (incluye `RowVersion` en Base64), `AumentoMasivoAplicarItemInput`, `AumentoMasivoAplicarInput`.
- `DTOs/ProyeccionFinancieraDtos.cs` — `ProyeccionFinancieraDto` (propiedades calculadas `GastosComprometidos`/`EgresosProyectadosTotal`/`TieneDeficit`).
- `DTOs/DashboardDtos.cs` — `ProductoMasVendidoDto`, `VentasPeriodoDto`, `ChequesPorVencerDashboardDto`, `DashboardVendedorDto` (sin un DTO agregado unico para Admin, ver decision #5).
- `Interfaces/IGastoService.cs`, `Interfaces/ICajaService.cs`, `Interfaces/IAumentoMasivoPrecioService.cs`, `Interfaces/IProyeccionFinancieraService.cs`, `Interfaces/IDashboardService.cs` — nuevas.
- `Interfaces/IProductoService.cs` — agregado `ListarModelosDistintosAsync()` (combo de M16, criterio "por modelo").

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — `DbSet<Gasto>` + Fluent config (`Descripcion` maxlength 500, indices `Categoria`/`Fecha`); config de `Producto.RowVersion` (`IsConcurrencyToken().ValueGeneratedNever().IsRequired()`); `OnBeforeSaveChanges` reasigna `producto.RowVersion = Guid.NewGuid().ToByteArray()` en cada `Add`/`Modify` de `Producto` (patron `REG-001`, mismo criterio que ShowroomGriffin).
- `Services/GastoService.cs` — `CrearAsync` (transaccion unica: Add Gasto → SaveChanges → `ICCLocalService.RegistrarMovimientoAsync(Egreso,...)` → SaveChanges → Commit), `AnularAsync` (contramovimiento `Ingreso` con `EsReversion=true`, nunca borra el original).
- `Services/CajaService.cs` — `ObtenerResumenAsync` agrega `MovimientoCCLocal` por rango de fecha (default: mes calendario actual) + periodo anterior (mismo rango desplazado 1 mes).
- `Services/AumentoMasivoPrecioService.cs` — `ObtenerPreviewAsync` (100% lectura, `AsNoTracking`, sin `SaveChanges`), `AplicarAsync` (fetch acotado por Ids de la previsualizacion, comparacion explicita de `RowVersion` por producto antes de mutar, `AuditLog` manual de la operacion batch + transaccion + `DbUpdateConcurrencyException` como backstop).
- `Services/ProyeccionFinancieraService.cs` — `ObtenerAsync(periodoMeses)`: promedio historico sobre `MovimientoCCLocal` + cheques Pendientes en el horizonte + saldo pendiente de OC (`Confirmada`/`Recibida`, excluyendo pagos con cheque Rechazado).
- `Services/DashboardService.cs` — `ObtenerVentasPeriodoAsync`, `ObtenerChequesPorVencerAsync`, `ObtenerProductosMasVendidosAsync`, `ObtenerVendedorAsync` (metodos independientes, ver decision #5 — nunca un unico metodo "traer todo").
- `Services/ProductoService.cs` — agregado `ListarModelosDistintosAsync()`.
- `DependencyInjection.cs` — registrados `IGastoService`, `ICajaService`, `IAumentoMasivoPrecioService`, `IProyeccionFinancieraService`, `IDashboardService` (Scoped).

**Web** (`MariHogar.Web`)
- `Controllers/GastosController.cs`, `Controllers/CajaController.cs`, `Controllers/AumentoMasivoPreciosController.cs`, `Controllers/ProyeccionFinancieraController.cs`, `Controllers/DashboardController.cs` — nuevos. Los primeros 4 con `[Authorize(Policy = "RequireAdministracion")]` a nivel clase (Gastos/Caja/Aumento masivo/Proyeccion son exclusivos de Administrador). `DashboardController` con `[Authorize]` simple a nivel clase (cualquier rol autenticado entra, la diferenciacion Admin/Vendedor ocurre adentro) + `[Authorize(Policy = "RequireAdministracion")]` puntual en los 2 endpoints financieros (`GetChequesPorVencer`/`GetBalanceCaja`).
- `Controllers/HomeController.cs` — `Index()` ahora redirige a `Dashboard/Index` cuando el usuario esta autenticado (una linea agregada), preservando intacto el resto del controller (`Error`/`StatusCode`, referenciados por `Program.cs`).
- `Models/GastoViewModels.cs`, `Models/CajaViewModels.cs`, `Models/AumentoMasivoViewModels.cs`, `Models/ProyeccionFinancieraViewModels.cs`, `Models/DashboardViewModels.cs` — nuevos.
- `Views/Gastos/{Index,Create}.cshtml` — Index con 4 filtros (Categoria, FormaPago, Descripcion texto libre, rango de fecha) persistidos en sesion + badge Activo/Anulado + accion Anular con SweetAlert2; Create card unica con alerta informativa ("se genera un movimiento de egreso automaticamente").
- `Views/Caja/Index.cshtml` — 3 KPI cards (Ingresos/Egresos/Balance del periodo con indicador de variacion % vs mes anterior, semantica de color correcta: egreso que sube es rojo) + tabla comparativa periodo actual vs anterior + link a CC Local para el detalle.
- `Views/AumentoMasivoPrecios/Index.cshtml` — formulario paso 1 (criterio+porcentaje) → previsualizacion paso 2 (AJAX, tabla precio actual/nuevo) → confirmar (AJAX, SweetAlert2, envia de vuelta el `RowVersion` de cada fila previsualizada). Cualquier cambio en el criterio despues de previsualizar invalida la previsualizacion vigente (evita confirmar contra un criterio distinto al previsualizado).
- `Views/ProyeccionFinanciera/Index.cshtml` — selector de periodo (1/3/6 meses) + alerta de deficit condicional + texto aclaratorio de "estimacion, no compromiso exacto" siempre visible + 3 KPI cards de detalle (egresos promedio, cheques por vencer, OC pendientes) + card de egresos proyectados totales.
- `Views/Dashboard/{Admin,Vendedor}.cshtml` — Admin con 4 KPI cards + card de productos mas vendidos, **cada una completada por su propio `$.get` independiente** (spinner propio, manejo de error propio, ver decision #5); Vendedor con card de ventas de hoy + card de leads pendientes (placeholder deshabilitado) + 3 accesos directos (Nueva venta/Nuevo presupuesto/Entregas).
- `Views/Shared/_Layout.cshtml` — sidebar: "Dashboard" como primer link de la seccion Principal (todos los roles autenticados); "Gastos"/"Caja mensual"/"Proyeccion financiera" agregados a la seccion "Financiero" ya existente; "Aumento masivo de precios" agregado a la seccion "Catalogo" (solo Administrador).

#### Migracion EF generada

- `20260724200956_AddGastos` — crea tabla `Gastos` (Categoria/Monto/FormaPago/Fecha/Descripcion/Anulado/FechaAnulacion + columnas de auditoria de `SoftDestroyable`, indices `Categoria`/`Fecha`) y agrega la columna `RowVersion` (`longblob`) a `Productos`. **Particularidad tecnica real de MySQL manejada correctamente**: MySQL no permite `DEFAULT` literal en columnas `BLOB`/`TEXT`/`JSON` ("BLOB, TEXT, GEOMETRY or JSON column 'RowVersion' can't have a default value"), asi que la migracion agrega la columna como nullable, la backfillea con `UUID_TO_BIN(UUID())` (valor unico por fila, para no violar la futura constraint `NOT NULL` con todas las filas en el mismo valor) y recien despues la vuelve `NOT NULL` — patron de 3 pasos (`AddColumn` nullable → `Sql` backfill → `AlterColumn` not null) necesario especificamente para este tipo de columna en este motor. **Generada y aplicada** exitosamente contra `marihogar_dev` (`dotnet ef database update` confirma `Done.` sin errores; `dotnet ef migrations list` confirma 6 migraciones totales — `InitialCreate`, `AddCatalogo`, `AddPresupuestosVentas`, `AddEntregas`, `AddComprasCCProveedoresCheques`, `AddGastos` — ninguna pendiente).

#### Evidencia de cierre — build + migracion + revision de codigo (sin smoke test, regla de proceso vigente)

- `dotnet build MariHogar.slnx` → **Compilacion correcta, 0 errores** (9 warnings, todos preexistentes: `NU1902` de MailKit/MimeKit + `CS0114` de `HomeController.StatusCode`; ninguno introducido por este sprint). Build re-ejecutado despues del ultimo ajuste (filtro de Descripcion en Gastos) y confirmado limpio otra vez.
- Migracion `20260724200956_AddGastos` generada y **aplicada** contra `marihogar_dev` (`dotnet ef database update` → `Done.`; `dotnet ef migrations list` → 6 migraciones, ninguna pendiente).
- **Revision de codigo propia, linea por linea, con foco explicito en los 3 puntos pedidos**:
  1. **`GastoService.CrearAsync`/`AnularAsync`** (movimiento de CC Local correcto y atomico): confirmado que `CrearAsync` valida (Monto>0, Fecha no futura, Descripcion obligatoria) **antes** de abrir la transaccion → `Add(Gasto)` → `SaveChangesAsync` (asigna `gasto.Id`, usado como `OrigenId`) → `ICCLocalService.RegistrarMovimientoAsync(TipoMovimientoCC.Egreso, gasto.Monto, "Gasto", gasto.Id, esReversion:false, ...)` (confirmado que este metodo NO llama `SaveChangesAsync` propio, mismo patron exacto que `VentaService`/`OrdenCompraService`) → `SaveChangesAsync` final → `CommitAsync`, con `RollbackAsync` en el `catch`. Signo correcto (`Egreso`, resta del saldo de CC Local via `ObtenerSaldoActualAsync` = `Σ Ingreso − Σ Egreso`). `AnularAsync` genera el contramovimiento correcto: `TipoMovimientoCC.Ingreso` (neutraliza exactamente el `Egreso` original) con `esReversion:true` y el mismo `OrigenId` del Gasto — nunca borra ni modifica el movimiento original, mismo criterio de todos los ledgers del proyecto. Guard `gasto.Anulado` ya `true` → rechaza doble anulacion.
  2. **Previsualizacion de Aumento masivo no persiste nada**: confirmado leyendo `AumentoMasivoPrecioService.ObtenerPreviewAsync` completo — la unica operacion es una query `AsNoTracking()` + proyeccion en memoria (calculo de `PrecioNuevo` con `Math.Round(precio * multiplicador, 2)`), **cero** llamadas a `SaveChangesAsync`/`Add`/`Update`/`Remove` en todo el metodo. `AumentoMasivoPreciosController.Preview()` (el endpoint HTTP que lo expone) tambien es de solo lectura: arma el input, valida, llama al Service y devuelve JSON, sin ningun efecto de persistencia. `Aplicar()`/`AplicarAsync` (la unica via de persistencia) exige explicitamente los `Items` con sus `RowVersion` — si el usuario nunca hizo click en "Previsualizar" (y por lo tanto `previewItems` esta vacio en el JS), el boton "Confirmar aumento" no tiene datos que enviar (`if (previewItems.length === 0 || !criterioUsado) return;` en el script de la vista) y el Service igual rechaza con "No hay productos para aplicar" si `Items.Count == 0` — doble guardia cliente+servidor.
  3. **Formula de Proyeccion financiera** (signos, ventana de fechas, sin duplicar cheques/OC ya en CC Local): confirmado que `MovimientoCCLocal` **nunca** recibe movimientos de pagos a proveedores/cheques (verificado leyendo `PagoOrdenCompraService.RegistrarPagoAsync` completo: solo escribe `PagoOrdenCompra`+`Cheque`+`MovimientoCCProveedor`, jamas `MovimientoCCLocal`) — por lo tanto el promedio historico (sobre `MovimientoCCLocal`) y los compromisos conocidos (Cheques+OC, sobre tablas de Compras) son fuentes disjuntas, sumarlas no duplica nada. Ventana historica: `hoy.AddMonths(-periodo)` hasta `hoy` (exclusivo), promedio = total/periodo. Horizonte de cheques: `FechaVencimiento <= hoy.AddMonths(periodo)`, sobre `Estado==Pendiente` (nunca cuenta cheques ya `Acreditado`/`Rechazado`). Saldo de OC: `Total − Σ Pagos(Cheque==null || Cheque.Estado!=Rechazado)`, mismo criterio exacto que `OrdenCompraDetailDto.MontoPagado` (Sprint 4) — confirmado que un Cheque `Pendiente` cuenta como "pagado" a los efectos del saldo de OC (no se duplica contra `ChequesPorVencer`, ver decision #3 de arriba). `TieneDeficit = GastosComprometidos > IngresosProyectados` (comparacion literal de CA-N22, sin mezclar el promedio de egresos operativos en la comparacion de la alerta).
  4. **Verificacion adicional (defensa en profundidad, no pedida explicitamente pero detectada durante la revision)**: los 2 endpoints AJAX del Dashboard que exponen datos financieros exclusivos de Administrador (`GetChequesPorVencer`/`GetBalanceCaja`) inicialmente solo heredaban el `[Authorize]` generico de la clase (cualquier usuario autenticado, incluido Vendedor) — corregido antes de cerrar, agregando `[Authorize(Policy = "RequireAdministracion")]` puntual a esos 2 endpoints (regla `REG-010`/`32-estandares-qa-implementador.instructions.md`: nunca ocultar un dato en la vista sin proteger tambien el endpoint).
  5. **RowVersion / MySQL (`REG-001`)**: confirmado que `Producto.RowVersion` esta configurado `IsConcurrencyToken().ValueGeneratedNever()` (nunca `ValueGeneratedOnAdd`, que MySQL no soporta para columnas `BLOB`) y que `AppDbContext.OnBeforeSaveChanges` reasigna el valor manualmente en cada `Add`/`Modify` de `Producto` — mismo patron exacto que el fix ya catalogado para `VarianteProducto` de ShowroomGriffin.

#### Checklist de verificacion manual para el usuario (equivalente al smoke test que ya no ejecuta el Implementador)

Con la app corriendo (`dotnet run --project MariHogar.Web`) y logueado como Administrador (salvo que se indique otro rol):

**M18 — Gastos**
1. `Gastos > Nuevo gasto` → completar Categoria/Monto/Forma de pago/Fecha/Descripcion → Guardar → verificar que redirige al listado con el gasto nuevo, badge "Activo".
2. Ir a `Cuenta corriente del local` (CCLocal) → verificar que aparece un movimiento "Egreso" nuevo por el mismo monto, con `OrigenTipo=Gasto`, y que el saldo actual bajo en esa cantidad.
3. Volver a `Gastos`, click "Anular" en ese gasto (confirmar en el SweetAlert2) → verificar que el badge pasa a "Anulado" (el gasto sigue visible en la grilla, no desaparece) → en `CCLocal`, verificar que aparece un nuevo movimiento "Ingreso" con badge de reversion, y que el saldo vuelve al valor previo al gasto.
4. Probar los 4 filtros (Categoria, Forma de pago, Descripcion, rango de fecha) uno por uno y "Limpiar filtros".

**M15 — Caja mensual**
5. Ir a `Caja mensual` → verificar que muestra Ingresos/Egresos/Balance del mes actual y el comparativo contra el mes anterior (si no hay movimientos del mes anterior, debe decir "Sin datos... para comparar" en vez de un error o un % raro).
6. Cambiar el rango de fecha con el selector → verificar que los 3 totales y la tabla comparativa se recalculan para el nuevo rango.

**M16 — Aumento masivo de precios**
7. Ir a `Aumento masivo de precios` → elegir "Por categoria" + una categoria con productos + "Precio de venta" + 10% → click "Previsualizar" → verificar que aparece la tabla con precio actual/nuevo (nuevo = actual x 1.10) **y que los precios en `Productos/Index` todavia NO cambiaron** (paso critico: confirmar que previsualizar no aplico nada).
8. Click "Confirmar aumento" (aceptar el SweetAlert2) → verificar que ahora si, en `Productos/Index`, los precios de venta de esos productos subieron un 10%.
9. Repetir el paso 7 con un criterio distinto sin confirmar, y verificar que cambiar cualquier campo del formulario despues de previsualizar hace desaparecer la tabla de previsualizacion (hay que previsualizar de nuevo antes de poder confirmar).

**M17 — Proyeccion financiera**
10. Ir a `Proyeccion financiera` → probar el selector de periodo (1/3/6 meses) → verificar que los numeros de "Ingresos proyectados"/"Egresos comprometidos" cambian al cambiar el periodo.
11. Si hay cheques Pendientes con vencimiento proximo o OCs con saldo pendiente, verificar que aparecen contados en las cards correspondientes con la cantidad correcta.
12. Verificar que el texto aclaratorio ("estimacion, no un compromiso exacto") esta siempre visible, y que la alerta roja de deficit solo aparece cuando Gastos comprometidos > Ingresos proyectados.

**M9 — Dashboard**
13. Loguearse como Administrador → verificar que la pantalla de inicio (`/` o `/Home`) redirige automaticamente a `/Dashboard` → verificar que las 4 cards superiores + la card de productos mas vendidos muestran un spinner brevemente y despues se completan cada una por separado (no toda la pantalla en blanco esperando a todas).
14. Cambiar el rango de fecha del Dashboard → verificar que "Ventas del periodo" y "Productos mas vendidos" se recalculan (Stock critico/Cheques por vencer/Balance de caja NO deberian cambiar, son de "estado actual").
15. Loguearse como Vendedor → verificar que ve un dashboard reducido (ventas de hoy + accesos directos), sin ninguna card financiera, y que navegar directo a `/Dashboard/GetBalanceCaja` o `/Dashboard/GetChequesPorVencer` responde 403 (Acceso denegado) en vez de devolver datos.

#### Riesgos residuales

- **Sin verificacion en caliente**: por la regla de proceso vigente, ningun tramo de este sprint fue ejecutado contra la app real ni contra MySQL real en este ciclo (mas alla de `dotnet build`/`dotnet ef database update`) — toda la evidencia funcional es revision de codigo linea por linea. La checklist de 15 pasos de arriba es la unica forma de cerrar ese riesgo, a cargo del usuario.
- **Cancelar una OC `Confirmada` que ya tiene un pago/cheque registrado (anticipo) no revierte ese pago/cheque** — gap heredado de `OrdenCompraService.CancelarAsync` (Sprint 4, no tocado este sprint): si esto ocurre, un Cheque `Pendiente` de una OC ya `Cancelada` seguiria contando en `ProyeccionFinancieraService.ObtenerChequesPorVencerAsync`/Dashboard, sobreestimando levemente los compromisos futuros. Escenario de baja probabilidad (exige cancelar una OC con anticipo ya cargado) y no introducido por este sprint — documentado para un sprint de endurecimiento futuro si el cliente lo pide (la correccion natural seria que `OrdenCompraService.CancelarAsync` revierta o marque como invalidados los pagos/cheques asociados al cancelar una `Confirmada` con anticipo).
- **`Gasto` no tiene Edicion** (ver decision #1) — si el cliente pide poder corregir un monto/fecha sin pasar por Anular+Nuevo Gasto, es un cambio de alcance menor (nuevo `UpdateAsync` con logica de reversion+reposteo del movimiento de CC Local).
- **Concurrencia de `AumentoMasivoPrecioService.AplicarAsync` bajo alta frecuencia de ediciones manuales de Producto simultaneas**: mitigada con `RowVersion` (mejor cobertura que la referencia de ShowroomGriffin, ver decision de reutilizacion), pero sigue siendo optimista (rechaza y pide reintentar, no bloquea) — comportamiento esperado y aceptado, consistente con el resto de los guards de concurrencia ya aceptados en sprints anteriores del proyecto.
- **Nota de proceso** (ver arriba): parte sustancial del codigo de este sprint fue escrita por un proceso concurrente detectado durante la sesion, no por esta ejecucion desde cero. Se aplico exactamente el mismo nivel de escrutinio (revision linea por linea + build + migracion) que si se hubiera escrito integramente en esta sesion, y se corrigieron las inconsistencias reales encontradas (ver arriba) antes de cerrar.

#### Proximos pasos pendientes (Sprint 5)

- Este era el ultimo sprint funcional planificado de los 6 (M1 CRM de Leads y M8 Bot WhatsApp de Etapa 2 siguen en pausa, sin fecha). Pendiente que QA valide este sprint y que el orquestador confirme si el plan de 6 sprints se da por cerrado o si el cliente pide un sprint adicional de cierre/hardening (ej. resolver los 2 riesgos residuales de arriba, completar las checklists manuales acumuladas de sprints anteriores).
- M7 (Facturacion AFIP/ARCA) y M8 (Bot WhatsApp) quedan como los unicos modulos del alcance original de 18 sin implementar, ambos con dependencias externas ya documentadas desde el analisis funcional (certificado .p12, numero de WhatsApp dedicado).

---

### Sprint 6 (de 6, ultimo sprint de Etapa 1) — M7 Facturacion electronica AFIP/ARCA

**Cierra el alcance funcional completo de Etapa 1.** Alcance cerrado segun `2-disenador-funcional.md` (HU-7.1 a HU-7.4, wireframe "Comprobante AFIP (alta desde Venta)") y `3-arquitecto-mvc.md` (entidades `ComprobanteAfip`/`ComprobanteAfipItem`, `IAfipService`, migracion `AddComprobantesAfip`, riesgo tecnico del certificado .p12), sin desvio de alcance respecto de lo aprobado. NO se toco CRM/Bot (Etapa 2, sigue en espera). Unico cambio fuera del modulo propio: habilitar el boton "Facturar" (placeholder deshabilitado desde Sprint 2 en la pantalla de exito de `Ventas/Create.cshtml`) y agregar la card "Comprobante AFIP" en `Ventas/Details.cshtml`, ademas de volver real el guard `VentaService.TieneComprobanteAsociadoAsync` (placeholder `false` desde Sprint 2/3).

#### Reutilizacion cross-proyecto obligatoria (confirmada por el cliente) — codigo real leido completo

Se leyeron completos, antes de escribir cualquier linea de `AfipService`, los 3 archivos indicados explicitamente por el cliente:
- **`C:/Sistemas/delicias-naturales/Helper/FacturacionAfipService.cs`** (213 lineas) — service de facturacion AFIP en produccion. Reutilizado el **conocimiento del protocolo**: login WSAA previo a cualquier llamada de negocio, cacheo del token (aca formalizado con vencimiento real informado por AFIP, no un TTL fijo asumido), armado de `FECAERequest` (cabecera `PtoVta`/`CantReg`/`CbteTipo` + detalle con `Concepto`/`DocTipo`/`DocNro`/`CbteDesde`/`CbteHasta`/`CbteFch`/importes/IVA), consulta previa de `FECompUltimoAutorizado` para calcular el proximo numero de comprobante, y lectura de la respuesta (`Resultado`, `CAE`, `CAEFchVto`, `Observaciones` en caso de rechazo).
- **`C:/Sistemas/delicias-naturales/Models/Afip/LoginTicket.cs`** (244 lineas) — modelo del ticket WSAA. Reutilizado el flujo completo del login: armado del XML `loginTicketRequest` (header con `uniqueId`/`generationTime`/`expirationTime` +/-10 min, `service`), firma CMS/PKCS#7 del XML con el certificado `.p12` (`EndCertOnly`, `ComputeSignature`), envio del CMS en Base64 al WSAA, y parseo de la respuesta (`token`/`sign`/`expirationTime`).
- **`C:/Sistemas/delicias-naturales/Web References/ws_factura_afip/Reference.cs`** — cliente SOAP autogenerado del WSDL de WSFEv1 (ASMX/Web References, .NET Framework). Reutilizado el mapeo exacto de campos del contrato SOAP (nombres de elementos, estructura `FeCabReq`/`FeDetReq`, `AlicIva`, `Tributo`), no el codigo generado en si (incompatible con .NET 10).

**No se copio codigo C# literal** de ningun archivo (stack incompatible: delicias-naturales es .NET Framework 4.7.2 + EF6 + Web References/ASMX autogenerados vs. .NET 10 + Clean Architecture). Se porto el **conocimiento del protocolo** (que llamar, en que orden, que campos van en el request, como se lee la respuesta), adaptado con una decision tecnica propia documentada abajo.

#### Decision tecnica: SOAP armado a mano (`System.Xml.Linq` + `HttpClient`), no proxy generado con `dotnet-svcutil`

Se evaluo generar un proxy SOAP con `dotnet-svcutil` a partir del WSDL real de AFIP (WSAA y WSFEv1) y se descarto por una razon concreta: `dotnet-svcutil` necesita descargar el WSDL (y sus imports/XSD referenciados) en el momento de generar el proxy, lo que exige conectividad real contra los endpoints de AFIP en el momento de desarrollar — sin garantia de que ese acceso este disponible en todos los entornos de trabajo del equipo, y sin ningun beneficio real dado que **tampoco hay forma de probar el proxy generado contra AFIP real en este entorno** (no hay certificado `.p12` todavia). En su lugar, `AfipService` arma los envelopes SOAP con `System.Xml.Linq` (`XElement`/`XDocument`, con escape automatico de XML — evita el riesgo de inyeccion/malformación de armar XML por concatenacion de strings) y los envia con `HttpClient` (`IHttpClientFactory`, cliente nombrado `"Afip"` sin el header `Accept: application/json` del cliente `"default"` ya existente). El costo de esta decision es mantener el mapeo de campos a mano (sin tipos generados); el beneficio es no depender de la disponibilidad del WSDL de AFIP para poder compilar el proyecto, y no perder tiempo generando un proxy que de todas formas no se puede validar contra AFIP real en este ciclo. Decision documentada explicitamente en el XML-doc de la clase `AfipService`.

La firma CMS/PKCS#7 del `loginTicketRequest` usa `System.Security.Cryptography.Pkcs` (`SignedCms`/`CmsSigner`/`ContentInfo`, con `IncludeOption = EndCertOnly`) — disponible inbox en .NET 10 (confirmado por build limpio, sin necesidad de agregar el paquete NuGet explicito), mismo mecanismo criptografico exacto que `CertificadosX509Lib.FirmaBytesMensaje` de delicias-naturales. La carga del certificado `.p12` usa `X509CertificateLoader.LoadPkcs12FromFile` (API vigente en .NET 9+, reemplaza los constructores de `X509Certificate2` marcados obsoletos `SYSLIB0057`).

#### Decisiones de implementacion no explicitas en el diseño (documentadas para trazabilidad)

1. **Token WSAA cacheado por vencimiento real informado por AFIP, no un TTL fijo de 24hs asumido a ciegas.** El alcance de este sprint menciona "renovar el token cada 24hs" (HU-7.4) como forma coloquial de describir el comportamiento esperado, pero AFIP informa su propio `expirationTime` en la respuesta del WSAA (en la practica suele ser ~12hs, no exactamente 24). `AfipTokenCache` (nuevo, **Singleton** — a diferencia de `AfipService`, que es Scoped, precisamente para que el token sobreviva entre requests) cachea el `Token`/`Sign` junto con la `expirationTime` real devuelta por AFIP, y lo considera vencido 10 minutos antes de esa fecha real (margen de seguridad, no un valor inventado). Un `SemaphoreSlim` evita que 2 requests concurrentes disparen 2 logins WSAA en simultaneo.
2. **`AfipComprobanteRequestDto` NO recibe una fecha de comprobante provista por el caller.** AFIP exige que `CbteFch` sea la fecha real (Argentina) del momento exacto de la llamada a `FECAESolicitar`, con una ventana de tolerancia chica (Concepto=1, Productos, exige +/-5 dias). Si `ComprobanteAfipService` fijara la fecha al crear el `ComprobanteAfip` y ese comprobante se reintentara dias despues (HU-7.3, "reintentable"), reenviar la fecha vieja original podria hacer que AFIP lo rechace por un motivo totalmente ajeno al del primer intento. `AfipService` calcula la fecha de "hoy" en huso horario Argentina en el momento exacto de cada intento (creacion o reintento), nunca reutiliza una fecha guardada. `ComprobanteAfip.Fecha` (el campo persistido, usado para el listado/auditoria) sigue siendo la fecha de creacion del registro — son dos conceptos distintos a proposito.
3. **`CondicionIVAReceptorId` incluido en el request** (campo real, obligatorio en el WSFEv1 vigente desde la RG 5616/2024, no existia cuando delicias-naturales se escribio originalmente). Factura A implica receptor Responsable Inscripto (1); Factura B en este alcance se emite siempre como Consumidor Final (5) — el diseño no distingue Monotributista/Exento en la captura de datos fiscales del cliente, asi que se tomo el valor mas general y comun para comercio minorista. Documentado como limitacion conocida (no bug) en el codigo.
4. **Alicuota de IVA uniforme (21% por defecto, configurable via `Afip:PorcentajeIva`), no por producto.** `Producto` no modela una alicuota de IVA propia (fuera del alcance de M2/M7 tal como fueron definidos en Arquitectura) — `AfipService.CalcularNetoIva` separa el total (IVA incluido) en Neto+IVA aplicando la tasa configurada de forma uniforme a todo el comprobante. Documentado como limitacion conocida: si el negocio llega a vender productos con IVA diferencial (10.5%/0%/exento), este calculo deja de ser preciso y requeriria agregar una alicuota por `Producto` — cambio de alcance medio, no trivial.
5. **`ComprobanteAfip.PuntoVenta` se persiste en el comprobante, no se lee de `appsettings` en cada consulta posterior.** El punto de venta usado al emitir queda fijo en el registro — si el negocio cambia de punto de venta AFIP en el futuro (escenario raro, pero posible), los comprobantes historicos no cambian de valor retroactivamente.
6. **`TieneComprobanteAsociadoAsync` (guard de cancelacion de Venta, HU-5.4) solo bloquea con un comprobante en estado `Emitido`.** Un comprobante en `Error` nunca llego a existir para AFIP (no tiene CAE, no es un documento fiscal real) — no tiene sentido que bloquee la cancelacion de la Venta. Mismo criterio aplicado a `VentaListItemDto.TieneComprobante` (listado) y a la card "Comprobante AFIP"/boton "Cancelar venta" de `Ventas/Details.cshtml`.
7. **El boton "Facturar" no exige que la Venta este Pagada/PagadaParcial** (a diferencia de "Programar entrega", que si lo exige). En Argentina la emision de una factura no depende del cobro — se puede facturar una venta con pago pendiente. El unico gate real es `CantidadPendienteDeFacturar > 0` (queda algo sin facturar) y que la Venta no este Cancelada.
8. **`ComprobanteAfipItem` hereda `SoftDestroyable`**, no es un ledger inmutable — es una linea de un documento con maquina de estados propia (`ComprobanteAfip.Estado`), mismo criterio que `VentaItem`/`PresupuestoItem`/`OrdenCompraItem` (documentos, no ledgers de movimientos como `MovimientoStock`/`MovimientoCCLocal`).
9. **Riesgo residual identificado durante la revision de codigo, documentado sin mitigar en este sprint** (cambio minimo, fuera del alcance de "cambios minimos" pedido): si `IAfipService.EmitirAsync` retorna `Exito=true` (AFIP ya emitio el CAE de verdad, del lado de AFIP) pero el `SaveChangesAsync` posterior que persiste ese resultado en `ComprobanteAfip` falla (ej. corte de conexion a MySQL en ese instante exacto), la transaccion completa hace `RollbackAsync` — incluida la fila `Pendiente` insertada al principio — y el sistema pierde el registro local de un CAE que **si existe realmente en AFIP** (documento fiscal ya emitido, huerfano de constancia local). El proximo intento de facturar esos mismos items pediria un numero de comprobante nuevo a AFIP (correcto, sin duplicar numero, porque `FECompUltimoAutorizado` ya refleja el numero usado), pero el comprobante huerfano quedaria sin PDF ni registro visible en el sistema. Riesgo de probabilidad muy baja (ventana de milisegundos entre la respuesta HTTP de AFIP y el `SaveChangesAsync`), consistente con otros riesgos de concurrencia ya aceptados en sprints anteriores (Sprint 2 "Convertir a venta", Sprint 4 `ChequeAcreditacionHostedService` multi-instancia) — no mitigado por directriz de cambios minimos, documentado para un sprint de endurecimiento futuro si el cliente lo pide (mitigacion natural: persistir el resultado crudo de AFIP en un log estructurado *antes* de intentar el `SaveChangesAsync`, para poder reconciliar manualmente un caso asi).

#### Archivos y capas modificadas

**Domain** (`MariHogar.Domain`)
- `Enums/TipoComprobanteAfip.cs` — nuevo, `FacturaA=1`/`FacturaB=6` (valores que coinciden deliberadamente con el codigo AFIP `CbteTipo` real, sin mapeo adicional).
- `Enums/EstadoComprobanteAfip.cs` — nuevo, `Pendiente=1`/`Emitido=2`/`Error=3`.
- `Entities/ComprobanteAfip.cs` — nueva, hereda `SoftDestroyable`. `VentaId`, `TipoComprobante`, `PuntoVenta`, `NumeroComprobante` (`long?`), `CAE`, `VencimientoCAE`, `Estado`, `DetalleError`, `ClienteNombre`/`ClienteCUIT`/`ClienteDNI` (datos fiscales capturados en el momento, mismo patron que `Venta.ClienteNombre` de Sprint 2 — no existe entidad Cliente en este alcance), `Total`, `Fecha`, coleccion `Items`.
- `Entities/ComprobanteAfipItem.cs` — nueva, hereda `SoftDestroyable`. `ComprobanteAfipId`, `VentaItemId`, `Cantidad`, `PrecioUnitario` (copiado del `VentaItem` al momento de emitir, registro historico).

**Application** (`MariHogar.Application`)
- `Settings/AfipSettings.cs` — nuevo. `Ambiente` (Homologacion/Produccion), `CertificadoPath`/`CertificadoPassword`, `CUIT`, `PuntoVenta`, `PorcentajeIva`, URLs de WSAA/WSFE por ambiente con los valores reales de homologacion/produccion de AFIP ya cargados por defecto. `WsaaUrl`/`WsfeUrl` resuelven segun `Ambiente`.
- `DTOs/AfipDtos.cs` — nuevo. `AfipComprobanteRequestDto` (contrato de bajo nivel hacia AFIP, sin `FechaComprobante` — ver decision #2), `AfipEmisionResultDto` (`Exito`/`NumeroComprobante`/`CAE`/`VencimientoCAE`/`DetalleError`).
- `DTOs/ComprobanteAfipDtos.cs` — nuevo. `ComprobanteAfipListItemDto`/`Filtro`, `ComprobanteAfipDetailDto`, `ComprobanteAfipItemDto`, `VentaItemParaFacturarDto` (expone `CantidadPendiente` calculado), `ComprobanteAfipPrecargaDto`, `ComprobanteAfipInput`/`ComprobanteAfipItemInput`.
- `DTOs/VentaDtos.cs` — `VentaDetailDto` ampliado con `ComprobanteAfipId`/`ComprobanteAfipEstado` (ultimo comprobante asociado, cualquier estado) y `CantidadPendienteDeFacturar` (suma de `Cantidad - CantidadFacturada` de todos los items); comentario de `VentaListItemDto.TieneComprobante` actualizado (ya no placeholder).
- `Interfaces/IAfipService.cs` — nuevo. Un unico metodo, `EmitirAsync(AfipComprobanteRequestDto) -> AfipEmisionResultDto`, documentado explicitamente como "nunca lanza excepcion de negocio, siempre devuelve `Exito=false` + `DetalleError`" (HU-7.3).
- `Interfaces/IComprobanteAfipService.cs` — nuevo. `ListarAsync`, `GetByIdAsync`, `ObtenerPrecargaAsync`, `EmitirAsync`, `ReintentarAsync`, `GenerarPdfAsync`.

**Infrastructure** (`MariHogar.Infrastructure`)
- `Data/AppDbContext.cs` — `DbSet<ComprobanteAfip>`/`DbSet<ComprobanteAfipItem>` + Fluent config (precision decimal 18,2, longitudes de string, FK `ComprobanteAfip.VentaId` -> `Ventas` `Restrict`, FK `ComprobanteAfipItem.ComprobanteAfipId` -> `Cascade` (hijo de su padre) y `VentaItemId` -> `Restrict`, indices `VentaId`/`Estado`/`Fecha`/`ComprobanteAfipId`/`VentaItemId`).
- `Services/AfipTokenCache.cs` — nuevo, **Singleton**. Cachea Token/Sign de WSAA con la `expirationTime` real de AFIP (margen de seguridad de 10 min), `SemaphoreSlim` para evitar logins concurrentes duplicados, metodo `Invalidar()` para forzar un login nuevo si AFIP llegara a rechazar un token que localmente se consideraba vigente.
- `Services/AfipService.cs` — nuevo. `EmitirAsync` (unico metodo publico): valida config (`CertificadoPath`/`CUIT` cargados, si no retorna error controlado sin excepcion), obtiene/renueva el token via `AfipTokenCache`, consulta `FECompUltimoAutorizado` para el proximo numero, arma y envia `FECAESolicitar`, parsea la respuesta (CAE+vencimiento si `Resultado=="A"`, `Observaciones`/`Errors` concatenados como `DetalleError` si no). Login WSAA completo (armado del XML `loginTicketRequest`, firma CMS con el certificado `.p12`, POST SOAP, parseo de `token`/`sign`/`expirationTime`). Helpers de armado/parseo SOAP con `System.Xml.Linq` (namespace-aware, `Descendants().Where(e => e.Name.LocalName == "X")` para tolerar variaciones de prefijo entre AFIP homologacion/produccion). Resolucion de zona horaria Argentina con el mismo fallback triple ya usado por `ChequeAcreditacionHostedService` (Sprint 4).
- `Services/ComprobanteAfipService.cs` — nuevo. `EmitirAsync` es el metodo critico del sprint: valida (venta existe y no Cancelada, al menos 1 item con `Cantidad>0` — GAN-001, cada `Cantidad <= Cantidad-CantidadFacturada` del `VentaItem`, CUIT/DNI segun `TipoComprobante`) -> crea `ComprobanteAfip`+`Items` en `Pendiente` -> `SaveChangesAsync` (asigna Id) -> llama a `IntentarEmitirYPersistirAsync` (metodo compartido con `ReintentarAsync`: llama a `IAfipService.EmitirAsync`, y **solo si `Exito=true`** incrementa `VentaItem.CantidadFacturada` por cada item — si `Exito=false`, el comprobante pasa a `Error` con `DetalleError` y `CantidadFacturada` **nunca se toca**) -> `SaveChangesAsync` final -> `CommitAsync`, con `RollbackAsync` en el `catch`. `ReintentarAsync` reusa los mismos `ComprobanteAfipItem` ya persistidos (sin volver a elegir items, HU-7.3) pero **revalida el tope pendiente contra el estado actual** antes de reintentar (por si otro comprobante ya consumio parte de la cantidad mientras tanto). `GenerarPdfAsync` con QuestPDF, mismo patron exacto que `PresupuestoService.GenerarPdfAsync` (Sprint 2) — solo genera PDF de comprobantes `Emitido` (con CAE real).
- `Services/VentaService.cs` — `TieneComprobanteAsociadoAsync` paso de placeholder (`Task.FromResult(false)`) a consulta real (`_db.ComprobantesAfip.AnyAsync(c => c.VentaId == ventaId && c.Estado == EstadoComprobanteAfip.Emitido)`, ver decision #6). `ListarAsync` resuelve `TieneComprobante` real (mismo patron MH-001-safe que `TieneEntrega`, consulta puntual acotada al tamaño de pagina). `GetByIdAsync` resuelve `ComprobanteAfipId`/`ComprobanteAfipEstado` (ultimo comprobante, cualquier estado) y `CantidadPendienteDeFacturar`.
- `DependencyInjection.cs` — `services.Configure<AfipSettings>(...)`, `AddSingleton<AfipTokenCache>()`, `AddScoped<IAfipService, AfipService>()`, `AddScoped<IComprobanteAfipService, ComprobanteAfipService>()`, `AddHttpClient("Afip", ...)` (cliente dedicado sin el header `Accept: application/json` del cliente `"default"`, timeout 45s — WSAA/WSFE en homologacion pueden responder lento).

**Web** (`MariHogar.Web`)
- `Controllers/ComprobantesAfipController.cs` — nuevo. `[Authorize(Policy = "RequireVentas")]` a nivel clase (Administrador y Vendedor por igual, tabla de permisos "Emitir comprobante AFIP"). `Create(int ventaId)` GET (precarga) + `Create()` POST (mismo patron de POST form-urlencoded con `itemsJson` embebido que `VentasController.Confirmar`/`EntregasController.RegistrarCobro`, reusa el antiforgery token sin ambiguedad). `Details(int id)`, `Reintentar(int id)` POST, `DescargarPdf(int id)`, `Index`/`GetData`/`LimpiarFiltros` con filtros persistidos en sesion bajo `Filtros:ComprobantesAfip:Index`.
- `Models/ComprobanteAfipViewModels.cs` — nuevo. `ComprobanteAfipIndexViewModel`, `ComprobanteAfipCreateViewModel`, `ComprobanteAfipDetailsViewModel`.
- `Views/ComprobantesAfip/Create.cshtml` — tabla de items de la Venta con checkbox+cantidad editable (tope = pendiente de facturar, validado en JS y revalidado siempre server-side), selector Factura A/B, datos fiscales del cliente (nombre/CUIT/DNI, precargados con el nombre de la Venta), boton "Emitir comprobante" (POST AJAX, redirige a `Details` con el resultado). Sigue el wireframe "Comprobante AFIP (alta desde Venta)" de `2-disenador-funcional.md`.
- `Views/ComprobantesAfip/Details.cshtml` — card con CAE+vencimiento+numero de comprobante si `Emitido` (alert verde), o el `DetalleError` + boton "Reintentar" si `Error` (alert roja) — HU-7.3. Boton "Descargar PDF" solo si `Emitido`.
- `Views/ComprobantesAfip/Index.cshtml` — DataTables server-side + 4 filtros (venta, tipo, estado, rango de fecha) persistidos en sesion, badge de color por estado (Pendiente=gris/Emitido=verde/Error=rojo), mismo patron exacto que `Cheques/Index.cshtml` (Sprint 4).
- `Views/Ventas/Details.cshtml` — agregada card "Comprobante AFIP" (analoga a la card "Entrega" de Sprint 3): "Ver comprobante" con badge de estado si ya existe alguno, "Facturar" si `CantidadPendienteDeFacturar > 0`. Boton "Cancelar venta" ahora tambien se oculta (con texto explicativo) cuando `ComprobanteAfipEstado == "Emitido"`, ademas del caso ya existente de Entrega asociada.
- `Views/Ventas/Create.cshtml` — habilitado el boton "Facturar" de la pantalla de exito (Sprint 2 lo dejaba deshabilitado con tooltip "Proximo sprint"): ahora es un link real a `ComprobantesAfip/Create?ventaId=X`, siempre habilitado tras confirmar (a diferencia de "Programar entrega", facturar no depende del estado de pago).
- `Views/Shared/_Layout.cshtml` — sidebar: link "Comprobantes AFIP" agregado dentro de la seccion "Ventas" ya existente (visible a SuperUsuario/Administrador/Vendedor, misma policy `RequireVentas` que protege el controller).
- `appsettings.json` — nueva seccion `"Afip"` con `Ambiente: "Homologacion"`, `CertificadoPath`/`CertificadoPassword`/`CUIT` vacios (placeholders, comentados explicando que es una dependencia pendiente del cliente), `PuntoVenta: 1`, `PorcentajeIva: 21`. Comentarios inline (JSONC, mismo patron ya usado en la seccion `"Features"` preexistente — el proveedor de configuracion JSON de .NET tolera comentarios y comas finales por defecto).

#### Migracion EF generada

- `20260724210125_AddComprobantesAfip` — crea `ComprobantesAfip` (FK `Restrict` a `Ventas`, indices `VentaId`/`Estado`/`Fecha`) y `ComprobanteAfipItems` (FK `Cascade` a `ComprobantesAfip`, FK `Restrict` a `VentaItems`, indices `ComprobanteAfipId`/`VentaItemId`). **Generada y aplicada** exitosamente contra `marihogar_dev` (`dotnet ef database update` -> `Done.`; `dotnet ef migrations list` confirma 7 migraciones totales — `InitialCreate`, `AddCatalogo`, `AddPresupuestosVentas`, `AddEntregas`, `AddComprasCCProveedoresCheques`, `AddGastos`, `AddComprobantesAfip` — ninguna pendiente).

#### Evidencia de cierre — build + migracion + revision de codigo (sin smoke test, regla de proceso vigente)

- `dotnet build MariHogar.slnx` -> **Compilacion correcta, 0 errores** (9 warnings, todos preexistentes: `NU1902` de MailKit/MimeKit + `CS0114` de `HomeController.StatusCode`; ninguno introducido por este sprint). Build ejecutado 3 veces durante la sesion (tras el primer error de compilacion real encontrado y corregido — ver abajo — y tras el refactor de `FechaComprobante`), confirmado limpio en la version final.
- **1 error de compilacion real encontrado y corregido durante el desarrollo** (antes de cualquier evidencia de cierre, no llego a quedar en el codigo final): `decimal.Add(decimal, decimal)` es un metodo **estatico** de `System.Decimal`, no invocable con sintaxis de instancia (`importeNeto.Add(importeIva)`) — corregido a `importeNeto + importeIva` en `AfipService.ArmarFecaeSolicitarEnvelope`.
- Migracion `20260724210125_AddComprobantesAfip` generada y **aplicada** contra `marihogar_dev` (confirmado con `dotnet ef migrations list`, 7 migraciones, ninguna pendiente). Nota tecnica relevante: tanto `dotnet ef migrations add` como `dotnet ef database update` ejecutan el `Program.cs` real hasta `builder.Build()` (mecanismo de `HostFactoryResolver` de EF Core, visible en el log como `HostAbortedException: The host was aborted` — comportamiento esperado, no un error) — esto significa que la carga de `appsettings.json` (incluida la nueva seccion `"Afip"` con comentarios JSONC) y el registro completo de servicios en `DependencyInjection.cs` (incluyendo `AfipTokenCache`/`AfipService`/`ComprobanteAfipService`/el `HttpClient` nombrado `"Afip"`) se ejercitaron con exito en ambos comandos, sin ninguna excepcion de configuracion o de resolucion de dependencias — evidencia real (no smoke test HTTP, no llamada a AFIP) de que el grafo de DI y el parseo de configuracion son correctos.
- **Revision de codigo propia, linea por linea, con foco explicito en los 4 puntos pedidos por el alcance de este sprint** (transaccion de emision, tope de cantidad facturable, renovacion de token cada 24hs, config homologacion/produccion):
  1. **Transaccion de emision (`ComprobanteAfipService.EmitirAsync`/`ReintentarAsync` + `IntentarEmitirYPersistirAsync`)**: confirmado que la secuencia es `BeginTransactionAsync` -> `Add(ComprobanteAfip+Items)` -> `SaveChangesAsync` (asigna Ids) -> `IAfipService.EmitirAsync` (llamada HTTP real a AFIP, fuera de cualquier lock de base de datos — no mantiene una transaccion abierta durante el tiempo de red) -> si `Exito`: `Estado=Emitido`+CAE+vencimiento+**recien ahi** `foreach item: ventaItem.CantidadFacturada += item.Cantidad` (confirmado que este incremento esta **dentro** del `if (resultado.Exito)`, nunca fuera ni antes) -> si no `Exito`: `Estado=Error`+`DetalleError`, **confirmado que ningun `VentaItem` se toca en esta rama** (no hay ninguna linea que modifique `CantidadFacturada` fuera del bloque `if`) -> `SaveChangesAsync` final -> `CommitAsync`, con `RollbackAsync` en el `catch` que envuelve todo el bloque (si el `SaveChangesAsync` final fallara, se revierte tambien el estado `Emitido`/`Error` recien escrito, consistente con "todo o nada" — ver riesgo residual #9 sobre el caso limite de AFIP-aprobo-pero-DB-fallo).
  2. **Tope de cantidad facturable por item**: confirmado en `EmitirAsync` que `pendiente = ventaItem.Cantidad - ventaItem.CantidadFacturada` se calcula contra el valor **actual** en base (no contra un valor cacheado del cliente) y que `itemInput.Cantidad > pendiente` rechaza **antes** de abrir la transaccion (validacion pura, sin efectos secundarios) — mismo criterio confirmado en `ReintentarAsync`, que **revalida el tope de nuevo** contra el estado actual de `VentaItem.CantidadFacturada` antes de reintentar (cubre el caso de que otro comprobante haya consumido parte de la cantidad entre el intento fallido original y el reintento — facturacion parcial concurrente). Confirmado tambien que la UI (`ComprobantesAfip/Create.cshtml`) limita el input `max="@i.CantidadPendiente"` en JS, pero esto es solo UX — el guard real y unico es el server-side ya descrito.
  3. **Renovacion de token cada 24hs (HU-7.4)**: confirmado que `AfipTokenCache.ObtenerAsync` nunca usa un TTL fijo de 24hs — usa la `expirationTime` real que AFIP devuelve en la respuesta del WSAA (parseada con `DateTimeOffset.Parse(...).UtcDateTime`, correcta con el offset `-03:00` que AFIP incluye), con un margen de seguridad de 10 minutos antes de esa fecha real. Confirmado el doble chequeo dentro del `SemaphoreSlim` (`EsValido()` antes y despues de esperar el lock) para que dos requests concurrentes con el token vencido no disparen 2 logins WSAA innecesarios — el segundo request reutiliza el token que el primero acaba de obtener.
  4. **Configuracion homologacion/produccion**: confirmado que `AfipSettings.WsaaUrl`/`WsfeUrl` resuelven exclusivamente por `Ambiente` (comparacion case-insensitive contra `"Produccion"`, cualquier otro valor —incluido vacio o mal escrito— cae a Homologacion por seguridad, nunca al reves) y que los 4 valores de URL por defecto (`WsaaUrlHomologacion`/`Produccion`, `WsfeUrlHomologacion`/`Produccion`) coinciden con los endpoints reales publicados por AFIP. Confirmado que `appsettings.json` documenta explicitamente, en comentarios, que pasar a produccion es *solo* un cambio de configuracion (`Ambiente=Produccion` + los 3 datos reales del cliente: certificado, password, CUIT), sin tocar ningun archivo de codigo — verificado leyendo `AfipService`/`ComprobanteAfipService` completos, ningun `if (ambiente == ...)` hardcodeado fuera de `AfipSettings`.
  5. **Verificacion adicional (defensa en profundidad, no pedida explicitamente)**: confirmado que `AfipService.EmitirAsync` nunca deja escapar una excepcion hacia el caller — el unico `try/catch` de todo el metodo envuelve *todo* el cuerpo (login, consulta de ultimo autorizado, armado y envio de `FECAESolicitar`, parseo de respuesta), y cualquier excepcion (incluidas `InvalidOperationException` lanzadas por `LanzarSiHayErroresWsfe` ante un `<Errors>`/`<Err>` de AFIP o un `faultstring` SOAP) se traduce siempre en `AfipEmisionResultDto{Exito=false, DetalleError=...}` — consistente con el contrato documentado en `IAfipService` ("nunca lanza excepcion de negocio hacia el caller").

#### Checklist de verificacion manual para el usuario (equivalente al smoke test que ya no ejecuta el Implementador)

Con la app corriendo (`dotnet run --project MariHogar.Web`) y logueado como Administrador o Vendedor. **Importante**: sin el certificado `.p12` real del cliente cargado en `appsettings.Development.json` (seccion `Afip`), el paso 3 (emision real) no se puede completar contra AFIP — ese paso queda condicionado a que el cliente/estudio contable gestione un certificado de testing de AFIP homologacion (paso externo a este sprint, ya documentado como riesgo desde el analisis funcional). Los pasos 1, 2, 4 y 5 son verificables igual sin certificado (validaciones de UI/servidor que nunca llegan a llamar a AFIP).

1. **Sin certificado configurado**: desde una Venta cualquiera, click "Facturar" → completar Factura B + DNI → click "Emitir comprobante" → verificar que el sistema responde con un error claro ("AFIP no esta configurado: falta el certificado...") en vez de un 500 o una pantalla en blanco — confirma el guard de configuracion de `AfipService.EmitirAsync`.
2. **Validaciones de formulario** (sin necesidad de AFIP real): intentar emitir Factura A sin CUIT → debe rechazar client-side (SweetAlert2) y, si se fuerza el POST, tambien server-side. Intentar emitir Factura B sin CUIT ni DNI → idem. Intentar emitir sin marcar ningun item (desmarcar todos los checkboxes) → boton/validacion debe impedirlo. Editar la cantidad de un item por encima del "Pendiente" mostrado en la grilla → el input debe recortarse solo al maximo (JS) y, si se fuerza un valor mayor via API, el servidor debe rechazar con el mensaje de tope.
3. **Con certificado de homologacion real cargado** (cuando el cliente lo gestione): emitir un comprobante de prueba → verificar que aparece el CAE + vencimiento en `Details` y que "Descargar PDF" genera un PDF valido con esos datos. Forzar un rechazo de AFIP (ej. CUIT invalido a proposito, si el ambiente de homologacion lo permite) → verificar que el comprobante queda en estado "Error" con el detalle visible, y que el boton "Reintentar" vuelve a intentar sin pedir los items de nuevo.
4. **Facturacion parcial**: en una Venta con un item de cantidad >= 2, facturar solo 1 unidad → verificar que en `Ventas/Details` la card "Comprobante AFIP" sigue ofreciendo "Facturar" (queda pendiente) ademas de "Ver comprobante" → facturar la unidad restante en un segundo comprobante → verificar que ahora "Facturar" ya no aparece (nada pendiente).
5. **Guard de cancelacion**: con un comprobante ya Emitido para una Venta, intentar cancelarla desde `Ventas/Details` → verificar que el boton "Cancelar venta" ya no aparece (reemplazado por el texto explicativo "tiene un comprobante AFIP emitido").
6. **Permisos**: loguearse como Vendedor y confirmar que "Comprobantes AFIP" aparece en el sidebar (seccion Ventas) y que puede facturar una venta igual que Administrador (tabla de permisos: "Emitir comprobante AFIP" es igual para ambos roles).
7. **Listado**: en `Comprobantes AFIP/Index`, probar los 4 filtros (N° de venta, tipo, estado, rango de fecha) uno por uno y el boton "Limpiar filtros" → confirmar que persisten al navegar a otra pantalla y volver (patron de sesion).

#### Riesgos residuales

- **Sin verificacion contra AFIP real**: el certificado `.p12` del CUIT del cliente todavia no fue provisto (riesgo ya documentado desde el analisis funcional, `1-analista-funcional.md`) — ningun tramo de este sprint fue ejecutado contra AFIP homologacion/produccion real. Toda la evidencia es build + migracion aplicada + revision de codigo linea por linea (que confirmo la correccion de la logica de transaccion, tope, cacheo de token y config, ver arriba). El checklist de 7 pasos de arriba es la unica forma de cerrar este riesgo, y el paso 3 especificamente depende de una gestion externa (certificado de testing de AFIP homologacion) que no es responsabilidad de este sprint.
- **Caso limite de "AFIP aprobo pero la escritura local fallo"** (decision #9 de implementacion) — probabilidad muy baja (ventana de milisegundos), no mitigado por directriz de cambios minimos, documentado para un sprint de endurecimiento futuro si el cliente lo pide.
- **Alicuota de IVA uniforme, no por producto** (decision #4) — limitacion conocida si el negocio llega a vender productos con IVA diferencial; cambio de alcance medio si se pide corregir (agregar alicuota a `Producto`).
- **Factura B siempre como Consumidor Final (`CondicionIVAReceptorId=5`)** (decision #3) — no distingue Monotributista/Exento; el diseño no pedia esa distincion en la captura de datos fiscales, documentado por si se pide en el futuro.
- **SOAP armado a mano en vez de proxy generado** (decision tecnica documentada arriba) — mantenimiento manual del mapeo de campos si AFIP cambia el contrato de WSFEv1 en el futuro (riesgo bajo, WSFEv1 es estable hace años, y el mismo criterio de "armar a mano" es el que usan numerosas integraciones AFIP en .NET Core/5+ en produccion).

#### Proximos pasos pendientes (Sprint 6 — cierre de Etapa 1)

- **Este era el ultimo sprint funcional planificado del plan de 6 sprints de Etapa 1.** No queda ningun modulo de Etapa 1 (16 modulos: M2, M3, M4, M5, M6, M7, M9, M10, M11, M12, M13, M14, M15, M16, M17, M18) sin implementar. Solo restan de Etapa 2 (en espera, sin fecha, por decision explicita del cliente del 2026-07-24): M1 CRM de Leads y M8 Bot WhatsApp, ambos con dependencias externas ya documentadas (certificado no aplica a estos, pero si numero de WhatsApp dedicado + listado de catalogo para M8).
- Pendiente que QA valide este Sprint 6 y que el orquestador/cliente confirmen el cierre formal de Etapa 1 (documentacion al cliente en lenguaje de negocio, etapa 7 del flujo del estudio) y decidan si Etapa 2 se retoma o si el proyecto queda en este alcance por ahora.
- Cuando el cliente provea el certificado `.p12` real: el unico cambio necesario es cargar `Afip:CertificadoPath`/`Afip:CertificadoPassword`/`Afip:CUIT`/`Afip:PuntoVenta` reales en `appsettings.Production.json` (o el mecanismo de secretos que se decida para produccion) y cambiar `Afip:Ambiente` a `"Produccion"` — cero cambios de codigo, tal como exigio el alcance de este sprint.

---

## Historial de ajustes
- 2026-07-24: Sprint 1 cerrado. M10 (rol Vendedor + policy RequireVentas), M2 (Categoria/Marca/Producto/ProductoFoto + CRUD + DataTables con filtros persistidos en sesion + fotos con portada), M3 (MovimientoStock ledger + Producto.StockActual desnormalizado + ajuste manual con guard de stock negativo + listado de movimientos con filtros persistidos en sesion) implementados, migrados y probados en caliente contra MySQL real. 2 bugs encontrados y corregidos durante el smoke test (IN de coleccion local no soportado por el provider MySQL de EF Core 10; enum serializado como int en vez de string en el DTO de listado de stock). Build limpio, 0 errores.
- 2026-07-24: Sprint 2 cerrado (reintento de una sesion anterior cortada por limite de cuenta, que solo habia llegado a crear los enums `EstadoPresupuesto`/`EstadoVenta`, reutilizados sin cambios). M4 (Presupuestos: maquina de estados completa Borrador/Enviado/Aprobado/Convertido/Rechazado/Expirado-calculado-al-leer, PDF con QuestPDF, buscador instantaneo de productos), M5 (Ventas: pantalla POS elevada de dos columnas con buscador instantaneo + carrito sticky + pago rapido + advertencia de stock no bloqueante + pantalla de exito, confirmacion atomica Venta+Items+Pagos+Stock+CC en una unica transaccion, cancelacion con contramovimientos) y M11 (CC Local: ledger inmutable con saldo visible + listado filtrado) implementados y migrados (1 migracion combinada `AddPresupuestosVentas` que incluye tambien CC Local, decision documentada), migracion aplicada y verificada con `dotnet ef migrations list`. Smoke test funcional real completo (server-side, via `curl` contra la app corriendo con MySQL real) ejecutado con exito: flujo Presupuesto→Enviado→Aprobado→Convertir→Venta con pago combinado, verificacion independiente de stock/CC/Presupuesto, cancelacion con reversion verificada en ambos ledgers, casos de borde de Ventas, permisos por rol, PDF valido — sin bugs encontrados (a diferencia de Sprint 1). Pendiente exclusivamente la verificacion visual/JS en navegador real (Select2, toasts, layout sticky/mobile), no cubierta por `curl`; checklist dejada en la memoria para que el usuario la recorra. Build limpio, 0 errores, 0 warnings nuevos.
- 2026-07-24: Sprint 3 cerrado (de 6, tras la pausa de Etapa 2). M6 Entregas a domicilio: entidad `Entrega` + `EntregaIntento` (historial de intentos, ledger inmutable), maquina de estados completa Pendiente→EnCamino→Entregada/NoEntregada→(reagendar)Pendiente, cobro en destino reutilizando `PagoVenta` directamente sobre la Venta asociada (mismo mecanismo y formula que `VentaService.ConfirmarAsync`, sin flujo de pago paralelo — cumple el pedido explicito del cliente), guard real de `VentaService.TieneEntregaAsociadaAsync` (ya no hardcodeado a `false`), pantalla mobile-first `Entregas/Details.cshtml` reutilizando tal cual el layout de una columna + barra inferior fija de `Ventas/Create.cshtml` (Sprint 2), listado desktop `Entregas/Index.cshtml` con DataTables + 3 filtros persistidos en sesion, boton "Programar entrega" habilitado en la pantalla de exito de Ventas y card nueva "Entrega" en `Ventas/Details.cshtml`. Escaneo de reutilizacion cross-proyecto sin match (no hay modulo de logistica/entregas comparable en el historial del estudio, confirmado). Migracion `AddEntregas` generada y aplicada contra `marihogar_dev` (4 migraciones totales, ninguna pendiente). Build limpio, 0 errores, 0 warnings nuevos. Sin smoke test funcional (regla de proceso vigente desde el cierre de Sprint 2, decision explicita del cliente) — cierre por build + migracion aplicada + revision de codigo linea por linea de la logica critica (maquina de estados, guard de cancelacion, reutilizacion de PagoVenta), con checklist de 11 pasos dejada para verificacion manual del usuario.
- 2026-07-24: Sprint 4 cerrado (de 6). M12 Compras a proveedores (`Proveedor`/`OrdenCompra`/`OrdenCompraItem`/`PagoOrdenCompra`, maquina de estados Borrador→Confirmada→Recibida/Cancelada con Cancelada solo desde Borrador/Confirmada, `RecibirAsync` incrementa stock + postea Cargo en CC proveedor en una unica transaccion), M13 CC Proveedores (`MovimientoCCProveedor`, ledger inmutable identico en patron a `MovimientoCCLocal`, saldo por proveedor calculado en tiempo real, listado de proveedores con columna de saldo + detalle de movimientos filtrable por fecha) y M14 Cheques (`Cheque` vinculado 1-a-1 a `PagoOrdenCompra`, `ChequeAcreditacionHostedService` — patron `AcreditacionIngresosHostedService` de ganaderia, idempotente por filtrar siempre `Estado==Pendiente` — y rechazo manual con contramovimiento que reabre la deuda) implementados. **Primer sprint con reutilizacion de codigo real de otro proyecto confirmada explicitamente por el cliente** (no solo patron de diseño): estructura de `MovimientoCCProveedor.cs` y campos de `ProveedorViewModels.cs` copiados casi literales de `vino-y-se-fue`, patron de maquina de estados y logica de ledger/reversion de `CompraProveedorService.cs` de vino-y-se-fue adaptados (evitando a proposito 2 bugs ya catalogados alli, VSF-001/VSF-002), y patron arquitectonico completo de `AcreditacionIngresosHostedService.cs` de ganaderia reutilizado casi literal (con la diferencia documentada de no necesitar tabla `JobEjecucion`, porque la idempotencia real la da el filtro `Estado==Pendiente`, ya definido asi en `3-arquitecto-mvc.md`). Bug real encontrado y corregido durante la revision de codigo propia antes de cerrar (sin necesidad de que lo detecte QA): `OrdenCompraDetailDto.MontoPagado`/`PagoOrdenCompraService.RegistrarPagoAsync` no excluian pagos de cheques ya Rechazados del calculo de saldo pendiente. Migracion combinada `AddComprasCCProveedoresCheques` generada y aplicada contra `marihogar_dev` (5 migraciones totales, ninguna pendiente). Build limpio, 0 errores, 0 warnings nuevos. Sin smoke test funcional (regla de proceso vigente) — cierre por build + migracion aplicada + revision de codigo linea por linea con foco explicito en la transaccion de "Recibida", la transaccion de pago+cheque y la simulacion mental de doble corrida del job de acreditacion, con checklist de 10 pasos dejada para verificacion manual del usuario.
- 2026-07-24: Sprint 5 cerrado (de 6, ultimo sprint funcional planificado). M18 Gastos (`Gasto`, alta con movimiento automatico de Egreso en `MovimientoCCLocal` en la misma transaccion + anulacion con contramovimiento de reversion, sin edicion), M15 Caja mensual (`CajaService` agrega `MovimientoCCLocal` por rango de fecha con comparativo automatico contra el mes anterior, sin entidad nueva), M16 Aumento masivo de precios (`Producto.RowVersion` agregado para concurrencia optimista via el patron `REG-001` ya catalogado — MySQL sin rowversion auto, gestionado manualmente en `AppDbContext.OnBeforeSaveChanges` — flujo de previsualizacion 100% de lectura seguido de confirmacion explicita que revalida el `RowVersion` visto en la previsualizacion contra el valor real al aplicar, mejora deliberada sobre el patron de referencia de ShowroomGriffin), M17 Proyeccion financiera (promedio historico sobre `MovimientoCCLocal` de los ultimos N meses + compromisos conocidos de Cheques Pendientes en el horizonte y saldo pendiente de OC Confirmada/Recibida, alerta de deficit y texto aclaratorio de estimacion siempre visibles) y M9 Dashboard (`DashboardController` reemplaza la landing autenticada via redireccion desde `HomeController`, dos vistas segun rol, KPIs del Administrador cargados via 5 endpoints AJAX independientes por card — ajustado durante la revision para cumplir literalmente HU-9.1 "cada KPI carga de forma independiente"). **Particularidad de esta sesion**: se detecto que un proceso concurrente ya habia escrito gran parte del codigo del sprint antes de que esta ejecucion escribiera nada propio; se adopto y se sometio a la misma revision linea por linea que exige el cierre de cualquier sprint (en vez de descartarlo o duplicarlo), encontrando y corrigiendo 2 problemas reales antes de cerrar: una inconsistencia de sincronizacion entre `IDashboardService`/`DashboardController`/`DashboardViewModels.cs` causada por una escritura tardia superpuesta del proceso concurrente, y una falta de proteccion por policy en los endpoints AJAX `GetChequesPorVencer`/`GetBalanceCaja` del Dashboard (datos financieros exclusivos de Administrador expuestos sin `[Authorize(Policy="RequireAdministracion")]` propio). Tambien se agrego el filtro de Descripcion faltante en `Gastos/Index` (regla de "un filtro por cada columna visible"). Reutilizacion cross-proyecto confirmada: patron de dos pasos previsualizacion/confirmacion con `RowVersion` de `AumentoMasivoService.cs` de ShowroomGriffin (match mas directo de todo el proyecto), patrones de estilo de `EgresoService.cs`/`CajaService.cs` de ganaderia (sin copiar codigo textual, dominio distinto). Migracion `AddGastos` generada y aplicada contra `marihogar_dev` (6 migraciones totales, ninguna pendiente) — incluye el manejo correcto de la limitacion de MySQL de no permitir `DEFAULT` en columnas `BLOB` (patron de 3 pasos: columna nullable → backfill con `UUID_TO_BIN(UUID())` → `NOT NULL`). Build limpio, 0 errores, 9 warnings preexistentes (ninguno nuevo). Sin smoke test funcional (regla de proceso vigente) — cierre por build + migracion aplicada + revision de codigo linea por linea con foco explicito en los 3 puntos pedidos (movimiento de CC Local de Gasto, no persistencia de la previsualizacion de Aumento masivo, formula de Proyeccion financiera sin duplicar Cheques/OC con CC Local), con checklist de 15 pasos dejada para verificacion manual del usuario.
- 2026-07-24: **Sprint 6 cerrado (de 6, ultimo sprint funcional planificado) — cierra el alcance funcional completo de Etapa 1.** M7 Facturacion electronica AFIP/ARCA: entidades `ComprobanteAfip`/`ComprobanteAfipItem`, `AfipService` (WSAA login con certificado .p12 + token cacheado por vencimiento real de AFIP (no un TTL fijo de 24hs) + WSFEv1 `FECompUltimoAutorizado`/`FECAESolicitar`, SOAP armado a mano con `System.Xml.Linq`+`HttpClient` en vez de proxy `dotnet-svcutil` generado — decision tecnica documentada, portando el conocimiento del protocolo de `delicias-naturales` sin copiar codigo C# literal por incompatibilidad de stack), `ComprobanteAfipService.EmitirAsync`/`ReintentarAsync` (transaccion unica: `VentaItem.CantidadFacturada` solo se incrementa si AFIP aprueba, comprobante rechazado queda en Error reintentable sin volver a elegir items, tope de facturacion parcial revalidado siempre contra el estado actual), guard real `VentaService.TieneComprobanteAsociadoAsync` (ya no placeholder), boton "Facturar" habilitado en la pantalla de exito de Ventas y card "Comprobante AFIP" en `Ventas/Details.cshtml`, listado de comprobantes con DataTables + 4 filtros persistidos en sesion, PDF con QuestPDF (mismo patron que Presupuestos). Config `Afip` en `appsettings.json` apuntando por defecto a homologacion, con placeholders documentados para el certificado real (pasar a produccion es solo cambio de config). Migracion `AddComprobantesAfip` generada y aplicada contra `marihogar_dev` (7 migraciones totales, ninguna pendiente). Build limpio, 0 errores, 9 warnings preexistentes (ninguno nuevo) — 1 error de compilacion real (`decimal.Add` estatico invocado con sintaxis de instancia) encontrado y corregido durante el desarrollo, antes de cualquier evidencia de cierre. Sin verificacion contra AFIP real (certificado .p12 del cliente todavia pendiente, riesgo ya documentado desde el analisis funcional) — cierre por build + migracion aplicada + revision de codigo linea por linea con foco explicito en los 4 puntos pedidos (transaccion de emision, tope de cantidad facturable, renovacion de token, config homologacion/produccion), con checklist de 7 pasos dejada para verificacion manual del usuario. **No queda ningun modulo de Etapa 1 sin implementar** — quedan pendientes exclusivamente M1 (CRM de Leads) y M8 (Bot WhatsApp) de Etapa 2, en espera por decision explicita del cliente.

@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="text-center mt-5">
    <div style="font-size: 6rem;">🔒</div>
    <h1 class="display-4 text-danger mt-3">403</h1>
    <h2 class="mb-3">Acceso denegado</h2>
    <p class="lead text-muted">No tenés permisos para acceder a esta sección.</p>
    <p class="text-muted">Si creés que deberías tener acceso, contactá al administrador del sistema.</p>
    <div class="mt-4">
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg">Volver al inicio</a>
    </div>
</div>

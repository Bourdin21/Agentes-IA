---
description: Delegar al Community Manager de Olvidata Soft — guiones de Reels, prompts de video IA, carruseles, stories, captions.
argument-hint: pieza de contenido a producir (ej. "Reel para dietéticas")
---

Delegá el siguiente pedido al subagente `olvidata-cm` (usá el tool de Agentes con `subagent_type: olvidata-cm`), pasándole como prompt completo el pedido de abajo. Esperá su respuesta y mostrásela al usuario tal cual (no la resumas ni la reescribas salvo que el subagente lo pida explícitamente).

## Pedido del usuario

$ARGUMENTS

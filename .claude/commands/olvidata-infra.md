---
description: Delegar al agente de Infraestructura de Olvidata Soft — servidor, SSL, dominios, application pools, bases de datos y stack tecnológico para el conjunto de aplicaciones.
argument-hint: consulta o pedido de análisis de infraestructura
---

Delegá el siguiente pedido al subagente `olvidata-infra` (usá el tool de Agentes con `subagent_type: olvidata-infra`), pasándole como prompt completo el pedido de abajo. Esperá su respuesta y mostrásela al usuario tal cual (no la resumas ni la reescribas salvo que el subagente lo pida explícitamente).

## Pedido del usuario

$ARGUMENTS

---
name: project-la-platense-status
description: "Estado y alcance del proyecto La Platense (ferretería) — fase, entregas en producción, ramas activas"
metadata: 
  node_type: memory
  type: project
  originSessionId: bc755a3e-0b32-4b42-9695-956808c26af7
  modified: 2026-08-25T02:18:52.555Z
---

Proyecto "La Platense": sistema de gestión integral para ferretería (catálogo con unidades de medida y conversión compra/venta, ventas editables pre-AFIP, cuenta corriente de clientes/proveedores/empleados, caja diaria/mensual, compras con listas de precios de proveedor, entregas, presupuestos, dashboard de 3 niveles). Cliente real de Olvidata Soft.

**Why:** presupuesto aprobado por el cliente el 2026-07-30 (USD 1.500/3 pagos o USD 1.800/12 pagos, mantenimiento PREMIUM año 1 gratis) — esto habilita formalmente el inicio de Implementación según la secuencia obligatoria del estudio (Discovery→Diseño→Arquitectura→Presupuesto→Implementación).

**Estado técnico al 2026-08-10 (post Entrega 1):**
- Repo real en `C:\Sistemas\Ferreteria La Platense` como copia saneada de `olvidatasoft-crm`. Renombrado completo (código + `.github/copilot-instructions.md` + `web.config` + `manifest.webmanifest`) de `OlvidataCRM` a `FerreteriaLaPlatense` — verificado 0 referencias residuales fuera de la caché local `.vs/`.
- Remoto Git: `git@gitlab.com:olvidata/ferreteria-la-platense.git` (mismas credenciales SSH que los demás repos del estudio). Primer commit (`f5e6af9`, root-commit) pusheado a `master` el 2026-08-10, tras QA GO + prueba manual de Joaquín.
- **Entrega 1 cerrada** (Catálogo: Marca/Modelo/Categoria/Producto con conversión de unidades; Stock: AjusteStock + ABC + arranque con negativo permitido; Código de barras: lookup service; Usuarios y roles). Migración `EntregaUno_CatalogoStockUsuarios` generada y aplicada a la base local `laplatense_dev`.
- Roles reales: `SuperUsuario` (gestión de Usuarios + Herramientas del Sistema `/System`, exclusivo — acceso técnico de Olvidata, no del cliente) · `Administrador` (todo lo demás: Catálogo/Stock, será Ventas/Caja/etc. en próximas entregas — **NO** gestiona usuarios ni System) · `Vendedor` (consulta de catálogo/stock) · `Repartidor` (sin pantallas propias todavía). Login redirige a `/Stock` por defecto (antes Home).
- Config de producción real ya cargada en `appsettings.Production.json` (gitignorado, nunca se commitea): SMTP `vps-5574162-x.dattaweb.com` cuenta `no-reply@olvidata.com.ar`, error a `olvidatasoft@gmail.com`, `AllowedHosts=ferreterialaplatense.com.ar` (sitio real ya en `https://ferreterialaplatense.com.ar/`). **Pendiente:** connection string de MySQL de producción (placeholder sin completar) — bloquea el deploy real hasta completarla.
- Deploy target IIS: `site17.PublishSettings` en la raíz del repo (perfiles MSDeploy y FTP) — contiene credenciales reales, agregado a `.gitignore` (`*.PublishSettings`), nunca se commitea.
- 3 hallazgos de seguridad corregidos al copiar el template original: credenciales reales en `appsettings.Production.json`, clave privada SSL de `portal.olvidata.com.ar`, publish profiles apuntando al CRM real en producción (`olvidatasoft-002-site12`) — saneado antes del primer commit.

**Alcance funcional (16 módulos, ~20 entidades nuevas):** Usuarios/roles (Admin/Vendedor/Repartidor), Catálogo (Producto/Marca/Modelo/Categoria), Unidades de medida y conversión compra↔venta (pieza sin precedente, `UnidadMedidaConversionService`), Stock + puesta a punto inicial (clasificación ABC + arranque suave con stock negativo permitido + `AjusteStock` auditado), Ventas con workflow Borrador→Facturada editable (`VentaWorkflowService`, pieza de mayor riesgo), CC clientes/proveedores/empleados, Facturación AFIP (WSAA/WSFE, reuso de `marihogar`/`delicias-naturales`), Devoluciones + NC/ND AFIP (reuso `ShowroomGriffin`), Proveedores/Compras con TC propio + % descuento + importación de listas, Caja diaria/mensual, Gastos, Presupuestos/cotizaciones PDF, Entregas (markup configurable, repartidor ve TODAS sin scoping), Aumento masivo de precios, Código de barras (solo vinculación a producto en venta — ticketeadora es manual, sin integración), Dashboard de 3 niveles ("foto completa del negocio", pantalla de mayor prioridad de diseño del cliente).

**Explícitamente excluido/pospuesto:** migración del catálogo existente (~17.000 productos) — pospuesta a fase posterior, pendiente de que Joaquín confirme si hay acceso directo a la BD real del cliente (más barato) vs. archivo Excel de formato desconocido (más caro); se cotiza aparte. Cambios/canjes de mercadería (solo devolución simple). Integración con hardware externo (balanzas, ticketeadora de etiquetas).

**Pregunta abierta pendiente (bloquea solo el módulo de anulación, no el resto):** quién puede iniciar la anulación de una venta facturada (¿solo admin, o también vendedor?) y si hay límite de tiempo (ej. solo el mismo día). Ver `1-analista-funcional.md` §9.

**Estado al 2026-08-17 — Etapa 3 (migración de catálogo) implementada y deployada a producción:**
- El cliente trajo el backup real de su sistema legado (`Migracion/`, SQL Server, nunca commiteado — en `.gitignore`) y aprobó cotizar/implementar la migración de catálogo (~112.485 productos) como Etapa 3 aparte, resuelta con un script de consola de una sola corrida (`tools/MigracionCatalogo`), no con la pantalla web de import que se había construido primero (el cliente pidió sacarla: la migración real es de una sola vez, distinta de la futura importación recurrente de listas de proveedor).
- Entrega 2 (Ventas/CC clientes/AFIP/Caja/Gastos/Entregas/Dashboard) está **construida en la rama `entrega-2`** pero **NO aprobada para producción** (pasó QA de código, nunca prueba manual en caliente del cliente).
- Para poder deployar "Etapa 1 + migración" sin arrastrar Entrega 2 (la rama de migración se había creado desde `entrega-2` porque necesitaba la entidad `Cliente`), se creó la rama `entrega-1-migracion` desde `entrega-1`, traída archivo por archivo (no cherry-pick limpio posible). Detalle completo en `5-implementador.md` sección "Rama de producción `entrega-1-migracion`".
- **Deploy real a producción ejecutado el 2026-08-17**: migración EF `EtapaTres_MigracionCatalogo` aplicada contra la base MySQL real (`db_a7251f_laplaten` en `mysql8001.site4now.net`), código de `entrega-1-migracion` publicado a `olvidatasoft-002-site17` vía Web Deploy, y `tools/MigracionCatalogo` corrido contra la base de producción real (fuente: backup del cliente restaurado en SQL Server local). Se encontró y borró 1 producto de prueba/smoke-test que ya existía en producción antes de correr el script (el script exige catálogo vacío por diseño).
- Cuando Entrega 2 se apruebe, hay que reconciliar `entrega-1-migracion` con `migracion-catalogo`/`entrega-2` (restituir el recálculo por lote de ABC que se sacó por depender de Ventas, y saltear la migración de `Clientes` de Entrega 2 porque la tabla ya existe con su forma final).
- **2026-08-18 — fix real de precios en producción**: Joaquín detectó que `PrecioConDescuento` migrado no representaba ninguna oferta real (bug: comparaba precio con IVA contra precio sin IVA). Fórmula real confirmada contra el legado: `PrecioVenta = [PrecioCompra × (1+Recargo%)] / (1+IVA%)`. Agregado `Producto.PorcentajeRecargo`; `PrecioConDescuento` renombrado a `PrecioOferta` con vigencia real (`Desde`/`Hasta`). `PrecioCompra`/`Bonificacion` quedan sin cambios estructurales — el precio de compra bruto por proveedor menos bonificación real queda diferido a un futuro módulo de Compras (Entrega 3, no construida) por decisión explícita de Joaquín. Deployado a producción: catálogo vaciado y recargado desde cero con la fórmula corregida (112.435 con recargo, 381 con oferta real). Un segundo bug (fecha de oferta huérfana sin precio en 65.487 productos) se encontró y corrigió en la misma corrida real. Detalle completo en `5-implementador.md` sección "Deploy real del fix de precios".

**2026-08-21 — código de barras múltiple por producto, deployado a producción:** el cliente tiene un código propio (impresora interna, 1:1, campo `Producto.CodigoBarras` sin cambios) y además el legado tenía 4.371 de 23.197 artículos con MÁS DE UN código de barras real de fábrica (variantes de un mismo producto interno, ej. "Recarga Aromatizador" con 22 EAN distintos por aroma) que la migración original descartaba como "ambiguos". Agregada entidad nueva `CodigoBarrasProducto` (1:N con `Producto`, índice único sobre `Codigo` SOLO —no compuesto, a diferencia de `CodigoProveedorProducto`—, migración `20260821163451_AgregarCodigoBarrasProducto`), navegación `Producto.CodigosBarrasAlternos`, `ICodigoBarrasLookupService` ampliado (resuelve primero por código propio, después por alternos activos), listado de Catálogo extendido a buscar/mostrar alternos (badge `+N`). Se encontró y corrigió un bug real en `tools/MigracionCatalogo/Program.cs` (el flujo principal de migración nunca insertaba `CodigoBarrasProducto`, solo el modo correctivo). **Deployado end-to-end a producción real** el mismo día: backup, migración EF aplicada, backfill corrido (16.974 `CodigoBarras` corregidos + 8.276 alternos sobre 3.865 productos, mismas magnitudes en dev y prod), código publicado vía Web Deploy, sitio verificado. Detalle completo en `5-implementador.md` sección "Código de barras múltiple — gaps cerrados + rollout real a producción" y `trazabilidad.md` 2026-08-21 (3).

**Regla de proceso nueva para todo el estudio (no solo La Platense):** toda modificación del modelo de datos requiere, en la misma ronda de trabajo, relevar y actualizar TODOS los sitios de uso existentes del campo/entidad que se extiende — no solo el punto de entrada del pedido original. Motivada directamente por el gap real de arriba (el listado de Catálogo quedó sin actualizar en la primera pasada). Catalogada como `LP-002` en `C:\Sistemas\Agentes-IA\.github\instructions\32-estandares-qa-implementador.instructions.md`, con paso de checklist espejo en `26-checklists.instructions.md`.

**2026-08-24 — Entrega 2 completa deployada a producción real:** tras 2 vueltas de QA (D1-D16, ver `6-qa.md`) y el cierre de decisiones pendientes (Descuento/Recargo de ItemVenta pasó de monto fijo a porcentaje, alineado con delicias-naturales/vino-y-se-fue; AFIP hardeado preventivamente contra los 2 bugs reales que rompieron marihogar en producción; `PAT-016` — búsqueda global por importe/fecha + filtros en Session — implementado en los 6 listados de Entrega 2), se mergeó `entrega-2` → `entrega-1-migracion` y se deployó a `ferreterialaplatense.com.ar`. **Facturación AFIP queda deshabilitada a propósito** (botón "Confirmar y facturar" implementado pero `disabled`, sin certificado real del cliente todavía) — evita que se puedan disparar 2 riesgos conocidos y aceptados: D8 (facturar no persiste el borrador antes de facturar) y el hecho de que el circuito posterior a una factura exitosa (descuento de stock, CC, caja) nunca se probó en la práctica. D9 (fecha de Caja mezcla UTC/hora Argentina cerca de medianoche) también queda como riesgo conocido, a corregir antes de una prueba prolongada o de habilitar AFIP. `entrega-2` ya no es una rama "sin aprobar" — está fusionada en producción.

**How to apply:** antes de implementar cualquier módulo, releer la definición correspondiente en `[[reference_la_platense_docs]]` para no reinventar decisiones ya cerradas con el cliente. No tocar `AnulacionVentaService` sin resolver la pregunta abierta con Joaquín primero. Antes de tocar producción de nuevo, confirmar en qué rama está realmente deployado (`entrega-1-migracion`, que desde el 2026-08-24 YA INCLUYE toda la Entrega 2) — ver [[reference_smarterasp_credentials]] para las credenciales de deploy. Antes de habilitar Facturación AFIP real, corregir D8 primero (ver `6-qa.md`). Al modificar el modelo de datos de cualquier proyecto del estudio (no solo este), aplicar la regla `LP-002` de arriba antes de cerrar la tarea.
